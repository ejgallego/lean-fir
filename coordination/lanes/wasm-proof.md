# wasm-proof lane

```text
lane: wasm-proof
owner: wasm-proof
branch: wasm/talos-runtime
worktree: .worktrees/wasm-talos
state: ready
base: f5cfd054 on main
functional-head: 2231901a
contract-base: f5cfd054; consume the landed resident Array, immediate-Nat, complete scalar Wasm, direct fixed-width/USize, source-Float, and standard-libm surfaces without changing their signatures or shared ABI
clean-at-update: true
slice: The resident-helper proof path factors the common immediate-Nat scalar fragments, exact adapted-body/terminal-suffix bridge, scratch-memory retyping, defined-function call/result-local composition, and host-polymorphic function-body termination. The actual adapted makeNatural helper now supplies its immediate contract without a handwritten target body. A type-independent unsigned 64-bit sum theorem proves low-word addition, carry propagation, both overflow guards, constructor-call composition, exact store threading, and return convention. Its naturalSum specialization derives bounded mathematical no-carry facts and composes the actual adapted makeNatural callee. The public generated Nat.add function then adapts to the shared pair dispatcher and composes actual adapted naturalSum/makeNatural calls, payload decoding, and scratch retyping; for a sum within the immediate range it returns the exact canonical tagged Nat with unchanged store while the arbitrary-precision fallback remains opaque. The existing Nat.mod immediate theorems now consume the same actual adapted constructor boundary. The generic resident replacement theorem remains certificate-free. Separately, proof-indexed Nat/USize Array admission derives live logical-size limits from the actual resident layout, retains the erased source bound, proves canonical Nat decode or exact USize narrowing, and reconstructs the selected semantic element.
files: Fir/Wasm/Concrete/NaturalDispatchCorrectness.lean; Fir/Wasm/Concrete/ArrayAdmissionCorrectness.lean; Fir/Wasm/Concrete.lean; integration/talos/FirTalos/ConcreteResidentMemory.lean; integration/talos/FirTalos/ConcreteResidentPrimitives.lean; integration/talos/FirTalos/ConcreteResidentNat.lean; integration/talos/FirTalos/ConcreteResidentReplacement.lean; integration/talos/FirTalos/Correctness/Composition.lean; integration/talos/FirTalos/Correctness/Function.lean; integration/talos/FirTalos.lean; integration/talos/W6-THEOREM-ROADMAP.md; coordination/lanes/wasm-proof.md
contracts: none. No helper signature, semantic ABI, concrete layout, ownership rule, source semantics, compiler relation, or W7-owned source changed. The Nat.add theorem consumes the existing generation surface exactly; the public locals-cardinality lemma prevents proof dependence on W7's private local-array constructor.
checks: After rebasing on f5cfd054, fresh Lean Beam update/sync/save passes with zero errors or warnings for ConcreteResidentNat.lean; lake build FirTalos.ConcreteResidentNat FirTalos passes (3,162 jobs); git diff --check passes; make check passes (163 scalar Wasm exports, 125 unit tests, 704 source cases across native/LCNF/V8, nine direct-machine cases, 2,121/2,121 comparisons equal, 713-case aggregate coverage, zero findings, 188 active bug cards, 25 mailbox tests); make talos-setup passes at Talos 0e05edbc; make talos-check passes (3,162 jobs)
bug-cards: none new. Existing FIR-BUG-wasm-none-structured-validation-provenance remains the compiler-admission frontier; FIR-BUG-wasm-none-finite-trace-address-space-safety remains fixed by the explicit compiler/resource split.
blockers: none for this handoff. W7-W6-20260814-007 is closed for bounded two-immediate Nat.add but still needs promoted/heap-backed sums for its full arithmetic domain. W7-W6-20260814-008 has the actual adapted makeNatural/Nat.mod immediate call chain but still needs the operation-specific StateRelated successor plugged into generic resident replacement. W7-W6-20260814-004 has its smallest W6 runtime premise; integration-owned compiler admission must derive erased bound/canonical operand evidence at each unchecked typed Array site.
handoff: Ready green W6 proof stack rebased on f5cfd054. Integration may land through functional head 2231901a and the containing mailbox commit. It is contract-neutral and does not claim promoted Nat.add, full resident external replacement, universal compiler admission, or finite-memory safety complete.
next: Prove promoted/heap-backed naturalSum/Nat.add, then instantiate the operation-specific StateRelated successor and generic resident replacement theorem. In parallel, integration transports proof-indexed Array admission from accepted compiler provenance; finite wasm32 headroom remains a separate execution premise.
```
