import FirTalos.ConcreteTraceSimulation
import FirTalos.ConcreteCompilerCorrectness
import FirTalos.ConcreteReuseCapacityCacheCorrectness
import FirTalos.Correctness.StructuredWasmAdequacy

/-!
# Compiler relation for the concrete structured Wasm target

This module begins the W6.7e construction of the compiler-derived ranked weak
simulation.  `ConcreteStructuredCodeFocus` is the local code-state component
of the eventual relation: it records the real two-stage compiler output, the
concrete runtime/local refinement, and the corresponding source and target
control states.  Source and target continuation stacks are deliberately left
outside this component so the same local proof can be lifted through the
frame relation constructed by later slices.

The first transition family is the compiler's genuinely silent case.
Persistent reference-count increments and decrements change only source
control and are erased by lowering.  Their target match is therefore the
reflexive structured path, and the structural source-control rank decreases.
-/

namespace FirTalos.Concrete

open Fir.Wasm
open Fir.Wasm.Concrete
open Fir.LeanIR.Impure
open FirTalos.Correctness

/-- A compiler-derived code fragment followed by physical residual code that
cannot be reached before the fragment's explicit return. The adapter's Lean
4.33 validation marker is the root instance; recursive compiler steps retain
it in their residual program or move it into a structured-control frame. -/
def CodeAdaptedWithSuffix
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (sourceCode : Lean.Compiler.LCNF.Code .impure)
    (targetCode : Wasm.Program) : Prop :=
  ∃ targetCore targetSuffix,
    CodeAdapted context sourceModule sourceFunction labels sourceCode
        targetCore ∧
      targetCode = targetCore ++ targetSuffix

theorem CodeAdapted.withSuffix
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetCore targetSuffix : Wasm.Program}
    (adapted :
      CodeAdapted context sourceModule sourceFunction labels sourceCode
        targetCore) :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels sourceCode
      (targetCore ++ targetSuffix) :=
  ⟨targetCore, targetSuffix, adapted, rfl⟩

theorem CodeAdapted.withEmptySuffix
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetCode : Wasm.Program}
    (adapted :
      CodeAdapted context sourceModule sourceFunction labels sourceCode
        targetCode) :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels sourceCode
      targetCode := by
  simpa using CodeAdapted.withSuffix (targetSuffix := []) adapted

/-- Suffix-aware inversion of a source return. The physical residual remains
after `ret`, so the explicit return discards it before it can execute. -/
theorem CodeAdaptedWithSuffix.return_eq
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {result : Lean.FVarId}
    {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (adapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels
        (.return result) target) :
    ∃ kind resultIndex targetSuffix,
      Fir.Wasm.getLocal context result =
          .ok (.localGet result, kind) ∧
        findFVar? (functionBindings sourceFunction) result = some resultIndex ∧
        (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
          some kind ∧
        target = [.localGet resultIndex, .ret] ++ targetSuffix := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨kind, resultIndex, localCompiled, resultFound, kindAt, rfl⟩ :=
    CodeAdapted.return_eq localsAligned coreAdapted
  exact ⟨kind, resultIndex, targetSuffix, localCompiled, resultFound, kindAt,
    rfl⟩

/-- Suffix-aware inversion of a direct `let`. The independently adapted
continuation inherits the same physical residual. -/
theorem CodeAdaptedWithSuffix.let_eq
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {target : Wasm.Program}
    (adapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels
        (.let decl continuation) target) :
    ∃ valueCode targetValue targetRest resultIndex,
      Fir.Wasm.compileLetValue context decl = .ok valueCode ∧
        instructions sourceModule sourceFunction labels valueCode =
          .ok targetValue ∧
        findFVar? (functionBindings sourceFunction) decl.fvarId =
          some resultIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = targetValue ++ .localSet resultIndex :: targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨valueCode, _restCode, targetValue, coreRest, resultIndex,
      valueCompiled, _restCompiled, valueAdapted, restAdapted, resultFound,
      rfl⟩ := CodeAdapted.let_eq coreAdapted
  refine ⟨valueCode, targetValue, coreRest ++ targetSuffix, resultIndex,
    valueCompiled, valueAdapted, resultFound, ?_, ?_⟩
  · exact CodeAdapted.withSuffix (targetSuffix := targetSuffix)
      ⟨_, _restCompiled, restAdapted⟩
  · simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.incPersistent_eq
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {amount : Nat} {check : Bool}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {target : Wasm.Program}
    (adapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels
        (.inc objectId amount check true continuation) target) :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      target := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  exact CodeAdapted.withSuffix (targetSuffix := targetSuffix)
    (CodeAdapted.incPersistent_eq coreAdapted)

theorem CodeAdaptedWithSuffix.decPersistent_eq
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {amount : Nat} {check : Bool}
    {objectFields? : Option Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {target : Wasm.Program}
    (adapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels
        (.dec objectId amount check true objectFields? continuation) target) :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      target := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  exact CodeAdapted.withSuffix (targetSuffix := targetSuffix)
    (CodeAdapted.decPersistent_eq coreAdapted)

theorem CodeAdaptedWithSuffix.inc_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {amount : Nat} {check : Bool}
    {objectKind : AbiKind}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, objectKind))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.inc objectId amount check false continuation) target) :
    ∃ objectIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some objectKind ∧
        callIndex? sourceModule (.runtime (.inc amount check)) = some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = [.localGet objectIndex, .call callIndex] ++ targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, callIndex, coreRest, objectFound, kindAt, callFound,
      continuationAdapted, rfl⟩ :=
    CodeAdapted.inc_eq localsAligned objectCompiled coreAdapted
  refine ⟨objectIndex, callIndex, coreRest ++ targetSuffix, objectFound, kindAt,
    callFound, CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.dec_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {amount : Nat} {check : Bool}
    {objectFields? : Option Nat} {objectKind : AbiKind}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, objectKind))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.dec objectId amount check false objectFields? continuation) target) :
    ∃ objectIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some objectKind ∧
        callIndex? sourceModule (.runtime (.dec amount check objectFields?)) =
          some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = [.localGet objectIndex, .call callIndex] ++ targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, callIndex, coreRest, objectFound, kindAt, callFound,
      continuationAdapted, rfl⟩ :=
    CodeAdapted.dec_eq localsAligned objectCompiled coreAdapted
  refine ⟨objectIndex, callIndex, coreRest ++ targetSuffix, objectFound, kindAt,
    callFound, CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.del_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {objectKind : AbiKind}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, objectKind))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.del objectId continuation) target) :
    ∃ objectIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some objectKind ∧
        callIndex? sourceModule (.runtime .delete) = some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = [.localGet objectIndex, .call callIndex] ++ targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, callIndex, coreRest, objectFound, kindAt, callFound,
      continuationAdapted, rfl⟩ :=
    CodeAdapted.del_eq localsAligned objectCompiled coreAdapted
  refine ⟨objectIndex, callIndex, coreRest ++ targetSuffix, objectFound, kindAt,
    callFound, CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.setTag_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {tag : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, .object))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.setTag objectId tag continuation) target) :
    ∃ objectIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some .object ∧
        callIndex? sourceModule (.runtime (.setTag tag)) = some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = [.localGet objectIndex, .call callIndex] ++ targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, callIndex, coreRest, objectFound, kindAt, callFound,
      continuationAdapted, rfl⟩ :=
    CodeAdapted.setTag_eq localsAligned objectCompiled coreAdapted
  refine ⟨objectIndex, callIndex, coreRest ++ targetSuffix, objectFound, kindAt,
    callFound, CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.objectSetFVar_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId fieldId : Lean.FVarId} {index : Nat} {fieldKind : AbiKind}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, .object))
    (fieldCompiled : Fir.Wasm.getLocal context fieldId =
      .ok (.localGet fieldId, fieldKind))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.oset objectId index (.fvar fieldId) continuation) target) :
    ∃ objectIndex fieldIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some .object ∧
        findFVar? (functionBindings sourceFunction) fieldId = some fieldIndex ∧
        (functionBindings sourceFunction)[fieldIndex]?.map Prod.snd =
          some fieldKind ∧
        callIndex? sourceModule (.runtime (.objectSet index fieldKind)) =
          some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target =
          [.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
            targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, fieldIndex, callIndex, coreRest, objectFound, objectKindAt,
      fieldFound, fieldKindAt, callFound, continuationAdapted, rfl⟩ :=
    CodeAdapted.objectSetFVar_eq localsAligned objectCompiled fieldCompiled
      coreAdapted
  refine ⟨objectIndex, fieldIndex, callIndex, coreRest ++ targetSuffix,
    objectFound, objectKindAt, fieldFound, fieldKindAt, callFound,
    CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.objectSetErased_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId : Lean.FVarId} {index : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, .object))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.oset objectId index .erased continuation) target) :
    ∃ objectIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some .object ∧
        callIndex? sourceModule (.runtime (.objectSet index .erased)) =
          some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target = [.localGet objectIndex, .const 0, .call callIndex] ++
          targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, callIndex, coreRest, objectFound, objectKindAt, callFound,
      continuationAdapted, rfl⟩ :=
    CodeAdapted.objectSetErased_eq localsAligned objectCompiled coreAdapted
  refine ⟨objectIndex, callIndex, coreRest ++ targetSuffix, objectFound,
    objectKindAt, callFound, CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.usizeSet_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId fieldId : Lean.FVarId} {index : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, .object))
    (fieldCompiled : Fir.Wasm.getLocal context fieldId =
      .ok (.localGet fieldId, .usize))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.uset objectId index fieldId continuation) target) :
    ∃ objectIndex fieldIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some .object ∧
        findFVar? (functionBindings sourceFunction) fieldId = some fieldIndex ∧
        (functionBindings sourceFunction)[fieldIndex]?.map Prod.snd =
          some .usize ∧
        callIndex? sourceModule (.runtime (.usizeSet index)) = some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target =
          [.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
            targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, fieldIndex, callIndex, coreRest, objectFound, objectKindAt,
      fieldFound, fieldKindAt, callFound, continuationAdapted, rfl⟩ :=
    CodeAdapted.usizeSet_eq localsAligned objectCompiled fieldCompiled coreAdapted
  refine ⟨objectIndex, fieldIndex, callIndex, coreRest ++ targetSuffix,
    objectFound, objectKindAt, fieldFound, fieldKindAt, callFound,
    CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.scalarSet_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {objectId fieldId : Lean.FVarId} {slotIndex byteOffset : Nat}
    {type : Lean.Expr} {fieldKind : AbiKind}
    {continuation : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (objectCompiled : Fir.Wasm.getLocal context objectId =
      .ok (.localGet objectId, .object))
    (fieldCompiled : Fir.Wasm.getLocal context fieldId =
      .ok (.localGet fieldId, fieldKind))
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.sset objectId slotIndex byteOffset fieldId type continuation) target) :
    ∃ objectIndex fieldIndex callIndex targetRest,
      findFVar? (functionBindings sourceFunction) objectId = some objectIndex ∧
        (functionBindings sourceFunction)[objectIndex]?.map Prod.snd =
          some .object ∧
        findFVar? (functionBindings sourceFunction) fieldId = some fieldIndex ∧
        (functionBindings sourceFunction)[fieldIndex]?.map Prod.snd =
          some fieldKind ∧
        callIndex? sourceModule
            (.runtime (.scalarSet slotIndex byteOffset fieldKind)) =
          some callIndex ∧
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels
          continuation targetRest ∧
        target =
          [.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
            targetRest := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨objectIndex, fieldIndex, callIndex, coreRest, objectFound, objectKindAt,
      fieldFound, fieldKindAt, callFound, continuationAdapted, rfl⟩ :=
    CodeAdapted.scalarSet_eq localsAligned objectCompiled fieldCompiled
      coreAdapted
  refine ⟨objectIndex, fieldIndex, callIndex, coreRest ++ targetSuffix,
    objectFound, objectKindAt, fieldFound, fieldKindAt, callFound,
    CodeAdapted.withSuffix continuationAdapted, ?_⟩
  simp [List.append_assoc]

theorem CodeAdaptedWithSuffix.defaultOnlyCases_selected
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {target : Wasm.Program}
    (onlyDefault : cases.alts.toList = [.default selected])
    (adapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels
        (.cases cases) target) :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels selected
      target := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  exact CodeAdapted.withSuffix (targetSuffix := targetSuffix)
    (CodeAdapted.defaultOnlyCases_selected onlyDefault coreAdapted)

theorem CodeAdaptedWithSuffix.singleObjectConstructorCases_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {info : Lean.Compiler.LCNF.CtorInfo}
    {selected : Lean.Compiler.LCNF.Code .impure} {target : Wasm.Program}
    (altsEq : cases.alts.toList = [.ctorAlt info selected])
    (modeEq : Fir.Wasm.caseDiscriminatorMode context cases.discr = .objectTag)
    (fits : Fir.Wasm.constructorTagFitsI32 info = true)
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.cases cases) target) :
    ∃ selectedTarget discrIndex getTagIndex targetSuffix,
      CodeAdapted context sourceModule sourceFunction labels selected
          selectedTarget ∧
      findFVar? (functionBindings sourceFunction) cases.discr =
        some discrIndex ∧
      callIndex? sourceModule (.runtime .getTag) = some getTagIndex ∧
      target = ([.localGet discrIndex, .call getTagIndex,
        .const (UInt32.ofNat info.cidx), .eq,
        .iff 0 0 selectedTarget [.unreachable]] : Wasm.Program) ++
          targetSuffix := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨selectedTarget, discrIndex, getTagIndex, selectedAdapted,
      discrFound, getTagFound, targetEq⟩ :=
    CodeAdapted.singleObjectConstructorCases_eq altsEq modeEq fits coreAdapted
  subst targetCore
  exact ⟨selectedTarget, discrIndex, getTagIndex, targetSuffix,
    selectedAdapted, discrFound, getTagFound, rfl⟩

theorem CodeAdaptedWithSuffix.twoObjectConstructorDefaultCases_eq
    {context : Fir.Wasm.Context} {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {labels : List Lean.FVarId}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {firstInfo secondInfo : Lean.Compiler.LCNF.CtorInfo}
    {firstBranch secondBranch defaultBranch :
      Lean.Compiler.LCNF.Code .impure}
    {target : Wasm.Program}
    (altsEq : cases.alts.toList =
      [.ctorAlt firstInfo firstBranch, .ctorAlt secondInfo secondBranch,
        .default defaultBranch])
    (modeEq : Fir.Wasm.caseDiscriminatorMode context cases.discr = .objectTag)
    (firstFits : Fir.Wasm.constructorTagFitsI32 firstInfo = true)
    (secondFits : Fir.Wasm.constructorTagFitsI32 secondInfo = true)
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.cases cases) target) :
    ∃ firstTarget secondTarget defaultTarget discrIndex getTagIndex
        targetSuffix,
      CodeAdapted context sourceModule sourceFunction labels firstBranch
          firstTarget ∧
      CodeAdapted context sourceModule sourceFunction labels secondBranch
          secondTarget ∧
      CodeAdapted context sourceModule sourceFunction labels defaultBranch
          defaultTarget ∧
      findFVar? (functionBindings sourceFunction) cases.discr =
        some discrIndex ∧
      callIndex? sourceModule (.runtime .getTag) = some getTagIndex ∧
      target = ([.localGet discrIndex, .call getTagIndex,
          .const (UInt32.ofNat firstInfo.cidx), .eq,
          .iff 0 0 firstTarget
            [.localGet discrIndex, .call getTagIndex,
              .const (UInt32.ofNat secondInfo.cidx), .eq,
              .iff 0 0 secondTarget defaultTarget]] : Wasm.Program) ++
        targetSuffix := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, rfl⟩
  obtain ⟨firstTarget, secondTarget, defaultTarget, discrIndex, getTagIndex,
      firstAdapted, secondAdapted, defaultAdapted, discrFound, getTagFound,
      targetEq⟩ :=
    CodeAdapted.twoObjectConstructorDefaultCases_eq altsEq modeEq firstFits
      secondFits coreAdapted
  subst targetCore
  exact ⟨firstTarget, secondTarget, defaultTarget, discrIndex, getTagIndex,
    targetSuffix, firstAdapted, secondAdapted, defaultAdapted, discrFound,
    getTagFound, rfl⟩

theorem CodeAdaptedWithSuffix.cases_eq
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {target : Wasm.Program}
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.cases cases) target) :
    ∃ fallback targetCore targetSuffix,
      Fir.Wasm.compileCaseFallback context cases.alts.toList = .ok fallback ∧
      CaseChainAdapted context sourceModule sourceFunction labels cases.discr
        cases.alts.toList fallback targetCore ∧
      target = targetCore ++ targetSuffix := by
  rcases adapted with ⟨targetCore, targetSuffix, coreAdapted, targetEq⟩
  rcases CodeAdapted.cases_eq coreAdapted with
    ⟨fallback, fallbackCompiled, chainAdapted⟩
  exact ⟨fallback, targetCore, targetSuffix, fallbackCompiled, chainAdapted,
    targetEq⟩

/-- Local compiler relation at a source code node and a running structured
target node.  It fixes the source program, runtime and environment and the
target store, locals and residual program, while leaving both continuation
stacks available to the later frame relation. -/
structure ConcreteStructuredCodeFocus
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (sourceCode : Lean.Compiler.LCNF.Code .impure)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (targetCode : Wasm.Program)
    (witness : RefinementWitness)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .code sourceCode
  sourceEnvEq : source.env = sourceEnv
  sourceRuntimeEq : source.runtime = sourceRuntime
  targetStoreEq : target.store = targetStore
  targetControlEq : target.control = .running targetLocals targetCode
  adapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels sourceCode
      targetCode
  stateRelated :
    StateRelated sourceFunction sourceRuntime sourceEnv targetStore targetLocals
      witness
  frameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv targetStore
      targetLocals witness

/-- The local compiler relation already implies exact world/trace observation
agreement.  Frame correspondence is irrelevant to observations and can be
conjoined later without reproving this fact. -/
theorem ConcreteStructuredCodeFocus.observes
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv sourceCode targetStore targetLocals
      targetCode witness source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.trace

/-- A total-correctness weakest-precondition proof with an exact successful
fallthrough postcondition already contains a finite Talos execution witness.
This is the executable-semantics boundary used below: operation refinements
produce the witness themselves, rather than asking the compiler-correctness
theorem's caller to supply an execution certificate. -/
theorem structuredWasmExecutes_fallthrough_of_wp
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals}
    {target : Wasm.Program} {tail : List Wasm.Value}
    (executed :
      Wasm.wp module target
        (fun continuation =>
          continuation =
            .Fallthrough nextStore { nextLocals with values := tail })
        targetStore { targetLocals with values := tail } hostEnv) :
    StructuredWasmExecutes module hostEnv targetStore
      { targetLocals with values := tail } target
      (.fallthrough nextStore { nextLocals with values := tail }) := by
  unfold Wasm.wp at executed
  obtain ⟨fuel, stable⟩ := executed
  exact ⟨fuel, by
    simpa [StructuredWasmOutcome.toContinuation] using
      stable fuel (Nat.le_refl fuel)⟩

/-- Exact-locals variant of `structuredWasmExecutes_fallthrough_of_wp`.
Unlike the same-tail specialization above, this boundary also reifies flat
prefixes that deliberately grow the operand stack, such as closure capture
and ordinary-argument assembly. -/
theorem structuredWasmExecutes_exactFallthrough_of_wp
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals}
    {target : Wasm.Program}
    (executed :
      Wasm.wp module target
        (fun continuation =>
          continuation = .Fallthrough nextStore nextLocals)
        targetStore targetLocals hostEnv) :
    StructuredWasmExecutes module hostEnv targetStore targetLocals target
      (.fallthrough nextStore nextLocals) := by
  unfold Wasm.wp at executed
  obtain ⟨fuel, stable⟩ := executed
  exact ⟨fuel, by
    simpa [StructuredWasmOutcome.toContinuation] using
      stable fuel (Nat.le_refl fuel)⟩

/-- Reify the target prefix guaranteed by an ordinary concrete `let` law as
an exact successful Talos execution.  The continuation-transformer law is
specialized to the empty continuation; no independent target-execution
premise is introduced. -/
theorem LetStepSimulates.structuredExecutes
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {targetValue : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    (step : LetStepSimulates context sourceFunction module hostEnv decl
      targetValue sourceRuntime nextRuntime sourceEnv sourceValue targetStore
      nextStore targetLocals nextLocals resultIndex witness nextWitness) :
    StructuredWasmExecutes module hostEnv targetStore
      { targetLocals with values := tail }
      (targetValue ++ [.localSet resultIndex])
      (.fallthrough nextStore { nextLocals with values := tail }) := by
  apply structuredWasmExecutes_fallthrough_of_wp
  let Q : Wasm.Assertion Host := fun continuation =>
    continuation =
      .Fallthrough nextStore { nextLocals with values := tail }
  have finalWP :
      Wasm.wp module [] Q nextStore
        { nextLocals with values := tail } hostEnv :=
    (Wasm.wp_nil).2 rfl
  simpa [Q] using step.2.2.2 [] Q tail finalWP

/-- A straight-line ordinary `let` prefix therefore advances the structured
machine beneath an arbitrary residual program and saved frame stack. -/
theorem LetStepSimulates.structuredFinitePath
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {targetValue targetRest : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    {frames : List StructuredWasmFrame}
    (flat : StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]))
    (step : LetStepSimulates context sourceFunction module hostEnv decl
      targetValue sourceRuntime nextRuntime sourceEnv sourceValue targetStore
      nextStore targetLocals nextLocals resultIndex witness nextWitness) :
    FinitePath (StructuredWasmStep module hostEnv)
      (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length
      ⟨targetStore,
        .running { targetLocals with values := tail }
          (targetValue ++ .localSet resultIndex :: targetRest),
        frames⟩
      ⟨nextStore,
        .running { nextLocals with values := tail } targetRest,
        frames⟩ := by
  simpa [List.append_assoc] using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      step.structuredExecutes

/-- One resolved imported call reifies as exactly one structured-machine
step.  The operand stack convention is stated for an arbitrary argument and
result row, so compiler-specific staging relations can stop immediately
before and after the host boundary. -/
theorem structuredWasmImportedCallFinitePath
    {module : Wasm.Module} {env : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {id : Nat} {imp : Wasm.ImportDecl}
    {step : Wasm.Store Host → List Wasm.Value → Wasm.HostResult Host}
    {initial final : Wasm.Store Host} {locals : Wasm.Locals}
    {physicalArgs results tail : List Wasm.Value}
    {targetRest : Wasm.Program} {frames : List StructuredWasmFrame}
    (imported : module.imports[id]? = some imp)
    (satisfies : env.Satisfies module spec)
    (inBounds : id < module.imports.length)
    (contracted : spec.contracts[id]? = some
      (fun initial args result => result = step initial args))
    (parameterCount : imp.params.length = physicalArgs.length)
    (resultCount : imp.results.length = results.length)
    (operation : step initial physicalArgs = .Return results final) :
    FinitePath (StructuredWasmStep module env) 1
      ⟨initial,
        .running
          { locals with values := physicalArgs.reverse ++ tail }
          (.call id :: targetRest),
        frames⟩
      ⟨final,
        .running { locals with values := results ++ tail } targetRest,
        frames⟩ := by
  have flat : StructuredWasmFlatProgram module [.call id] :=
    .cons (.importedCall imported) .nil
  have executed :
      StructuredWasmExecutes module env initial
        { locals with values := physicalArgs.reverse ++ tail }
        [.call id]
        (.fallthrough final { locals with values := results ++ tail }) := by
    apply structuredWasmExecutes_exactFallthrough_of_wp
    let Q : Wasm.Assertion Host := fun continuation =>
      continuation =
        .Fallthrough final { locals with values := results ++ tail }
    have finalWP :
        Wasm.wp module [] Q final
          { locals with values := results ++ tail } env :=
      (Wasm.wp_nil).2 rfl
    apply wp_exact_host_call_of_return
      (physicalArgs := physicalArgs) (results := results)
      imported satisfies inBounds contracted
    · simp [parameterCount]
    · exact operation
    · simpa [parameterCount, resultCount, Q] using finalWP
  simpa using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      executed

/-- A proved unary no-result host operation reifies as the exact generated
two-instruction structured prefix.  The host contract supplies execution;
flatness only decomposes that execution into the local-read and imported-call
machine steps beneath an arbitrary continuation and frame stack. -/
theorem structuredWasmUnaryHostEffectPrefixFinitePath
    {module : Wasm.Module} {env : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {id : Nat} {imp : Wasm.ImportDecl}
    {step : Wasm.Store Host → List Wasm.Value → Wasm.HostResult Host}
    {initial final : Wasm.Store Host} {locals : Wasm.Locals}
    {objectIndex : Nat} {physicalObject : Wasm.Value}
    {targetRest : Wasm.Program} {tail : List Wasm.Value}
    {frames : List StructuredWasmFrame}
    (objectFound : locals.get objectIndex = some physicalObject)
    (imported : module.imports[id]? = some imp)
    (satisfies : env.Satisfies module spec)
    (inBounds : id < module.imports.length)
    (contracted : spec.contracts[id]? = some
      (fun initial args result => result = step initial args))
    (parameterCount : imp.params.length = 1)
    (resultCount : imp.results.length = 0)
    (operation : step initial [physicalObject] = .Return [] final) :
    FinitePath (StructuredWasmStep module env) 2
      ⟨initial,
        .running { locals with values := tail }
          ([.localGet objectIndex, .call id] ++ targetRest),
        frames⟩
      ⟨final, .running { locals with values := tail } targetRest, frames⟩ := by
  have flat :
      StructuredWasmFlatProgram module [.localGet objectIndex, .call id] :=
    .cons (.atomic (by trivial)) (.cons (.importedCall imported) .nil)
  have executed :
      StructuredWasmExecutes module env initial
        { locals with values := tail } [.localGet objectIndex, .call id]
        (.fallthrough final { locals with values := tail }) := by
    apply structuredWasmExecutes_fallthrough_of_wp
    let Q : Wasm.Assertion Host := fun continuation =>
      continuation = .Fallthrough final { locals with values := tail }
    have finalWP :
        Wasm.wp module [] Q final { locals with values := tail } env :=
      (Wasm.wp_nil).2 rfl
    simpa [Q] using
      wp_effect_localGets
        (indices := [objectIndex]) (physicalArgs := [physicalObject])
        (rest := []) (tail := tail) (.cons objectFound .nil) imported satisfies
        inBounds contracted parameterCount resultCount operation finalWP
  simpa using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      executed

/-- A proved binary no-result host operation reifies as the exact generated
three-instruction structured prefix beneath arbitrary residual code and saved
frames. -/
theorem structuredWasmBinaryHostEffectPrefixFinitePath
    {module : Wasm.Module} {env : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {id : Nat} {imp : Wasm.ImportDecl}
    {step : Wasm.Store Host → List Wasm.Value → Wasm.HostResult Host}
    {initial final : Wasm.Store Host} {locals : Wasm.Locals}
    {firstIndex secondIndex : Nat}
    {physicalFirst physicalSecond : Wasm.Value}
    {targetRest : Wasm.Program} {tail : List Wasm.Value}
    {frames : List StructuredWasmFrame}
    (firstFound : locals.get firstIndex = some physicalFirst)
    (secondFound : locals.get secondIndex = some physicalSecond)
    (imported : module.imports[id]? = some imp)
    (satisfies : env.Satisfies module spec)
    (inBounds : id < module.imports.length)
    (contracted : spec.contracts[id]? = some
      (fun initial args result => result = step initial args))
    (parameterCount : imp.params.length = 2)
    (resultCount : imp.results.length = 0)
    (operation : step initial [physicalFirst, physicalSecond] =
      .Return [] final) :
    FinitePath (StructuredWasmStep module env) 3
      ⟨initial,
        .running { locals with values := tail }
          ([.localGet firstIndex, .localGet secondIndex, .call id] ++
            targetRest),
        frames⟩
      ⟨final, .running { locals with values := tail } targetRest, frames⟩ := by
  have flat :
      StructuredWasmFlatProgram module
        [.localGet firstIndex, .localGet secondIndex, .call id] :=
    .cons (.atomic (by trivial))
      (.cons (.atomic (by trivial)) (.cons (.importedCall imported) .nil))
  have executed :
      StructuredWasmExecutes module env initial
        { locals with values := tail }
        [.localGet firstIndex, .localGet secondIndex, .call id]
        (.fallthrough final { locals with values := tail }) := by
    apply structuredWasmExecutes_fallthrough_of_wp
    let Q : Wasm.Assertion Host := fun continuation =>
      continuation = .Fallthrough final { locals with values := tail }
    have finalWP :
        Wasm.wp module [] Q final { locals with values := tail } env :=
      (Wasm.wp_nil).2 rfl
    simpa [Q] using
      wp_effect_localGets
        (indices := [firstIndex, secondIndex])
        (physicalArgs := [physicalFirst, physicalSecond])
        (rest := []) (tail := tail)
        (.cons firstFound (.cons secondFound .nil)) imported satisfies
        inBounds contracted parameterCount resultCount operation finalWP
  simpa using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      executed

/-- A proved local/constant binary host operation reifies as the exact
three-instruction structured prefix used for erased object-field writes. -/
theorem structuredWasmLocalI32ConstHostEffectPrefixFinitePath
    {module : Wasm.Module} {env : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {id : Nat} {imp : Wasm.ImportDecl}
    {step : Wasm.Store Host → List Wasm.Value → Wasm.HostResult Host}
    {initial final : Wasm.Store Host} {locals : Wasm.Locals}
    {firstIndex : Nat} {physicalFirst : Wasm.Value} {constant : UInt32}
    {targetRest : Wasm.Program} {tail : List Wasm.Value}
    {frames : List StructuredWasmFrame}
    (firstFound : locals.get firstIndex = some physicalFirst)
    (imported : module.imports[id]? = some imp)
    (satisfies : env.Satisfies module spec)
    (inBounds : id < module.imports.length)
    (contracted : spec.contracts[id]? = some
      (fun initial args result => result = step initial args))
    (parameterCount : imp.params.length = 2)
    (resultCount : imp.results.length = 0)
    (operation : step initial [physicalFirst, .i32 constant] =
      .Return [] final) :
    FinitePath (StructuredWasmStep module env) 3
      ⟨initial,
        .running { locals with values := tail }
          ([.localGet firstIndex, .const constant, .call id] ++ targetRest),
        frames⟩
      ⟨final, .running { locals with values := tail } targetRest, frames⟩ := by
  have flat :
      StructuredWasmFlatProgram module
        [.localGet firstIndex, .const constant, .call id] :=
    .cons (.atomic (by trivial))
      (.cons (.atomic (by trivial)) (.cons (.importedCall imported) .nil))
  have executed :
      StructuredWasmExecutes module env initial
        { locals with values := tail }
        [.localGet firstIndex, .const constant, .call id]
        (.fallthrough final { locals with values := tail }) := by
    apply structuredWasmExecutes_fallthrough_of_wp
    let Q : Wasm.Assertion Host := fun continuation =>
      continuation = .Fallthrough final { locals with values := tail }
    have finalWP :
        Wasm.wp module [] Q final { locals with values := tail } env :=
      (Wasm.wp_nil).2 rfl
    simp only [Wasm.wp_localGet_cons]
    have firstFoundNext :
        ({ locals with values := tail } : Wasm.Locals).get firstIndex =
          some physicalFirst := by
      simpa [Wasm.Locals.get] using firstFound
    rw [firstFoundNext]
    simp only
    rw [Wasm.wp_const_cons]
    apply wp_exact_host_call_of_return
      (physicalArgs := [physicalFirst, .i32 constant]) (results := [])
      imported satisfies inBounds contracted
    · simp [parameterCount]
    · exact operation
    · convert finalWP using 1
      all_goals simp [parameterCount, resultCount]
  simpa using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      executed

/-- The concrete target for any admitted immediate literal followed by its
destination write is a straight-line structured fragment. -/
theorem ImmediateLiteralKind.structuredFlatProgram
    {literal : Lean.Compiler.LCNF.LitValue} {kind : AbiKind}
    (shape : ImmediateLiteralKind literal kind)
    (module : Wasm.Module) (resultIndex : Nat) :
    StructuredWasmFlatProgram module
      ([shape.targetInstruction] ++ [.localSet resultIndex]) := by
  cases shape <;>
    exact .cons (.atomic (by trivial))
      (.cons (.atomic (by trivial)) .nil)

/-- Successful production compilation and adaptation determine the immediate
literal's flat target prefix; no target syntax is admitted independently. -/
theorem ImmediateLiteralSupported.structuredFlatProgram
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction} {targetValue : Wasm.Program}
    {resultIndex : Nat}
    (supported : ImmediateLiteralSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro literal resultKind valueEq valueKind _ shape =>
      have expectedCompiled :=
        shape.compileLetValue_eq (context := context) valueEq valueKind
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      have expectedAdapted :=
        shape.instructions_eq (sourceModule := sourceModule)
          (sourceFunction := sourceFunction) (labels := labels)
      rw [expectedAdapted] at valueAdapted
      injection valueAdapted with targetValueEq
      subst targetValue
      exact shape.structuredFlatProgram module resultIndex

/-- A local read followed by the generated destination write is the other
cost-zero straight-line prefix used by the first recursive spine. -/
theorem structuredWasmFlatProgram_localGet_localSet
    (module : Wasm.Module) (sourceIndex resultIndex : Nat) :
    StructuredWasmFlatProgram module
      ([.localGet sourceIndex] ++ [.localSet resultIndex]) := by
  exact .cons (.atomic (by trivial))
    (.cons (.atomic (by trivial)) .nil)

/-- A compiler-generated call that resolves to a target import, followed by
the generated destination write, is a two-step flat target fragment. -/
theorem structuredWasmFlatProgram_importCall_localSet
    {module : Wasm.Module} {functionIndex resultIndex : Nat}
    {imp : Wasm.ImportDecl}
    (imported : module.imports[functionIndex]? = some imp) :
    StructuredWasmFlatProgram module
      ([.call functionIndex] ++ [.localSet resultIndex]) := by
  exact .cons (.importedCall imported)
    (.cons (.atomic (by trivial)) .nil)

/-- Flat target fragments are closed under program concatenation. -/
theorem StructuredWasmFlatProgram.append
    {module : Wasm.Module} {left right : Wasm.Program}
    (leftFlat : StructuredWasmFlatProgram module left)
    (rightFlat : StructuredWasmFlatProgram module right) :
    StructuredWasmFlatProgram module (left ++ right) := by
  induction leftFlat with
  | nil => simpa using rightFlat
  | cons head tail ih => exact .cons head ih

/-- The production argument compiler emits only local reads and erased-zero
constants.  Successful adaptation therefore maps its entire output to a flat
target fragment, independently of runtime state or argument values. -/
theorem ConstructorArgsCompiled.structuredFlatProgram
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module}
    {args : List (Lean.Compiler.LCNF.Arg .impure)}
    {argumentCode : List Fir.Wasm.Instruction}
    {fieldKinds : List AbiKind} {targetArguments : Wasm.Program}
    (compiled : ConstructorArgsCompiled context args argumentCode fieldKinds)
    (adapted :
      instructions sourceModule sourceFunction labels argumentCode =
        .ok targetArguments) :
    StructuredWasmFlatProgram module targetArguments := by
  induction compiled generalizing targetArguments with
  | nil =>
      have targetEq : targetArguments = [] := by
        have adaptedEq :
            (Except.ok [] : Except AdapterError Wasm.Program) =
              .ok targetArguments := by
          simpa [instructions, pure, Except.pure] using adapted
        exact (Except.ok.inj adaptedEq).symm
      subst targetArguments
      exact .nil
  | erased rest ih =>
      obtain ⟨targetHead, targetRest, headAdapted, restAdapted, targetEq⟩ :=
        instructions_cons_eq_ok adapted
      have targetHeadEq : targetHead = .const 0 := by
        have adaptedEq :
            (Except.ok (.const 0) : Except AdapterError Wasm.Instruction) =
              .ok targetHead := by
          simpa [instruction, pure, Except.pure] using headAdapted
        exact (Except.ok.inj adaptedEq).symm
      subst targetHead
      subst targetArguments
      exact .cons (.atomic (by trivial)) (ih restAdapted)
  | @fvar fvarId kind args argumentCode fieldKinds kindFound rest ih =>
      obtain ⟨targetHead, targetRest, headAdapted, restAdapted, targetEq⟩ :=
        instructions_cons_eq_ok adapted
      cases sourceFound :
          findFVar?
            (sourceFunction.params.toList ++ sourceFunction.locals.toList)
            fvarId with
      | none =>
          simp [instruction, sourceFound] at headAdapted
      | some sourceIndex =>
          have targetHeadEq : targetHead = .localGet sourceIndex := by
            have adaptedEq :
                (Except.ok (.localGet sourceIndex) :
                    Except AdapterError Wasm.Instruction) =
                  .ok targetHead := by
              simpa [instruction, sourceFound, pure, Except.pure] using
                headAdapted
            exact (Except.ok.inj adaptedEq).symm
          subst targetHead
          subst targetArguments
          exact .cons (.atomic (by trivial)) (ih restAdapted)

/-- A successfully compiled argument row followed by any runtime call is flat
after production adaptation whenever the selected call is covered by the
module-wide runtime-call alignment theorem. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_compileArgs_runtimeCall
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {args : Array (Lean.Compiler.LCNF.Arg .impure)}
    {argumentCode : List Fir.Wasm.Instruction}
    {fieldKinds : Array AbiKind} {operation : RuntimeOp}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (argumentsCompiled :
      Fir.Wasm.compileArgs context args = .ok (argumentCode, fieldKinds))
    (valueAdapted :
      instructions sourceModule sourceFunction labels
          (argumentCode ++ [.call (.runtime operation)]) =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  obtain ⟨targetArguments, callIndex, argumentsAdapted, callFound,
      targetEq⟩ :=
    instructions_append_call_eq valueAdapted
  obtain ⟨imp, imported, _⟩ := spec.runtimeCallsAligned callFound
  have argumentsFlat :
      StructuredWasmFlatProgram target.wasmModule targetArguments :=
    (ConstructorArgsCompiled.ofCompileArgs argumentsCompiled).structuredFlatProgram
      argumentsAdapted
  subst targetValue
  simpa [List.append_assoc] using
    StructuredWasmFlatProgram.append argumentsFlat
      (structuredWasmFlatProgram_importCall_localSet
        (resultIndex := resultIndex) imported)

/-- A production argument row followed by a compiler-selected external
declaration call is flat after adaptation.  Import status is derived from the
whole-pipeline external-call alignment, not assumed from target syntax. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_compileArgs_externalCall
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {name : Lean.Name} {declaration : Lean.Compiler.LCNF.Decl .impure}
    {args : Array (Lean.Compiler.LCNF.Arg .impure)}
    {argumentCode : List Fir.Wasm.Instruction}
    {argumentKinds : Array AbiKind}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (argumentsCompiled :
      Fir.Wasm.compileArgs context args =
        .ok (argumentCode, argumentKinds))
    (declarationFound : program.findDecl? name = some declaration)
    (declarationExternal : ∃ metadata, declaration.value = .extern metadata)
    (valueAdapted :
      instructions sourceModule sourceFunction labels
          (argumentCode ++ [.call (.declaration name)]) =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  obtain ⟨targetArguments, callIndex, argumentsAdapted, callFound,
      targetEq⟩ :=
    instructions_append_declaration_call_eq valueAdapted
  obtain ⟨_operation, _resultKind, imp, _operationName, _operationMatches,
      _resultSignature, imported, _inBounds, _contracted, _parameterCount,
      _resultCount⟩ :=
    spec.externalCall declarationFound declarationExternal callFound
  have argumentsFlat :
      StructuredWasmFlatProgram target.wasmModule targetArguments :=
    (ConstructorArgsCompiled.ofCompileArgs argumentsCompiled).structuredFlatProgram
      argumentsAdapted
  subst targetValue
  simpa [List.append_assoc] using
    StructuredWasmFlatProgram.append argumentsFlat
      (structuredWasmFlatProgram_importCall_localSet
        (resultIndex := resultIndex) imported)

/-- Every admitted pure external result has a compiler-derived straight-line
target prefix: compiled arguments, one resolved import call, and the generated
destination write. -/
theorem PureExternalSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value} {stepCost : Nat}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost)
    (valueCompiled : Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  rcases supported with integer | natural | scalar
  · rcases integer with
      ⟨name, args, argumentCode, argumentKinds, _semanticArgs, declaration,
        _value, valueEq, _operation, nonempty, targetFound, targetExternal,
        valueKind, argumentsCompiled, _argumentsEvaluated, _signature,
        _resultCompiled, _semanticCalled, _nextRuntimeEq, _sourceValueEq,
        _stepCostEq⟩
    have expectedCompiled :
        Fir.Wasm.compileLetValue context decl =
          .ok (argumentCode ++ [.call (.declaration name)]) := by
      simp [Fir.Wasm.compileLetValue, valueEq, valueKind, argumentsCompiled,
        targetFound, nonempty, Bind.bind, Except.bind, pure, Except.pure]
    rw [expectedCompiled] at valueCompiled
    have valueCodeEq := Except.ok.inj valueCompiled
    subst valueCode
    have declarationFound : program.findDecl? name = some declaration := by
      rw [← spec.contextProgram]
      exact targetFound
    exact spec.structuredFlatProgram_compileArgs_externalCall
      argumentsCompiled declarationFound targetExternal valueAdapted
  · rcases natural with
      ⟨name, args, argumentCode, argumentKinds, _semanticArgs, declaration,
        _value, valueEq, _operation, nonempty, targetFound, targetExternal,
        valueKind, argumentsCompiled, _argumentsEvaluated, _signature,
        _resultCompiled, _semanticCalled, _nextRuntimeEq, _sourceValueEq,
        _stepCostEq⟩
    have expectedCompiled :
        Fir.Wasm.compileLetValue context decl =
          .ok (argumentCode ++ [.call (.declaration name)]) := by
      simp [Fir.Wasm.compileLetValue, valueEq, valueKind, argumentsCompiled,
        targetFound, nonempty, Bind.bind, Except.bind, pure, Except.pure]
    rw [expectedCompiled] at valueCompiled
    have valueCodeEq := Except.ok.inj valueCompiled
    subst valueCode
    have declarationFound : program.findDecl? name = some declaration := by
      rw [← spec.contextProgram]
      exact targetFound
    exact spec.structuredFlatProgram_compileArgs_externalCall
      argumentsCompiled declarationFound targetExternal valueAdapted
  · rcases scalar with
      ⟨name, args, argumentCode, argumentKinds, _semanticArgs, declaration,
        _value, valueEq, _operation, nonempty, targetFound, targetExternal,
        valueKind, argumentsCompiled, _argumentsEvaluated, _signature,
        _resultCompiled, _semanticCalled, _nextRuntimeEq, _sourceValueEq,
        _stepCostEq⟩
    have expectedCompiled :
        Fir.Wasm.compileLetValue context decl =
          .ok (argumentCode ++ [.call (.declaration name)]) := by
      simp [Fir.Wasm.compileLetValue, valueEq, valueKind, argumentsCompiled,
        targetFound, nonempty, Bind.bind, Except.bind, pure, Except.pure]
    rw [expectedCompiled] at valueCompiled
    have valueCodeEq := Except.ok.inj valueCompiled
    subst valueCode
    have declarationFound : program.findDecl? name = some declaration := by
      rw [← spec.contextProgram]
      exact targetFound
    exact spec.structuredFlatProgram_compileArgs_externalCall
      argumentsCompiled declarationFound targetExternal valueAdapted

/-- Successful adaptation of one symbolic local read determines one atomic
target local read, regardless of the source local's numeric target index. -/
theorem structuredWasmFlatProgram_localGet_of_instructions
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module}
    {fvarId : Lean.FVarId} {targetArguments : Wasm.Program}
    (adapted :
      instructions sourceModule sourceFunction labels [.localGet fvarId] =
        .ok targetArguments) :
    StructuredWasmFlatProgram module targetArguments := by
  obtain ⟨targetHead, targetRest, headAdapted, restAdapted, targetEq⟩ :=
    instructions_cons_eq_ok adapted
  have restEq : targetRest = [] := by
    have adaptedEq :
        (Except.ok [] : Except AdapterError Wasm.Program) =
          .ok targetRest := by
      simpa [instructions, pure, Except.pure] using restAdapted
    exact (Except.ok.inj adaptedEq).symm
  cases sourceFound :
      findFVar?
        (sourceFunction.params.toList ++ sourceFunction.locals.toList)
        fvarId with
  | none => simp [instruction, sourceFound] at headAdapted
  | some sourceIndex =>
      have headEq : targetHead = .localGet sourceIndex := by
        have adaptedEq :
            (Except.ok (.localGet sourceIndex) :
                Except AdapterError Wasm.Instruction) =
              .ok targetHead := by
          simpa [instruction, sourceFound, pure, Except.pure] using
            headAdapted
        exact (Except.ok.inj adaptedEq).symm
      subst targetHead
      subst targetRest
      subst targetArguments
      exact .cons (.atomic (by trivial)) .nil

/-- Every direct operation whose compiler shape is one local read followed by
one runtime call has a flat adapted prefix. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_localGet_runtimeCall
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} {fvarId : Lean.FVarId}
    {operation : RuntimeOp} {targetValue : Wasm.Program} {resultIndex : Nat}
    (valueAdapted :
      instructions sourceModule sourceFunction labels
          [.localGet fvarId, .call (.runtime operation)] =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  obtain ⟨targetArguments, callIndex, argumentsAdapted, callFound,
      targetEq⟩ :=
    instructions_append_call_eq
      (argumentCode := [.localGet fvarId]) valueAdapted
  obtain ⟨imp, imported, _⟩ := spec.runtimeCallsAligned callFound
  have argumentsFlat :
      StructuredWasmFlatProgram target.wasmModule targetArguments :=
    structuredWasmFlatProgram_localGet_of_instructions argumentsAdapted
  subst targetValue
  simpa [List.append_assoc] using
    StructuredWasmFlatProgram.append argumentsFlat
      (structuredWasmFlatProgram_importCall_localSet
        (resultIndex := resultIndex) imported)

/-- Prefix-only form of the local-read/runtime-call flatness theorem.  Closure
capture assembly needs the projected value left on the operand stack rather
than immediately written to a destination local. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_localGet_runtimeCallPrefix
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} {fvarId : Lean.FVarId}
    {operation : RuntimeOp} {targetValue : Wasm.Program}
    (valueAdapted :
      instructions sourceModule sourceFunction labels
          [.localGet fvarId, .call (.runtime operation)] =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule targetValue := by
  obtain ⟨targetArguments, callIndex, argumentsAdapted, callFound,
      targetEq⟩ :=
    instructions_append_call_eq
      (argumentCode := [.localGet fvarId]) valueAdapted
  obtain ⟨imp, imported, _⟩ := spec.runtimeCallsAligned callFound
  have argumentsFlat :
      StructuredWasmFlatProgram target.wasmModule targetArguments :=
    structuredWasmFlatProgram_localGet_of_instructions argumentsAdapted
  subst targetValue
  exact StructuredWasmFlatProgram.append argumentsFlat
    (.cons (.importedCall imported) .nil)

/-- Any compiler-selected fixed-capture row adapts to a flat target prefix.
Erased captures become constants; represented captures become one local read
and one resolver-proved imported projection call. -/
theorem ClosureCaptureRows.structuredFlatProgram_of_adapted
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    {labels : List Lean.FVarId}
    {closureId : Lean.FVarId} {function : Lean.Name}
    {arity fixed : Nat}
    {captures : Array Value} {captureKinds parameterKinds : Array AbiKind}
    {target : Lean.Compiler.LCNF.Decl .impure}
    {indices : List Nat} {expectedKinds : List AbiKind}
    {semanticValues : List Value} {targetCode : Wasm.Program}
    (rows : ClosureCaptureRows parameterKinds captureKinds captures indices
      expectedKinds semanticValues)
    (targetName : target.name = function)
    (adapted : instructions sourceModule sourceFunction labels
      (indices.flatMap
        (Fir.Wasm.compileFixedClosureField closureId target arity fixed
          parameterKinds)) = .ok targetCode) :
    StructuredWasmFlatProgram targetModule.wasmModule targetCode := by
  induction rows generalizing targetCode with
  | nil =>
      simp only [List.flatMap_nil, instructions] at adapted
      change (Except.ok [] : Except AdapterError Wasm.Program) =
        .ok targetCode at adapted
      have targetEq : targetCode = [] := (Except.ok.inj adapted).symm
      subst targetCode
      exact .nil
  | @cons index expectedKind actualKind value indices expectedKinds
      semanticValues expectedAt actualAt valueAt kindRefines rest ih =>
      obtain ⟨targetField, targetRest, fieldAdapted, restAdapted,
          targetEq⟩ :=
        instructions_append_eq_ok (by simpa using adapted)
      subst targetCode
      have restFlat := ih restAdapted
      by_cases expectedErased : expectedKind = .erased
      · subst expectedKind
        have fieldSource :
            Fir.Wasm.compileFixedClosureField closureId target arity fixed
                parameterKinds index = [.i32Const .erased 0] := by
          simp [Fir.Wasm.compileFixedClosureField, expectedAt]
        rw [fieldSource] at fieldAdapted
        have targetFieldEq : targetField = [.const 0] := by
          have adaptedEq :
              (Except.ok [.const 0] : Except AdapterError Wasm.Program) =
                .ok targetField := by
            simpa [instructions, instruction, Bind.bind, Except.bind, pure,
              Except.pure] using fieldAdapted
          exact (Except.ok.inj adaptedEq).symm
        subst targetField
        exact StructuredWasmFlatProgram.append
          (.cons (.atomic (by trivial)) .nil) restFlat
      · have fieldSource :
            Fir.Wasm.compileFixedClosureField closureId target arity fixed
                parameterKinds index = [
              .localGet closureId,
              .call (.runtime
                (.closureProj function arity fixed index expectedKind))] := by
          simp [Fir.Wasm.compileFixedClosureField, expectedAt, targetName]
        rw [fieldSource] at fieldAdapted
        exact StructuredWasmFlatProgram.append
          (spec.structuredFlatProgram_localGet_runtimeCallPrefix fieldAdapted)
          restFlat
  | @snoc indices expectedKinds semanticValues index expectedKind actualKind
      value rest expectedAt actualAt valueAt kindRefines ih =>
      obtain ⟨targetPrefix, targetField, prefixAdapted, fieldAdapted,
          targetEq⟩ :=
        instructions_append_eq_ok
          (by simpa [List.flatMap_append] using adapted)
      subst targetCode
      have prefixFlat := ih prefixAdapted
      by_cases expectedErased : expectedKind = .erased
      · subst expectedKind
        have fieldSource :
            Fir.Wasm.compileFixedClosureField closureId target arity fixed
                parameterKinds index = [.i32Const .erased 0] := by
          simp [Fir.Wasm.compileFixedClosureField, expectedAt]
        rw [fieldSource] at fieldAdapted
        have targetFieldEq : targetField = [.const 0] := by
          have adaptedEq :
              (Except.ok [.const 0] : Except AdapterError Wasm.Program) =
                .ok targetField := by
            simpa [instructions, instruction, Bind.bind, Except.bind, pure,
              Except.pure] using fieldAdapted
          exact (Except.ok.inj adaptedEq).symm
        subst targetField
        exact StructuredWasmFlatProgram.append prefixFlat
          (.cons (.atomic (by trivial)) .nil)
      · have fieldSource :
            Fir.Wasm.compileFixedClosureField closureId target arity fixed
                parameterKinds index = [
              .localGet closureId,
              .call (.runtime
                (.closureProj function arity fixed index expectedKind))] := by
          simp [Fir.Wasm.compileFixedClosureField, expectedAt, targetName]
        rw [fieldSource] at fieldAdapted
        exact StructuredWasmFlatProgram.append prefixFlat
          (spec.structuredFlatProgram_localGet_runtimeCallPrefix fieldAdapted)

/-- The complete selected saturated-call argument prefix—fixed capture
projections followed by ordinary compiled arguments—is flat after real
production adaptation.  Capture ABI compatibility chooses the exact
projection row; no target syntax is supplied separately. -/
theorem SaturatedClosureCallResolution.argumentsStructuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {sourceEnv : Env} {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    {labels : List Lean.FVarId}
    {witness : RefinementWitness} {application : ClosureApplication}
    {address : Word32} {captureKinds : Array AbiKind}
    {targetCode : Wasm.Program}
    (aligned : ClosureAllocationsAbiAligned context.program witness)
    (adapted : instructions sourceModule sourceFunction labels
      (Fir.Wasm.compileFixedClosureFields site.closureId resolution.target
          resolution.arity resolution.captures.size
            resolution.parameterKinds ++ site.argumentCode) =
        .ok targetCode)
    (applicationRelated : ClosureApplicationRel witness application address
      resolution.function resolution.arity captureKinds
        resolution.captures) :
    StructuredWasmFlatProgram targetModule.wasmModule targetCode := by
  obtain ⟨targetCaptures, targetArguments, capturesAdapted,
      argumentsAdapted, targetEq⟩ :=
    instructions_append_eq_ok adapted
  subst targetCode
  have captureKindsRefine :=
    resolution.captureKindsRefine aligned applicationRelated
  have rows := ClosureCaptureRows.prefix
    applicationRelated.captureKindsSize captureKindsRefine
    resolution.captures.size (le_refl _)
  have capturesFlat :
      StructuredWasmFlatProgram targetModule.wasmModule targetCaptures := by
    apply rows.structuredFlatProgram_of_adapted spec resolution.targetName
    simpa [Fir.Wasm.compileFixedClosureFields] using capturesAdapted
  have argumentsFlat :
      StructuredWasmFlatProgram targetModule.wasmModule targetArguments :=
    (ConstructorArgsCompiled.ofCompileArgs site.argumentsCompiled).structuredFlatProgram
      argumentsAdapted
  exact StructuredWasmFlatProgram.append capturesFlat argumentsFlat

/-- Recover the one-local/one-runtime-call target shape from the actual
`compileLetValue` result before applying the generic adapter theorem. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_localRuntimeCall_of_compiler
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {fvarId : Lean.FVarId} {operation : RuntimeOp}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (expectedCompiled :
      Fir.Wasm.compileLetValue context decl =
        .ok [.localGet fvarId, .call (.runtime operation)])
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  rw [expectedCompiled] at valueCompiled
  injection valueCompiled with valueCodeEq
  subst valueCode
  exact spec.structuredFlatProgram_localGet_runtimeCall valueAdapted

/-- Admitted `USize` projections satisfy the production flatness law. -/
theorem USizeProjectionSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : USizeProjectionSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro index objectId objectKind valueEq resultKind objectCompiled
      objectRefines resultCompiled =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_usizeProjection valueEq resultKind objectCompiled)
        valueCompiled valueAdapted

/-- Admitted object projections satisfy the same production flatness law. -/
theorem ObjectProjectionSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : ObjectProjectionSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro index objectId objectKind resultKind valueEq valueKind objectCompiled
      objectRefines resultCompiled fieldKindAligned =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_objectProjection valueEq valueKind objectCompiled)
        valueCompiled valueAdapted

/-- Admitted packed-scalar projections satisfy production flatness. -/
theorem ScalarProjectionSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : ScalarProjectionSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro width offset objectId objectKind resultKind valueEq valueKind
      objectCompiled objectRefines resultCompiled valueKindAligned =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_scalarProjection valueEq valueKind objectCompiled)
        valueCompiled valueAdapted

/-- Admitted scalar boxing satisfies production flatness independently of
the concrete tagged-or-heap representation selected at runtime. -/
theorem BoxSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : BoxSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro scalarId kind resultKind valueEq valueKind resultKindEq
      scalarCompiled annotationKind resultCompiled =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_box valueEq valueKind scalarCompiled annotationKind)
        valueCompiled valueAdapted

/-- Admitted typed unboxing satisfies production flatness. -/
theorem UnboxSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : UnboxSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro objectId objectKind kind valueEq resultTypeEq valueKind
      objectCompiled objectRefines resultCompiled kindCompatible =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_unbox valueEq valueKind objectCompiled)
        valueCompiled valueAdapted

/-- Admitted sharing observations satisfy production flatness. -/
theorem IsSharedSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : IsSharedSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro objectId objectKind valueEq valueKind objectCompiled objectRefines
      resultCompiled =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_isShared valueEq valueKind objectCompiled)
        valueCompiled valueAdapted

/-- Ownership reset has the same one-local/one-runtime-call compiler shape. -/
theorem ResetSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : ResetSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro count objectId objectKind valueEq valueKind objectCompiled
      objectRefines resultCompiled =>
      exact spec.structuredFlatProgram_localRuntimeCall_of_compiler
        (compileLetValue_reset valueEq valueKind objectCompiled)
        valueCompiled valueAdapted

/-- Admitted nonempty constructors use the compiler-characterized argument
prefix followed by one aligned allocation import. -/
theorem NonemptyConstructorSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : NonemptyConstructorSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro info args argumentCode fieldKinds resultKind valueEq tagFits
      valueKind argumentsCompiled resultCompiled operationWellFormed nonempty
      objectFieldsFit usizeFieldsFit scalarBytesFit =>
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok (argumentCode ++
              [.call (.runtime (.allocCtor info fieldKinds resultKind))]) :=
        compileLetValue_constructor valueEq tagFits valueKind
          argumentsCompiled
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      exact spec.structuredFlatProgram_compileArgs_runtimeCall
        argumentsCompiled valueAdapted

/-- A leading symbolic local read may be prepended to a compiled argument row
and runtime call without leaving the flat target fragment.  This is the exact
production shape used by reuse. -/
theorem
    ConcreteSupportedFunction.structuredFlatProgram_localGet_compileArgs_runtimeCall
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} {fvarId : Lean.FVarId}
    {args : Array (Lean.Compiler.LCNF.Arg .impure)}
    {argumentCode : List Fir.Wasm.Instruction}
    {fieldKinds : Array AbiKind} {operation : RuntimeOp}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (argumentsCompiled :
      Fir.Wasm.compileArgs context args = .ok (argumentCode, fieldKinds))
    (valueAdapted :
      instructions sourceModule sourceFunction labels
          (.localGet fvarId ::
            argumentCode ++ [.call (.runtime operation)]) =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  have appendedAdapted :
      instructions sourceModule sourceFunction labels
          ([.localGet fvarId] ++
            (argumentCode ++ [.call (.runtime operation)])) =
        .ok targetValue := by
    simpa using valueAdapted
  obtain ⟨targetLocal, targetRest, localAdapted, restAdapted, targetEq⟩ :=
    instructions_append_eq_ok appendedAdapted
  have localFlat :
      StructuredWasmFlatProgram target.wasmModule targetLocal :=
    structuredWasmFlatProgram_localGet_of_instructions localAdapted
  have restFlat :
      StructuredWasmFlatProgram target.wasmModule
        (targetRest ++ [.localSet resultIndex]) :=
    spec.structuredFlatProgram_compileArgs_runtimeCall argumentsCompiled
      restAdapted
  subst targetValue
  simpa [List.append_assoc] using
    StructuredWasmFlatProgram.append localFlat restFlat

/-- Capacity-admitted reuse satisfies production flatness for arbitrary mixed
local/erased constructor arguments. -/
theorem ReuseSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {facts : ReuseCapacityFacts} {labels : List Lean.FVarId}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction}
    {targetValue : Wasm.Program} {resultIndex : Nat}
    (supported : ReuseSupported context facts decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro tokenId info updateHeader args argumentCode fieldKinds resultKind
      evidence valueEq tagFits valueKind tokenCompiled argumentsCompiled
      resultCompiled operationWellFormed capacityFitting resultCompatible
      objectFieldsFit usizeFieldsFit scalarBytesFit =>
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok (.localGet tokenId :: argumentCode ++
              [.call
                (.runtime
                  (.reuse info updateHeader fieldKinds resultKind))]) :=
        compileLetValue_reuse valueEq valueKind tokenCompiled
          argumentsCompiled
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      exact spec.structuredFlatProgram_localGet_compileArgs_runtimeCall
        argumentsCompiled valueAdapted

/-- Production compilation of an admitted natural literal emits exactly one
runtime import call followed by the destination write.  Runtime-call
alignment supplies the target-import fact, so flatness is derived from the
compiler and adapter rather than stated as a separate certificate. -/
theorem NaturalLiteralSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {target : AdaptedModule}
    {hosts : ResolvedHosts}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction} {targetValue : Wasm.Program}
    {resultIndex : Nat}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    (supported : NaturalLiteralSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro value valueEq valueKind _ =>
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok [.call (.runtime (.literal (.nat value) .tobject))] :=
        compileLetValue_naturalLiteral valueEq valueKind
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      cases callFound :
          callIndex? sourceModule
            (.runtime (.literal (.nat value) .tobject)) with
      | none =>
          simp [instructions, instruction, callFound] at valueAdapted
          change
            Except.error AdapterError.unknownCallTarget =
              Except.ok targetValue at valueAdapted
          cases valueAdapted
      | some callIndex =>
          have expectedAdapted :
              instructions sourceModule sourceFunction labels
                  [.call (.runtime (.literal (.nat value) .tobject))] =
                .ok [.call callIndex] := by
            simp [instructions, instruction, callFound]
            rfl
          rw [expectedAdapted] at valueAdapted
          injection valueAdapted with targetValueEq
          subst targetValue
          obtain ⟨imp, imported, _⟩ := spec.naturalLiteralCall callFound
          exact structuredWasmFlatProgram_importCall_localSet imported

/-- The corresponding UTF-8 String literal prefix is likewise recovered from
the production compiler and adapter and ends in a resolved target import. -/
theorem StringLiteralSupported.structuredFlatProgram
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {target : AdaptedModule}
    {hosts : ResolvedHosts}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction} {targetValue : Wasm.Program}
    {resultIndex : Nat}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    (supported : StringLiteralSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram target.wasmModule
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro value valueEq valueKind _ =>
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok [.call (.runtime (.literal (.str value) .object))] :=
        compileLetValue_stringLiteral valueEq valueKind
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      cases callFound :
          callIndex? sourceModule
            (.runtime (.literal (.str value) .object)) with
      | none =>
          simp [instructions, instruction, callFound] at valueAdapted
          change
            Except.error AdapterError.unknownCallTarget =
              Except.ok targetValue at valueAdapted
          cases valueAdapted
      | some callIndex =>
          have expectedAdapted :
              instructions sourceModule sourceFunction labels
                  [.call (.runtime (.literal (.str value) .object))] =
                .ok [.call callIndex] := by
            simp [instructions, instruction, callFound]
            rfl
          rw [expectedAdapted] at valueAdapted
          injection valueAdapted with targetValueEq
          subst targetValue
          obtain ⟨imp, imported, _⟩ := spec.stringLiteralCall callFound
          exact structuredWasmFlatProgram_importCall_localSet imported

/-- Local-alias admission plus the executable compiler and adapter uniquely
recover that two-instruction target prefix. -/
theorem LocalAliasSupported.structuredFlatProgram
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {valueCode : List Fir.Wasm.Instruction} {targetValue : Wasm.Program}
    {resultIndex : Nat}
    (supported : LocalAliasSupported context decl)
    (valueCompiled :
      Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue) :
    StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]) := by
  cases supported with
  | intro sourceId kind valueEq resultKind sourceCompiled _ =>
      have emptyCompiled :
          Fir.Wasm.compileArgs context #[] = .ok ([], #[]) := by
        rfl
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok [.localGet sourceId] := by
        simp [Fir.Wasm.compileLetValue, valueEq, resultKind, sourceCompiled,
          emptyCompiled]
        rfl
      rw [expectedCompiled] at valueCompiled
      injection valueCompiled with valueCodeEq
      subst valueCode
      cases sourceFound :
          findFVar?
            (sourceFunction.params.toList ++ sourceFunction.locals.toList)
            sourceId with
      | none =>
          simp [instructions, instruction, sourceFound, Bind.bind,
            Except.bind] at valueAdapted
      | some sourceIndex =>
          have expectedAdapted :
              instructions sourceModule sourceFunction labels
                  [.localGet sourceId] =
                .ok [.localGet sourceIndex] := by
            simp [instructions, instruction, sourceFound]
            rfl
          rw [expectedAdapted] at valueAdapted
          injection valueAdapted with targetValueEq
          subst targetValue
          exact structuredWasmFlatProgram_localGet_localSet module sourceIndex
            resultIndex

/-- Uniform compiler-shape law for a facts-indexed family of direct values.
It quantifies over the real lowering and adapter results and concludes only
that their generated call/write prefix is structurally flat.  This is a
compiler theorem interface, not a per-program target certificate. -/
def ReuseCapacityDirectTargetFlat
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (module : Wasm.Module)
    (Supported :
      ReuseCapacityFacts → Lean.Compiler.LCNF.LetDecl .impure → Prop) : Prop :=
  ∀ {facts : ReuseCapacityFacts}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {valueCode : List Fir.Wasm.Instruction}
      {targetValue : Wasm.Program}
      {resultIndex : Nat},
    Supported facts decl →
      Fir.Wasm.compileLetValue context decl = .ok valueCode →
      instructions sourceModule sourceFunction labels valueCode =
          .ok targetValue →
      StructuredWasmFlatProgram module
        (targetValue ++ [.localSet resultIndex])

/-- Flat compiler-shape laws compose across source-admission disjunction. -/
theorem ReuseCapacityDirectTargetFlat.or
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module}
    {Left Right :
      ReuseCapacityFacts → Lean.Compiler.LCNF.LetDecl .impure → Prop}
    (left : ReuseCapacityDirectTargetFlat context sourceModule sourceFunction
      labels module Left)
    (right : ReuseCapacityDirectTargetFlat context sourceModule sourceFunction
      labels module Right) :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      module (fun facts decl => Left facts decl ∨ Right facts decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  cases supported with
  | inl supported => exact left supported valueCompiled valueAdapted
  | inr supported => exact right supported valueCompiled valueAdapted

/-- Immediate literals satisfy the uniform facts-indexed compiler-shape law. -/
theorem reuseCapacityDirectTargetFlat_immediateLiteral
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      module (fun _ decl => ImmediateLiteralSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram valueCompiled valueAdapted

/-- Local aliases satisfy the same uniform facts-indexed shape law. -/
theorem reuseCapacityDirectTargetFlat_localAlias
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId} {module : Wasm.Module} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      module (fun _ decl => LocalAliasSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram valueCompiled valueAdapted

/-- Runtime-call alignment discharges the natural-literal shape law. -/
theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_naturalLiteral
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => NaturalLiteralSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

/-- Runtime-call alignment also discharges the String-literal shape law. -/
theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_stringLiteral
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => StringLiteralSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_reuse
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule (ReuseSupported context) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_usizeProjection
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => USizeProjectionSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_objectProjection
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => ObjectProjectionSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_scalarProjection
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => ScalarProjectionSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_unbox
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule (fun _ decl => UnboxSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_isShared
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule (fun _ decl => IsSharedSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_constructor
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule
      (fun _ decl => NonemptyConstructorSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

theorem ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_box
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule (fun _ decl => BoxSupported context decl) := by
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  exact supported.structuredFlatProgram spec valueCompiled valueAdapted

/-- The complete facts-indexed direct fragment used by the existing resource
theorem has one uniform compiler-derived flatness law. -/
theorem
    ConcreteSupportedFunction.reuseCapacityDirectTargetFlat_reuseBudgetedDirect
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId} :
    ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
      target.wasmModule (ReuseBudgetedDirectSupported context) := by
  unfold ReuseBudgetedDirectSupported ReuseConstructorBoxSupported
    ReuseReadOnlyConstructorSupported ReuseReadOnlySupported
    ReuseAliasSupported
  intro facts decl valueCode targetValue resultIndex supported valueCompiled
    valueAdapted
  cases supported with
  | inl constructorBox =>
      cases constructorBox with
      | inl readOnlyConstructor =>
          cases readOnlyConstructor with
          | inl readOnly =>
              cases readOnly with
              | inl reuseOrReader =>
                  cases reuseOrReader with
                  | inl reuseAlias =>
                      cases reuseAlias with
                      | inl reuse =>
                          exact reuse.structuredFlatProgram spec valueCompiled
                            valueAdapted
                      | inr localAlias =>
                          exact localAlias.structuredFlatProgram valueCompiled
                            valueAdapted
                  | inr reader =>
                      cases reader with
                      | inl immediate =>
                          exact immediate.structuredFlatProgram valueCompiled
                            valueAdapted
                      | inr projection =>
                          cases projection with
                          | inl usize =>
                              exact usize.structuredFlatProgram spec
                                valueCompiled valueAdapted
                          | inr objectOrScalar =>
                              cases objectOrScalar with
                              | inl object =>
                                  exact object.structuredFlatProgram spec
                                    valueCompiled valueAdapted
                              | inr scalar =>
                                  exact scalar.structuredFlatProgram spec
                                    valueCompiled valueAdapted
              | inr unboxOrShared =>
                  cases unboxOrShared with
                  | inl unbox =>
                      exact unbox.structuredFlatProgram spec valueCompiled
                        valueAdapted
                  | inr shared =>
                      exact shared.structuredFlatProgram spec valueCompiled
                        valueAdapted
          | inr constructor =>
              exact constructor.structuredFlatProgram spec valueCompiled
                valueAdapted
      | inr box =>
          exact box.structuredFlatProgram spec valueCompiled valueAdapted
  | inr literal =>
      cases literal with
      | inl natural =>
          exact natural.structuredFlatProgram spec valueCompiled valueAdapted
      | inr string =>
          exact string.structuredFlatProgram spec valueCompiled valueAdapted

/-- Interprocedural concrete `let` laws expose the same exact executable
prefix.  Internal-call execution is therefore obtained from the proved
runtime law and will be structurally reified by the shared target theorem. -/
theorem CallLetStepSimulates.structuredExecutes
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host} {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetValue : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    (step : CallLetStepSimulates context sourceFunction module hostEnv
      externals decl continuation targetValue sourceRuntime nextRuntime
      sourceEnv sourceValue targetStore nextStore targetLocals nextLocals
      resultIndex witness nextWitness) :
    StructuredWasmExecutes module hostEnv targetStore
      { targetLocals with values := tail }
      (targetValue ++ [.localSet resultIndex])
      (.fallthrough nextStore { nextLocals with values := tail }) := by
  apply structuredWasmExecutes_fallthrough_of_wp
  let Q : Wasm.Assertion Host := fun continuation =>
    continuation =
      .Fallthrough nextStore { nextLocals with values := tail }
  have finalWP :
      Wasm.wp module [] Q nextStore
        { nextLocals with values := tail } hostEnv :=
    (Wasm.wp_nil).2 rfl
  simpa [Q] using step.2.2.2 [] Q tail finalWP

/-- External-call refinements expose their generated prefix at the identical
executable boundary. -/
theorem ExternalLetStepSimulates.structuredExecutes
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host} {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetValue : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    (step : ExternalLetStepSimulates context sourceFunction module hostEnv
      externals decl continuation targetValue sourceRuntime nextRuntime
      sourceEnv sourceValue targetStore nextStore targetLocals nextLocals
      resultIndex witness nextWitness) :
    StructuredWasmExecutes module hostEnv targetStore
      { targetLocals with values := tail }
      (targetValue ++ [.localSet resultIndex])
      (.fallthrough nextStore { nextLocals with values := tail }) := by
  apply structuredWasmExecutes_fallthrough_of_wp
  let Q : Wasm.Assertion Host := fun continuation =>
    continuation =
      .Fallthrough nextStore { nextLocals with values := tail }
  have finalWP :
      Wasm.wp module [] Q nextStore
        { nextLocals with values := tail } hostEnv :=
    (Wasm.wp_nil).2 rfl
  simpa [Q] using step.2.2.2 [] Q tail finalWP

/-- A straight-line external-call prefix advances beneath the same arbitrary
residual program and saved frame stack. -/
theorem ExternalLetStepSimulates.structuredFinitePath
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host} {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetValue targetRest : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    {frames : List StructuredWasmFrame}
    (flat : StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]))
    (step : ExternalLetStepSimulates context sourceFunction module hostEnv
      externals decl continuation targetValue sourceRuntime nextRuntime
      sourceEnv sourceValue targetStore nextStore targetLocals nextLocals
      resultIndex witness nextWitness) :
    FinitePath (StructuredWasmStep module hostEnv)
      (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length
      ⟨targetStore,
        .running { targetLocals with values := tail }
          (targetValue ++ .localSet resultIndex :: targetRest),
        frames⟩
      ⟨nextStore,
        .running { nextLocals with values := tail } targetRest,
        frames⟩ := by
  simpa [List.append_assoc] using
    flat.finitePathWithSuffix (suffix := targetRest) (frames := frames)
      step.structuredExecutes

/-- Both lazy-cache paths obtain their concrete execution from the proved
cache law, so hits and misses share the same structural reification boundary. -/
theorem LazyLetStepSimulates.structuredExecutes
    {path : LazyCachePath} {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function} {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host} {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetValue : Wasm.Program}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals} {resultIndex : Nat}
    {witness nextWitness : RefinementWitness} {tail : List Wasm.Value}
    (step : LazyLetStepSimulates path context sourceFunction module hostEnv
      externals decl continuation targetValue sourceRuntime nextRuntime
      sourceEnv sourceValue targetStore nextStore targetLocals nextLocals
      resultIndex witness nextWitness) :
    StructuredWasmExecutes module hostEnv targetStore
      { targetLocals with values := tail }
      (targetValue ++ [.localSet resultIndex])
      (.fallthrough nextStore { nextLocals with values := tail }) := by
  apply structuredWasmExecutes_fallthrough_of_wp
  let Q : Wasm.Assertion Host := fun continuation =>
    continuation =
      .Fallthrough nextStore { nextLocals with values := tail }
  have finalWP :
      Wasm.wp module [] Q nextStore
        { nextLocals with values := tail } hostEnv :=
    (Wasm.wp_nil).2 rfl
  simpa [Q] using step.2.2.2 [] Q tail finalWP

/-- The concrete state relation is insensitive to the operand-stack suffix;
all environment bindings live in parameter or local slots. -/
theorem StateRelated.withValues
    {sourceFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (related : StateRelated sourceFunction sourceRuntime sourceEnv targetStore
      targetLocals witness)
    (values : List Wasm.Value) :
    StateRelated sourceFunction sourceRuntime sourceEnv targetStore
      { targetLocals with values := values } witness := by
  refine ⟨related.1, related.2.1, ?_⟩
  intro fvar value sourceLookup
  obtain ⟨index, kind, physical, found, kindAt, targetLookup,
      valueRelated⟩ := related.2.2 sourceLookup
  exact ⟨index, kind, physical, found, kindAt, by simpa using targetLookup,
    valueRelated⟩

/-- Static reuse-capacity facts are interpreted through parameter/local slots
only, so changing the operand stack preserves their dynamic meaning. -/
theorem ReuseCapacityFactsRel.withValues
    {facts : ReuseCapacityFacts}
    {bindings : List (Lean.FVarId × AbiKind)} {sourceEnv : Env}
    {targetLocals : Wasm.Locals} {heap : MemoryState}
    {witness : RefinementWitness}
    (related :
      ReuseCapacityFactsRel facts bindings sourceEnv targetLocals heap witness)
    (values : List Wasm.Value) :
    ReuseCapacityFactsRel facts bindings sourceEnv
      { targetLocals with values } heap witness := by
  intro fvarId evidence found
  obtain ⟨index, kind, lane, semantic, sourceLookup, localFound, kindAt,
      targetLookup, valueRelated⟩ := related fvarId evidence found
  exact ⟨index, kind, lane, semantic, sourceLookup, localFound, kindAt,
    by simpa using targetLookup, valueRelated⟩

/-- The combined state-plus-capacity relation is operand-stack insensitive. -/
theorem ReuseCapacityStateRelated.withValues
    {facts : ReuseCapacityFacts} {sourceFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (related :
      ReuseCapacityStateRelated facts sourceFunction sourceRuntime sourceEnv
        targetStore targetLocals witness)
    (values : List Wasm.Value) :
    ReuseCapacityStateRelated facts sourceFunction sourceRuntime sourceEnv
      targetStore { targetLocals with values } witness := by
  exact ⟨related.1.withValues values, related.2.withValues values⟩

/-- Local-frame shape is likewise independent of the operand stack. -/
theorem ConcreteLocalFrameAligned.withValues
    {sourceFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (aligned : ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv
      targetStore targetLocals witness)
    (values : List Wasm.Value) :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv targetStore
      { targetLocals with values := values } witness := by
  exact aligned

/-- The authoritative resource frame is also operand-stack insensitive. -/
theorem ConcreteReuseCapacityFrame.withValues
    {sourceFunction : Fir.Wasm.Function} {facts : ReuseCapacityFacts}
    {remainingBytes : Nat} {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (frame :
      ConcreteReuseCapacityFrame sourceFunction facts remainingBytes
        sourceRuntime sourceEnv targetStore targetLocals witness)
    (values : List Wasm.Value) :
    ConcreteReuseCapacityFrame sourceFunction facts remainingBytes
      sourceRuntime sourceEnv targetStore { targetLocals with values }
      witness := by
  rcases frame with ⟨related, ordinary, aligned, budget⟩
  exact ⟨related.withValues values, ordinary, aligned.withValues values,
    budget⟩

/-- The hereditary cache frame also depends only on parameter/local slots;
the operand stack may change while a structured call is staged or resumed. -/
theorem ConcreteReuseCapacityCacheFrame.withValues
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {externals : ExternalImpl}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (frame :
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        facts remainingBytes sourceRuntime sourceEnv targetStore targetLocals
        witness)
    (values : List Wasm.Value) :
    ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals facts
      remainingBytes sourceRuntime sourceEnv targetStore
      { targetLocals with values } witness := by
  rcases frame with
    ⟨⟨⟨⟨related, ordinary, aligned, budget⟩, integer, natural, scalar⟩,
      descriptors⟩, cache, closureTables⟩
  exact
    ⟨⟨⟨⟨related.withValues values, ordinary, aligned.withValues values,
      budget⟩, integer, natural, scalar⟩, descriptors⟩, cache, closureTables⟩

/-- Entry-relative transports are independent of the current operand stack. -/
theorem ReuseCapacityEntryRelativeFrame.withValues
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function} {externals : ExternalImpl}
    {entryRuntime currentRuntime : RuntimeState}
    {entryStore currentStore : Wasm.Store Host}
    {entryWitness currentWitness : RefinementWitness}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {currentEnv : Env} {currentLocals : Wasm.Locals}
    (frame :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes currentRuntime
        currentEnv currentStore currentLocals currentWitness)
    (values : List Wasm.Value) :
    ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
      entryRuntime entryStore entryWitness facts remainingBytes currentRuntime
      currentEnv currentStore { currentLocals with values } currentWitness :=
  ⟨frame.1.withValues values, frame.2⟩

/-- Rebuild the complete entry-relative cache frame after a same-witness heap
effect.  This is the common algebra behind reference-count and field-mutation
steps: the operation law supplies the executable effect, capacity and source
ownership transports, while this theorem preserves pure external handlers,
cache globals, immutable closure tables, and the cumulative entry relation. -/
theorem ReuseCapacityEntryRelativeFrame.ofReplaceHeapEffectStep
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl} {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {entryStore targetStore : Wasm.Store Host} {heap : MemoryState}
    {targetLocals : Wasm.Locals}
    {entryWitness witness : RefinementWitness}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    {target targetRest : Wasm.Program}
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness)
    (step :
      EffectStepSimulates context sourceModule sourceFunction labels module
        hostEnv sourceRuntime nextRuntime sourceEnv code continuation target
        targetRest targetStore (replaceHeap targetStore heap) targetLocals
        witness witness)
    (capacity :
      HeaderCapacityTransport targetStore.host.runtime.heap heap witness)
    (ordinary : OrdinaryPersistenceTransport sourceRuntime nextRuntime)
    (sourceGlobals : nextRuntime.globals = sourceRuntime.globals)
    (cursor : heap.heapCursor = targetStore.host.runtime.heap.heapCursor) :
    ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
      entryRuntime entryStore entryWitness facts remainingBytes nextRuntime
      sourceEnv (replaceHeap targetStore heap) targetLocals witness := by
  have currentBase := invariant.1.1.1.1
  have integerImplementation :
      targetStore.host.externals.IntegerResultRefines externals :=
    invariant.1.1.1.2.1
  have naturalImplementation :
      FirTalos.Concrete.ConcreteExternalImpl.NaturalResultRefines
        targetStore.host.externals externals :=
    invariant.1.1.1.2.2.1
  have scalarImplementation :
      FirTalos.Concrete.ConcreteExternalImpl.ScalarResultRefines
        targetStore.host.externals externals :=
    invariant.1.1.1.2.2.2
  have currentOwnership :
      ConcreteReuseCapacityOwnershipFrame sourceFunction facts remainingBytes
        sourceRuntime sourceEnv targetStore targetLocals witness :=
    ⟨currentBase, invariant.1.1.2⟩
  have nextOwnership :
      ConcreteReuseCapacityOwnershipFrame sourceFunction facts remainingBytes
        nextRuntime sourceEnv (replaceHeap targetStore heap) targetLocals
        witness :=
    currentOwnership.ofReplaceHeapEffectStep step capacity ordinary cursor
  have externalsEq :
      (replaceHeap targetStore heap).host.externals =
        targetStore.host.externals := by
    simp [replaceHeap, clearFailure]
  have nextIntegerImplementation :
      (replaceHeap targetStore heap).host.externals.IntegerResultRefines
        externals := by
    rw [externalsEq]
    exact @integerImplementation
  have nextNaturalImplementation :
      FirTalos.Concrete.ConcreteExternalImpl.NaturalResultRefines
        (replaceHeap targetStore heap).host.externals externals := by
    rw [externalsEq]
    exact @naturalImplementation
  have nextScalarImplementation :
      FirTalos.Concrete.ConcreteExternalImpl.ScalarResultRefines
        (replaceHeap targetStore heap).host.externals externals := by
    rw [externalsEq]
    exact @scalarImplementation
  let transports :
      EffectStepTransports sourceRuntime nextRuntime targetStore
        (replaceHeap targetStore heap) witness witness :=
    EffectStepTransports.replaceHeap capacity ordinary sourceGlobals
  have nextCache :
      LazyCacheGlobalsRel witness sourceModule nextRuntime
        (replaceHeap targetStore heap) :=
    invariant.1.2.1.transport transports.witnessTransport
      transports.sourceGlobals transports.wasmGlobals
      transports.hostStaticLayout
  have nextClosureTables :
      ClosureTablesAgree (replaceHeap targetStore heap) witness :=
    transports.toClosureTablesTransport.agree invariant.1.2.2
  have nextEntry :
      ReuseCapacityCodeEntryTransports entryRuntime nextRuntime entryStore
        (replaceHeap targetStore heap) entryWitness witness :=
    invariant.2.step transports.witnessTransport
      transports.closureAllocationsPersistent transports.capacity
      transports.ordinary externalsEq transports.toClosureTablesTransport
  exact
    ⟨⟨⟨⟨nextOwnership.1, nextIntegerImplementation,
            nextNaturalImplementation, nextScalarImplementation⟩,
          nextOwnership.2⟩,
        nextCache, nextClosureTables⟩,
      nextEntry⟩

/-- One direct source `let` whose generated prefix is straight-line advances
both machines to the recursively compiled continuation.  The target path is
derived from the runtime law's exact WP execution plus compiler-side flatness;
the caller supplies neither an execution trace nor a translation certificate.
The operand-stack suffix and both continuation stacks are preserved exactly. -/
theorem ConcreteStructuredCodeFocus.advance_flatLet
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals}
    {targetCode targetValue targetRest : Wasm.Program}
    {resultIndex : Nat} {witness nextWitness : RefinementWitness}
    {source : MachineState} {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (targetCodeEq :
      targetCode = targetValue ++ .localSet resultIndex :: targetRest)
    (continuationAdapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
        targetRest)
    (flat : StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]))
    (step : LetStepSimulates context sourceFunction module hostEnv decl
      targetValue sourceRuntime nextRuntime sourceEnv sourceValue targetStore
      nextStore targetLocals nextLocals resultIndex witness nextWitness)
    (nextAligned :
      ConcreteLocalFrameAligned sourceFunction nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
        nextWitness) :
    ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv)
        (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length
        target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        nextRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
        nextStore { nextLocals with values := targetLocals.values } targetRest
        nextWitness sourceAfter targetAfter ∧
      sourceAfter.joins = source.joins ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames := by
  let resumedLocals : Wasm.Locals :=
    { nextLocals with values := targetLocals.values }
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind sourceEnv decl.fvarId sourceValue
      runtime := nextRuntime }
  let targetAfter : StructuredWasmState Host :=
    { target with
      store := nextStore
      control := .running resumedLocals targetRest }
  refine ⟨sourceAfter, targetAfter, ?_, ?_, ?_, ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have programEq := related.sourceProgramEq
    change program = context.program at programEq
    subst program
    have controlEq := related.sourceControlEq
    change control = .code (.let decl continuation) at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = sourceEnv at envEq
    subst env
    have runtimeEq := related.sourceRuntimeEq
    change runtime = sourceRuntime at runtimeEq
    subst runtime
    have evaluated :
        evalLetValue {
          program := context.program
          control := .code (.let decl continuation)
          env := sourceEnv
          joins := joins
          frames := frames
          runtime := sourceRuntime } decl =
          .ok (nextRuntime, .value sourceValue) := by
      have sourceStep := step.1
      unfold SourceLetResult at sourceStep
      cases decl.value <;> exact sourceStep
    simp [sourceAfter, executeStep, coreStep, evaluated]
  · rcases target with ⟨store, control, frames⟩
    have storeEq := related.targetStoreEq
    change store = targetStore at storeEq
    subst store
    have controlEq := related.targetControlEq
    rw [targetCodeEq] at controlEq
    change control =
      .running targetLocals
        (targetValue ++ .localSet resultIndex :: targetRest) at controlEq
    subst control
    simpa [resumedLocals, targetAfter] using
      step.structuredFinitePath (targetRest := targetRest)
        (tail := targetLocals.values) (frames := frames) flat
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter]
      targetStoreEq := by simp [targetAfter]
      targetControlEq := by simp [targetAfter, resumedLocals]
      adapted := continuationAdapted
      stateRelated := by
        simpa [resumedLocals] using
          (step.2.2.1.withValues targetLocals.values)
      frameAligned := by
        simpa [resumedLocals] using
          (nextAligned.withValues targetLocals.values) }
  · simp [sourceAfter]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- Interpreter `ExecSteps` are the source transition system's exact finite
paths. -/
theorem ExecSteps.toFinitePath
    {externals : ExternalImpl} {count : Nat}
    {first last : MachineState}
    (steps : ExecSteps externals count first last) :
    FinitePath
      (fun before after => executeStep externals before = .next after)
      count first last := by
  induction steps with
  | refl state => exact .refl state
  | step head _tail ih =>
      simpa [Nat.succ_eq_add_one] using
        FinitePath.cons
          (step :=
            fun before after =>
              executeStep externals before = .next after)
          head ih

/-- One supported external-result `let` advances through the interpreter's
three-step request protocol and the exact flat imported-call target prefix.

The current recursive fragment has no join constructors, so its local source
focus carries the empty join environment.  Saved bind/call frames may still be
arbitrarily deep; the executable external prefix is lifted beneath that exact
frame suffix. -/
theorem ConcreteStructuredCodeFocus.advance_flatExternalLet
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value}
    {targetStore nextStore : Wasm.Store Host}
    {targetLocals nextLocals : Wasm.Locals}
    {targetCode targetValue targetRest : Wasm.Program}
    {resultIndex : Nat} {witness nextWitness : RefinementWitness}
    {source : MachineState} {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (sourceJoins : source.joins = [])
    (targetCodeEq :
      targetCode = targetValue ++ .localSet resultIndex :: targetRest)
    (continuationAdapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
        targetRest)
    (flat : StructuredWasmFlatProgram module
      (targetValue ++ [.localSet resultIndex]))
    (step : ExternalLetStepSimulates context sourceFunction module hostEnv
      externals decl continuation targetValue sourceRuntime nextRuntime
      sourceEnv sourceValue targetStore nextStore targetLocals nextLocals
      resultIndex witness nextWitness)
    (nextAligned :
      ConcreteLocalFrameAligned sourceFunction nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
        nextWitness) :
    ∃ sourceAfter targetAfter,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          3 source sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv)
        (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length
        target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        nextRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
        nextStore { nextLocals with values := targetLocals.values } targetRest
        nextWitness sourceAfter targetAfter ∧
      sourceAfter.joins = [] ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames := by
  let resumedLocals : Wasm.Locals :=
    { nextLocals with values := targetLocals.values }
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind sourceEnv decl.fvarId sourceValue
      runtime := nextRuntime }
  let targetAfter : StructuredWasmState Host :=
    { target with
      store := nextStore
      control := .running resumedLocals targetRest }
  refine ⟨sourceAfter, targetAfter, ?_, ?_, ?_, ?_, ?_, ?_⟩
  · apply ExecSteps.toFinitePath
    rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have programEq := related.sourceProgramEq
    change program = context.program at programEq
    subst program
    have controlEq := related.sourceControlEq
    change control = .code (.let decl continuation) at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = sourceEnv at envEq
    subst env
    have runtimeEq := related.sourceRuntimeEq
    change runtime = sourceRuntime at runtimeEq
    subst runtime
    change joins = [] at sourceJoins
    subst joins
    have lifted :=
      FirTalos.Correctness.ExecSteps.withFrameSuffix
        (suffix := frames) step.1
    simpa [SourceExternalLetResult, withFrameSuffix, sourceAfter] using lifted
  · rcases target with ⟨store, control, frames⟩
    have storeEq := related.targetStoreEq
    change store = targetStore at storeEq
    subst store
    have controlEq := related.targetControlEq
    rw [targetCodeEq] at controlEq
    change control =
      .running targetLocals
        (targetValue ++ .localSet resultIndex :: targetRest) at controlEq
    subst control
    simpa [resumedLocals, targetAfter] using
      step.structuredFinitePath (targetRest := targetRest)
        (tail := targetLocals.values) (frames := frames) flat
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter]
      targetStoreEq := by simp [targetAfter]
      targetControlEq := by simp [targetAfter, resumedLocals]
      adapted := continuationAdapted
      stateRelated := by
        simpa [resumedLocals] using
          (step.2.2.1.withValues targetLocals.values)
      frameAligned := by
        simpa [resumedLocals] using
          (nextAligned.withValues targetLocals.values) }
  · simp [sourceAfter, sourceJoins]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- Local compiler relation after a source return has yielded its semantic
value and the generated target has entered explicit return mode.  The
pre-return locals remain available for the later call-frame relation; the
physical result at the head of the returning stack is related at the exact
ABI kind selected by the compiler. -/
structure ConcreteStructuredYieldFocus
    (context : Fir.Wasm.Context)
    (sourceFunction : Fir.Wasm.Function)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (sourceValue : Value)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (witness : RefinementWitness)
    (kind : AbiKind)
    (physical : Wasm.Value)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .yielded sourceValue
  sourceEnvEq : source.env = sourceEnv
  sourceRuntimeEq : source.runtime = sourceRuntime
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .returning (physical :: targetLocals.values)
  stateRelated :
    StateRelated sourceFunction sourceRuntime sourceEnv targetStore targetLocals
      witness
  frameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv targetStore
      targetLocals witness
  valueRelated : PhysicalValueRel witness kind physical sourceValue

theorem ConcreteStructuredYieldFocus.observes
    {context : Fir.Wasm.Context}
    {sourceFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredYieldFocus context sourceFunction
      sourceRuntime sourceEnv sourceValue targetStore targetLocals witness kind
      physical source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.trace

/-- A source return is matched by the generated positive target path:
`local.get resultIndex` followed by the structured machine's explicit `ret`
transition.  Successful two-stage adaptation determines the index and ABI
kind, while `StateRelated.resolve` supplies the physical result and its
refinement proof. -/
theorem ConcreteStructuredCodeFocus.advance_return
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {result : Lean.FVarId}
    {sourceValue : Value}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (sourceLookup : lookup sourceEnv result = some sourceValue)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.return result) targetStore targetLocals
      targetCode witness source target) :
    ∃ kind physical sourceAfter targetAfter,
      Fir.Wasm.getLocal context result = .ok (.localGet result, kind) ∧
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredYieldFocus context sourceFunction sourceRuntime
        sourceEnv sourceValue targetStore targetLocals witness kind physical
        sourceAfter targetAfter ∧
      sourceAfter.joins = source.joins ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames := by
  obtain ⟨kind, resultIndex, targetSuffix, localCompiled, resultFound, kindAt,
      targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.return_eq localsAligned related.adapted
  obtain ⟨physical, targetLookup, valueRelated⟩ :=
    related.stateRelated.resolve sourceLookup resultFound kindAt
  subst targetCode
  let sourceAfter : MachineState :=
    { source with control := .yielded sourceValue }
  let targetAfterGet : StructuredWasmState Host :=
    { target with
      control := .running
        { targetLocals with
          values := physical :: targetLocals.values }
        (.ret :: targetSuffix) }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .returning (physical :: targetLocals.values) }
  refine ⟨kind, physical, sourceAfter, targetAfter, localCompiled, ?_, ?_, ?_,
    ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = .code (.return result) at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = sourceEnv at envEq
    subst env
    simp [sourceAfter, executeStep, coreStep, lookupValue, sourceLookup]
  · have getStep :
        StructuredWasmStep module hostEnv target targetAfterGet := by
      rcases target with ⟨store, control, frames⟩
      have storeEq := related.targetStoreEq
      change store = targetStore at storeEq
      subst store
      have controlEq := related.targetControlEq
      change control =
        .running targetLocals
          ([.localGet resultIndex, .ret] ++ targetSuffix) at controlEq
      subst control
      apply StructuredWasmStep.atomic (fuel := 1)
      · trivial
      · simp only [Wasm.execOne.eq_def, targetLookup]
    have returnStep :
        StructuredWasmStep module hostEnv targetAfterGet targetAfter := by
      rcases target with ⟨store, control, frames⟩
      have storeEq := related.targetStoreEq
      change store = targetStore at storeEq
      subst store
      exact StructuredWasmStep.beginReturn
    exact .cons getStep (.cons returnStep (.refl targetAfter))
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      stateRelated := related.stateRelated
      frameAligned := related.frameAligned
      valueRelated }
  · simp [sourceAfter]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- Simulation-facing return rule.  The generic advance theorem supplies a
successful source step, so this wrapper recovers the returned binding from
that step and invokes `advance_return`; clients do not provide an extra
source-execution certificate or lookup premise. -/
theorem ConcreteStructuredCodeFocus.advance_return_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {result : Lean.FVarId}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.return result) targetStore targetLocals
      targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ sourceValue kind physical targetAfter,
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredYieldFocus context sourceFunction sourceRuntime
        sourceEnv sourceValue targetStore targetLocals witness kind physical
        sourceAfter targetAfter := by
  cases sourceLookup : lookup sourceEnv result with
  | none =>
      rcases source with
        ⟨program, control, env, joins, frames, runtime⟩
      have controlEq := related.sourceControlEq
      change control = .code (.return result) at controlEq
      subst control
      have envEq := related.sourceEnvEq
      change env = sourceEnv at envEq
      subst env
      simp [executeStep, coreStep, lookupValue, sourceLookup, fail] at sourceStep
  | some sourceValue =>
      obtain ⟨kind, physical, computedAfter, targetAfter, _localCompiled,
          computedStep, path, focus, _sourceFramesEq, _targetFramesEq⟩ :=
        related.advance_return localsAligned sourceLookup
      have afterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        injection computedStep
      subst computedAfter
      exact ⟨sourceValue, kind, physical, targetAfter, path, focus⟩

/-- Partial correctness for an arbitrary finite direct-value spine in the
structured target machine.

The source evaluation supplies only source steps, admission, fact transfer,
and its allocation-cost index.  At every `let`, the uniform runtime theorem
constructs the exact concrete successor and the uniform compiler-shape law
proves that the production target prefix is flat; `advance_flatLet` then
reifies that exact execution as structured steps.  The return rule closes the
path at a related yielded source value and explicit target return state.

Both path lengths remain explicit.  This theorem therefore exposes the local
progress information needed by the later weak-simulation theorem while making
no termination claim for programs outside the supplied finite source
evaluation. -/
theorem
    ConcreteStructuredCodeFocus.reachesYield_of_reuseCapacityCodeEvaluates
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {DirectSupported :
      ReuseCapacityFacts → Lean.Compiler.LCNF.LetDecl .impure → Prop}
    {letCost : Lean.Compiler.LCNF.LetDecl .impure → Nat}
    {Invariant :
      ReuseCapacityFacts → Nat → RuntimeState → Env →
        Wasm.Store Host → Wasm.Locals → RefinementWitness → Prop}
    {facts resultFacts : ReuseCapacityFacts}
    {sourceRuntime resultRuntime : RuntimeState}
    {sourceEnv resultEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {resultValue : Value} {requiredBytes : Nat}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program} {witness : RefinementWitness}
    {source : MachineState} {target : StructuredWasmState Host}
    (evaluation :
      ReuseCapacityCodeEvaluates context DirectSupported letCost facts
        sourceRuntime sourceEnv sourceCode resultFacts resultRuntime resultEnv
        resultValue requiredBytes)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv sourceCode targetStore targetLocals targetCode
        witness source target)
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (invariant :
      Invariant facts requiredBytes sourceRuntime sourceEnv targetStore
        targetLocals witness)
    (runtimeRefines :
      ReuseCapacityDirectLetRuntimeRefinesWithCost context sourceModule
        sourceFunction labels module hostEnv DirectSupported letCost Invariant)
    (targetFlat :
      ReuseCapacityDirectTargetFlat context sourceModule sourceFunction labels
        module DirectSupported)
    (invariantFrameAligned :
      ∀ {frameFacts frameBytes frameRuntime frameEnv frameStore frameLocals
          frameWitness},
        Invariant frameFacts frameBytes frameRuntime frameEnv frameStore
            frameLocals frameWitness →
          ConcreteLocalFrameAligned sourceFunction frameRuntime frameEnv
            frameStore frameLocals frameWitness)
    (invariantWithValues :
      ∀ {frameFacts frameBytes frameRuntime frameEnv frameStore frameLocals
          frameWitness},
        Invariant frameFacts frameBytes frameRuntime frameEnv frameStore
            frameLocals frameWitness →
          ∀ values,
            Invariant frameFacts frameBytes frameRuntime frameEnv frameStore
              { frameLocals with values } frameWitness) :
    ∃ sourceAfter targetAfter resultStore resultLocals resultWitness kind
        physical sourceCount targetCount,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          sourceCount source sourceAfter ∧
        FinitePath (StructuredWasmStep module hostEnv) targetCount target
          targetAfter ∧
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
            resultEnv resultValue resultStore resultLocals resultWitness kind
            physical sourceAfter targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames := by
  induction evaluation generalizing targetStore targetLocals targetCode witness
      source target with
  | ret sourceLookup =>
      obtain ⟨kind, physical, sourceAfter, targetAfter, _localCompiled,
          sourceStep, targetPath, focus, _sourceJoinsEq, sourceFramesEq,
          targetFramesEq⟩ :=
        related.advance_return localsAligned sourceLookup
      exact ⟨sourceAfter, targetAfter, targetStore, targetLocals, witness,
        kind, physical, 1, 2, .single sourceStep, targetPath, focus,
        sourceFramesEq, targetFramesEq⟩
  | @letValue facts decl letSourceRuntime letSourceEnv letNextRuntime
      letSourceValue nextFacts continuation resultFacts resultRuntime resultEnv
      resultValue continuationCost supported sourceStep transfer continued ih =>
      obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
          valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.let_eq related.adapted
      have stepFits :
          letCost decl ≤ letCost decl + continuationCost :=
        Nat.le_add_right _ _
      obtain ⟨nextStore, nextLocals, nextWitness, producedFacts, step,
          _externalsPreserved, _hostDescriptorsPreserved,
          _witnessDescriptorsPreserved, _transports, producedTransfer,
          nextInvariant⟩ :=
        runtimeRefines supported stepFits invariant sourceStep
          valueCompiled valueAdapted resultFound
      rw [transfer] at producedTransfer
      have factsEq := Option.some.inj producedTransfer
      subst producedFacts
      have continuationInvariant :
          Invariant nextFacts continuationCost letNextRuntime
            (bind letSourceEnv decl.fvarId letSourceValue) nextStore nextLocals
            nextWitness := by
        simpa using nextInvariant
      have flat :
          StructuredWasmFlatProgram module
            (targetValue ++ [.localSet resultIndex]) :=
        targetFlat supported valueCompiled valueAdapted
      obtain ⟨sourceMiddle, targetMiddle, firstSourceStep, targetPrefix,
          nextFocus, _firstSourceJoinsEq, firstSourceFramesEq,
          firstTargetFramesEq⟩ :=
        related.advance_flatLet targetCodeEq continuationAdapted flat step
          (invariantFrameAligned continuationInvariant)
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yieldFocus, tailSourceFramesEq, tailTargetFramesEq⟩ :=
        ih nextFocus
          (invariantWithValues continuationInvariant targetLocals.values)
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount,
        (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length +
          targetCount,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          firstSourceStep).trans sourceTail,
        targetPrefix.trans targetTail, yieldFocus,
        tailSourceFramesEq.trans firstSourceFramesEq,
        tailTargetFramesEq.trans firstTargetFramesEq⟩

/-- Concrete endpoint for the complete current facts-indexed direct fragment.
It combines the production runtime theorem and compiler-derived shape theorem
with the generic structured spine, leaving only the source evaluation and the
ordinary initial resource relation as dynamic premises. -/
theorem ConcreteSupportedFunction.reachesYield_reuseBudgetedDirect
    {program : Fir.LeanIR.ImpureProgram} {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction targetModule hosts)
    {labels : List Lean.FVarId} {externals : ExternalImpl}
    {facts resultFacts : ReuseCapacityFacts}
    {sourceRuntime resultRuntime : RuntimeState}
    {sourceEnv resultEnv : Env}
    {code : Lean.Compiler.LCNF.Code .impure}
    {resultValue : Value} {requiredBytes : Nat}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program} {witness : RefinementWitness}
    {source : MachineState} {target : StructuredWasmState Host}
    (evaluation :
      ReuseCapacityCodeEvaluates context (ReuseBudgetedDirectSupported context)
        directLetAllocationCost facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ConcreteReuseCapacityFrame sourceFunction facts requiredBytes
        sourceRuntime sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter resultStore resultLocals resultWitness kind
        physical sourceCount targetCount,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          sourceCount source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            targetCount target targetAfter ∧
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
            resultEnv resultValue resultStore resultLocals resultWitness kind
            physical sourceAfter targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames := by
  exact
    ConcreteStructuredCodeFocus.reachesYield_of_reuseCapacityCodeEvaluates
      evaluation related spec.localsAligned invariant
      spec.reuseCapacityDirectLetRuntimeRefinesWithCost_reuseBudgetedDirect
      spec.reuseCapacityDirectTargetFlat_reuseBudgetedDirect
      (by
        intro frameFacts frameBytes frameRuntime frameEnv frameStore frameLocals
          frameWitness frame
        exact frame.2.2.1)
      (by
        intro frameFacts frameBytes frameRuntime frameEnv frameStore frameLocals
          frameWitness frame values
        exact frame.withValues values)

/-- Changing only the operand stack commutes with a successful checked local
write.  The structured call-frame proof uses this to expose the returned value
as a stack head, execute `local.set`, and restore the caller's saved operand
tail. -/
theorem locals_set?_with_values
    {locals updated : Wasm.Locals}
    {index : Nat}
    {value : Wasm.Value}
    (values : List Wasm.Value)
    (set : locals.set? index value = some updated) :
    ({ locals with values }.set? index value) =
      some { updated with values } := by
  unfold Wasm.Locals.set? at set
  split at set
  · rename_i inParams
    cases set
    simp [Wasm.Locals.set?, inParams]
  · rename_i notInParams
    split at set
    · rename_i inLocals
      cases set
      simp [Wasm.Locals.set?, notInParams, inLocals]
    · contradiction

/-- Repeating the same successful checked local write is a no-op.  This is
the local-state fact needed by closure dispatch: the selected candidate body
writes its result slot, then the enclosing generic `let` wrapper reloads and
writes that same value once more. -/
theorem locals_set?_idempotent
    {locals updated : Wasm.Locals}
    {index : Nat} {value : Wasm.Value}
    (set : locals.set? index value = some updated) :
    updated.set? index value = some updated := by
  unfold Wasm.Locals.set? at set ⊢
  split at set
  · rename_i inParams
    cases set
    simp [inParams, List.set_set]
  · rename_i notInParams
    split at set
    · rename_i inLocals
      cases set
      simp [notInParams, inLocals, List.set_set]
    · contradiction

/-- A populated generated lazy-cache slot takes the exact five target steps
of the cache-hit path: load the published flag, enter and leave the empty
`then` arm, load the published value, and write the destination local.  The
miss body is retained syntactically but is not executed. -/
theorem structuredWasmLazyHitFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {locals nextLocals : Wasm.Locals}
    {flagIndex valueIndex resultIndex : Nat} {physical : Wasm.Value}
    {missBody rest : Wasm.Program} {frames : List StructuredWasmFrame}
    (tail : List Wasm.Value)
    (flagPublished :
      store.globals.globals[flagIndex]? = some (.i32 1))
    (valuePublished :
      store.globals.globals[valueIndex]? = some physical)
    (targetSet : locals.set? resultIndex physical = some nextLocals) :
    FinitePath (StructuredWasmStep module hostEnv) 5
      ⟨store, .running { locals with values := tail }
        ([.globalGet flagIndex, .iff 0 0 [] missBody,
            .globalGet valueIndex, .localSet resultIndex] ++ rest), frames⟩
      ⟨store, .running { nextLocals with values := tail } rest, frames⟩ := by
  let afterFlag : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values := .i32 1 :: tail }
        ([.iff 0 0 [] missBody, .globalGet valueIndex,
          .localSet resultIndex] ++ rest),
      frames⟩
  let inThen : StructuredWasmState Host :=
    ⟨store, .running { locals with values := tail } [],
      .label 0 tail
          ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let afterThen : StructuredWasmState Host :=
    ⟨store, .running { locals with values := tail }
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest), frames⟩
  let afterValue : StructuredWasmState Host :=
    ⟨store, .running { locals with values := physical :: tail }
        (.localSet resultIndex :: rest), frames⟩
  have loadFlag :
      StructuredWasmStep module hostEnv
        ⟨store, .running { locals with values := tail }
          ([.globalGet flagIndex, .iff 0 0 [] missBody,
              .globalGet valueIndex, .localSet resultIndex] ++ rest), frames⟩
        afterFlag := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, flagPublished]
  have enterThen :
      StructuredWasmStep module hostEnv afterFlag inThen := by
    exact StructuredWasmStep.enterIffThen (by decide)
  have leaveThen :
      StructuredWasmStep module hostEnv inThen afterThen := by
    exact StructuredWasmStep.leaveLabel
  have loadValue :
      StructuredWasmStep module hostEnv afterThen afterValue := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, valuePublished]
  have stackSet :
      ({ locals with values := physical :: tail }.set? resultIndex physical) =
        some { nextLocals with values := physical :: tail } :=
    locals_set?_with_values (physical :: tail) targetSet
  have setResult :
      StructuredWasmStep module hostEnv afterValue
        ⟨store, .running { nextLocals with values := tail } rest, frames⟩ := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, stackSet]
  exact
    .cons loadFlag
      (.cons enterThen
        (.cons leaveThen (.cons loadValue (.cons setResult (.refl _)))))

/-- The generated cache-miss prefix loads an empty flag, enters the emitted
`else` body, and enters the selected internal initializer. -/
theorem structuredWasmLazyMissPrefixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {flagIndex valueIndex resultIndex declarationId cacheSetId : Nat}
    {function : Wasm.Function} {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    (tail : List Wasm.Value)
    (flagEmpty : store.globals.globals[flagIndex]? = some (.i32 0))
    (notImport : module.imports[declarationId]? = none)
    (functionFound :
      module.funcs[declarationId - module.imports.length]? = some function)
    (parameterCount : function.numParams = 0)
    (resultCount : function.results.length = 1) :
    FinitePath (StructuredWasmStep module hostEnv) 3
      ⟨store, .running { locals with values := tail }
        ([.globalGet flagIndex,
          .iff 0 0 [] [
            .call declarationId,
            .call cacheSetId,
            .globalSet valueIndex,
            .const 1,
            .globalSet flagIndex],
          .globalGet valueIndex,
          .localSet resultIndex] ++ rest), frames⟩
      ⟨store, .running (function.toLocals []) function.body,
        .call 1 tail { locals with values := tail } [
            .call cacheSetId,
            .globalSet valueIndex,
            .const 1,
            .globalSet flagIndex] ::
          .label 0 tail
            ([.globalGet valueIndex, .localSet resultIndex] ++ rest) ::
            frames⟩ := by
  let afterFlag : StructuredWasmState Host :=
    ⟨store, .running { locals with values := .i32 0 :: tail }
      ([.iff 0 0 [] [
          .call declarationId,
          .call cacheSetId,
          .globalSet valueIndex,
          .const 1,
          .globalSet flagIndex],
        .globalGet valueIndex,
        .localSet resultIndex] ++ rest), frames⟩
  let inElse : StructuredWasmState Host :=
    ⟨store, .running { locals with values := tail } [
        .call declarationId,
        .call cacheSetId,
        .globalSet valueIndex,
        .const 1,
        .globalSet flagIndex],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  have loadFlag :
      StructuredWasmStep module hostEnv
        ⟨store, .running { locals with values := tail }
          ([.globalGet flagIndex,
            .iff 0 0 [] [
              .call declarationId,
              .call cacheSetId,
              .globalSet valueIndex,
              .const 1,
              .globalSet flagIndex],
            .globalGet valueIndex,
            .localSet resultIndex] ++ rest), frames⟩
        afterFlag := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, flagEmpty]
  have enterElse : StructuredWasmStep module hostEnv afterFlag inElse := by
    exact StructuredWasmStep.enterIffElse
  have enterCall :
      StructuredWasmStep module hostEnv inElse
        ⟨store, .running (function.toLocals []) function.body,
          .call 1 tail { locals with values := tail } [
              .call cacheSetId,
              .globalSet valueIndex,
              .const 1,
              .globalSet flagIndex] ::
            .label 0 tail
              ([.globalGet valueIndex, .localSet resultIndex] ++ rest) ::
              frames⟩ := by
    simpa [inElse, parameterCount, resultCount] using
      (StructuredWasmStep.enterCall (env := hostEnv)
        (module := module) (store := store)
        (locals := { locals with values := tail })
        (rest := [
          .call cacheSetId,
          .globalSet valueIndex,
          .const 1,
          .globalSet flagIndex])
        (frames :=
          .label 0 tail
            (.globalGet valueIndex :: .localSet resultIndex :: rest) :: frames)
        notImport functionFound)
  exact .cons loadFlag (.cons enterElse (.cons enterCall (.refl _)))

/-- After a generated initializer returns, the structured machine performs
the value-preserving concrete cache update, publishes the physical value and
the populated flag, exits the conditional, reloads the value, and writes the
caller's destination local. -/
theorem structuredWasmLazyMissSuffixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host}
    {cacheSetId : Nat} {imp : Wasm.ImportDecl}
    {declaration : Lean.Name} {kind : AbiKind}
    {afterCall afterCache valueStore : Wasm.Store Host}
    {callerLocals calleeLocals nextLocals : Wasm.Locals}
    {physical oldValue oldFlag : Wasm.Value}
    {flagIndex valueIndex resultIndex : Nat}
    {rest : Wasm.Program} {frames : List StructuredWasmFrame}
    (tail : List Wasm.Value)
    (importFound : module.imports[cacheSetId]? = some imp)
    (hostSatisfies : hostEnv.Satisfies module spec)
    (importInBounds : cacheSetId < module.imports.length)
    (contractFound :
      spec.contracts[cacheSetId]? =
        some (cacheSetContract declaration kind))
    (parameterCount : imp.params.length = 1)
    (resultCount : imp.results.length = 1)
    (operation :
      cacheSetStep declaration kind afterCall [physical] =
        .Return [physical] afterCache)
    (valueGlobal :
      afterCache.globals.globals[valueIndex]? = some oldValue)
    (valueStoreEq :
      valueStore = writeWasmGlobal afterCache valueIndex physical)
    (flagGlobal :
      valueStore.globals.globals[flagIndex]? = some oldFlag)
    (distinct : valueIndex ≠ flagIndex)
    (targetSet : callerLocals.set? resultIndex physical = some nextLocals) :
    FinitePath (StructuredWasmStep module hostEnv) 8
      ⟨afterCall, .returning (physical :: calleeLocals.values),
        .call 1 tail { callerLocals with values := tail } [
            .call cacheSetId,
            .globalSet valueIndex,
            .const 1,
            .globalSet flagIndex] ::
          .label 0 tail
            ([.globalGet valueIndex, .localSet resultIndex] ++ rest) ::
            frames⟩
      ⟨writeWasmGlobal valueStore flagIndex (.i32 1),
        .running { nextLocals with values := tail } rest, frames⟩ := by
  let afterReturn : StructuredWasmState Host :=
    ⟨afterCall,
      .running { callerLocals with values := physical :: tail } [
        .call cacheSetId,
        .globalSet valueIndex,
        .const 1,
        .globalSet flagIndex],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let afterHost : StructuredWasmState Host :=
    ⟨afterCache,
      .running { callerLocals with values := physical :: tail } [
        .globalSet valueIndex,
        .const 1,
        .globalSet flagIndex],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let afterValue : StructuredWasmState Host :=
    ⟨valueStore,
      .running { callerLocals with values := tail }
        [.const 1, .globalSet flagIndex],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let afterConst : StructuredWasmState Host :=
    ⟨valueStore,
      .running { callerLocals with values := .i32 1 :: tail }
        [.globalSet flagIndex],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let published := writeWasmGlobal valueStore flagIndex (.i32 1)
  let afterFlag : StructuredWasmState Host :=
    ⟨published, .running { callerLocals with values := tail } [],
      .label 0 tail
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest) :: frames⟩
  let afterLabel : StructuredWasmState Host :=
    ⟨published,
      .running { callerLocals with values := tail }
        ([.globalGet valueIndex, .localSet resultIndex] ++ rest), frames⟩
  let afterReload : StructuredWasmState Host :=
    ⟨published,
      .running { callerLocals with values := physical :: tail }
        (.localSet resultIndex :: rest), frames⟩
  have returnToCaller :
      StructuredWasmStep module hostEnv
        ⟨afterCall, .returning (physical :: calleeLocals.values),
          .call 1 tail { callerLocals with values := tail } [
              .call cacheSetId,
              .globalSet valueIndex,
              .const 1,
              .globalSet flagIndex] ::
            .label 0 tail
              ([.globalGet valueIndex, .localSet resultIndex] ++ rest) ::
              frames⟩
        afterReturn := by
    simpa [afterReturn] using
      (StructuredWasmStep.returnCall (module := module) (env := hostEnv)
        (values := physical :: calleeLocals.values) (resultArity := 1)
        (callerRemainder := tail)
        (callerLocals := { callerLocals with values := tail })
        (rest := [.call cacheSetId, .globalSet valueIndex, .const 1,
          .globalSet flagIndex])
        (frames := .label 0 tail
          (.globalGet valueIndex :: .localSet resultIndex :: rest) :: frames))
  obtain ⟨hostFunction, hostFound, hostContract⟩ :=
    hostSatisfies.lookup_contract importInBounds contractFound
  have invoked :
      hostFunction.invoke afterCall [physical] =
        .Return [physical] afterCache := by
    have contract := hostContract afterCall [physical]
    change hostFunction.invoke afterCall [physical] =
      cacheSetStep declaration kind afterCall [physical] at contract
    rw [operation] at contract
    exact contract
  have callHost :
      StructuredWasmStep module hostEnv afterReturn afterHost := by
    apply StructuredWasmStep.importedCall (fuel := 1) importFound
    simp only [Wasm.execOne.eq_def, Wasm.run,
      importFound, hostFound, parameterCount, resultCount, invoked,
      List.take, List.drop, List.reverse_cons, List.reverse_nil,
      List.nil_append, List.singleton_append]
  have storeValue :
      StructuredWasmStep module hostEnv afterHost afterValue := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · subst valueStore
      simp only [Wasm.execOne.eq_def, valueGlobal, writeWasmGlobal]
  have pushFlag :
      StructuredWasmStep module hostEnv afterValue afterConst := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def]
  have storeFlag :
      StructuredWasmStep module hostEnv afterConst afterFlag := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [published, Wasm.execOne.eq_def, flagGlobal, writeWasmGlobal]
  have leaveConditional :
      StructuredWasmStep module hostEnv afterFlag afterLabel := by
    simpa [afterFlag, afterLabel, published] using
      (StructuredWasmStep.leaveLabel (module := module) (env := hostEnv)
        (resultArity := 0) (belowStack := tail)
        (rest := .globalGet valueIndex :: .localSet resultIndex :: rest)
        (store := published) (locals := { callerLocals with values := tail })
        (frames := frames))
  have valueAtValueStore :
      valueStore.globals.globals[valueIndex]? = some physical := by
    rw [valueStoreEq]
    exact writeWasmGlobal_get_self valueGlobal
  have valuePublished :
      published.globals.globals[valueIndex]? = some physical := by
    simp only [published]
    rw [writeWasmGlobal_get_ne distinct.symm]
    exact valueAtValueStore
  have reloadValue :
      StructuredWasmStep module hostEnv afterLabel afterReload := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, valuePublished]
  have stackSet :
      ({ callerLocals with values := physical :: tail }.set? resultIndex
          physical) = some { nextLocals with values := physical :: tail } :=
    locals_set?_with_values (physical :: tail) targetSet
  have setResult :
      StructuredWasmStep module hostEnv afterReload
        ⟨published, .running { nextLocals with values := tail } rest,
          frames⟩ := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, stackSet]
  exact .cons returnToCaller
    (.cons callHost
      (.cons storeValue
        (.cons pushFlag
          (.cons storeFlag
            (.cons leaveConditional
              (.cons reloadValue (.cons setResult (.refl _))))))))

/-- A successful object-case test emitted by the compiler takes five
structured steps before entering its selected arm: local read, concrete tag
import, expected-tag constant, equality, and conditional entry. -/
theorem structuredWasmObjectCaseHitPrefixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {store : Wasm.Store Host}
    {locals : Wasm.Locals} {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    {thenTarget elseTarget : Wasm.Program} {discrIndex getTagIndex : Nat}
    {imp : Wasm.ImportDecl} {word : Word32} {actualTag expectedTag : Nat}
    (tagEq : actualTag = expectedTag)
    (localFound :
      locals.get discrIndex =
        some (.i32 (UInt32.ofNat word.value)))
    (importFound : module.imports[getTagIndex]? = some imp)
    (hostSatisfies : hostEnv.Satisfies module spec)
    (importInBounds : getTagIndex < module.imports.length)
    (contractFound : spec.contracts[getTagIndex]? = some getTagContract)
    (parameterCount : imp.params.length = 1)
    (resultCount : imp.results.length = 1)
    (operation :
      getTagStep store [.i32 (UInt32.ofNat word.value)] =
        .Return [.i32 (UInt32.ofNat actualTag)] store) :
    FinitePath (StructuredWasmStep module hostEnv) 5
      ⟨store, .running locals ([
          .localGet discrIndex,
          .call getTagIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
      ⟨store, .running { locals with values := locals.values } thenTarget,
        .label 0 (locals.values.drop 0) rest :: frames⟩ := by
  let afterLocal : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat word.value) :: locals.values } ([
          .call getTagIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterHost : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat actualTag) :: locals.values } ([
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterConst : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values :=
        (.i32 (UInt32.ofNat expectedTag) ::
          .i32 (UInt32.ofNat actualTag) :: locals.values) } ([
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterEq : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values := .i32 1 :: locals.values }
        ([.iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  have loadLocal :
      StructuredWasmStep module hostEnv
        ⟨store, .running locals ([
            .localGet discrIndex,
            .call getTagIndex,
            .const (UInt32.ofNat expectedTag),
            .eq,
            .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
        afterLocal := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, localFound]
  obtain ⟨hostFunction, hostFound, hostContract⟩ :=
    hostSatisfies.lookup_contract importInBounds contractFound
  have invoked :
      hostFunction.invoke store [.i32 (UInt32.ofNat word.value)] =
        .Return [.i32 (UInt32.ofNat actualTag)] store := by
    have contract :=
      hostContract store [.i32 (UInt32.ofNat word.value)]
    change hostFunction.invoke store [.i32 (UInt32.ofNat word.value)] =
      getTagStep store [.i32 (UInt32.ofNat word.value)] at contract
    rw [operation] at contract
    exact contract
  have callHost :
      StructuredWasmStep module hostEnv afterLocal afterHost := by
    apply StructuredWasmStep.importedCall (fuel := 1) importFound
    simp only [Wasm.execOne.eq_def, Wasm.run,
      importFound, hostFound, parameterCount, resultCount, invoked,
      List.take, List.drop, List.reverse_cons, List.reverse_nil,
      List.nil_append, List.singleton_append]
  have pushExpected :
      StructuredWasmStep module hostEnv afterHost afterConst := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def]
  have compare : StructuredWasmStep module hostEnv afterConst afterEq := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp [Wasm.execOne.eq_def, tagEq]
  have enterSelected :
      StructuredWasmStep module hostEnv afterEq
        ⟨store,
          .running { locals with values := locals.values } thenTarget,
          .label 0 (locals.values.drop 0) rest :: frames⟩ := by
    simpa only [afterEq, List.singleton_append] using
      (StructuredWasmStep.enterIffThen (module := module) (env := hostEnv)
        (store := store) (locals := locals) (thenBody := thenTarget)
        (elseBody := elseTarget) (rest := rest) (frames := frames)
        (condition := 1) (by decide))
  exact .cons loadLocal
    (.cons callHost
      (.cons pushExpected
        (.cons compare (.cons enterSelected (.refl _)))))

/-- A failed object-case test takes the same five generated steps and enters
the recursively compiled remainder of the ordered case chain. -/
theorem structuredWasmObjectCaseMissPrefixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host} {store : Wasm.Store Host}
    {locals : Wasm.Locals} {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    {thenTarget elseTarget : Wasm.Program} {discrIndex getTagIndex : Nat}
    {imp : Wasm.ImportDecl} {word : Word32} {actualTag expectedTag : Nat}
    (tagNe : actualTag ≠ expectedTag)
    (actualFits : actualTag < UInt32.size)
    (expectedFits : expectedTag < UInt32.size)
    (localFound :
      locals.get discrIndex =
        some (.i32 (UInt32.ofNat word.value)))
    (importFound : module.imports[getTagIndex]? = some imp)
    (hostSatisfies : hostEnv.Satisfies module spec)
    (importInBounds : getTagIndex < module.imports.length)
    (contractFound : spec.contracts[getTagIndex]? = some getTagContract)
    (parameterCount : imp.params.length = 1)
    (resultCount : imp.results.length = 1)
    (operation :
      getTagStep store [.i32 (UInt32.ofNat word.value)] =
        .Return [.i32 (UInt32.ofNat actualTag)] store) :
    FinitePath (StructuredWasmStep module hostEnv) 5
      ⟨store, .running locals ([
          .localGet discrIndex,
          .call getTagIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
      ⟨store, .running { locals with values := locals.values } elseTarget,
        .label 0 (locals.values.drop 0) rest :: frames⟩ := by
  let afterLocal : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat word.value) :: locals.values } ([
          .call getTagIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterHost : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat actualTag) :: locals.values } ([
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterConst : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values :=
        (.i32 (UInt32.ofNat expectedTag) ::
          .i32 (UInt32.ofNat actualTag) :: locals.values) } ([
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterEq : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values := .i32 0 :: locals.values }
        ([.iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  have loadLocal :
      StructuredWasmStep module hostEnv
        ⟨store, .running locals ([
            .localGet discrIndex,
            .call getTagIndex,
            .const (UInt32.ofNat expectedTag),
            .eq,
            .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
        afterLocal := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, localFound]
  obtain ⟨hostFunction, hostFound, hostContract⟩ :=
    hostSatisfies.lookup_contract importInBounds contractFound
  have invoked :
      hostFunction.invoke store [.i32 (UInt32.ofNat word.value)] =
        .Return [.i32 (UInt32.ofNat actualTag)] store := by
    have contract :=
      hostContract store [.i32 (UInt32.ofNat word.value)]
    change hostFunction.invoke store [.i32 (UInt32.ofNat word.value)] =
      getTagStep store [.i32 (UInt32.ofNat word.value)] at contract
    rw [operation] at contract
    exact contract
  have callHost :
      StructuredWasmStep module hostEnv afterLocal afterHost := by
    apply StructuredWasmStep.importedCall (fuel := 1) importFound
    simp only [Wasm.execOne.eq_def, Wasm.run,
      importFound, hostFound, parameterCount, resultCount, invoked,
      List.take, List.drop, List.reverse_cons, List.reverse_nil,
      List.nil_append, List.singleton_append]
  have pushExpected :
      StructuredWasmStep module hostEnv afterHost afterConst := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def]
  have physicalDifferent :
      UInt32.ofNat actualTag ≠ UInt32.ofNat expectedTag := by
    intro physicalEqual
    exact tagNe <|
      (constructorTag_i32_eq_iff actualFits expectedFits).mp physicalEqual
  have compare : StructuredWasmStep module hostEnv afterConst afterEq := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp [Wasm.execOne.eq_def, physicalDifferent]
  have enterRemainder :
      StructuredWasmStep module hostEnv afterEq
        ⟨store,
          .running { locals with values := locals.values } elseTarget,
          .label 0 (locals.values.drop 0) rest :: frames⟩ := by
    simpa only [afterEq, List.singleton_append] using
      (StructuredWasmStep.enterIffElse (module := module) (env := hostEnv)
        (store := store) (locals := locals) (thenBody := thenTarget)
        (elseBody := elseTarget) (rest := rest) (frames := frames))
  exact .cons loadLocal
    (.cons callHost
      (.cons pushExpected
        (.cons compare (.cons enterRemainder (.refl _)))))

/-- A related scalar `UInt8` discriminator occupies its direct i32 lane with
exactly the semantic tag selected by FIR. -/
theorem scalarUInt8Local_eq_of_related
    {sourceFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness} {discr : Lean.FVarId}
    {discrIndex : Nat} {sourceValue : Value} {actualTag : Nat}
    (stateRelated :
      StateRelated sourceFunction sourceRuntime sourceEnv targetStore
        targetLocals witness)
    (sourceLookup : lookup sourceEnv discr = some sourceValue)
    (discrFound :
      findFVar? (functionBindings sourceFunction) discr = some discrIndex)
    (discrKind :
      (functionBindings sourceFunction)[discrIndex]?.map Prod.snd =
        some .uint8)
    (tagged : getTag sourceRuntime sourceValue = .ok actualTag) :
    targetLocals.get discrIndex =
        some (.i32 (UInt32.ofNat actualTag)) ∧
      actualTag < UInt8.size := by
  obtain ⟨index, kind, physical, found, kindAt, localValue,
      physicalRelated⟩ := stateRelated.2.2 sourceLookup
  rw [discrFound] at found
  have indexEq := Option.some.inj found
  subst index
  rw [discrKind] at kindAt
  have kindEq := Option.some.inj kindAt
  subst kind
  cases physicalRelated with
  | word32 valueRelated =>
      cases valueRelated with
      | @uint8 word value encoded =>
          have fits64 : value.toNat < UInt64.size := by
            have sizeLe : UInt8.size ≤ UInt64.size := by native_decide
            exact lt_of_lt_of_le (UInt8.toNat_lt_size value) sizeLe
          have valueToNat :
              (UInt64.ofNat value.toNat).toNat = value.toNat :=
            UInt64.toNat_ofNat_of_lt' fits64
          have tagEq : actualTag = value.toNat := by
            simpa [getTag, ScalarValue.toUInt64, ScalarValue.rawBits,
              valueToNat] using tagged.symm
          subst actualTag
          exact ⟨by simpa [Wasm.Locals.get, encoded] using localValue,
            UInt8.toNat_lt_size value⟩
  | word64 valueRelated => cases valueRelated
  | float32Bits valueRelated => cases valueRelated
  | float64Bits valueRelated => cases valueRelated

/-- A successful scalar `UInt8` constructor test takes four structured steps:
local read, expected-tag constant, equality, and conditional entry. -/
theorem structuredWasmScalarUInt8CaseHitPrefixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {rest : Wasm.Program} {frames : List StructuredWasmFrame}
    {thenTarget elseTarget : Wasm.Program} {discrIndex : Nat}
    {actualTag expectedTag : Nat}
    (tagEq : actualTag = expectedTag)
    (localFound :
      locals.get discrIndex = some (.i32 (UInt32.ofNat actualTag))) :
    FinitePath (StructuredWasmStep module hostEnv) 4
      ⟨store, .running locals ([
          .localGet discrIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
      ⟨store, .running { locals with values := locals.values } thenTarget,
        .label 0 (locals.values.drop 0) rest :: frames⟩ := by
  let afterLocal : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat actualTag) :: locals.values } ([
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterConst : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values :=
        (.i32 (UInt32.ofNat expectedTag) ::
          .i32 (UInt32.ofNat actualTag) :: locals.values) } ([
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterEq : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values := .i32 1 :: locals.values }
        ([.iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  have loadLocal :
      StructuredWasmStep module hostEnv
        ⟨store, .running locals ([
            .localGet discrIndex,
            .const (UInt32.ofNat expectedTag),
            .eq,
            .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
        afterLocal := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, localFound]
  have pushExpected :
      StructuredWasmStep module hostEnv afterLocal afterConst := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def]
  have compare : StructuredWasmStep module hostEnv afterConst afterEq := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp [Wasm.execOne.eq_def, tagEq]
  have enterSelected :
      StructuredWasmStep module hostEnv afterEq
        ⟨store,
          .running { locals with values := locals.values } thenTarget,
          .label 0 (locals.values.drop 0) rest :: frames⟩ := by
    simpa only [afterEq, List.singleton_append] using
      (StructuredWasmStep.enterIffThen (module := module) (env := hostEnv)
        (store := store) (locals := locals) (thenBody := thenTarget)
        (elseBody := elseTarget) (rest := rest) (frames := frames)
        (condition := 1) (by decide))
  exact .cons loadLocal
    (.cons pushExpected
      (.cons compare (.cons enterSelected (.refl _))))

/-- A failed scalar `UInt8` constructor test takes four steps and enters the
recursively compiled remainder of the case chain. -/
theorem structuredWasmScalarUInt8CaseMissPrefixFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {rest : Wasm.Program} {frames : List StructuredWasmFrame}
    {thenTarget elseTarget : Wasm.Program} {discrIndex : Nat}
    {actualTag expectedTag : Nat}
    (tagNe : actualTag ≠ expectedTag)
    (actualFits : actualTag < UInt8.size)
    (expectedFits : expectedTag < UInt8.size)
    (localFound :
      locals.get discrIndex = some (.i32 (UInt32.ofNat actualTag))) :
    FinitePath (StructuredWasmStep module hostEnv) 4
      ⟨store, .running locals ([
          .localGet discrIndex,
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
      ⟨store, .running { locals with values := locals.values } elseTarget,
        .label 0 (locals.values.drop 0) rest :: frames⟩ := by
  let afterLocal : StructuredWasmState Host :=
    ⟨store,
      .running { locals with
        values := .i32 (UInt32.ofNat actualTag) :: locals.values } ([
          .const (UInt32.ofNat expectedTag),
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterConst : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values :=
        (.i32 (UInt32.ofNat expectedTag) ::
          .i32 (UInt32.ofNat actualTag) :: locals.values) } ([
          .eq,
          .iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  let afterEq : StructuredWasmState Host :=
    ⟨store,
      .running { locals with values := .i32 0 :: locals.values }
        ([.iff 0 0 thenTarget elseTarget] ++ rest),
      frames⟩
  have loadLocal :
      StructuredWasmStep module hostEnv
        ⟨store, .running locals ([
            .localGet discrIndex,
            .const (UInt32.ofNat expectedTag),
            .eq,
            .iff 0 0 thenTarget elseTarget] ++ rest), frames⟩
        afterLocal := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, localFound]
  have pushExpected :
      StructuredWasmStep module hostEnv afterLocal afterConst := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def]
  have physicalDifferent :
      UInt32.ofNat actualTag ≠ UInt32.ofNat expectedTag := by
    intro physicalEqual
    exact tagNe <|
      (constructorTag_uint8_eq_iff actualFits expectedFits).mp physicalEqual
  have compare : StructuredWasmStep module hostEnv afterConst afterEq := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp [Wasm.execOne.eq_def, physicalDifferent]
  have enterRemainder :
      StructuredWasmStep module hostEnv afterEq
        ⟨store,
          .running { locals with values := locals.values } elseTarget,
          .label 0 (locals.values.drop 0) rest :: frames⟩ := by
    simpa only [afterEq, List.singleton_append] using
      (StructuredWasmStep.enterIffElse (module := module) (env := hostEnv)
        (store := store) (locals := locals) (thenBody := thenTarget)
        (elseBody := elseTarget) (rest := rest) (frames := frames))
  exact .cons loadLocal
    (.cons pushExpected
      (.cons compare (.cons enterRemainder (.refl _))))

/-- Returning through `count` nested generated case conditionals pops exactly
`count` empty-result label frames and preserves the enclosing frame stack. -/
theorem structuredWasmReturnReplicatedCaseLabelsFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {values belowStack : List Wasm.Value}
    {frames : List StructuredWasmFrame} (count : Nat) :
    FinitePath (StructuredWasmStep module hostEnv) count
      ⟨store, .returning values,
        List.replicate count (.label 0 belowStack []) ++ frames⟩
      ⟨store, .returning values, frames⟩ := by
  induction count with
  | zero => exact .refl _
  | succ count ih =>
      rw [List.replicate_succ]
      exact .cons
        (StructuredWasmStep.returnLabel
          (module := module) (env := hostEnv) (values := values)
          (resultArity := 0) (belowStack := belowStack) (rest := [])
          (store := store)
          (frames := List.replicate count
            (StructuredWasmFrame.label 0 belowStack []) ++ frames))
        ih

/-- Normal fallthrough from a selected closure-dispatch body exits every
empty-result conditional introduced by the failed matcher prefix and then the
selected matcher's conditional.  The final label restores the caller operand
tail and exposes the compiler-generated continuation; no branch or execution
certificate is needed. -/
theorem structuredWasmLeaveReplicatedClosureLabelsFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {belowStack : List Wasm.Value} {rest : Wasm.Program}
    {frames : List StructuredWasmFrame} (count : Nat) :
    FinitePath (StructuredWasmStep module hostEnv) count.succ
      ⟨store, .running locals [],
        List.replicate count (.label 0 belowStack []) ++
          .label 0 belowStack rest :: frames⟩
      ⟨store, .running { locals with values := belowStack } rest, frames⟩ := by
  induction count generalizing locals with
  | zero =>
      exact .single StructuredWasmStep.leaveLabel
  | succ count ih =>
      rw [List.replicate_succ]
      exact .cons StructuredWasmStep.leaveLabel
        (ih (locals := { locals with values := belowStack }))

/-- A recursively evaluated saturated closure callee returns through the saved
call frame, writes the selected candidate's result slot, exits every matcher
conditional, and executes the enclosing generic `let` reload/write pair.  The
result is the caller continuation with its original operand tail in exactly
`count + 5` structured steps. -/
theorem structuredWasmSaturatedCalleeReturnAndResumeFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host}
    {callerLocals calleeLocals updated : Wasm.Locals}
    {physical : Wasm.Value} {physicalArgs tail : List Wasm.Value}
    {resultIndex count : Nat} {targetRest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    (targetSet :
      callerLocals.set? resultIndex physical = some updated) :
    FinitePath (StructuredWasmStep module hostEnv) (count + 5)
      ⟨store, .returning (physical :: calleeLocals.values),
        .call 1 tail
            { callerLocals with
              values := physicalArgs.reverse ++ tail }
            [.localSet resultIndex] ::
          (List.replicate count (.label 0 tail []) ++
            .label 0 tail
                ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
              frames)⟩
      ⟨store, .running { updated with values := tail } targetRest, frames⟩ := by
  let matcherFrames : List StructuredWasmFrame :=
    List.replicate count (.label 0 tail []) ++
      .label 0 tail
          ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
        frames
  let afterReturn : StructuredWasmState Host :=
    ⟨store,
      .running { callerLocals with values := physical :: tail }
        [.localSet resultIndex],
      matcherFrames⟩
  let afterSelectedSet : StructuredWasmState Host :=
    ⟨store, .running { updated with values := tail } [], matcherFrames⟩
  let afterLabels : StructuredWasmState Host :=
    ⟨store, .running { updated with values := tail }
      ([.localGet resultIndex, .localSet resultIndex] ++ targetRest), frames⟩
  let afterReload : StructuredWasmState Host :=
    ⟨store, .running { updated with values := physical :: tail }
      (.localSet resultIndex :: targetRest), frames⟩
  have returnToCaller :
      StructuredWasmStep module hostEnv
        ⟨store, .returning (physical :: calleeLocals.values),
          .call 1 tail
              { callerLocals with
                values := physicalArgs.reverse ++ tail }
              [.localSet resultIndex] :: matcherFrames⟩
        afterReturn := by
    simpa [afterReturn, matcherFrames] using
      (StructuredWasmStep.returnCall
        (module := module) (env := hostEnv)
        (values := physical :: calleeLocals.values) (resultArity := 1)
        (callerRemainder := tail)
        (callerLocals :=
          { callerLocals with values := physicalArgs.reverse ++ tail })
        (rest := [.localSet resultIndex]) (store := store)
        (frames := matcherFrames))
  have selectedStackSet :
      ({ callerLocals with values := physical :: tail }.set? resultIndex
          physical) =
        some { updated with values := physical :: tail } :=
    locals_set?_with_values (physical :: tail) targetSet
  have writeSelected :
      StructuredWasmStep module hostEnv afterReturn afterSelectedSet := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, selectedStackSet]
  have leaveMatchers :
      FinitePath (StructuredWasmStep module hostEnv) count.succ
        afterSelectedSet afterLabels := by
    simpa only [afterSelectedSet, afterLabels, matcherFrames] using
      (structuredWasmLeaveReplicatedClosureLabelsFinitePath
        (module := module) (hostEnv := hostEnv) (store := store)
        (locals := { updated with values := tail }) (belowStack := tail)
        (rest := [.localGet resultIndex, .localSet resultIndex] ++ targetRest)
        (frames := frames) count)
  have updatedGet : updated.get resultIndex = some physical :=
    (FirTalos.Correctness.localUpdate_of_set? targetSet).1
  have updatedGetWithStack :
      ({ updated with values := tail } : Wasm.Locals).get resultIndex =
        some physical := by
    simpa [Wasm.Locals.get] using updatedGet
  have reloadResult :
      StructuredWasmStep module hostEnv afterLabels afterReload := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, updatedGetWithStack]
  have repeatedSet : updated.set? resultIndex physical = some updated :=
    locals_set?_idempotent targetSet
  have repeatedStackSet :
      ({ updated with values := physical :: tail }.set? resultIndex physical) =
        some { updated with values := physical :: tail } :=
    locals_set?_with_values (physical :: tail) repeatedSet
  have writeOuter :
      StructuredWasmStep module hostEnv afterReload
        ⟨store, .running { updated with values := tail } targetRest,
          frames⟩ := by
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, repeatedStackSet]
  have prefixPath :
      FinitePath (StructuredWasmStep module hostEnv) 2
        ⟨store, .returning (physical :: calleeLocals.values),
          .call 1 tail
              { callerLocals with
                values := physicalArgs.reverse ++ tail }
              [.localSet resultIndex] :: matcherFrames⟩
        afterSelectedSet :=
    .cons returnToCaller (.cons writeSelected (.refl _))
  have suffix :=
    (leaveMatchers.trans (.single reloadResult)).trans (.single writeOuter)
  simpa [matcherFrames, Nat.succ_eq_add_one, Nat.add_assoc,
    Nat.add_comm, Nat.add_left_comm] using (prefixPath.trans suffix)

/-- One suspended source bind frame corresponds to a saturated closure call
frame surrounded by the compiler-generated matcher labels.  Unlike the
ordinary direct-call focus, the selected body first stores its result, exits
the failed-matcher prefix and selected matcher, and then executes the generic
enclosing `let` reload/write pair. -/
structure ConcreteStructuredSaturatedBindFrameFocus
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (callerEnv : Env)
    (sourceValue : Value)
    (result : Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals calleeLocals : Wasm.Locals)
    (physicalArgs callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (kind : AbiKind)
    (physical : Wasm.Value)
    (resultIndex matcherCount : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .yielded sourceValue
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceFramesEq :
    source.frames = .bind result continuation callerEnv callerJoins ::
      sourceFrames
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .returning (physical :: calleeLocals.values)
  targetFramesEq :
    target.frames =
      .call 1 callerRemainder
          { callerLocals with
            values := physicalArgs.reverse ++ callerRemainder }
          [.localSet resultIndex] ::
        (List.replicate matcherCount (.label 0 callerRemainder []) ++
          .label 0 callerRemainder
              ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
            targetFrames)
  continuationAdapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      targetRest
  stateRelated :
    StateRelated sourceFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  frameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings sourceFunction) result = some resultIndex
  kindAt :
    (functionBindings sourceFunction)[resultIndex]?.map Prod.snd = some kind
  valueRelated : PhysicalValueRel witness kind physical sourceValue

theorem ConcreteStructuredSaturatedBindFrameFocus.observes
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.trace

/-- Resume a saturated closure call after its recursive callee has yielded.
The source takes its single bind-frame step.  The target takes the exact
`matcherCount + 5` path proved above and re-enters the adapted continuation
with the result installed in the caller local frame. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target) :
    ∃ sourceAfter targetAfter updated resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5)
          target targetAfter ∧
      callerLocals.set? resultIndex physical = some updated ∧
      resumedLocals = { updated with values := callerRemainder } ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
          sourceRuntime (bind callerEnv result sourceValue) continuation
          targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
        sourceAfter.joins = callerJoins ∧
        sourceAfter.frames = sourceFrames ∧
        targetAfter.frames = targetFrames := by
  obtain ⟨updated, targetSet, updatedAligned⟩ :=
    related.frameAligned.set?
      (nextRuntime := sourceRuntime)
      (nextEnv := bind callerEnv result sourceValue)
      (nextStore := targetStore)
      (nextWitness := witness)
      related.resultFound
  let resumedLocals : Wasm.Locals :=
    { updated with values := callerRemainder }
  have updatedRelated :
      StateRelated sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    have bound := related.stateRelated.bindPhysical related.resultFound
      related.kindAt related.valueRelated targetSet
    rw [related.stateRelated.clearFailure] at bound
    simpa [resumedLocals, StateRelated, EnvLocalsRelated, Wasm.Locals.get]
      using bound
  have resumedAligned :
      ConcreteLocalFrameAligned sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    simpa [resumedLocals, ConcreteLocalFrameAligned] using updatedAligned
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind callerEnv result sourceValue
      joins := callerJoins
      frames := sourceFrames }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running resumedLocals targetRest
      frames := targetFrames }
  refine ⟨sourceAfter, targetAfter, updated, resumedLocals, ?_, ?_, targetSet,
    rfl, ?_, ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = .yielded sourceValue at controlEq
    subst control
    have framesEq := related.sourceFramesEq
    change frames = _ at framesEq
    subst frames
    simp [sourceAfter, executeStep, coreStep]
  · rcases target with ⟨store, control, frames⟩
    have storeEq := related.targetStoreEq
    change store = targetStore at storeEq
    subst store
    have controlEq := related.targetControlEq
    change control = .returning (physical :: calleeLocals.values) at controlEq
    subst control
    have framesEq := related.targetFramesEq
    change frames = _ at framesEq
    subst frames
    simpa [targetAfter, resumedLocals] using
      (structuredWasmSaturatedCalleeReturnAndResumeFinitePath
        (module := module) (hostEnv := hostEnv) (store := targetStore)
        (callerLocals := callerLocals) (calleeLocals := calleeLocals)
        (physical := physical) (physicalArgs := physicalArgs)
        (tail := callerRemainder) (resultIndex := resultIndex)
        (count := matcherCount) (targetRest := targetRest)
        (frames := targetFrames) targetSet)
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      adapted := related.continuationAdapted
      stateRelated := updatedRelated
      frameAligned := resumedAligned }
  · simp [sourceAfter]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- Simulation-facing saturated bind rule.  A supplied source successor is
identified with the deterministic bind-frame successor constructed by
`advance`; the target path remains derived from the structured semantics. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter resumedLocals,
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5)
          target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind callerEnv result sourceValue) continuation
        targetStore resumedLocals targetRest witness sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, _updated, resumedLocals, computedStep,
      path, _targetSet, _resumedEq, focus, _sourceJoinsEq, _sourceFramesEq,
      _targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have afterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    injection computedStep
  subst computedAfter
  exact ⟨targetAfter, resumedLocals, path, focus⟩

/-- Appending one more copy to a replicated prefix is insensitive to whether
that copy is exposed at the front or at the end of the identical prefix. -/
theorem List.replicate_succ_append
    {α : Type} (value : α) (count : Nat) (tail : List α) :
    List.replicate (count + 1) value ++ tail =
      List.replicate count value ++ value :: tail := by
  induction count with
  | zero => rfl
  | succ count ih =>
      simpa only [Nat.succ_eq_add_one, List.replicate_succ,
        List.cons_append, List.cons.injEq, true_and] using ih

/-- Labels retained after selecting one arm of a generated case chain.  The
outermost test saves the physical function suffix; all nested tests save an
empty remainder because their code already lives inside that outer arm. -/
def structuredWasmCaseLabels (belowStack : List Wasm.Value)
    (rest : Wasm.Program) : Nat → List StructuredWasmFrame
  | 0 => []
  | count + 1 =>
      List.replicate count (.label 0 belowStack []) ++
        [.label 0 belowStack rest]

theorem structuredWasmCaseLabels_empty_then_outer
    (belowStack : List Wasm.Value) (rest : Wasm.Program) (count : Nat)
    (frames : List StructuredWasmFrame) :
    structuredWasmCaseLabels belowStack [] count ++
        .label 0 belowStack rest :: frames =
      structuredWasmCaseLabels belowStack rest (count + 1) ++ frames := by
  cases count with
  | zero => rfl
  | succ count =>
      simp [structuredWasmCaseLabels, List.append_assoc,
        List.replicate_succ_append]

/-- Returning from a selected case arm pops every generated conditional label,
including the outer label that stores the function-level physical suffix. -/
theorem structuredWasmReturnCaseLabelsFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {store : Wasm.Store Host} {values belowStack : List Wasm.Value}
    {rest : Wasm.Program} {frames : List StructuredWasmFrame} (count : Nat) :
    FinitePath (StructuredWasmStep module hostEnv) count
      ⟨store, .returning values,
        structuredWasmCaseLabels belowStack rest count ++ frames⟩
      ⟨store, .returning values, frames⟩ := by
  cases count with
  | zero => exact .refl _
  | succ count =>
      have emptyLabels :=
        structuredWasmReturnReplicatedCaseLabelsFinitePath
          (module := module) (hostEnv := hostEnv) (store := store)
          (values := values) (belowStack := belowStack)
          (frames := .label 0 belowStack rest :: frames) count
      have outer :
          StructuredWasmStep module hostEnv
            ⟨store, .returning values,
              .label 0 belowStack rest :: frames⟩
            ⟨store, .returning values, frames⟩ := by
        exact StructuredWasmStep.returnLabel
      simpa [structuredWasmCaseLabels, Nat.succ_eq_add_one] using
        emptyLabels.trans (.single outer)

/-- Exact structured path through an arbitrary normalized object-constructor
case chain. Production compiler inversion supplies every generated branch and
test. Source selection determines how many five-step tests execute; the
result retains one empty-result label for each of those tests. -/
theorem ConcreteSupportedFunction.objectConstructorCaseChainFinitePath
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {discr : Lean.FVarId} {alts : List (Lean.Compiler.LCNF.Alt .impure)}
    {fallback : List Fir.Wasm.Instruction} {chainTarget : Wasm.Program}
    {targetSuffix : Wasm.Program}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness} {sourceObject : Value} {actualTag : Nat}
    {frames : List StructuredWasmFrame}
    (supported : ObjectConstructorCaseAltsSupported alts)
    (modeEq : Fir.Wasm.caseDiscriminatorMode context discr = .objectTag)
    (discrCompiled :
      Fir.Wasm.getLocal context discr = .ok (.localGet discr, .tobject))
    (selection : chooseAlt actualTag alts = some selected)
    (sourceLookup : lookup sourceEnv discr = some sourceObject)
    (tagged : getTag sourceRuntime sourceObject = .ok actualTag)
    (actualFits : actualTag < UInt32.size)
    (stateRelated :
      StateRelated sourceFunction sourceRuntime sourceEnv targetStore
        targetLocals witness)
    (fallbackCompiled :
      Fir.Wasm.compileCaseFallback context alts = .ok fallback)
    (chainAdapted :
      CaseChainAdapted context sourceModule sourceFunction labels discr alts
        fallback chainTarget) :
    ∃ selectedTarget testCount,
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels selected
          selectedTarget ∧
        FinitePath (StructuredWasmStep target.wasmModule hosts.env)
          (5 * testCount)
          ⟨targetStore, .running targetLocals
            (chainTarget ++ targetSuffix), frames⟩
          ⟨targetStore,
            .running { targetLocals with values := targetLocals.values }
              selectedTarget,
            structuredWasmCaseLabels (targetLocals.values.drop 0)
                targetSuffix testCount ++ frames⟩ := by
  induction supported generalizing chainTarget targetSuffix selected frames with
  | nil =>
      simp [chooseAlt, findCtorAlt, findDefaultAlt] at selection
  | default code =>
      have selectedEq : selected = code := by
        simpa [chooseAlt, findCtorAlt, findDefaultAlt] using selection.symm
      subst selected
      have branchCompiled :
          Fir.Wasm.compileCode context code = .ok fallback := by
        simpa [Fir.Wasm.compileCaseFallback,
          Fir.Wasm.compileCaseFallbackWithM, Fir.Wasm.isDefaultAlt]
          using fallbackCompiled
      have branchAdapted :
          CodeAdapted context sourceModule sourceFunction labels code
            chainTarget :=
        ⟨fallback, branchCompiled,
          CaseChainAdapted.nil_eq
            (CaseChainAdapted.default_eq chainAdapted)⟩
      exact ⟨chainTarget ++ targetSuffix, 0,
        CodeAdapted.withSuffix (targetSuffix := targetSuffix) branchAdapted,
        .refl _⟩
  | @ctor info alts code fits rest ih =>
      have fallbackCompiledRest :
          Fir.Wasm.compileCaseFallback context alts = .ok fallback := by
        simpa [Fir.Wasm.compileCaseFallback,
          Fir.Wasm.compileCaseFallbackWithM, Fir.Wasm.isDefaultAlt]
          using fallbackCompiled
      obtain ⟨thenTarget, elseTarget, discrIndex, getTagIndex, thenAdapted,
          elseAdapted, discrFound, getTagFound, targetEq⟩ :=
        CaseChainAdapted.objectConstructor_eq modeEq fits chainAdapted
      obtain ⟨alignedIndex, alignedFound, discrKind⟩ :=
        spec.localsAligned discrCompiled
      rw [discrFound] at alignedFound
      have alignedEq : alignedIndex = discrIndex :=
        Option.some.inj alignedFound.symm
      subst alignedIndex
      obtain ⟨discrPhysical, targetLookup, physicalRelated⟩ :=
        stateRelated.resolve sourceLookup discrFound discrKind
      obtain ⟨word, physicalEq, objectRelated⟩ :
          ∃ word : Word32,
            discrPhysical = .i32 (UInt32.ofNat word.value) ∧
              ValueRel witness .tobject (.word32 word) sourceObject := by
        cases physicalRelated with
        | word32 valueRelated => exact ⟨_, rfl, valueRelated⟩
        | word64 valueRelated => cases valueRelated
        | float32Bits valueRelated => cases valueRelated
        | float64Bits valueRelated => cases valueRelated
      subst discrPhysical
      obtain ⟨imp, imported, inBounds, contracted, params, results⟩ :=
        spec.runtimeCallsAligned getTagFound
      have getTagContracted :
          hosts.spec.contracts[getTagIndex]? = some getTagContract := by
        change hosts.spec.contracts[getTagIndex]? =
          some (fun initial args result => result = getTagStep initial args)
        simpa only [resolvedContract?, hostFn?, Option.map_some, getTagFn]
          using contracted
      have parameterCount : imp.params.length = 1 := by
        change imp.params.length = 1 at params
        exact params
      have resultCount : imp.results.length = 1 := by
        change imp.results.length = 1 at results
        exact results
      have expectedFits : info.cidx < UInt32.size := by
        simpa [Fir.Wasm.constructorTagFitsI32] using fits
      have tagOperation :
          getTagStep targetStore [.i32 (UInt32.ofNat word.value)] =
            .Return [.i32 (UInt32.ofNat actualTag)] targetStore := by
        have operation :=
          getTagStep_of_refines stateRelated.1 objectRelated tagged actualFits
        rw [stateRelated.clearFailure] at operation
        exact operation
      by_cases hit : actualTag = info.cidx
      · have selectedEq : selected = code := by
          have branchEq : code = selected := by
            simpa [chooseAlt, findCtorAlt, findDefaultAlt, hit]
              using selection
          exact branchEq.symm
        subst selected
        refine ⟨thenTarget, 1, CodeAdapted.withEmptySuffix thenAdapted, ?_⟩
        rw [targetEq]
        simpa [structuredWasmCaseLabels] using
          structuredWasmObjectCaseHitPrefixFinitePath
            (module := target.wasmModule) (hostEnv := hosts.env)
            (spec := hosts.spec) (store := targetStore)
            (locals := targetLocals) (frames := frames)
            (rest := targetSuffix)
            (thenTarget := thenTarget) (elseTarget := elseTarget)
            (discrIndex := discrIndex) (getTagIndex := getTagIndex)
            (imp := imp) (word := word) (actualTag := actualTag)
            (expectedTag := info.cidx) hit targetLookup imported
            spec.hostsSatisfy inBounds getTagContracted parameterCount
            resultCount tagOperation
      · have reverseMiss : info.cidx ≠ actualTag :=
          fun equal => hit equal.symm
        have selectionRest : chooseAlt actualTag alts = some selected := by
          simpa [chooseAlt, findCtorAlt, findDefaultAlt, hit, reverseMiss]
            using selection
        let caseLabel : StructuredWasmFrame :=
          .label 0 (targetLocals.values.drop 0) targetSuffix
        obtain ⟨selectedTarget, testCount, selectedAdapted, tailPath⟩ :=
          ih selectionRest fallbackCompiledRest elseAdapted
            (targetSuffix := []) (frames := caseLabel :: frames)
        have headPath :
            FinitePath (StructuredWasmStep target.wasmModule hosts.env) 5
              ⟨targetStore, .running targetLocals
                (chainTarget ++ targetSuffix), frames⟩
              ⟨targetStore,
                .running { targetLocals with values := targetLocals.values }
                  elseTarget,
                caseLabel :: frames⟩ := by
          rw [targetEq]
          simpa [caseLabel] using
            structuredWasmObjectCaseMissPrefixFinitePath
              (module := target.wasmModule) (hostEnv := hosts.env)
              (spec := hosts.spec) (store := targetStore)
              (locals := targetLocals) (frames := frames)
              (rest := targetSuffix)
              (thenTarget := thenTarget) (elseTarget := elseTarget)
              (discrIndex := discrIndex) (getTagIndex := getTagIndex)
              (imp := imp) (word := word) (actualTag := actualTag)
              (expectedTag := info.cidx) hit actualFits expectedFits
              targetLookup imported spec.hostsSatisfy inBounds
              getTagContracted parameterCount resultCount tagOperation
        refine ⟨selectedTarget, testCount + 1, selectedAdapted, ?_⟩
        simp only [List.append_nil] at tailPath
        simpa [caseLabel, Nat.mul_add, Nat.add_comm,
          structuredWasmCaseLabels_empty_then_outer] using
            headPath.trans tailPath

/-- Exact five-step structured prefix for the singleton object-constructor
case admitted by the pointwise simulation.  This is the non-WP presentation
of the existing runtime refinement boundary: production adaptation supplies
the test, and the source selection fixes it to the hit arm. -/
theorem ConcreteSupportedFunction.singleObjectConstructorCaseFinitePath
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {frames : List StructuredWasmFrame}
    (supported : SingleObjectConstructorCaseSupported context sourceRuntime
      sourceEnv cases selected)
    (sourceResult : SourceCaseResult sourceRuntime sourceEnv cases selected)
    (stateRelated : StateRelated sourceFunction sourceRuntime sourceEnv
      targetStore targetLocals witness)
    (adapted : CodeAdaptedWithSuffix context sourceModule sourceFunction labels
      (.cases cases) targetCode) :
    ∃ selectedTarget targetSuffix,
      CodeAdapted context sourceModule sourceFunction labels selected
          selectedTarget ∧
        FinitePath (StructuredWasmStep target.wasmModule hosts.env) 5
          ⟨targetStore, .running targetLocals targetCode, frames⟩
          ⟨targetStore,
            .running { targetLocals with values := targetLocals.values }
              selectedTarget,
            structuredWasmCaseLabels (targetLocals.values.drop 0)
                targetSuffix 1 ++ frames⟩ := by
  rcases supported with
    ⟨info, altsEq, modeEq, expectedTagFits, discrCompiled, actualTagFits⟩
  rcases sourceResult with
    ⟨sourceObject, actualTag, lookupFound, tagged, chosen⟩
  have sourceLookup : lookup sourceEnv cases.discr = some sourceObject := by
    cases lookupEq : lookup sourceEnv cases.discr with
    | none => simp [lookupValue, lookupEq] at lookupFound
    | some value =>
        have valueEq : value = sourceObject := by
          simpa [lookupValue, lookupEq] using lookupFound
        subst value
        rfl
  have tagEq : actualTag = info.cidx := by
    rw [altsEq] at chosen
    simp [chooseAlt, findCtorAlt, findDefaultAlt] at chosen
    omega
  have actualFits : actualTag < UInt32.size :=
    actualTagFits lookupFound tagged
  obtain ⟨selectedTarget, discrIndex, getTagIndex, targetSuffix,
      selectedAdapted, discrFound, getTagFound, targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.singleObjectConstructorCases_eq altsEq modeEq
      expectedTagFits adapted
  obtain ⟨alignedIndex, alignedFound, discrKind⟩ :=
    spec.localsAligned discrCompiled
  rw [discrFound] at alignedFound
  have alignedEq : alignedIndex = discrIndex :=
    Option.some.inj alignedFound.symm
  subst alignedIndex
  obtain ⟨discrPhysical, targetLookup, physicalRelated⟩ :=
    stateRelated.resolve sourceLookup discrFound discrKind
  obtain ⟨word, physicalEq, objectRelated⟩ :
      ∃ word : Word32,
        discrPhysical = .i32 (UInt32.ofNat word.value) ∧
          ValueRel witness .tobject (.word32 word) sourceObject := by
    cases physicalRelated with
    | word32 valueRelated => exact ⟨_, rfl, valueRelated⟩
    | word64 valueRelated => cases valueRelated
    | float32Bits valueRelated => cases valueRelated
    | float64Bits valueRelated => cases valueRelated
  subst discrPhysical
  obtain ⟨imp, imported, inBounds, contracted, params, results⟩ :=
    spec.runtimeCallsAligned getTagFound
  have getTagContracted :
      hosts.spec.contracts[getTagIndex]? = some getTagContract := by
    change hosts.spec.contracts[getTagIndex]? =
      some (fun initial args result => result = getTagStep initial args)
    simpa only [resolvedContract?, hostFn?, Option.map_some, getTagFn] using
      contracted
  have parameterCount : imp.params.length = 1 := by
    change imp.params.length = 1 at params
    exact params
  have resultCount : imp.results.length = 1 := by
    change imp.results.length = 1 at results
    exact results
  have tagOperation :
      getTagStep targetStore [.i32 (UInt32.ofNat word.value)] =
        .Return [.i32 (UInt32.ofNat info.cidx)] targetStore := by
    have operation :=
      getTagStep_of_refines stateRelated.1 objectRelated tagged actualFits
    rw [stateRelated.clearFailure, tagEq] at operation
    exact operation
  refine ⟨selectedTarget, targetSuffix, selectedAdapted, ?_⟩
  rw [targetCodeEq]
  simpa [structuredWasmCaseLabels] using
    structuredWasmObjectCaseHitPrefixFinitePath
      (module := target.wasmModule) (hostEnv := hosts.env)
      (spec := hosts.spec) (store := targetStore) (locals := targetLocals)
      (rest := targetSuffix) (frames := frames)
      (thenTarget := selectedTarget) (elseTarget := [.unreachable])
      (discrIndex := discrIndex) (getTagIndex := getTagIndex) (imp := imp)
      (word := word) (actualTag := info.cidx) (expectedTag := info.cidx) rfl
      targetLookup imported spec.hostsSatisfy inBounds getTagContracted
      parameterCount resultCount tagOperation

/-- Exact structured path through an arbitrary normalized scalar `UInt8` case
chain. Each compiler-generated direct comparison costs four steps and retains
one empty-result label; no concrete host operation is involved. -/
theorem ConcreteSupportedFunction.scalarUInt8CaseChainFinitePath
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {target : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context sourceCode sourceModule
        sourceFunction target hosts)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {discr : Lean.FVarId} {alts : List (Lean.Compiler.LCNF.Alt .impure)}
    {fallback : List Fir.Wasm.Instruction} {chainTarget : Wasm.Program}
    {targetSuffix : Wasm.Program}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness} {sourceValue : Value} {actualTag : Nat}
    {frames : List StructuredWasmFrame}
    (supported : ScalarUInt8CaseAltsSupported alts)
    (modeEq : Fir.Wasm.caseDiscriminatorMode context discr = .scalarUInt8)
    (discrCompiled :
      Fir.Wasm.getLocal context discr = .ok (.localGet discr, .uint8))
    (selection : chooseAlt actualTag alts = some selected)
    (sourceLookup : lookup sourceEnv discr = some sourceValue)
    (tagged : getTag sourceRuntime sourceValue = .ok actualTag)
    (stateRelated :
      StateRelated sourceFunction sourceRuntime sourceEnv targetStore
        targetLocals witness)
    (fallbackCompiled :
      Fir.Wasm.compileCaseFallback context alts = .ok fallback)
    (chainAdapted :
      CaseChainAdapted context sourceModule sourceFunction labels discr alts
        fallback chainTarget) :
    ∃ selectedTarget testCount,
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels selected
          selectedTarget ∧
        FinitePath (StructuredWasmStep target.wasmModule hosts.env)
          (4 * testCount)
          ⟨targetStore, .running targetLocals
            (chainTarget ++ targetSuffix), frames⟩
          ⟨targetStore,
            .running { targetLocals with values := targetLocals.values }
              selectedTarget,
            structuredWasmCaseLabels (targetLocals.values.drop 0)
                targetSuffix testCount ++ frames⟩ := by
  induction supported generalizing chainTarget targetSuffix selected frames with
  | nil =>
      simp [chooseAlt, findCtorAlt, findDefaultAlt] at selection
  | default code =>
      have selectedEq : selected = code := by
        simpa [chooseAlt, findCtorAlt, findDefaultAlt] using selection.symm
      subst selected
      have branchCompiled :
          Fir.Wasm.compileCode context code = .ok fallback := by
        simpa [Fir.Wasm.compileCaseFallback,
          Fir.Wasm.compileCaseFallbackWithM, Fir.Wasm.isDefaultAlt]
          using fallbackCompiled
      have branchAdapted :
          CodeAdapted context sourceModule sourceFunction labels code
            chainTarget :=
        ⟨fallback, branchCompiled,
          CaseChainAdapted.nil_eq
            (CaseChainAdapted.default_eq chainAdapted)⟩
      exact ⟨chainTarget ++ targetSuffix, 0,
        CodeAdapted.withSuffix (targetSuffix := targetSuffix) branchAdapted,
        .refl _⟩
  | @ctor info alts code fits rest ih =>
      have fallbackCompiledRest :
          Fir.Wasm.compileCaseFallback context alts = .ok fallback := by
        simpa [Fir.Wasm.compileCaseFallback,
          Fir.Wasm.compileCaseFallbackWithM, Fir.Wasm.isDefaultAlt]
          using fallbackCompiled
      obtain ⟨thenTarget, elseTarget, discrIndex, thenAdapted, elseAdapted,
          discrFound, targetEq⟩ :=
        CaseChainAdapted.scalarUInt8Constructor_eq modeEq fits chainAdapted
      obtain ⟨alignedIndex, alignedFound, discrKind⟩ :=
        spec.localsAligned discrCompiled
      rw [discrFound] at alignedFound
      have alignedEq : alignedIndex = discrIndex :=
        Option.some.inj alignedFound.symm
      subst alignedIndex
      obtain ⟨localFound, actualFits⟩ :=
        scalarUInt8Local_eq_of_related stateRelated sourceLookup discrFound
          discrKind tagged
      have expectedFits : info.cidx < UInt8.size := by
        simpa [Fir.Wasm.constructorTagFitsUInt8] using fits
      by_cases hit : actualTag = info.cidx
      · have selectedEq : selected = code := by
          have branchEq : code = selected := by
            simpa [chooseAlt, findCtorAlt, findDefaultAlt, hit]
              using selection
          exact branchEq.symm
        subst selected
        refine ⟨thenTarget, 1, CodeAdapted.withEmptySuffix thenAdapted, ?_⟩
        rw [targetEq]
        simpa [structuredWasmCaseLabels] using
          structuredWasmScalarUInt8CaseHitPrefixFinitePath
            (module := target.wasmModule) (hostEnv := hosts.env)
            (store := targetStore) (locals := targetLocals) (frames := frames)
            (rest := targetSuffix)
            (thenTarget := thenTarget) (elseTarget := elseTarget)
            (discrIndex := discrIndex) (actualTag := actualTag)
            (expectedTag := info.cidx) hit localFound
      · have reverseMiss : info.cidx ≠ actualTag :=
          fun equal => hit equal.symm
        have selectionRest : chooseAlt actualTag alts = some selected := by
          simpa [chooseAlt, findCtorAlt, findDefaultAlt, hit, reverseMiss]
            using selection
        let caseLabel : StructuredWasmFrame :=
          .label 0 (targetLocals.values.drop 0) targetSuffix
        obtain ⟨selectedTarget, testCount, selectedAdapted, tailPath⟩ :=
          ih selectionRest fallbackCompiledRest elseAdapted
            (targetSuffix := []) (frames := caseLabel :: frames)
        have headPath :
            FinitePath (StructuredWasmStep target.wasmModule hosts.env) 4
              ⟨targetStore, .running targetLocals
                (chainTarget ++ targetSuffix), frames⟩
              ⟨targetStore,
                .running { targetLocals with values := targetLocals.values }
                  elseTarget,
                caseLabel :: frames⟩ := by
          rw [targetEq]
          simpa [caseLabel] using
            structuredWasmScalarUInt8CaseMissPrefixFinitePath
              (module := target.wasmModule) (hostEnv := hosts.env)
              (store := targetStore) (locals := targetLocals)
              (frames := frames) (rest := targetSuffix)
              (thenTarget := thenTarget)
              (elseTarget := elseTarget) (discrIndex := discrIndex)
              (actualTag := actualTag) (expectedTag := info.cidx) hit
              actualFits expectedFits localFound
        refine ⟨selectedTarget, testCount + 1, selectedAdapted, ?_⟩
        simp only [List.append_nil] at tailPath
        simpa [caseLabel, Nat.mul_add, Nat.add_comm,
          structuredWasmCaseLabels_empty_then_outer] using
            headPath.trans tailPath

/-- Compiler-derived structured simulation of one generated lazy-cache hit.

The source admission contains only the nullary declaration facts.  Compiler
inversion recovers the cache indices and exact conditional body; the generated
cache relation recovers the populated physical slot.  Consequently neither
the five-step target path nor any numeric index is supplied by the caller. -/
theorem ConcreteStructuredCodeFocus.advance_lazyHit_of_compiler
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (spec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value}
    {declaration : Lean.Name}
    {sourceDeclaration : Lean.Compiler.LCNF.Decl .impure}
    {resultKind : AbiKind}
    {valueCode : List Fir.Wasm.Instruction}
    {targetCode targetValue targetRest : Wasm.Program}
    {resultIndex remainingBytes : Nat}
    {targetStore : Wasm.Store Host} {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    {source : MachineState} {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (sourceJoins : source.joins = [])
    (targetCodeEq :
      targetCode = targetValue ++ .localSet resultIndex :: targetRest)
    (continuationAdapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
        targetRest)
    (supported :
      LazyCacheCallSupported context decl declaration sourceDeclaration
        resultKind)
    (generated : LazyCacheGeneratedEnvironment context sourceModule)
    (sourceStep :
      SourceLazyLetResult .hit context externals sourceRuntime sourceEnv decl
        continuation nextRuntime sourceValue)
    (invariant :
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        facts remainingBytes sourceRuntime sourceEnv targetStore targetLocals
        witness)
    (valueCompiled : Fir.Wasm.compileLetValue context decl = .ok valueCode)
    (valueAdapted :
      instructions sourceModule sourceFunction labels valueCode =
        .ok targetValue)
    (resultFound :
      findFVar? (functionBindings sourceFunction) decl.fvarId =
        some resultIndex) :
    ∃ sourceAfter targetAfter nextLocals physical,
      BudgetedCapacityPreservingLazyStep .hit facts context sourceFunction
          targetModule.wasmModule hosts.env externals decl continuation
          targetValue sourceRuntime nextRuntime sourceEnv sourceValue
          targetStore targetStore targetLocals nextLocals resultIndex witness
          witness physical 0 ∧
        reuseCapacityLetFacts? facts decl =
            some (eraseReuseCapacityFact facts decl.fvarId) ∧
          LazyCacheGlobalsRel witness sourceModule nextRuntime targetStore ∧
            FinitePath
                (fun before after =>
                  executeStep externals before = .next after)
                3 source sourceAfter ∧
              FinitePath
                  (StructuredWasmStep targetModule.wasmModule hosts.env)
                  5 target targetAfter ∧
                ConcreteStructuredCodeFocus context sourceModule sourceFunction
                    labels nextRuntime
                    (bind sourceEnv decl.fvarId sourceValue) continuation
                    targetStore { nextLocals with values := targetLocals.values }
                    targetRest witness sourceAfter targetAfter ∧
                  sourceAfter.joins = [] ∧
                    sourceAfter.frames = source.frames ∧
                      targetAfter.frames = target.frames := by
  rcases supported with
    ⟨valueEq, kindEq, targetEq, targetResultEq, _resultRefines, paramsEq,
      resultCompiled⟩
  obtain ⟨runtimeEq, semanticFound⟩ :=
    SourceLazyLetResult.hit_cacheFacts_of_valueEq valueEq targetEq paramsEq
      sourceStep
  subst nextRuntime
  obtain ⟨targetResultKind, cacheIndex, _declarationId, _cacheSetId,
      recoveredTargetResultEq, cacheEq, _declarationCall, _cacheSetCall,
      _valueCodeEq, targetValueEq⟩ :=
    compileCachedLetValue_adapted_inv context sourceModule sourceFunction labels
      decl declaration sourceDeclaration _ valueCode targetValue valueEq kindEq
      targetEq paramsEq valueCompiled valueAdapted
  have targetKindEq : targetResultKind = resultKind :=
    Option.some.inj (recoveredTargetResultEq.symm.trans targetResultEq)
  subst targetResultKind
  obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
    spec.localsAligned resultCompiled
  rw [resultFound] at alignedResultFound
  injection alignedResultFound with resultIndexEq
  subst alignedResultIndex
  rcases invariant with
    ⟨⟨⟨⟨initialRelated, _ordinary, frameAligned, _budget⟩,
      _integer, _natural, _scalar⟩, _descriptors⟩, cacheTable,
      _closureTables⟩
  obtain ⟨initializerFound, signature⟩ :=
    generated.select kindEq targetEq targetResultEq paramsEq cacheEq
  obtain ⟨physical, slot⟩ :=
    cacheTable.populatedSlot initializerFound signature semanticFound
  obtain ⟨nextLocals, targetSet, nextAligned⟩ :=
    frameAligned.set? (nextRuntime := sourceRuntime)
      (nextEnv := bind sourceEnv decl.fvarId sourceValue)
      (nextStore := targetStore) (nextWitness := witness)
      (physical := physical) resultFound
  have hit :
      BudgetedCapacityPreservingLazyStep .hit facts context sourceFunction
        targetModule.wasmModule hosts.env externals decl continuation
        [.globalGet (2 * cacheIndex),
          .iff 0 0 [] [
            .call _declarationId,
            .call _cacheSetId,
            .globalSet (2 * cacheIndex + 1),
            .const 1,
            .globalSet (2 * cacheIndex)],
          .globalGet (2 * cacheIndex + 1)]
        sourceRuntime sourceRuntime sourceEnv sourceValue targetStore targetStore
        targetLocals nextLocals resultIndex witness witness physical 0 :=
    BudgetedCapacityPreservingLazyStep.hit sourceStep
      initialRelated.stateRelated slot.flagPublished slot.valuePublished
      targetSet resultFound resultKindAt slot.valueRelated
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind sourceEnv decl.fvarId sourceValue }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running
        { nextLocals with values := targetLocals.values } targetRest }
  have sourcePath :
      FinitePath
        (fun before after => executeStep externals before = .next after)
        3 source sourceAfter := by
    rcases source with ⟨sourceProgram, sourceControl, sourceStateEnv,
      sourceStateJoins, sourceFrames, sourceStateRuntime⟩
    have programEq := related.sourceProgramEq
    change sourceProgram = context.program at programEq
    subst sourceProgram
    have controlEq := related.sourceControlEq
    change sourceControl = .code (.let decl continuation) at controlEq
    subst sourceControl
    have envEq := related.sourceEnvEq
    change sourceStateEnv = sourceEnv at envEq
    subst sourceStateEnv
    have runtimeStateEq := related.sourceRuntimeEq
    change sourceStateRuntime = sourceRuntime at runtimeStateEq
    subst sourceStateRuntime
    change sourceStateJoins = [] at sourceJoins
    subst sourceStateJoins
    have lifted :=
      FirTalos.Correctness.ExecSteps.withFrameSuffix
        (suffix := sourceFrames) sourceStep
    exact ExecSteps.toFinitePath (by
      simpa [SourceLazyLetResult, withFrameSuffix, sourceAfter] using lifted)
  have targetPath :
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 5
        target targetAfter := by
    rcases target with ⟨targetStateStore, targetControl, targetFrames⟩
    have storeEq := related.targetStoreEq
    change targetStateStore = targetStore at storeEq
    subst targetStateStore
    have controlEq := related.targetControlEq
    rw [targetCodeEq, targetValueEq] at controlEq
    change targetControl =
      .running targetLocals
        ([.globalGet (2 * cacheIndex),
          .iff 0 0 [] [
            .call _declarationId,
            .call _cacheSetId,
            .globalSet (2 * cacheIndex + 1),
            .const 1,
            .globalSet (2 * cacheIndex)],
          .globalGet (2 * cacheIndex + 1),
          .localSet resultIndex] ++ targetRest) at controlEq
    subst targetControl
    simpa [targetAfter] using
      structuredWasmLazyHitFinitePath
        (module := targetModule.wasmModule) (hostEnv := hosts.env)
        (rest := targetRest) (frames := targetFrames)
        targetLocals.values slot.flagPublished slot.valuePublished targetSet
  have focus :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
        targetStore { nextLocals with values := targetLocals.values } targetRest
        witness sourceAfter targetAfter := {
    sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
    sourceControlEq := by simp [sourceAfter]
    sourceEnvEq := by simp [sourceAfter]
    sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
    targetStoreEq := by simp [targetAfter, related.targetStoreEq]
    targetControlEq := by simp [targetAfter]
    adapted := continuationAdapted
    stateRelated := by
      simpa using hit.simulates.2.2.1.withValues targetLocals.values
    frameAligned := by
      simpa using nextAligned.withValues targetLocals.values }
  refine ⟨sourceAfter, targetAfter, nextLocals, physical, ?_, ?_, cacheTable,
    sourcePath, targetPath, focus, ?_, ?_, ?_⟩
  · simpa [targetValueEq] using hit
  · simp [reuseCapacityLetFacts?, valueEq]
  · simp [sourceAfter, sourceJoins]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- The compiler-characterized argument prefix has an exact structured-machine
execution: every generated local read or erased zero takes one atomic target
step, the source-order physical arguments are left in Wasm stack order, and
the caller operand tail, store, and continuation frames are preserved. -/
theorem ConstructorArgsReady.finitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {argumentCode : Wasm.Program} {physicalArgs : List Wasm.Value}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {rest : Wasm.Program} {frames : List StructuredWasmFrame}
    (tail : List Wasm.Value)
    (ready : ConstructorArgsReady locals argumentCode physicalArgs) :
    FinitePath (StructuredWasmStep module hostEnv) argumentCode.length
      ⟨store, .running { locals with values := tail }
        (argumentCode ++ rest), frames⟩
      ⟨store, .running
        { locals with values := physicalArgs.reverse ++ tail } rest, frames⟩ := by
  induction ready generalizing tail with
  | nil =>
      exact .refl _
  | @localGet index physical targetArgs remainingPhysical found ready ih =>
      have foundWithStack :
          ({ locals with values := tail } : Wasm.Locals).get index =
            some physical := by
        simpa [Wasm.Locals.get] using found
      have head :
          StructuredWasmStep module hostEnv
            ⟨store, .running { locals with values := tail }
              (.localGet index :: targetArgs ++ rest), frames⟩
            ⟨store, .running { locals with values := physical :: tail }
              (targetArgs ++ rest), frames⟩ := by
        apply StructuredWasmStep.atomic (fuel := 1)
        · trivial
        · simp only [Wasm.execOne.eq_def, foundWithStack]
      simpa [List.reverse_cons, List.append_assoc] using
        FinitePath.cons head (ih (tail := physical :: tail))
  | @erased targetArgs remainingPhysical ready ih =>
      have head :
          StructuredWasmStep module hostEnv
            ⟨store, .running { locals with values := tail }
              (.const 0 :: targetArgs ++ rest), frames⟩
            ⟨store, .running { locals with values := .i32 0 :: tail }
              (targetArgs ++ rest), frames⟩ := by
        apply StructuredWasmStep.atomic (fuel := 1)
        · trivial
        · simp only [Wasm.execOne.eq_def]
      simpa [List.reverse_cons, List.append_assoc] using
        FinitePath.cons head (ih (tail := .i32 0 :: tail))

/-- A proved closure capture/argument assembly contains an exact Talos
fallthrough that leaves the physical argument row on the operand stack. -/
theorem ClosureArgumentAssembly.structuredExecutes
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {argumentCode : Wasm.Program} {physicalArgs : List Wasm.Value}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {tail : List Wasm.Value}
    (assembled : ClosureArgumentAssembly module hostEnv argumentCode
      physicalArgs store locals) :
    StructuredWasmExecutes module hostEnv store
      { locals with values := tail } argumentCode
      (.fallthrough store
        { locals with values := physicalArgs.reverse ++ tail }) := by
  apply structuredWasmExecutes_exactFallthrough_of_wp
  let Q : Wasm.Assertion Host := fun continuation =>
    continuation = .Fallthrough store
      { locals with values := physicalArgs.reverse ++ tail }
  have finalWP :
      Wasm.wp module [] Q store
        { locals with values := physicalArgs.reverse ++ tail } hostEnv :=
    (Wasm.wp_nil).2 rfl
  simpa [Q] using assembled [] Q tail finalWP

/-- When compiler inversion establishes that the adapted assembly prefix is
flat, its WP law reifies to one structured target step per emitted
instruction beneath arbitrary residual code and saved control frames. -/
theorem ClosureArgumentAssembly.structuredFinitePath
    {module : Wasm.Module} {hostEnv : Wasm.HostEnv Host}
    {argumentCode : Wasm.Program} {physicalArgs : List Wasm.Value}
    {store : Wasm.Store Host} {locals : Wasm.Locals}
    {tail : List Wasm.Value} {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    (assembled : ClosureArgumentAssembly module hostEnv argumentCode
      physicalArgs store locals)
    (flat : StructuredWasmFlatProgram module argumentCode) :
    FinitePath (StructuredWasmStep module hostEnv) argumentCode.length
      ⟨store,
        .running { locals with values := tail } (argumentCode ++ rest),
        frames⟩
      ⟨store,
        .running { locals with values := physicalArgs.reverse ++ tail } rest,
        frames⟩ := by
  exact flat.finitePathWithSuffix (suffix := rest) (frames := frames)
    assembled.structuredExecutes

/-- End-to-end exact structured execution of the selected saturated-call
capture/argument prefix.  The physical row and its semantic relation are
derived from source resolution, allocation ABI alignment, concrete closure
application refinement, and the real adapter equation. -/
theorem SaturatedClosureCallResolution.argumentsStructuredFinitePath
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {sourceEnv : Env} {sourceRuntime stateRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    {labels : List Lean.FVarId}
    {initial : Wasm.Store Host} {locals : Wasm.Locals}
    {witness : RefinementWitness} {application : ClosureApplication}
    {closureIndex : Nat} {address : Word32}
    {captureKinds : Array AbiKind} {targetCode rest : Wasm.Program}
    {tail : List Wasm.Value} {frames : List StructuredWasmFrame}
    (aligned : ClosureAllocationsAbiAligned context.program witness)
    (closureFound :
      findFVar? (functionBindings sourceFunction) site.closureId =
        some closureIndex)
    (closureLocal : locals.get closureIndex =
      some (.i32 (UInt32.ofNat address.value)))
    (adapted : instructions sourceModule sourceFunction labels
      (Fir.Wasm.compileFixedClosureFields site.closureId resolution.target
          resolution.arity resolution.captures.size
            resolution.parameterKinds ++ site.argumentCode) =
        .ok targetCode)
    (applicationFound : initial.host.closureApplication? = some application)
    (applicationRelated : ClosureApplicationRel witness application address
      resolution.function resolution.arity captureKinds
        resolution.captures)
    (stateRelated : StateRelated sourceFunction stateRuntime sourceEnv initial
      locals witness)
    (failureClear : clearFailure initial = initial) :
    ∃ physicalArgs,
      ConstructorArgumentsRelated witness resolution.parameterKinds.toList
          physicalArgs (resolution.captures ++ site.semanticArgs).toList ∧
        FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCode.length
          ⟨initial,
            .running { locals with values := tail } (targetCode ++ rest),
            frames⟩
          ⟨initial,
            .running
              { locals with values := physicalArgs.reverse ++ tail } rest,
            frames⟩ := by
  obtain ⟨physicalArgs, assembled, argumentsRelated⟩ :=
    resolution.argumentsAssembly spec aligned closureFound closureLocal adapted
      applicationFound applicationRelated stateRelated failureClear
  have flat := resolution.argumentsStructuredFlatProgram spec aligned adapted
    applicationRelated
  exact ⟨physicalArgs, argumentsRelated,
    assembled.structuredFinitePath flat⟩

/-- The source half of an exactly saturated closure call reaches the resolved
callee in exactly two interpreter steps.  The first step stages the semantic
closure application and saves the caller bind frame; the second consumes the
closure and enters the resolved declaration with the combined capture and
argument environment.  All arity facts are reconstructed from the source
evaluation and compiler ABI equations. -/
theorem SaturatedClosureCallResolution.sourceStageAndEnterFinitePath
    {externals : ExternalImpl}
    {context : Fir.Wasm.Context}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {sourceEnv : Env} {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv} {sourceFrames : List Frame}
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    FinitePath
      (fun before after => executeStep externals before = .next after) 2
      { program := context.program
        control := .code (.let decl continuation)
        env := sourceEnv
        joins := callerJoins
        frames := sourceFrames
        runtime := sourceRuntime }
      { program := context.program
        control := .code resolution.calleeCode
        env := resolution.calleeEnv
        joins := []
        frames :=
          .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
        runtime := callRuntime } := by
  have semanticArgumentSize : site.semanticArgs.size = site.args.size :=
    site.semanticArgs_size
  have applicationArgumentSize :
      (resolution.captures ++ site.semanticArgs).size =
        resolution.target.params.size := by
    rw [Array.size_append, semanticArgumentSize,
      ← site.argumentKinds_size, ← resolution.parameterKinds_size]
    exact resolution.saturated
  have callArgs :
      (resolution.captures ++ site.semanticArgs).extract 0
          resolution.target.params.size =
        resolution.captures ++ site.semanticArgs := by
    rw [← applicationArgumentSize]
    exact Array.extract_size
  have semanticArgumentsPositive : 0 < site.semanticArgs.size := by
    rw [semanticArgumentSize]
    exact Array.size_pos_iff.mpr
      (Array.isEmpty_eq_false_iff.mp site.nonempty)
  have semanticArgumentsNonempty : site.semanticArgs.isEmpty = false :=
    Array.isEmpty_eq_false_iff.mpr
      (Array.size_pos_iff.mp semanticArgumentsPositive)
  have applicationArgumentsPositive :
      0 < (resolution.captures ++ site.semanticArgs).size := by
    simp only [Array.size_append]
    omega
  have applicationArgumentsNonempty :
      (resolution.captures ++ site.semanticArgs).isEmpty = false :=
    Array.isEmpty_eq_false_iff.mpr
      (Array.size_pos_iff.mp applicationArgumentsPositive)
  let staged : MachineState :=
    { program := context.program
      control := .invokeValue site.sourceClosure site.semanticArgs
      env := sourceEnv
      joins := callerJoins
      frames :=
        .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
      runtime := sourceRuntime }
  have stageStep :
      executeStep externals
          { program := context.program
            control := .code (.let decl continuation)
            env := sourceEnv
            joins := callerJoins
            frames := sourceFrames
            runtime := sourceRuntime } =
        .next staged := by
    simp [staged, executeStep, coreStep, evalLetValue, lookupValue,
      site.valueEq, site.sourceLookup, site.argumentsEvaluated,
      semanticArgumentsNonempty, pushBindFrame, Bind.bind, Except.bind,
      pure, Except.pure]
  have enterStep :
      executeStep externals staged =
        .next
          { program := context.program
            control := .code resolution.calleeCode
            env := resolution.calleeEnv
            joins := []
            frames :=
              .bind decl.fvarId continuation sourceEnv callerJoins ::
                sourceFrames
            runtime := callRuntime } := by
    simp [staged, executeStep, coreStep, invokeClosure,
      resolution.sourceClosureEq, application, invokeDecl,
      resolution.targetFound, applicationArgumentSize, callArgs,
      applicationArgumentsNonempty, resolution.parametersBound,
      resolution.bodyEq]
  exact .cons stageStep (.cons enterStep (.refl _))

/-- The second source step of an exactly saturated closure call, isolated at
the staged `.invokeValue` boundary used by the per-step simulation. -/
theorem SaturatedClosureCallResolution.sourceEnterStep
    {externals : ExternalImpl}
    {context : Fir.Wasm.Context}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {sourceEnv : Env} {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv} {sourceFrames : List Frame}
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    executeStep externals
        { program := context.program
          control := .invokeValue site.sourceClosure site.semanticArgs
          env := sourceEnv
          joins := callerJoins
          frames :=
            .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
          runtime := sourceRuntime } =
      .next
        { program := context.program
          control := .code resolution.calleeCode
          env := resolution.calleeEnv
          joins := []
          frames :=
            .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
          runtime := callRuntime } := by
  have semanticArgumentSize : site.semanticArgs.size = site.args.size :=
    site.semanticArgs_size
  have applicationArgumentSize :
      (resolution.captures ++ site.semanticArgs).size =
        resolution.target.params.size := by
    rw [Array.size_append, semanticArgumentSize,
      ← site.argumentKinds_size, ← resolution.parameterKinds_size]
    exact resolution.saturated
  have callArgs :
      (resolution.captures ++ site.semanticArgs).extract 0
          resolution.target.params.size =
        resolution.captures ++ site.semanticArgs := by
    rw [← applicationArgumentSize]
    exact Array.extract_size
  have semanticArgumentsPositive : 0 < site.semanticArgs.size := by
    rw [semanticArgumentSize]
    exact Array.size_pos_iff.mpr
      (Array.isEmpty_eq_false_iff.mp site.nonempty)
  have applicationArgumentsPositive :
      0 < (resolution.captures ++ site.semanticArgs).size := by
    simp only [Array.size_append]
    omega
  have applicationArgumentsNonempty :
      (resolution.captures ++ site.semanticArgs).isEmpty = false :=
    Array.isEmpty_eq_false_iff.mpr
      (Array.size_pos_iff.mp applicationArgumentsPositive)
  simp [executeStep, coreStep, invokeClosure, resolution.sourceClosureEq,
    application, invokeDecl, resolution.targetFound, applicationArgumentSize,
    callArgs, applicationArgumentsNonempty, resolution.parametersBound,
    resolution.bodyEq]

/-- One compiler-resolved closure matcher has an exact two-step structured
execution.  The concrete host contract determines the matcher bit and next
store; no execution witness is supplied by the caller. -/
theorem ClosureCandidateCase.matcherFinitePath
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host}
    {initial : Wasm.Store Host}
    {locals : Wasm.Locals}
    {closureId : Lean.FVarId}
    {closureIndex : Nat}
    {address : Word32}
    {tail : List Wasm.Value}
    {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    (candidate : ClosureCandidateCase sourceModule sourceFunction labels
      module spec initial closureId closureIndex address)
    (closureLocal :
      locals.get closureIndex =
        some (.i32 (UInt32.ofNat address.value)))
    (hostSatisfies : hostEnv.Satisfies module spec) :
    FinitePath (StructuredWasmStep module hostEnv) 2
      ⟨initial,
        .running { locals with values := tail }
          ([.localGet closureIndex, .call candidate.matcherIndex] ++ rest),
        frames⟩
      ⟨candidate.nextStore,
        .running { locals with values := .i32 candidate.matched :: tail }
          rest,
        frames⟩ := by
  let afterLocal : StructuredWasmState Host :=
    ⟨initial,
      .running
        { locals with
          values := .i32 (UInt32.ofNat address.value) :: tail }
        (.call candidate.matcherIndex :: rest),
      frames⟩
  have loadLocal :
      StructuredWasmStep module hostEnv
        ⟨initial,
          .running { locals with values := tail }
            ([.localGet closureIndex, .call candidate.matcherIndex] ++ rest),
          frames⟩
        afterLocal := by
    have closureLocalWithStack :
        ({ locals with values := tail } : Wasm.Locals).get closureIndex =
          some (.i32 (UInt32.ofNat address.value)) := by
      simpa [Wasm.Locals.get] using closureLocal
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, closureLocalWithStack]
  obtain ⟨hostFunction, hostFound, hostContract⟩ :=
    hostSatisfies.lookup_contract candidate.importInBounds
      candidate.contractFound
  have invoked :
      hostFunction.invoke initial
          [.i32 (UInt32.ofNat address.value)] =
        .Return [.i32 candidate.matched] candidate.nextStore := by
    have contract :=
      hostContract initial [.i32 (UInt32.ofNat address.value)]
    change hostFunction.invoke initial
        [.i32 (UInt32.ofNat address.value)] =
      closureMatchesStep candidate.function candidate.arity candidate.fixed
        initial [.i32 (UInt32.ofNat address.value)] at contract
    rw [candidate.operation] at contract
    exact contract
  have callHost :
      StructuredWasmStep module hostEnv afterLocal
        ⟨candidate.nextStore,
          .running
            { locals with values := .i32 candidate.matched :: tail } rest,
          frames⟩ := by
    apply StructuredWasmStep.importedCall (fuel := 1)
      candidate.importFound
    simp only [Wasm.execOne.eq_def, Wasm.run, candidate.importFound,
      hostFound, candidate.parameterCount, candidate.resultCount, invoked,
      List.take, List.drop, List.reverse_cons, List.reverse_nil,
      List.nil_append, List.singleton_append]
  exact .cons loadLocal (.cons callHost (.refl _))

/-- Execute exactly the matcher prefix of the compiler-generated closure
candidate fold.  Every earlier candidate evaluates to zero and contributes
one nested conditional label; the selected candidate contributes the first
nonzero matcher and leaves the machine at its body.  Candidates after the
selected one are unreachable. -/
theorem structuredWasmResolvedClosureCandidateChainSelectedPrefixFinitePath
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {spec : Wasm.HostSpec Host}
    {initial : Wasm.Store Host}
    {locals : Wasm.Locals}
    {closureId : Lean.FVarId}
    {closureIndex : Nat}
    {address : Word32}
    {tail : List Wasm.Value}
    {rest : Wasm.Program}
    {frames : List StructuredWasmFrame}
    (before : List
      (ClosureCandidateCase sourceModule sourceFunction labels module spec
        initial closureId closureIndex address))
    (selected :
      ClosureCandidateCase sourceModule sourceFunction labels module spec
        initial closureId closureIndex address)
    (suffix : List
      (ClosureCandidateCase sourceModule sourceFunction labels module spec
        initial closureId closureIndex address))
    (closureLocal :
      locals.get closureIndex =
        some (.i32 (UInt32.ofNat address.value)))
    (hostSatisfies : hostEnv.Satisfies module spec)
    (failureClear : clearFailure initial = initial)
    (beforeNonmatching :
      ∀ candidate, candidate ∈ before →
        candidate.matched = (0 : UInt32))
    (selectedMatches : (selected.matched != 0) = true) :
    FinitePath (StructuredWasmStep module hostEnv)
      (3 * (before.length + 1))
      ⟨initial,
        .running { locals with values := tail }
          (resolvedClosureCandidateChain (before ++ selected :: suffix) ++
            rest),
        frames⟩
      ⟨selected.nextStore,
        .running { locals with values := tail } selected.targetBody,
        List.replicate before.length (.label 0 tail []) ++
          .label 0 tail rest :: frames⟩ := by
  induction before generalizing rest frames with
  | nil =>
      have matcher :=
        selected.matcherFinitePath (rest :=
          .iff 0 0 selected.targetBody
              (resolvedClosureCandidateChain suffix) :: rest)
          (tail := tail) (frames := frames) closureLocal hostSatisfies
      have selectedNonzero : selected.matched ≠ 0 := by
        intro matchedZero
        rw [matchedZero] at selectedMatches
        simp at selectedMatches
      have enter :
          StructuredWasmStep module hostEnv
            ⟨selected.nextStore,
              .running
                { locals with values := .i32 selected.matched :: tail }
                (.iff 0 0 selected.targetBody
                    (resolvedClosureCandidateChain suffix) :: rest),
              frames⟩
            ⟨selected.nextStore,
              .running { locals with values := tail } selected.targetBody,
              .label 0 tail rest :: frames⟩ :=
        StructuredWasmStep.enterIffThen selectedNonzero
      simpa [resolvedClosureCandidateChain] using
        matcher.trans (.single enter)
  | cons candidate before ih =>
      have candidateNonmatching : candidate.matched = 0 :=
        beforeNonmatching candidate (by simp)
      have remainingNonmatching :
          ∀ next, next ∈ before → next.matched = (0 : UInt32) := by
        intro next found
        exact beforeNonmatching next (by simp [found])
      have candidateOperationZero :
          closureMatchesStep candidate.function candidate.arity
              candidate.fixed initial
              [.i32 (UInt32.ofNat address.value)] =
            .Return [.i32 0] candidate.nextStore := by
        simpa [candidateNonmatching] using candidate.operation
      have candidateStore : candidate.nextStore = initial :=
        (closureMatchesStep_zero_store candidateOperationZero).trans
          failureClear
      have matcher :=
        candidate.matcherFinitePath (rest :=
          .iff 0 0 candidate.targetBody
              (resolvedClosureCandidateChain (before ++ selected :: suffix)) ::
            rest)
          (tail := tail) (frames := frames) closureLocal hostSatisfies
      have enter :
          StructuredWasmStep module hostEnv
            ⟨candidate.nextStore,
              .running
                { locals with values := .i32 candidate.matched :: tail }
                (.iff 0 0 candidate.targetBody
                    (resolvedClosureCandidateChain
                      (before ++ selected :: suffix)) :: rest),
              frames⟩
            ⟨candidate.nextStore,
              .running { locals with values := tail }
                (resolvedClosureCandidateChain
                  (before ++ selected :: suffix)),
              .label 0 tail rest :: frames⟩ := by
        rw [candidateNonmatching]
        exact StructuredWasmStep.enterIffElse
      have recursive :=
        ih (rest := []) (frames := .label 0 tail rest :: frames)
          remainingNonmatching
      have recursive' :
          FinitePath (StructuredWasmStep module hostEnv)
            (3 * (before.length + 1))
            ⟨initial,
              .running { locals with values := tail }
                (resolvedClosureCandidateChain
                  (before ++ selected :: suffix)),
              .label 0 tail rest :: frames⟩
            ⟨selected.nextStore,
              .running { locals with values := tail } selected.targetBody,
              List.replicate before.length (.label 0 tail []) ++
                .label 0 tail [] :: .label 0 tail rest :: frames⟩ := by
        simpa using recursive
      have framesEq :
          List.replicate before.length (.label 0 tail []) ++
              .label 0 tail [] :: .label 0 tail rest :: frames =
            List.replicate (candidate :: before).length
                (.label 0 tail []) ++
              .label 0 tail rest :: frames := by
        have rotate : ∀ n : Nat,
            List.replicate n (StructuredWasmFrame.label 0 tail []) ++
                StructuredWasmFrame.label 0 tail [] ::
                  StructuredWasmFrame.label 0 tail rest :: frames =
              List.replicate (n + 1)
                  (StructuredWasmFrame.label 0 tail []) ++
                StructuredWasmFrame.label 0 tail rest :: frames := by
          intro n
          induction n with
          | zero => rfl
          | succ n ih =>
              simp [List.replicate_succ, ih, Nat.add_assoc]
        simpa using rotate before.length
      rw [framesEq] at recursive'
      rw [candidateStore] at matcher enter
      simpa [resolvedClosureCandidateChain, List.replicate_succ,
        List.append_assoc, Nat.mul_add, Nat.add_assoc, Nat.add_comm,
        Nat.add_left_comm] using
        (matcher.trans (.single enter)).trans recursive'

/-- Exact target-side entry for one compiler-resolved saturated closure call.
The real matcher chain selects the first matching candidate, the selected
body executes its compiler-derived capture/argument prefix, and the generated
internal call enters the callee.  The resulting call and conditional frames
record all control needed to return to the caller; neither an execution path
nor target syntax is supplied as a premise. -/
theorem
    SaturatedClosureCallResolution.targetDispatchArgumentsAndEnterFinitePath
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {sourceEnv : Env} {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      callerFunction targetModule hosts)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {initial : Wasm.Store Host} {locals : Wasm.Locals}
    {witness : RefinementWitness} {application : ClosureApplication}
    {closureIndex : Nat} {address : Word32}
    {captureKinds : Array AbiKind}
    {before : List
      (ClosureCandidateCase sourceModule callerFunction labels
        targetModule.wasmModule hosts.spec initial site.closureId closureIndex
          address)}
    {selected :
      ClosureCandidateCase sourceModule callerFunction labels
        targetModule.wasmModule hosts.spec initial site.closureId closureIndex
          address}
    {suffix : List
      (ClosureCandidateCase sourceModule callerFunction labels
        targetModule.wasmModule hosts.spec initial site.closureId closureIndex
          address)}
    {resultIndex : Nat} {argumentTarget rest : Wasm.Program}
    {tail : List Wasm.Value} {frames : List StructuredWasmFrame}
    (aligned : ClosureAllocationsAbiAligned context.program witness)
    (closureFound :
      findFVar? (functionBindings callerFunction) site.closureId =
        some closureIndex)
    (closureLocal : locals.get closureIndex =
      some (.i32 (UInt32.ofNat address.value)))
    (argumentAdapted : instructions sourceModule callerFunction labels
      (Fir.Wasm.compileFixedClosureFields site.closureId resolution.target
          resolution.arity resolution.captures.size
            resolution.parameterKinds ++ site.argumentCode) =
        .ok argumentTarget)
    (applicationFound :
      selected.nextStore.host.closureApplication? = some application)
    (applicationRelated : ClosureApplicationRel witness application address
      resolution.function resolution.arity captureKinds resolution.captures)
    (postMatcherRelated :
      StateRelated callerFunction callRuntime sourceEnv selected.nextStore
        locals witness)
    (initialFailureClear : clearFailure initial = initial)
    (selectedFailureClear :
      clearFailure selected.nextStore = selected.nextStore)
    (beforeNonmatching :
      ∀ candidate, candidate ∈ before →
        candidate.matched = (0 : UInt32))
    (selectedMatches : (selected.matched != 0) = true)
    (selectedBodyEq :
      selected.targetBody =
        argumentTarget ++
          [.call row.targetFunctionIndex, .localSet resultIndex]) :
    ∃ physicalArgs storedCallerLocals,
      ConstructorArgumentsRelated witness resolution.parameterKinds.toList
          physicalArgs (resolution.captures ++ site.semanticArgs).toList ∧
        storedCallerLocals =
          { locals with values := physicalArgs.reverse ++ tail } ∧
        FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          (3 * (before.length + 1) + argumentTarget.length + 1)
          ⟨initial,
            .running { locals with values := tail }
              (resolvedClosureCandidateChain
                  (before ++ selected :: suffix) ++ rest),
            frames⟩
          ⟨selected.nextStore,
            .running (row.targetFunction.toLocals physicalArgs)
              row.targetFunction.body,
            .call 1 tail storedCallerLocals [.localSet resultIndex] ::
              (List.replicate before.length (.label 0 tail []) ++
                .label 0 tail rest :: frames)⟩ := by
  have dispatch :=
    structuredWasmResolvedClosureCandidateChainSelectedPrefixFinitePath
      (module := targetModule.wasmModule) (hostEnv := hosts.env)
      (spec := hosts.spec) (initial := initial) (locals := locals)
      (before := before) (selected := selected) (suffix := suffix)
      (tail := tail) (rest := rest) (frames := frames) closureLocal
      spec.hostsSatisfy initialFailureClear beforeNonmatching selectedMatches
  rw [selectedBodyEq] at dispatch
  obtain ⟨physicalArgs, argumentsRelated, argumentPath⟩ :=
    resolution.argumentsStructuredFinitePath spec aligned closureFound
      closureLocal argumentAdapted applicationFound applicationRelated
      postMatcherRelated selectedFailureClear
      (tail := tail)
      (rest := [.call row.targetFunctionIndex, .localSet resultIndex])
      (frames :=
        List.replicate before.length (.label 0 tail []) ++
          .label 0 tail rest :: frames)
  let storedCallerLocals : Wasm.Locals :=
    { locals with values := physicalArgs.reverse ++ tail }
  have parameterCount :
      physicalArgs.reverse.length = row.targetFunction.numParams :=
    row.targetParameterCount_saturatedClosure site resolution argumentsRelated
  have takeParameters :
      storedCallerLocals.values.take row.targetFunction.numParams =
        physicalArgs.reverse := by
    simp [storedCallerLocals, ← parameterCount]
  have dropParameters :
      storedCallerLocals.values.drop row.targetFunction.numParams = tail := by
    simp [storedCallerLocals, ← parameterCount]
  have enter :
      StructuredWasmStep targetModule.wasmModule hosts.env
        ⟨selected.nextStore,
          .running storedCallerLocals
            [.call row.targetFunctionIndex, .localSet resultIndex],
          List.replicate before.length (.label 0 tail []) ++
            .label 0 tail rest :: frames⟩
        ⟨selected.nextStore,
          .running (row.targetFunction.toLocals physicalArgs)
            row.targetFunction.body,
          .call 1 tail storedCallerLocals [.localSet resultIndex] ::
            (List.replicate before.length (.label 0 tail []) ++
              .label 0 tail rest :: frames)⟩ := by
    simpa only [takeParameters, dropParameters, row.singleResult,
      List.reverse_reverse] using
      (StructuredWasmStep.enterCall
        (module := targetModule.wasmModule) (env := hosts.env)
        (id := row.targetFunctionIndex) (function := row.targetFunction)
        (store := selected.nextStore) (locals := storedCallerLocals)
        (rest := [.localSet resultIndex])
        (frames :=
          List.replicate before.length (.label 0 tail []) ++
            .label 0 tail rest :: frames)
        row.notImport row.targetFunctionFound)
  refine ⟨physicalArgs, storedCallerLocals, argumentsRelated, rfl, ?_⟩
  simpa only [storedCallerLocals, Nat.add_assoc] using
    (dispatch.trans argumentPath).trans (.single enter)

/-- Relation after the source stages an exactly saturated closure invocation
and before either machine consumes the closure.  The target deliberately stays
at the compiler-produced dispatch prefix: matching this source step by a
reflexive target path keeps the unchanged source runtime related to the
unchanged concrete store.  The following source step can then be matched by
the complete matcher/argument/callee-entry target path. -/
structure ConcreteStructuredSaturatedCallReadyFocus
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (targetValue targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq :
    source.control = .invokeValue site.sourceClosure site.semanticArgs
  sourceEnvEq : source.env = callerEnv
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceJoinsEq : source.joins = callerJoins
  sourceFramesEq :
    source.frames =
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .running callerLocals
      (targetValue ++ .localSet resultIndex :: targetRest)
  targetFramesEq : target.frames = targetFrames
  valueAdapted :
    instructions sourceModule sourceFunction labels
        (compileClosureDispatch context decl.fvarId site.closureId
          site.resultKind site.argumentCode site.argumentKinds) =
      .ok targetValue
  continuationAdapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      targetRest
  callerStateRelated :
    StateRelated sourceFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  callerFrameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings sourceFunction) decl.fvarId = some resultIndex
  resultKindAt :
    (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
      some site.resultKind

theorem ConcreteStructuredSaturatedCallReadyFocus.observes
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : SaturatedClosureCallSite context decl callerEnv}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
      sourceFunction targetModule site labels sourceRuntime continuation
      callerJoins sourceFrames targetStore callerLocals targetValue targetRest
      targetFrames witness resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.trace

/-- Stage one exactly saturated closure call at an individual source-step
boundary.  Production lowering and adaptation determine the untouched target
dispatch; the target takes zero steps, so no matcher effect is observed before
the source consumes the closure. -/
theorem ConcreteStructuredCodeFocus.advance_saturatedCall_stage
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime callerEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target) :
    ∃ targetValue targetRest resultIndex sourceAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule
        hostEnv) 0 target target ∧
      ConcreteStructuredSaturatedCallReadyFocus context sourceModule
        sourceFunction targetModule site labels sourceRuntime continuation
        source.joins source.frames targetStore targetLocals targetValue
        targetRest target.frames witness resultIndex sourceAfter target := by
  obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
      valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.let_eq related.adapted
  have expectedCompiled :
      Fir.Wasm.compileLetValue context decl =
        .ok (compileClosureDispatch context decl.fvarId site.closureId
          site.resultKind site.argumentCode site.argumentKinds) := by
    simp [Fir.Wasm.compileLetValue, Fir.Wasm.letValueKind, site.valueEq,
      site.kindEq, site.closureCompiled, site.argumentsCompiled, site.nonempty,
      Bind.bind, Except.bind, pure, Except.pure]
  have valueCodeEq :
      valueCode =
        compileClosureDispatch context decl.fvarId site.closureId
          site.resultKind site.argumentCode site.argumentKinds := by
    rw [expectedCompiled] at valueCompiled
    exact (Except.ok.inj valueCompiled).symm
  subst valueCode
  obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
    localsAligned site.resultCompiled
  rw [resultFound] at alignedResultFound
  have resultIndexEq : alignedResultIndex = resultIndex :=
    (Option.some.inj alignedResultFound).symm
  subst alignedResultIndex
  let sourceAfter : MachineState :=
    { source with
      control := .invokeValue site.sourceClosure site.semanticArgs
      frames :=
        .bind decl.fvarId continuation source.env source.joins :: source.frames }
  refine ⟨targetValue, targetRest, resultIndex, sourceAfter, ?_, .refl target,
    ?_⟩
  · rcases source with ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = .code (.let decl continuation) at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = callerEnv at envEq
    subst env
    have runtimeEq := related.sourceRuntimeEq
    change runtime = sourceRuntime at runtimeEq
    subst runtime
    have semanticArgumentsNonempty : site.semanticArgs.isEmpty = false := by
      rw [Array.isEmpty_eq_false_iff, ← Array.size_pos_iff,
        site.semanticArgs_size]
      exact Array.size_pos_iff.mpr
        (Array.isEmpty_eq_false_iff.mp site.nonempty)
    simp [sourceAfter, executeStep, coreStep, evalLetValue, lookupValue,
      site.valueEq, site.sourceLookup, site.argumentsEvaluated,
      semanticArgumentsNonempty, pushBindFrame, Bind.bind, Except.bind,
      pure, Except.pure]
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      sourceJoinsEq := by simp [sourceAfter]
      sourceFramesEq := by simp [sourceAfter, related.sourceEnvEq]
      targetStoreEq := related.targetStoreEq
      targetControlEq := by
        rw [related.targetControlEq, targetCodeEq]
      targetFramesEq := rfl
      valueAdapted
      continuationAdapted
      callerStateRelated := related.stateRelated
      callerFrameAligned := related.frameAligned
      resultFound
      resultKindAt }

/-- Relation immediately after both machines enter a compiler-resolved
saturated closure callee.  The callee code focus is accompanied by the exact
saved source bind frame, target call frame, failed-matcher labels, and selected
matcher label that must survive recursive effects until return. -/
structure ConcreteStructuredSaturatedCallEntryFocus
    (callerContext calleeContext : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (callerFunction calleeFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite callerContext decl callerEnv)
    (resolution : SaturatedClosureCallResolution callerContext sourceRuntime
      site)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (resultIndex matcherCount : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  calleeFocus :
    ConcreteStructuredCodeFocus calleeContext sourceModule calleeFunction []
      sourceRuntime resolution.calleeEnv resolution.calleeCode targetStore
      (row.targetFunction.toLocals physicalArgs) row.targetFunction.body witness
      source target
  sourceJoinsEq : source.joins = []
  sourceFramesEq :
    source.frames =
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
  targetFramesEq :
    target.frames =
      .call 1 callerRemainder
          { callerLocals with
            values := physicalArgs.reverse ++ callerRemainder }
          [.localSet resultIndex] ::
        (List.replicate matcherCount (.label 0 callerRemainder []) ++
          .label 0 callerRemainder
              ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
            targetFrames)
  continuationAdapted :
    CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels continuation
      targetRest
  callerStateRelated :
    StateRelated callerFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  callerFrameAligned :
    ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings callerFunction) decl.fvarId = some resultIndex
  resultKindAt :
    (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
      some site.resultKind
  argumentsRelated :
    ConstructorArgumentsRelated witness resolution.parameterKinds.toList
      physicalArgs (resolution.captures ++ site.semanticArgs).toList

theorem ConcreteStructuredSaturatedCallEntryFocus.observes
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite callerContext decl callerEnv}
    {resolution : SaturatedClosureCallResolution callerContext sourceRuntime
      site}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (entry : ConcreteStructuredSaturatedCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site resolution row labels sourceRuntime continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs resultIndex matcherCount source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  entry.calleeFocus.observes

/-- Consume a staged exactly saturated closure invocation and enter the
compiler-selected generated callee.  The source takes its single closure
consumption step.  The target independently resolves the first matching
compiler candidate, executes the capture/argument prefix, and enters the
generated function.  All target code and execution are derived here; callers
supply only source resolution/admission facts and the ordinary concrete cache
and ABI frame. -/
theorem ConcreteStructuredSaturatedCallReadyFocus.advance_enter
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
      sourceFunction targetModule site labels sourceRuntime continuation
      callerJoins sourceFrames targetStore callerLocals targetValue targetRest
      targetFrames witness resultIndex source target)
    (invariant : ConcreteReuseCapacityCacheAbiFrame context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime callerEnv
      targetStore callerLocals witness)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    ∃ sourceEntry targetEntry nextStore physicalArgs matcherCount
        argumentCount,
      executeStep externals source = .next sourceEntry ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (matcherCount + 1) + argumentCount + 1) target targetEntry ∧
      ConcreteStructuredSaturatedCallEntryFocus context calleeContext
        sourceModule sourceFunction calleeFunction targetModule site resolution
        row labels callRuntime continuation callerJoins sourceFrames nextStore
        callerLocals callerLocals.values targetRest targetFrames witness
        physicalArgs resultIndex matcherCount sourceEntry targetEntry ∧
      ClosureMatchStoreFrame targetStore nextStore ∧
      HeaderCapacityTransport targetStore.host.runtime.heap
        nextStore.host.runtime.heap witness ∧
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        facts remainingBytes callRuntime callerEnv nextStore callerLocals
        witness := by
  obtain ⟨closureIndex, closureFound, closureKindAt⟩ :=
    spec.localsAligned site.closureCompiled
  obtain ⟨address, closureLocal, mapped⟩ :=
    invariant.cacheFrame.resolveClosureAddress site resolution closureFound
      closureKindAt
  obtain ⟨staticCandidates, staticCandidatesEq⟩ :=
    spec.saturatedClosureCandidateAdapterResolver site related.valueAdapted
  obtain ⟨candidates, executedSources⟩ :=
    ClosureCandidateAdapterCase.executeAll_of_refines staticCandidates
      invariant.cacheFrame.stateRelated.stateRelated.1
      invariant.cacheFrame.2.2.dispatch
      invariant.cacheFrame.2.2.descriptors.symm mapped resolution.cellFound
      resolution.cellLive resolution.cellObjectEq sharedCapacity application
  have candidatesEq := staticCandidatesEq.trans executedSources
  obtain ⟨before, selected, suffix, candidatesSplit, _selectedSource,
      beforeNonmatching, selectedMatches, selectedIdentity⟩ :=
    invariant.cacheFrame.closureCandidates_exists_first_match_of_resolution
      site resolution closureLocal mapped candidates candidatesEq
  have selectedMem : selected ∈ candidates := by
    rw [candidatesSplit]
    simp
  obtain ⟨_matcherIdentity, _applicationSnapshot, captureKinds,
      applicationFound, applicationRelated, matcherFrame, matcherCapacity,
      postMatcher⟩ :=
    invariant.cacheFrame.selectedClosureMatcher selected mapped
      resolution.cellFound resolution.cellLive resolution.cellObjectEq
      selectedMatches sharedCapacity application
  obtain ⟨functionIndex, argumentTarget, callFound, argumentAdapted,
      selectedBodyEq⟩ :=
    resolution.candidateBody_of_identity site spec candidatesEq selectedMem
      selectedIdentity related.resultFound
  have exactCallIndex :
      callIndex? sourceModule (.declaration resolution.function) =
        some row.targetFunctionIndex := by
    simpa [resolution.targetName] using row.callIndexEq
  rw [exactCallIndex] at callFound
  have functionIndexEq : row.targetFunctionIndex = functionIndex :=
    Option.some.inj callFound
  subst functionIndex
  have dispatchAdapted :
      instructions sourceModule sourceFunction labels
          (compileClosureDispatch context decl.fvarId site.closureId
            site.resultKind site.argumentCode site.argumentKinds) =
        .ok
          (resolvedClosureCandidateChain (before ++ selected :: suffix) ++
            [.localGet resultIndex]) := by
    have generatedEq := candidatesEq
    rw [candidatesSplit] at generatedEq
    exact instructions_compileClosureDispatch (before ++ selected :: suffix)
      generatedEq closureFound related.resultFound
  have targetValueEq :
      targetValue =
        resolvedClosureCandidateChain (before ++ selected :: suffix) ++
          [.localGet resultIndex] := by
    have adapted := related.valueAdapted
    rw [dispatchAdapted] at adapted
    exact (Except.ok.inj adapted).symm
  obtain ⟨physicalArgs, storedCallerLocals, argumentsRelated,
      storedCallerEq, targetCanonicalPath⟩ :=
    resolution.targetDispatchArgumentsAndEnterFinitePath spec row
      invariant.closureAbi closureFound closureLocal argumentAdapted
      applicationFound applicationRelated postMatcher.stateRelated.1
      related.callerStateRelated.clearFailure
      postMatcher.stateRelated.1.clearFailure beforeNonmatching selectedMatches
      selectedBodyEq (tail := callerLocals.values)
      (rest := [.localGet resultIndex, .localSet resultIndex] ++ targetRest)
      (frames := target.frames)
  let sourceEntry : MachineState := {
    program := context.program
    control := .code resolution.calleeCode
    env := resolution.calleeEnv
    joins := []
    frames :=
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
    runtime := callRuntime }
  let targetEntry : StructuredWasmState Host := {
    store := selected.nextStore
    control := .running (row.targetFunction.toLocals physicalArgs)
      row.targetFunction.body
    frames :=
      .call 1 callerLocals.values storedCallerLocals [.localSet resultIndex] ::
        (List.replicate before.length
            (.label 0 callerLocals.values []) ++
          .label 0 callerLocals.values
              ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
            targetFrames) }
  have sourceStep : executeStep externals source = .next sourceEntry := by
    rcases source with
      ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv, actualFrames,
        actualRuntime⟩
    have sourceProgramEq := related.sourceProgramEq
    change sourceProgram = context.program at sourceProgramEq
    subst sourceProgram
    have sourceControlEq := related.sourceControlEq
    change sourceControl =
      .invokeValue site.sourceClosure site.semanticArgs at sourceControlEq
    subst sourceControl
    have sourceEnvEq := related.sourceEnvEq
    change actualEnv = callerEnv at sourceEnvEq
    subst actualEnv
    have sourceRuntimeEq := related.sourceRuntimeEq
    change actualRuntime = sourceRuntime at sourceRuntimeEq
    subst actualRuntime
    have sourceJoinsEq := related.sourceJoinsEq
    change sourceJoinEnv = callerJoins at sourceJoinsEq
    subst sourceJoinEnv
    have sourceFramesEq := related.sourceFramesEq
    change actualFrames =
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
      at sourceFramesEq
    subst actualFrames
    simpa [sourceEntry] using
      (resolution.sourceEnterStep (externals := externals)
        (continuation := continuation) (callerJoins := callerJoins)
        (sourceFrames := sourceFrames) application)
  have targetPath :
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (before.length + 1) + argumentTarget.length + 1) target
        targetEntry := by
    rcases target with ⟨actualStore, actualControl, actualFrames⟩
    have targetStoreEq := related.targetStoreEq
    change actualStore = targetStore at targetStoreEq
    subst actualStore
    have targetControlEq := related.targetControlEq
    rw [targetValueEq] at targetControlEq
    have targetControlNormalized :
        actualControl = .running callerLocals
          (resolvedClosureCandidateChain
              (before ++ selected :: suffix) ++
            .localGet resultIndex :: .localSet resultIndex :: targetRest) := by
      simpa only [List.append_assoc, List.singleton_append] using
        targetControlEq
    subst actualControl
    have targetFramesEq := related.targetFramesEq
    change actualFrames = targetFrames at targetFramesEq
    subst actualFrames
    simpa [targetEntry, List.append_assoc] using targetCanonicalPath
  have calleeFrame :
      ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals []
        remainingBytes callRuntime resolution.calleeEnv selected.nextStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    postMatcher.generatedSaturatedClosureCalleeEntryAtCost site resolution row
      argumentsRelated (Nat.le_refl remainingBytes)
  have entry :
      ConcreteStructuredSaturatedCallEntryFocus context calleeContext
        sourceModule sourceFunction calleeFunction targetModule site resolution
        row labels callRuntime continuation callerJoins sourceFrames
        selected.nextStore callerLocals callerLocals.values targetRest
        targetFrames witness physicalArgs resultIndex before.length sourceEntry
        targetEntry := {
    calleeFocus := {
      sourceProgramEq := row.contextProgram.symm
      sourceControlEq := by simp [sourceEntry]
      sourceEnvEq := by simp [sourceEntry]
      sourceRuntimeEq := by simp [sourceEntry]
      targetStoreEq := by simp [targetEntry]
      targetControlEq := by simp [targetEntry]
      adapted := by
        rw [row.targetBodyEq]
        exact CodeAdapted.withSuffix row.bodyAdapted
      stateRelated := calleeFrame.1.1.1.1.1
      frameAligned := calleeFrame.1.1.1.2.2.1 }
    sourceJoinsEq := by simp [sourceEntry]
    sourceFramesEq := by simp [sourceEntry]
    targetFramesEq := by simp [targetEntry, storedCallerEq]
    continuationAdapted := related.continuationAdapted
    callerStateRelated := postMatcher.1.1.1.1.1
    callerFrameAligned := postMatcher.1.1.1.2.2.1
    resultFound := related.resultFound
    resultKindAt := related.resultKindAt
    argumentsRelated }
  exact ⟨sourceEntry, targetEntry, selected.nextStore, physicalArgs,
    before.length, argumentTarget.length, sourceStep, targetPath, entry,
    matcherFrame, matcherCapacity, postMatcher⟩

/-- Strengthen saturated entry with the canonical hereditary cache frame.  The
post-matcher caller frame already carries closure-consumption effects; entry
only re-indexes its locals to the generated callee row. -/
theorem
    ConcreteStructuredSaturatedCallEntryFocus.calleeEntryRelativeCacheFrame
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {originalRuntime sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite callerContext decl callerEnv}
    {resolution : SaturatedClosureCallResolution callerContext originalRuntime
      site}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (entry : ConcreteStructuredSaturatedCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site resolution row labels sourceRuntime continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs resultIndex matcherCount source target)
    (callerFrame :
      ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals
        facts remainingBytes sourceRuntime callerEnv targetStore callerLocals
        witness) :
    ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals)
      sourceRuntime targetStore witness [] remainingBytes sourceRuntime
      resolution.calleeEnv targetStore
      (row.targetFunction.toLocals physicalArgs) witness := by
  exact
    ⟨callerFrame.generatedSaturatedClosureCalleeEntryAtCost site resolution
      row entry.argumentsRelated (Nat.le_refl remainingBytes),
      ReuseCapacityCodeEntryTransports.refl sourceRuntime targetStore witness⟩

/-- Close a saturated recursive callee at a related yield.  Entry-relative
transports reinterpret the saved caller locals in the evolved store/witness;
the explicit frame equalities retain the call and matcher-label stack required
by `ConcreteStructuredSaturatedBindFrameFocus.advance`. -/
theorem
    ConcreteStructuredSaturatedCallEntryFocus.bindFrame_of_yield_cacheFrame
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {originalRuntime entryRuntime currentRuntime : RuntimeState}
    {site : SaturatedClosureCallSite callerContext decl callerEnv}
    {resolution : SaturatedClosureCallResolution callerContext originalRuntime
      site}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryStore currentStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {entryWitness currentWitness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex matcherCount : Nat}
    {entrySource : MachineState}
    {entryTarget : StructuredWasmState Host}
    {currentEnv : Env}
    {sourceValue : Value}
    {actualKind : AbiKind}
    {physical : Wasm.Value}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {resultFacts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (entry : ConcreteStructuredSaturatedCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site resolution row labels entryRuntime continuation callerJoins
      sourceFrames entryStore callerLocals callerRemainder targetRest
      targetFrames entryWitness physicalArgs resultIndex matcherCount
      entrySource entryTarget)
    (yielded : ConcreteStructuredYieldFocus calleeContext calleeFunction
      currentRuntime currentEnv sourceValue currentStore calleeLocals
      currentWitness actualKind physical source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals)
        entryRuntime entryStore entryWitness resultFacts remainingBytes
        currentRuntime currentEnv currentStore calleeLocals currentWitness)
    (sourceFramesEq :
      source.frames =
        .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames)
    (targetFramesEq :
      target.frames =
        .call 1 callerRemainder
            { callerLocals with
              values := physicalArgs.reverse ++ callerRemainder }
            [.localSet resultIndex] ::
          (List.replicate matcherCount (.label 0 callerRemainder []) ++
            .label 0 callerRemainder
                ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
              targetFrames))
    (resultRefines : actualKind.refines site.resultKind = true) :
    ConcreteStructuredSaturatedBindFrameFocus callerContext sourceModule
      callerFunction labels currentRuntime callerEnv sourceValue decl.fvarId
      continuation callerJoins sourceFrames currentStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames
      currentWitness site.resultKind physical resultIndex matcherCount source
      target := by
  have callerStateRelated :
      StateRelated callerFunction currentRuntime callerEnv currentStore
        callerLocals currentWitness :=
    ⟨yielded.stateRelated.1, yielded.stateRelated.2.1,
      EnvLocalsRelated.witnessTransport invariant.2.witness
        entry.callerStateRelated.2.2⟩
  have callerFrameAligned :
      ConcreteLocalFrameAligned callerFunction currentRuntime callerEnv
        currentStore callerLocals currentWitness := by
    simpa [ConcreteLocalFrameAligned] using entry.callerFrameAligned
  exact {
    sourceProgramEq := yielded.sourceProgramEq.trans row.contextProgram
    sourceControlEq := yielded.sourceControlEq
    sourceRuntimeEq := yielded.sourceRuntimeEq
    sourceFramesEq
    targetStoreEq := yielded.targetStoreEq
    targetControlEq := yielded.targetControlEq
    targetFramesEq
    continuationAdapted := entry.continuationAdapted
    stateRelated := callerStateRelated
    frameAligned := callerFrameAligned
    resultFound := entry.resultFound
    kindAt := entry.resultKindAt
    valueRelated := yielded.valueRelated.ofRefines resultRefines }

/-- Production argument compilation preserves the source/ABI row length. -/
theorem ConstructorArgsCompiled.argumentLength
    {context : Fir.Wasm.Context}
    {args : List (Lean.Compiler.LCNF.Arg .impure)}
    {argumentCode : List Fir.Wasm.Instruction}
    {kinds : List AbiKind}
    (compiled : ConstructorArgsCompiled context args argumentCode kinds) :
    args.length = kinds.length := by
  induction compiled with
  | nil => rfl
  | erased _ ih => simp [ih]
  | fvar _ _ ih => simp [ih]

/-- Operation-specific source result retained behind the common external-call
staging boundary.  It is proof-side data reconstructed from production
admission and selects the exact ABI result, response, and allocation cost. -/
inductive PureExternalResultShape where
  | integer (value : Int)
  | natural (value : Nat)
  | scalar (value : BoxedScalar)

def PureExternalResultShape.resultKind : PureExternalResultShape → AbiKind
  | .integer _ | .natural _ => .tobject
  | .scalar value => value.kind.abiKind

def PureExternalResultShape.response
    (sourceRuntime : RuntimeState) : PureExternalResultShape → ExternalResponse
  | .integer value => semanticIntegerExternalResponse sourceRuntime value
  | .natural value => semanticNaturalExternalResponse sourceRuntime value
  | .scalar value => semanticScalarExternalResponse sourceRuntime value

def PureExternalResultShape.cost : PureExternalResultShape → Nat
  | .integer value => integerAllocationBytes value
  | .natural value => naturalAllocationBytes value
  | .scalar _ => 0

/-- Common compiler and source-semantic shape of every currently admitted
pure external call.  Integer, natural, and scalar results differ only after
the imported call executes; their first source step and generated argument
prefix have this exact shared boundary. -/
structure PureExternalCallShape
    (context : Fir.Wasm.Context) (externals : ExternalImpl)
    (sourceRuntime : RuntimeState) (sourceEnv : Env)
    (decl : Lean.Compiler.LCNF.LetDecl .impure)
    (nextRuntime : RuntimeState) (sourceValue : Value) (stepCost : Nat) where
  name : Lean.Name
  args : Array (Lean.Compiler.LCNF.Arg .impure)
  argumentCode : List Fir.Wasm.Instruction
  argumentKinds : Array AbiKind
  semanticArgs : Array Value
  declaration : Lean.Compiler.LCNF.Decl .impure
  resultShape : PureExternalResultShape
  resultKind : AbiKind
  response : ExternalResponse
  valueEq : decl.value = .fap name args
  nonempty : args.isEmpty = false
  declarationFound : context.program.findDecl? name = some declaration
  declarationExternal : ∃ metadata, declaration.value = .extern metadata
  valueKind : Fir.Wasm.letValueKind decl = .ok resultKind
  argumentsCompiled :
    Fir.Wasm.compileArgs context args = .ok (argumentCode, argumentKinds)
  argumentsEvaluated : evalArgs sourceEnv args = .ok semanticArgs
  signature :
    ExternalTypes.signature {
      params := declaration.params.map (·.type)
      result := declaration.type } =
        .ok { params := argumentKinds, results := #[resultKind] }
  resultCompiled :
    Fir.Wasm.getLocal context decl.fvarId =
      .ok (.localGet decl.fvarId, resultKind)
  semanticCalled :
    externals.call (declarationExternalRequest declaration semanticArgs)
        sourceRuntime = .ok response
  nextRuntimeEq :
    nextRuntime =
      semanticExternalRuntimeAfter
        (declarationExternalRequest declaration semanticArgs) sourceRuntime
        response
  sourceValueEq : sourceValue = response.value
  resultKindEq : resultKind = resultShape.resultKind
  responseEq : response = resultShape.response sourceRuntime
  stepCostEq : stepCost = resultShape.cost

/-- Forget only the operation-specific result representation while retaining
all facts needed to stage a pure external request.  This is a projection from
the production admission relation, not an additional admission premise. -/
theorem PureExternalSupported.callShapeExists
    {context : Fir.Wasm.Context} {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value} {stepCost : Nat}
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost) :
    Nonempty
      (PureExternalCallShape context externals sourceRuntime sourceEnv decl
        nextRuntime sourceValue stepCost) := by
  rcases supported with integer | natural | scalar
  · rcases integer with
      ⟨name, args, argumentCode, argumentKinds, semanticArgs, declaration,
        value, valueEq, _operation, nonempty, declarationFound,
        declarationExternal, valueKind, argumentsCompiled, argumentsEvaluated,
        signature, resultCompiled, semanticCalled, nextRuntimeEq,
        sourceValueEq, stepCostEq⟩
    exact ⟨{
      name
      args
      argumentCode
      argumentKinds
      semanticArgs
      declaration
      resultShape := .integer value
      resultKind := .tobject
      response := semanticIntegerExternalResponse sourceRuntime value
      valueEq
      nonempty
      declarationFound
      declarationExternal
      valueKind
      argumentsCompiled
      argumentsEvaluated
      signature
      resultCompiled
      semanticCalled
      nextRuntimeEq
      sourceValueEq
      resultKindEq := rfl
      responseEq := rfl
      stepCostEq }⟩
  · rcases natural with
      ⟨name, args, argumentCode, argumentKinds, semanticArgs, declaration,
        value, valueEq, _operation, nonempty, declarationFound,
        declarationExternal, valueKind, argumentsCompiled, argumentsEvaluated,
        signature, resultCompiled, semanticCalled, nextRuntimeEq,
        sourceValueEq, stepCostEq⟩
    exact ⟨{
      name
      args
      argumentCode
      argumentKinds
      semanticArgs
      declaration
      resultShape := .natural value
      resultKind := .tobject
      response := semanticNaturalExternalResponse sourceRuntime value
      valueEq
      nonempty
      declarationFound
      declarationExternal
      valueKind
      argumentsCompiled
      argumentsEvaluated
      signature
      resultCompiled
      semanticCalled
      nextRuntimeEq
      sourceValueEq
      resultKindEq := rfl
      responseEq := rfl
      stepCostEq }⟩
  · rcases scalar with
      ⟨name, args, argumentCode, argumentKinds, semanticArgs, declaration,
        value, valueEq, _operation, nonempty, declarationFound,
        declarationExternal, valueKind, argumentsCompiled, argumentsEvaluated,
        signature, resultCompiled, semanticCalled, nextRuntimeEq,
        sourceValueEq, stepCostEq⟩
    exact ⟨{
      name
      args
      argumentCode
      argumentKinds
      semanticArgs
      declaration
      resultShape := .scalar value
      resultKind := value.kind.abiKind
      response := semanticScalarExternalResponse sourceRuntime value
      valueEq
      nonempty
      declarationFound
      declarationExternal
      valueKind
      argumentsCompiled
      argumentsEvaluated
      signature
      resultCompiled
      semanticCalled
      nextRuntimeEq
      sourceValueEq
      resultKindEq := rfl
      responseEq := rfl
      stepCostEq }⟩

/-- Canonical proof-side projection of the shared external staging shape.
Choice is used only to eliminate a static admission proof into proof-indexing
data; every selected field carries its production compiler and semantic
equation. -/
noncomputable def PureExternalSupported.callShape
    {context : Fir.Wasm.Context} {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value} {stepCost : Nat}
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost) :
    PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost :=
  Classical.choice supported.callShapeExists

/-- Intermediate relation after the source has staged a pure external request
and the generated target has evaluated its argument prefix.  Both machines
are poised at the same resolved imported call.  The static import contract is
retained so the next source step can be matched by one target imported-call
step without rediscovering lowering or resolver facts. -/
structure ConcreteStructuredExternalCallReadyFocus
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (externals : ExternalImpl)
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    (site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost)
    (operation : ExternalOperation)
    (resolvedResultKind : AbiKind)
    (targetImport : Wasm.ImportDecl)
    (labels : List Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (callIndex resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .invokeName site.name site.semanticArgs
  sourceEnvEq : source.env = sourceEnv
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceJoinsEq : source.joins = callerJoins
  sourceFramesEq :
    source.frames =
      .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
  supported :
    PureExternalSupported context externals sourceRuntime sourceEnv decl
      continuation nextRuntime sourceValue stepCost
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .running
      { callerLocals with values := physicalArgs.reverse ++ callerRemainder }
      (.call callIndex :: .localSet resultIndex :: targetRest)
  targetFramesEq : target.frames = targetFrames
  continuationAdapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      targetRest
  callerStateRelated :
    StateRelated sourceFunction sourceRuntime sourceEnv targetStore callerLocals
      witness
  callerFrameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings sourceFunction) decl.fvarId = some resultIndex
  resultKindAt :
    (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
      some site.resultKind
  argumentsRelated :
    ConstructorArgumentsRelated witness site.argumentKinds.toList physicalArgs
      site.semanticArgs.toList
  operationName : operation.name = site.name
  operationMatches :
    ExternalOperationMatchesDeclaration operation site.declaration
  operationSignature :
    operation.signature = {
      params := site.argumentKinds
      results := #[site.resultKind] }
  resultSignature : operation.signature.results = #[resolvedResultKind]
  imported : targetModule.wasmModule.imports[callIndex]? = some targetImport
  inBounds : callIndex < targetModule.wasmModule.imports.length
  contracted :
    hosts.spec.contracts[callIndex]? =
      some (externalContract operation resolvedResultKind)
  hostsSatisfy : hosts.env.Satisfies targetModule.wasmModule hosts.spec
  parameterCount : targetImport.params.length = operation.signature.params.size
  resultCount : targetImport.results.length = 1

theorem ConcreteStructuredExternalCallReadyFocus.observes
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyFocus program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs callIndex resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.trace

/-- The first pure-external simulation step is compiler-derived and stops at
the imported-call boundary.  The source pushes its bind frame and exposes the
semantic request; the target executes only the generated local-read/erased
argument prefix.  No external response or terminating target suffix is
assumed by this theorem. -/
theorem ConcreteStructuredCodeFocus.advance_external_stage
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {rootCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context rootCode sourceModule
      sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value} {stepCost : Nat}
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost)
    {labels : List Lean.FVarId}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target) :
    ∃ site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
        nextRuntime sourceValue stepCost,
      ∃ physicalArgs operation resolvedResultKind targetImport,
      ∃ callIndex resultIndex,
      ∃ targetArguments targetRest : Wasm.Program,
      ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath
        (StructuredWasmStep targetModule.wasmModule hosts.env)
        targetArguments.length target targetAfter ∧
      ConcreteStructuredExternalCallReadyFocus program context sourceModule
        sourceFunction targetModule hosts externals site operation
        resolvedResultKind targetImport labels continuation source.joins
        source.frames targetStore targetLocals targetLocals.values targetRest
        target.frames witness physicalArgs callIndex resultIndex sourceAfter
        targetAfter := by
  let site := supported.callShape
  obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
      valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.let_eq related.adapted
  have expectedCompiled :
      Fir.Wasm.compileLetValue context decl =
        .ok (site.argumentCode ++ [.call (.declaration site.name)]) := by
    simp [Fir.Wasm.compileLetValue, site.valueEq, site.valueKind,
      site.argumentsCompiled, site.declarationFound, site.nonempty, Bind.bind,
      Except.bind, pure, Except.pure]
  have valueCodeEq :
      valueCode = site.argumentCode ++ [.call (.declaration site.name)] := by
    rw [expectedCompiled] at valueCompiled
    exact (Except.ok.inj valueCompiled).symm
  subst valueCode
  obtain ⟨targetArguments, callIndex, argumentsAdapted, callFound,
      targetValueEq⟩ :=
    instructions_append_declaration_call_eq valueAdapted
  subst targetValue
  have declarationFound : program.findDecl? site.name = some site.declaration := by
    rw [← spec.contextProgram]
    exact site.declarationFound
  obtain ⟨operation, resolvedResultKind, targetImport, operationName,
      operationMatches, resultSignature, imported, inBounds, contracted,
      parameterCount, resultCount⟩ :=
    spec.externalCall declarationFound site.declarationExternal callFound
  have operationSignature :
      operation.signature = {
        params := site.argumentKinds
        results := #[site.resultKind] } := by
    exact Except.ok.inj (operationMatches.signature.symm.trans site.signature)
  obtain ⟨physicalArgs, argumentsReady, _physicalLength,
      argumentsRelated⟩ :=
    constructorArgsReady_of_compileArgs localsAligned site.argumentsCompiled
      argumentsAdapted site.argumentsEvaluated related.stateRelated
  obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
    localsAligned site.resultCompiled
  rw [resultFound] at alignedResultFound
  have resultIndexEq : alignedResultIndex = resultIndex :=
    (Option.some.inj alignedResultFound).symm
  subst alignedResultIndex
  subst targetCode
  let sourceAfter : MachineState :=
    { source with
      control := .invokeName site.name site.semanticArgs
      frames :=
        .bind decl.fvarId continuation source.env source.joins :: source.frames }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running
        { targetLocals with
          values := physicalArgs.reverse ++ targetLocals.values }
        (.call callIndex :: .localSet resultIndex :: targetRest) }
  refine ⟨site, physicalArgs, operation, resolvedResultKind, targetImport,
    callIndex, resultIndex, targetArguments, targetRest, sourceAfter,
    targetAfter, ?_, ?_, ?_⟩
  · rcases source with ⟨sourceProgram, sourceControl, sourceStateEnv,
      sourceJoins, sourceFrames, sourceStateRuntime⟩
    have sourceControlEq := related.sourceControlEq
    change sourceControl = .code (.let decl continuation) at sourceControlEq
    subst sourceControl
    have sourceEnvEq := related.sourceEnvEq
    change sourceStateEnv = sourceEnv at sourceEnvEq
    subst sourceStateEnv
    have sourceRuntimeEq := related.sourceRuntimeEq
    change sourceStateRuntime = sourceRuntime at sourceRuntimeEq
    subst sourceStateRuntime
    simp [sourceAfter, executeStep, coreStep, evalLetValue, site.valueEq,
      site.argumentsEvaluated, pushBindFrame]
  · rcases target with ⟨stateStore, stateControl, stateFrames⟩
    have targetStoreEq := related.targetStoreEq
    change stateStore = targetStore at targetStoreEq
    subst stateStore
    have targetControlEq := related.targetControlEq
    change stateControl = .running targetLocals
      ((targetArguments ++ [.call callIndex]) ++
        .localSet resultIndex :: targetRest) at targetControlEq
    subst stateControl
    simpa [targetAfter, List.append_assoc] using
      (argumentsReady.finitePath
        (module := targetModule.wasmModule) (hostEnv := hosts.env)
        (store := targetStore)
        (rest := .call callIndex :: .localSet resultIndex :: targetRest)
        (frames := stateFrames) targetLocals.values)
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      sourceJoinsEq := by simp [sourceAfter]
      sourceFramesEq := by simp [sourceAfter, related.sourceEnvEq]
      supported
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      targetFramesEq := by simp [targetAfter]
      continuationAdapted
      callerStateRelated := related.stateRelated
      callerFrameAligned := related.frameAligned
      resultFound
      resultKindAt
      argumentsRelated
      operationName
      operationMatches
      operationSignature
      resultSignature
      imported
      inBounds
      contracted
      hostsSatisfy := spec.hostsSatisfy
      parameterCount
      resultCount }

/-- Runtime/resource evidence required to cross one resolved external import.
This boundary is deliberately separate from compiler control: the three pure
result families construct it from their installed handler laws and allocation
budgets, while the control theorem below consumes only the resulting concrete
step and refinement facts. -/
structure ConcreteExternalCallEvidence
    (operation : ExternalOperation) (resolvedResultKind : AbiKind)
    (initial : Wasm.Store Host) (physicalArgs : List Wasm.Value)
    (sourceRuntime nextRuntime : RuntimeState) (sourceValue : Value)
    (beforeWitness : RefinementWitness)
    (nextStore : Wasm.Store Host) (nextWitness : RefinementWitness)
    (physicalResult : Wasm.Value) : Prop where
  operationStep :
    externalStep operation resolvedResultKind initial physicalArgs =
      .Return [physicalResult] nextStore
  witnessExtension : beforeWitness.Extends nextWitness
  runtimeRelated :
    ConcreteRuntimeRel nextStore.host.runtime nextWitness nextRuntime
  failureClear : nextStore.host.failure? = none
  valueRelated :
    PhysicalValueRel nextWitness resolvedResultKind physicalResult sourceValue
  externalsPreserved :
    nextStore.host.externals = initial.host.externals
  closureTables :
    ClosureTablesTransport initial nextStore beforeWitness nextWitness
  witnessTransport : WitnessTransport beforeWitness nextWitness
  closureAllocationsPersistent :
    ClosureAllocationsPersistent beforeWitness nextWitness
  capacityTransport :
    HeaderCapacityTransport initial.host.runtime.heap
      nextStore.host.runtime.heap beforeWitness
  ordinaryTransport :
    OrdinaryPersistenceTransport sourceRuntime nextRuntime
  runtimeGlobals : nextRuntime.globals = sourceRuntime.globals
  storeGlobals :
    nextStore.globals.globals = initial.globals.globals
  hostStaticLayout :
    nextStore.host.runtime.globals.staticLayout =
      initial.host.runtime.globals.staticLayout

/-- The ordinary pure-external resource frame constructs the exact evidence
needed by the structured imported-call transition.  The construction is by
the semantic result family selected by production admission: integer and
natural results consume their exact allocation budget, while scalar results
retain the current witness.  No target execution or representation
certificate is supplied by the caller. -/
theorem ConcreteStructuredExternalCallReadyFocus.callEvidence_of_budget
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost remainingBytes : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyFocus program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs callIndex resultIndex source target)
    (invariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime sourceEnv targetStore callerLocals witness)
    (stepFits : stepCost ≤ remainingBytes) :
    ∃ nextStore nextWitness physicalResult,
      ConcreteExternalCallEvidence operation resolvedResultKind targetStore
        physicalArgs sourceRuntime nextRuntime sourceValue witness nextStore nextWitness
        physicalResult ∧
      ConcreteBudgetedPureExternalFrame sourceFunction externals
        (remainingBytes - stepCost) nextRuntime sourceEnv nextStore callerLocals
        nextWitness := by
  obtain ⟨concreteArgs, decoded, concreteLength, semanticLength,
      concreteRelated⟩ :=
    related.argumentsRelated.decodePhysicalLanes 0
  have resultKindEq : resolvedResultKind = site.resultKind := by
    have resultAt :=
      congrArg (fun results : Array AbiKind => results[0]?)
        related.resultSignature
    symm
    simpa [related.operationSignature] using resultAt
  have resultShapeKindEq :
      resolvedResultKind = site.resultShape.resultKind :=
    resultKindEq.trans site.resultKindEq
  have declarationName : site.name = site.declaration.name :=
    related.operationName.symm.trans related.operationMatches.name
  have requestEq :
      operation.request site.semanticArgs =
        declarationExternalRequest site.declaration site.semanticArgs := by
    simp [ExternalOperation.request, declarationExternalRequest,
      related.operationName, declarationName,
      related.operationMatches.paramTypes,
      related.operationMatches.resultType]
  have requestRelated :
      ConcreteExternalRequestRel witness
        (concreteExternalRequest operation resolvedResultKind
          concreteArgs.toArray)
        (operation.request site.semanticArgs) := by
    refine {
      name := rfl
      paramTypes := rfl
      resultType := rfl
      paramTypesSize := related.operationMatches.paramTypesSize
      paramKindsSize := ?_
      argsSize := ?_
      arguments := ?_ }
    · change operation.signature.params.size = site.semanticArgs.size
      rw [related.operationSignature]
      simpa using semanticLength.symm
    · change concreteArgs.toArray.size = site.semanticArgs.size
      simpa using concreteLength.trans semanticLength.symm
    · intro index kind lane semantic kindAt laneAt semanticAt
      change operation.signature.params[index]? = some kind at kindAt
      change concreteArgs.toArray[index]? = some lane at laneAt
      change site.semanticArgs[index]? = some semantic at semanticAt
      exact concreteRelated index kind lane semantic
        (by rw [related.operationSignature] at kindAt; simpa using kindAt)
        (by simpa using laneAt) (by simpa using semanticAt)
  have decoded' :
      decodePhysicalLanes 0 operation.signature.params.toList physicalArgs =
        .ok concreteArgs := by
    simpa [related.operationSignature] using decoded
  have semanticCalled :
      externals.call (operation.request site.semanticArgs) sourceRuntime =
        .ok (site.resultShape.response sourceRuntime) := by
    rw [requestEq, ← site.responseEq]
    exact site.semanticCalled
  have nextRuntimeEq :
      nextRuntime =
        semanticExternalRuntimeAfter (operation.request site.semanticArgs)
          sourceRuntime (site.resultShape.response sourceRuntime) := by
    calc
      nextRuntime =
          semanticExternalRuntimeAfter
            (declarationExternalRequest site.declaration site.semanticArgs)
            sourceRuntime site.response := site.nextRuntimeEq
      _ = semanticExternalRuntimeAfter (operation.request site.semanticArgs)
            sourceRuntime (site.resultShape.response sourceRuntime) := by
          rw [requestEq, site.responseEq]
  have sourceValueEq :
      sourceValue = (site.resultShape.response sourceRuntime).value := by
    exact site.sourceValueEq.trans (congrArg ExternalResponse.value site.responseEq)
  cases shapeEq : site.resultShape with
  | integer value =>
      have kindEq : resolvedResultKind = .tobject := by
        simpa [shapeEq, PureExternalResultShape.resultKind] using
          resultShapeKindEq
      have called :
          externals.call (operation.request site.semanticArgs) sourceRuntime =
            .ok (semanticIntegerExternalResponse sourceRuntime value) := by
        simpa [shapeEq, PureExternalResultShape.response] using semanticCalled
      have costEq : stepCost = integerAllocationBytes value := by
        simpa [shapeEq, PureExternalResultShape.cost] using site.stepCostEq
      have fits : integerAllocationBytes value ≤ remainingBytes := by
        rw [costEq] at stepFits
        exact stepFits
      obtain ⟨result, address, allocated, operationStep, _semanticCalled,
          witnessExtension, runtimeRelated, valueRelated, remainingBudget,
          closureAllocationsPersistent⟩ :=
        integerExternalStep_of_budget operation resolvedResultKind targetStore
          physicalArgs concreteArgs site.semanticArgs witness sourceRuntime
          externals value remainingBytes decoded' related.callerStateRelated.1
          requestRelated invariant.2.1 kindEq called invariant.1.2 fits
      let nextStore :=
        replaceRuntime targetStore
          (targetStore.host.runtime.applyExternalResponse
            (concreteExternalRequest operation resolvedResultKind
              concreteArgs.toArray)
            (concreteIntegerExternalResponse targetStore.host.runtime result
              address))
      let nextWitness :=
        witness.bindInteger sourceRuntime.nextLocation address value
      let physicalResult :=
        physicalOfLane
          (concreteIntegerExternalResponse targetStore.host.runtime result
            address).value
      have nextBudget :
          nextStore.host.runtime.heap.AddressSpaceBudget
            (remainingBytes - stepCost) := by
        rw [costEq]
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteIntegerExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using remainingBudget
      have externalsEq :
          nextStore.host.externals = targetStore.host.externals := by
        simp [nextStore, replaceRuntime, clearFailure]
      have witnessTransport : WitnessTransport witness nextWitness :=
        WitnessTransport.ofExtension witnessExtension
      have capacityTransport :
          HeaderCapacityTransport targetStore.host.runtime.heap
            nextStore.host.runtime.heap witness := by
        have allocatedTransport :=
          HeaderCapacityTransport.allocateInteger
            targetStore.host.runtime.heap result witness value address
            related.callerStateRelated.1.heap.frontier allocated
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteIntegerExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using allocatedTransport
      have ordinaryTransport :
          OrdinaryPersistenceTransport sourceRuntime nextRuntime := by
        rw [nextRuntimeEq]
        simpa [shapeEq, PureExternalResultShape.response] using
          (semanticIntegerExternalResponse_ordinaryPersistenceTransport
            (operation.request site.semanticArgs) sourceRuntime value)
      have closureTables :
          ClosureTablesTransport targetStore nextStore witness nextWitness := {
        hostDispatchPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDispatchPreserved := witnessExtension.closureDispatch
        hostDescriptorsPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDescriptorsPreserved := witnessExtension.closureDescriptors }
      have nextFrame :
          ConcreteBudgetedPureExternalFrame sourceFunction externals
            (remainingBytes - stepCost) nextRuntime sourceEnv nextStore
            callerLocals nextWitness := by
        refine ⟨⟨invariant.1.1, nextBudget⟩, ?_, ?_, ?_⟩
        · rw [externalsEq]
          exact invariant.2.1
        · rw [externalsEq]
          exact invariant.2.2.1
        · rw [externalsEq]
          exact invariant.2.2.2
      refine ⟨nextStore, nextWitness, physicalResult, ?_, nextFrame⟩
      exact {
        operationStep := by
          simpa [nextStore, physicalResult] using operationStep
        witnessExtension := by
          simpa [nextWitness] using witnessExtension
        runtimeRelated := by
          simpa [nextStore, nextWitness, replaceRuntime, clearFailure,
            nextRuntimeEq, shapeEq, PureExternalResultShape.response] using
            runtimeRelated
        failureClear := by
          simp [nextStore, replaceRuntime, clearFailure]
        valueRelated := by
          simpa [nextWitness, physicalResult, sourceValueEq, shapeEq,
            PureExternalResultShape.response] using valueRelated
        externalsPreserved := externalsEq
        closureTables
        witnessTransport
        closureAllocationsPersistent
        capacityTransport
        ordinaryTransport
        runtimeGlobals := by
          rw [nextRuntimeEq]
          simp [semanticExternalRuntimeAfter]
        storeGlobals := by
          simp [nextStore, replaceRuntime, clearFailure]
        hostStaticLayout := by
          simp [nextStore, replaceRuntime, clearFailure,
            concreteIntegerExternalResponse,
            ConcreteRuntimeState.applyExternalResponse] }
  | natural value =>
      have kindEq : resolvedResultKind = .tobject := by
        simpa [shapeEq, PureExternalResultShape.resultKind] using
          resultShapeKindEq
      have called :
          externals.call (operation.request site.semanticArgs) sourceRuntime =
            .ok (semanticNaturalExternalResponse sourceRuntime value) := by
        simpa [shapeEq, PureExternalResultShape.response] using semanticCalled
      have costEq : stepCost = naturalAllocationBytes value := by
        simpa [shapeEq, PureExternalResultShape.cost] using site.stepCostEq
      have fits : naturalAllocationBytes value ≤ remainingBytes := by
        rw [costEq] at stepFits
        exact stepFits
      obtain ⟨result, word, nextWitness, allocated, operationStep,
          _semanticCalled, witnessExtension, runtimeRelated, valueRelated,
          remainingBudget, closureAllocationsPersistent⟩ :=
        naturalExternalStep_of_budget operation resolvedResultKind targetStore
          physicalArgs concreteArgs site.semanticArgs witness sourceRuntime
          externals value remainingBytes decoded' related.callerStateRelated.1
          requestRelated invariant.2.2.1 kindEq called invariant.1.2 fits
      let nextStore :=
        replaceRuntime targetStore
          (targetStore.host.runtime.applyExternalResponse
            (concreteExternalRequest operation resolvedResultKind
              concreteArgs.toArray)
            (concreteNaturalExternalResponse targetStore.host.runtime result
              word))
      let physicalResult :=
        physicalOfLane
          (concreteNaturalExternalResponse targetStore.host.runtime result
            word).value
      have nextBudget :
          nextStore.host.runtime.heap.AddressSpaceBudget
            (remainingBytes - stepCost) := by
        rw [costEq]
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteNaturalExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using remainingBudget
      have externalsEq :
          nextStore.host.externals = targetStore.host.externals := by
        simp [nextStore, replaceRuntime, clearFailure]
      have witnessTransport : WitnessTransport witness nextWitness :=
        WitnessTransport.ofExtension witnessExtension
      have capacityTransport :
          HeaderCapacityTransport targetStore.host.runtime.heap
            nextStore.host.runtime.heap witness := by
        have allocatedTransport :=
          HeaderCapacityTransport.allocateNatural
            targetStore.host.runtime.heap result witness value word
            related.callerStateRelated.1.heap.frontier allocated
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteNaturalExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using allocatedTransport
      have ordinaryTransport :
          OrdinaryPersistenceTransport sourceRuntime nextRuntime := by
        rw [nextRuntimeEq]
        simpa [shapeEq, PureExternalResultShape.response] using
          (semanticNaturalExternalResponse_ordinaryPersistenceTransport
            (operation.request site.semanticArgs) sourceRuntime value)
      have closureTables :
          ClosureTablesTransport targetStore nextStore witness nextWitness := {
        hostDispatchPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDispatchPreserved := witnessExtension.closureDispatch
        hostDescriptorsPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDescriptorsPreserved := witnessExtension.closureDescriptors }
      have nextFrame :
          ConcreteBudgetedPureExternalFrame sourceFunction externals
            (remainingBytes - stepCost) nextRuntime sourceEnv nextStore
            callerLocals nextWitness := by
        refine ⟨⟨invariant.1.1, nextBudget⟩, ?_, ?_, ?_⟩
        · rw [externalsEq]
          exact invariant.2.1
        · rw [externalsEq]
          exact invariant.2.2.1
        · rw [externalsEq]
          exact invariant.2.2.2
      refine ⟨nextStore, nextWitness, physicalResult, ?_, nextFrame⟩
      exact {
        operationStep := by
          simpa [nextStore, physicalResult] using operationStep
        witnessExtension
        runtimeRelated := by
          simpa [nextStore, replaceRuntime, clearFailure, nextRuntimeEq,
            shapeEq, PureExternalResultShape.response] using runtimeRelated
        failureClear := by
          simp [nextStore, replaceRuntime, clearFailure]
        valueRelated := by
          simpa [physicalResult, sourceValueEq, shapeEq,
            PureExternalResultShape.response] using valueRelated
        externalsPreserved := externalsEq
        closureTables
        witnessTransport
        closureAllocationsPersistent
        capacityTransport
        ordinaryTransport
        runtimeGlobals := by
          rw [nextRuntimeEq]
          simp [semanticExternalRuntimeAfter]
        storeGlobals := by
          simp [nextStore, replaceRuntime, clearFailure]
        hostStaticLayout := by
          simp [nextStore, replaceRuntime, clearFailure,
            concreteNaturalExternalResponse,
            ConcreteRuntimeState.applyExternalResponse] }
  | scalar value =>
      have kindEq : resolvedResultKind = value.kind.abiKind := by
        simpa [shapeEq, PureExternalResultShape.resultKind] using
          resultShapeKindEq
      have called :
          externals.call (operation.request site.semanticArgs) sourceRuntime =
            .ok (semanticScalarExternalResponse sourceRuntime value) := by
        simpa [shapeEq, PureExternalResultShape.response] using semanticCalled
      have costEq : stepCost = 0 := by
        simpa [shapeEq, PureExternalResultShape.cost] using site.stepCostEq
      obtain ⟨operationStep, _semanticCalled, runtimeRelated, valueRelated⟩ :=
        scalarExternalStep operation resolvedResultKind targetStore
          physicalArgs concreteArgs site.semanticArgs witness sourceRuntime
          externals value decoded' related.callerStateRelated.1 requestRelated
          invariant.2.2.2 kindEq called
      let nextStore :=
        replaceRuntime targetStore
          (targetStore.host.runtime.applyExternalResponse
            (concreteExternalRequest operation resolvedResultKind
              concreteArgs.toArray)
            (concreteScalarExternalResponse targetStore.host.runtime value))
      let physicalResult :=
        physicalOfLane
          (concreteScalarExternalResponse targetStore.host.runtime value).value
      have nextBudget :
          nextStore.host.runtime.heap.AddressSpaceBudget
            (remainingBytes - stepCost) := by
        rw [costEq]
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteScalarExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using invariant.1.2
      have externalsEq :
          nextStore.host.externals = targetStore.host.externals := by
        simp [nextStore, replaceRuntime, clearFailure]
      have capacityTransport :
          HeaderCapacityTransport targetStore.host.runtime.heap
            nextStore.host.runtime.heap witness := by
        simpa [nextStore, replaceRuntime, clearFailure,
          concreteScalarExternalResponse,
          ConcreteRuntimeState.applyExternalResponse] using
          (HeaderCapacityTransport.refl targetStore.host.runtime.heap witness)
      have ordinaryTransport :
          OrdinaryPersistenceTransport sourceRuntime nextRuntime := by
        rw [nextRuntimeEq]
        simpa [shapeEq, PureExternalResultShape.response] using
          (semanticScalarExternalResponse_ordinaryPersistenceTransport
            (operation.request site.semanticArgs) sourceRuntime value)
      have closureTables :
          ClosureTablesTransport targetStore nextStore witness witness := {
        hostDispatchPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDispatchPreserved := rfl
        hostDescriptorsPreserved := by
          simp [nextStore, replaceRuntime, clearFailure]
        witnessDescriptorsPreserved := rfl }
      have nextFrame :
          ConcreteBudgetedPureExternalFrame sourceFunction externals
            (remainingBytes - stepCost) nextRuntime sourceEnv nextStore
            callerLocals witness := by
        refine ⟨⟨invariant.1.1, nextBudget⟩, ?_, ?_, ?_⟩
        · rw [externalsEq]
          exact invariant.2.1
        · rw [externalsEq]
          exact invariant.2.2.1
        · rw [externalsEq]
          exact invariant.2.2.2
      refine ⟨nextStore, witness, physicalResult, ?_, nextFrame⟩
      exact {
        operationStep := by
          simpa [nextStore, physicalResult] using operationStep
        witnessExtension := .refl witness
        runtimeRelated := by
          simpa [nextStore, replaceRuntime, clearFailure, nextRuntimeEq,
            shapeEq, PureExternalResultShape.response] using runtimeRelated
        failureClear := by
          simp [nextStore, replaceRuntime, clearFailure]
        valueRelated := by
          simpa [physicalResult, sourceValueEq, shapeEq,
            PureExternalResultShape.response] using valueRelated
        externalsPreserved := externalsEq
        closureTables
        witnessTransport := WitnessTransport.refl witness
        closureAllocationsPersistent := ClosureAllocationsPersistent.refl witness
        capacityTransport
        ordinaryTransport
        runtimeGlobals := by
          rw [nextRuntimeEq]
          simp [semanticExternalRuntimeAfter]
        storeGlobals := by
          simp [nextStore, replaceRuntime, clearFailure]
        hostStaticLayout := by
          simp [nextStore, replaceRuntime, clearFailure,
            concreteScalarExternalResponse,
            ConcreteRuntimeState.applyExternalResponse] }

/-- After the imported call returns, the source has yielded the semantic
result under its saved bind frame and the target is poised to write the one
physical result into the compiler-selected caller local. -/
structure ConcreteStructuredExternalBindFocus
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (callerEnv : Env)
    (sourceValue : Value)
    (result : Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (kind : AbiKind)
    (physical : Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .yielded sourceValue
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceFramesEq :
    source.frames = .bind result continuation callerEnv callerJoins ::
      sourceFrames
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .running
      { callerLocals with values := physical :: callerRemainder }
      (.localSet resultIndex :: targetRest)
  targetFramesEq : target.frames = targetFrames
  continuationAdapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      targetRest
  stateRelated :
    StateRelated sourceFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  frameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings sourceFunction) result = some resultIndex
  kindAt :
    (functionBindings sourceFunction)[resultIndex]?.map Prod.snd = some kind
  valueRelated : PhysicalValueRel witness kind physical sourceValue

theorem ConcreteStructuredExternalBindFocus.observes
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalBindFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness kind physical resultIndex
      source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.trace

/-- Cross the external call at one source step and one target step, assuming
the orthogonal runtime/resource layer has constructed the concrete response
evidence.  The target stops before the generated destination write, matching
the source's yielded bind-frame state exactly. -/
theorem ConcreteStructuredExternalCallReadyFocus.advance_call
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore nextStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness nextWitness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {physicalResult : Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyFocus program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs callIndex resultIndex source target)
    (evidence : ConcreteExternalCallEvidence operation resolvedResultKind
      targetStore physicalArgs sourceRuntime nextRuntime sourceValue witness nextStore
      nextWitness physicalResult) :
    ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1
        target targetAfter ∧
      ConcreteStructuredExternalBindFocus context sourceModule sourceFunction
        labels nextRuntime sourceEnv sourceValue decl.fvarId continuation
        callerJoins sourceFrames nextStore callerLocals callerRemainder
        targetRest targetFrames nextWitness site.resultKind physicalResult
        resultIndex sourceAfter targetAfter := by
  have resultKindEq : resolvedResultKind = site.resultKind := by
    have resultAt :=
      congrArg (fun results : Array AbiKind => results[0]?)
        related.resultSignature
    symm
    simpa [related.operationSignature] using resultAt
  have parameterCount' : targetImport.params.length = physicalArgs.length := by
    calc
      targetImport.params.length = operation.signature.params.size :=
        related.parameterCount
      _ = site.argumentKinds.size := by simp [related.operationSignature]
      _ = physicalArgs.length := by
        simpa using related.argumentsRelated.physicalLength.symm
  have declarationName : site.name = site.declaration.name :=
    related.operationName.symm.trans related.operationMatches.name
  have requestEq :
      operation.request site.semanticArgs =
        declarationExternalRequest site.declaration site.semanticArgs := by
    simp [ExternalOperation.request, declarationExternalRequest,
      related.operationName, declarationName,
      related.operationMatches.paramTypes,
      related.operationMatches.resultType]
  have parameterSize :
      site.declaration.params.size = site.semanticArgs.size := by
    calc
      site.declaration.params.size = operation.paramTypes.size := by
        rw [related.operationMatches.paramTypes]
        simp
      _ = operation.signature.params.size :=
        related.operationMatches.paramTypesSize
      _ = site.argumentKinds.size := by simp [related.operationSignature]
      _ = site.semanticArgs.size := by
        simpa using related.argumentsRelated.semanticLength.symm
  have semanticArgsNonempty : site.semanticArgs.isEmpty = false := by
    have argumentSize : site.semanticArgs.size = site.args.size := by
      calc
        site.semanticArgs.size = site.argumentKinds.size := by
          simpa using related.argumentsRelated.semanticLength
        _ = site.args.size := by
          have compiledLength :=
            (ConstructorArgsCompiled.ofCompileArgs
              site.argumentsCompiled).argumentLength
          simpa using compiledLength.symm
    simpa [Array.isEmpty, argumentSize] using site.nonempty
  have declarationFound :
      context.program.findDecl? site.declaration.name = some site.declaration := by
    simpa [declarationName] using site.declarationFound
  have callArgs :
      site.semanticArgs.extract 0 site.declaration.params.size =
        site.semanticArgs := by
    simp [parameterSize]
  have extraArgs :
      site.semanticArgs.extract site.declaration.params.size = #[] := by
    simp [parameterSize]
  let waitingEnv : Env :=
    (site.declaration.params.toList.zip site.semanticArgs.toList).foldl
      (fun env pair => bind env pair.fst.fvarId pair.snd) []
  have parametersBound :
      bindParams site.declaration.params site.semanticArgs = .ok waitingEnv := by
    simp [bindParams, parameterSize, waitingEnv]
  have semanticCalled :
      externals.call {
          name := site.declaration.name
          paramTypes := site.declaration.params.map (·.type)
          resultType := site.declaration.type
          args := site.semanticArgs }
        sourceRuntime = .ok site.response := by
    simpa [declarationExternalRequest] using site.semanticCalled
  obtain ⟨metadata, declarationExternal⟩ := site.declarationExternal
  let sourceAfter : MachineState := {
    program := context.program
    control := .yielded sourceValue
    env := waitingEnv
    joins := []
    frames := .bind decl.fvarId continuation sourceEnv callerJoins ::
      sourceFrames
    runtime := nextRuntime }
  let targetAfter : StructuredWasmState Host := {
    store := nextStore
    control := .running
      { callerLocals with values := physicalResult :: callerRemainder }
      (.localSet resultIndex :: targetRest)
    frames := targetFrames }
  refine ⟨sourceAfter, targetAfter, ?_, ?_, ?_⟩
  · rcases source with
      ⟨sourceProgram, sourceControl, sourceStateEnv, sourceStateJoins,
        sourceStateFrames, sourceStateRuntime⟩
    have programEq := related.sourceProgramEq
    change sourceProgram = context.program at programEq
    subst sourceProgram
    have controlEq := related.sourceControlEq
    change sourceControl = .invokeName site.name site.semanticArgs at controlEq
    subst sourceControl
    have envEq := related.sourceEnvEq
    change sourceStateEnv = sourceEnv at envEq
    subst sourceStateEnv
    have runtimeEq := related.sourceRuntimeEq
    change sourceStateRuntime = sourceRuntime at runtimeEq
    subst sourceStateRuntime
    have joinsEq := related.sourceJoinsEq
    change sourceStateJoins = callerJoins at joinsEq
    subst sourceStateJoins
    have framesEq := related.sourceFramesEq
    change sourceStateFrames =
      .bind decl.fvarId continuation sourceEnv callerJoins :: sourceFrames
      at framesEq
    subst sourceStateFrames
    simp [sourceAfter, executeStep, coreStep, invokeDecl,
      semanticArgsNonempty, declarationFound, parameterSize,
      parametersBound, declarationExternal, semanticCalled,
      declarationName, declarationExternalRequest, MachineState.withValue,
      resumeExternal,
      semanticExternalRuntimeAfter, semanticExternalEvent,
      site.nextRuntimeEq, site.sourceValueEq]
  · rcases target with ⟨stateStore, stateControl, stateFrames⟩
    have storeEq := related.targetStoreEq
    change stateStore = targetStore at storeEq
    subst stateStore
    have controlEq := related.targetControlEq
    change stateControl = .running
      { callerLocals with values := physicalArgs.reverse ++ callerRemainder }
      (.call callIndex :: .localSet resultIndex :: targetRest) at controlEq
    subst stateControl
    have framesEq := related.targetFramesEq
    change stateFrames = targetFrames at framesEq
    subst stateFrames
    simpa [targetAfter] using
      structuredWasmImportedCallFinitePath related.imported
        related.hostsSatisfy
        related.inBounds related.contracted parameterCount'
        (by simpa using related.resultCount) evidence.operationStep
  · have stateRelated :
        StateRelated sourceFunction nextRuntime sourceEnv nextStore callerLocals
          nextWitness :=
      ⟨evidence.runtimeRelated, evidence.failureClear,
        EnvLocalsRelated.witnessTransport
          (WitnessTransport.ofExtension evidence.witnessExtension)
          related.callerStateRelated.2.2⟩
    exact {
      sourceProgramEq := by simp [sourceAfter]
      sourceControlEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter]
      sourceFramesEq := by simp [sourceAfter]
      targetStoreEq := by simp [targetAfter]
      targetControlEq := by simp [targetAfter]
      targetFramesEq := by simp [targetAfter]
      continuationAdapted := related.continuationAdapted
      stateRelated
      frameAligned := by
        simpa [ConcreteLocalFrameAligned] using related.callerFrameAligned
      resultFound := related.resultFound
      kindAt := related.resultKindAt
      valueRelated := by simpa [resultKindEq] using evidence.valueRelated }

/-- Cross the imported-call boundary directly from the threaded resource
frame.  This is the caller-facing progress theorem: concrete execution
evidence is constructed internally by `callEvidence_of_budget` and is not an
extra premise. -/
theorem ConcreteStructuredExternalCallReadyFocus.advance_call_of_budget
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost remainingBytes : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyFocus program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs callIndex resultIndex source target)
    (invariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime sourceEnv targetStore callerLocals witness)
    (stepFits : stepCost ≤ remainingBytes) :
    ∃ nextStore nextWitness physicalResult sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1
        target targetAfter ∧
      ConcreteStructuredExternalBindFocus context sourceModule sourceFunction
        labels nextRuntime sourceEnv sourceValue decl.fvarId continuation
        callerJoins sourceFrames nextStore callerLocals callerRemainder
        targetRest targetFrames nextWitness site.resultKind physicalResult
        resultIndex sourceAfter targetAfter ∧
      ConcreteBudgetedPureExternalFrame sourceFunction externals
        (remainingBytes - stepCost) nextRuntime sourceEnv nextStore callerLocals
        nextWitness := by
  obtain ⟨nextStore, nextWitness, physicalResult, evidence, nextInvariant⟩ :=
    related.callEvidence_of_budget invariant stepFits
  obtain ⟨sourceAfter, targetAfter, sourceStep, targetPath, nextFocus⟩ :=
    related.advance_call evidence
  exact ⟨nextStore, nextWitness, physicalResult, sourceAfter, targetAfter,
    sourceStep, targetPath, nextFocus, nextInvariant⟩

/-- Consume the external result's saved source bind frame and generated
destination write.  This closes the three-step source protocol with exactly
one final target instruction and re-enters ordinary compiled code. -/
theorem ConcreteStructuredExternalBindFocus.advance
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalBindFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness kind physical resultIndex
      source target) :
    ∃ sourceAfter targetAfter updated resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 1 target targetAfter ∧
      callerLocals.set? resultIndex physical = some updated ∧
      resumedLocals = { updated with values := callerRemainder } ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
          sourceRuntime (bind callerEnv result sourceValue) continuation
          targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
        sourceAfter.joins = callerJoins ∧
        sourceAfter.frames = sourceFrames ∧
        targetAfter.frames = targetFrames := by
  obtain ⟨updated, targetSet, updatedAligned⟩ :=
    related.frameAligned.set?
      (nextRuntime := sourceRuntime)
      (nextEnv := bind callerEnv result sourceValue)
      (nextStore := targetStore)
      (nextWitness := witness)
      related.resultFound
  let resumedLocals : Wasm.Locals :=
    { updated with values := callerRemainder }
  have updatedRelated :
      StateRelated sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    have bound := related.stateRelated.bindPhysical related.resultFound
      related.kindAt related.valueRelated targetSet
    rw [related.stateRelated.clearFailure] at bound
    simpa [resumedLocals, StateRelated, EnvLocalsRelated, Wasm.Locals.get]
      using bound
  have resumedAligned :
      ConcreteLocalFrameAligned sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    simpa [resumedLocals, ConcreteLocalFrameAligned] using updatedAligned
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind callerEnv result sourceValue
      joins := callerJoins
      frames := sourceFrames }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running resumedLocals targetRest
      frames := targetFrames }
  refine ⟨sourceAfter, targetAfter, updated, resumedLocals, ?_, ?_, targetSet,
    rfl, ?_, ?_, ?_, ?_⟩
  · rcases source with
      ⟨sourceProgram, sourceControl, sourceEnv, sourceJoins, stateFrames,
        sourceStateRuntime⟩
    have controlEq := related.sourceControlEq
    change sourceControl = .yielded sourceValue at controlEq
    subst sourceControl
    have framesEq := related.sourceFramesEq
    change stateFrames = _ at framesEq
    subst stateFrames
    simp [sourceAfter, executeStep, coreStep]
  · rcases target with ⟨stateStore, stateControl, stateFrames⟩
    have storeEq := related.targetStoreEq
    change stateStore = targetStore at storeEq
    subst stateStore
    have controlEq := related.targetControlEq
    change stateControl = .running
      { callerLocals with values := physical :: callerRemainder }
      (.localSet resultIndex :: targetRest) at controlEq
    subst stateControl
    have framesEq := related.targetFramesEq
    change stateFrames = targetFrames at framesEq
    subst stateFrames
    have stackSet :
        ({ callerLocals with values := physical :: callerRemainder }.set?
          resultIndex physical) =
          some { updated with values := physical :: callerRemainder } :=
      locals_set?_with_values (physical :: callerRemainder) targetSet
    apply FinitePath.single
    apply StructuredWasmStep.atomic (fuel := 1)
    · trivial
    · simp only [Wasm.execOne.eq_def, stackSet]
      rfl
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      adapted := related.continuationAdapted
      stateRelated := updatedRelated
      frameAligned := resumedAligned }
  · simp [sourceAfter]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- The generated destination write preserves the residual pure-external
resource frame.  Runtime, heap budget, installed handlers, and witness are
unchanged; only the source binding and compiler-selected local slot advance. -/
theorem ConcreteStructuredExternalBindFocus.advance_with_frame
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalBindFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness kind physical resultIndex
      source target)
    (invariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime callerEnv targetStore callerLocals witness) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 1 target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind callerEnv result sourceValue) continuation
        targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime (bind callerEnv result sourceValue) targetStore
        resumedLocals witness ∧
      sourceAfter.joins = callerJoins ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨sourceAfter, targetAfter, _updated, resumedLocals, sourceStep,
      targetPath, _targetSet, _resumedEq, nextFocus, sourceJoinsEq,
      sourceFramesEq, targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have nextInvariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime (bind callerEnv result sourceValue) targetStore
        resumedLocals witness := by
    exact ⟨⟨nextFocus.frameAligned, invariant.1.2⟩,
      invariant.2.1, invariant.2.2.1, invariant.2.2.2⟩
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    nextFocus, nextInvariant, sourceJoinsEq, sourceFramesEq, targetFramesEq⟩

/-- Complete the source's external resume-and-bind suffix from a call-ready
focus.  Two source steps match exactly two target instructions (the resolved
imported call and generated destination write), and ordinary compiled code is
re-entered with the exact residual budget. -/
theorem ConcreteStructuredExternalCallReadyFocus.advance_call_bind_of_budget
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {sourceValue : Value} {stepCost remainingBytes : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyFocus program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs callIndex resultIndex source target)
    (invariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime sourceEnv targetStore callerLocals witness)
    (stepFits : stepCost ≤ remainingBytes) :
    ∃ nextStore nextWitness sourceAfter targetAfter resumedLocals,
      FinitePath
        (fun before after => executeStep externals before = .next after) 2
        source sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2
        target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        nextRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
        nextStore resumedLocals targetRest nextWitness sourceAfter targetAfter ∧
      ConcreteBudgetedPureExternalFrame sourceFunction externals
        (remainingBytes - stepCost) nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) nextStore resumedLocals
        nextWitness ∧
      sourceAfter.joins = callerJoins ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨nextStore, nextWitness, _physicalResult, sourceMiddle, targetMiddle,
      sourceCall, targetCall, bindFocus, middleInvariant⟩ :=
    related.advance_call_of_budget invariant stepFits
  obtain ⟨sourceAfter, targetAfter, resumedLocals, sourceBind, targetBind,
      nextFocus, nextInvariant, sourceJoinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    bindFocus.advance_with_frame
      (module := targetModule.wasmModule) (hostEnv := hosts.env)
      middleInvariant
  have sourceCallPath :
      FinitePath
        (fun before after => executeStep externals before = .next after) 1
        source sourceMiddle :=
    FinitePath.single sourceCall
  have sourceBindPath :
      FinitePath
        (fun before after => executeStep externals before = .next after) 1
        sourceMiddle sourceAfter :=
    FinitePath.single sourceBind
  exact ⟨nextStore, nextWitness, sourceAfter, targetAfter, resumedLocals,
    sourceCallPath.trans sourceBindPath,
    targetCall.trans targetBind, nextFocus, nextInvariant, sourceJoinsEq,
    sourceFramesEq, targetFramesEq⟩

/-- Complete one admitted pure-external `let` from ordinary compiled code.
The source follows its exact three-step request/resume/bind protocol.  The
target evaluates precisely the production argument prefix, executes one
resolved imported call, writes one destination local, and returns to the
adapted continuation with the exact residual resource frame. -/
theorem ConcreteStructuredCodeFocus.advance_external_of_budget
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {rootCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    (spec : ConcreteSupportedFunction program context rootCode sourceModule
      sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState} {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value} {stepCost remainingBytes : Nat}
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost)
    {labels : List Lean.FVarId}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (invariant :
      ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
        sourceRuntime sourceEnv targetStore targetLocals witness)
    (stepFits : stepCost ≤ remainingBytes) :
    ∃ nextStore nextWitness sourceAfter targetAfter resumedLocals,
    ∃ targetArguments targetRest : Wasm.Program,
      FinitePath
        (fun before after => executeStep externals before = .next after) 3
        source sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (targetArguments.length + 2) target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        nextRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
        nextStore resumedLocals targetRest nextWitness sourceAfter targetAfter ∧
      ConcreteBudgetedPureExternalFrame sourceFunction externals
        (remainingBytes - stepCost) nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) nextStore resumedLocals
        nextWitness ∧
      sourceAfter.joins = source.joins ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames := by
  obtain ⟨site, physicalArgs, operation, resolvedResultKind, targetImport,
      callIndex, resultIndex, targetArguments, targetRest, sourceMiddle,
      targetMiddle, sourceStage, targetStage, callReady⟩ :=
    related.advance_external_stage spec supported localsAligned
  obtain ⟨nextStore, nextWitness, sourceAfter, targetAfter, resumedLocals,
      sourceSuffix, targetSuffix, nextFocus, nextInvariant, sourceJoinsEq,
      sourceFramesEq, targetFramesEq⟩ :=
    callReady.advance_call_bind_of_budget invariant stepFits
  have sourceStagePath :
      FinitePath
        (fun before after => executeStep externals before = .next after) 1
        source sourceMiddle :=
    FinitePath.single sourceStage
  exact ⟨nextStore, nextWitness, sourceAfter, targetAfter, resumedLocals,
    targetArguments, targetRest, sourceStagePath.trans sourceSuffix,
    targetStage.trans targetSuffix, nextFocus, nextInvariant, sourceJoinsEq,
    sourceFramesEq, targetFramesEq⟩

/-- Intermediate relation after the source has staged an internal named call
and the generated target has evaluated its argument prefix.  Both machines
are poised to enter the callee.  The relation retains the caller continuation
and local refinement that the call frame must protect, while the generated
callee row and related physical arguments determine the entry state without a
translation certificate or assumed target execution. -/
structure ConcreteStructuredDirectCallReadyFocus
    (callerContext calleeContext : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (callerFunction calleeFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = callerContext.program
  sourceControlEq :
    source.control = .invokeName site.declaration site.semanticArgs
  sourceEnvEq : source.env = callerEnv
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceJoinsEq : source.joins = callerJoins
  sourceFramesEq :
    source.frames =
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .running
      { callerLocals with
        values := physicalArgs.reverse ++ callerRemainder }
      (.call row.targetFunctionIndex ::
        .localSet resultIndex :: targetRest)
  targetFramesEq : target.frames = targetFrames
  continuationAdapted :
    CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
      continuation
      targetRest
  callerStateRelated :
    StateRelated callerFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  callerFrameAligned :
    ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings callerFunction) decl.fvarId = some resultIndex
  resultKindAt :
    (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
      some site.resultKind
  argumentsRelated :
    ConstructorArgumentsRelated witness site.argumentKinds.toList physicalArgs
      site.semanticArgs.toList

theorem ConcreteStructuredDirectCallReadyFocus.observes
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallReadyFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.callerStateRelated.1.trace

/-- The first direct-call simulation step is entirely compiler-derived.  The
source stages the named invocation under its bind frame while the target
executes the exact local-read/erased-zero argument prefix recovered from
two-stage adaptation.  Both sides finish poised at their respective call
entry transitions, with the caller continuation relation protected by
`ConcreteStructuredDirectCallReadyFocus`. -/
theorem ConcreteStructuredCodeFocus.advance_directCall_stage
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned callerContext callerFunction)
    (related : ConcreteStructuredCodeFocus callerContext sourceModule
      callerFunction labels sourceRuntime callerEnv (.let decl continuation)
      targetStore targetLocals targetCode witness source target) :
    ∃ physicalArgs resultIndex,
      ∃ targetArguments targetRest : Wasm.Program,
      ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv)
        targetArguments.length target targetAfter ∧
      ConcreteStructuredDirectCallReadyFocus callerContext calleeContext
        sourceModule callerFunction calleeFunction targetModule site row labels
        sourceRuntime continuation source.joins source.frames targetStore
        targetLocals targetLocals.values targetRest target.frames witness
        physicalArgs resultIndex sourceAfter targetAfter := by
  obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
      valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.let_eq related.adapted
  have expectedCompiled :
      Fir.Wasm.compileLetValue callerContext decl =
        .ok (site.argumentCode ++
          [.call (.declaration site.declaration)]) := by
    simp [Fir.Wasm.compileLetValue, Fir.Wasm.letValueKind, site.valueEq,
      site.kindEq, site.argumentsCompiled, site.declarationFound,
      site.nonCached, Bind.bind, Except.bind, pure, Except.pure]
  have valueCodeEq :
      valueCode =
        site.argumentCode ++ [.call (.declaration site.declaration)] := by
    rw [expectedCompiled] at valueCompiled
    exact (Except.ok.inj valueCompiled).symm
  subst valueCode
  obtain ⟨targetArguments, functionIndex, argumentsAdapted, callFound,
      targetValueEq⟩ :=
    instructions_append_declaration_call_eq valueAdapted
  subst targetValue
  have declarationNameEq :
      site.sourceDeclaration.name = site.declaration := by
    have selected :=
      (Array.find?_eq_some_iff_getElem.mp site.declarationFound).1
    simpa [Fir.LeanIR.Program.findDecl?] using selected
  have exactCallIndex :
      callIndex? sourceModule (.declaration site.declaration) =
        some row.targetFunctionIndex := by
    simpa [declarationNameEq] using row.callIndexEq
  rw [exactCallIndex] at callFound
  have functionIndexEq : row.targetFunctionIndex = functionIndex :=
    Option.some.inj callFound
  subst functionIndex
  obtain ⟨physicalArgs, argumentsReady, _physicalLength,
      argumentsRelated⟩ :=
    constructorArgsReady_of_compileArgs localsAligned site.argumentsCompiled
      argumentsAdapted site.argumentsEvaluated related.stateRelated
  obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
    localsAligned site.resultCompiled
  rw [resultFound] at alignedResultFound
  have resultIndexEq : alignedResultIndex = resultIndex :=
    (Option.some.inj alignedResultFound).symm
  subst alignedResultIndex
  subst targetCode
  let sourceAfter : MachineState :=
    { source with
      control := .invokeName site.declaration site.semanticArgs
      frames :=
        .bind decl.fvarId continuation source.env source.joins :: source.frames }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running
        { targetLocals with
          values := physicalArgs.reverse ++ targetLocals.values }
        (.call row.targetFunctionIndex ::
          .localSet resultIndex :: targetRest) }
  refine ⟨physicalArgs, resultIndex, targetArguments, targetRest, sourceAfter,
    targetAfter, ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = .code (.let decl continuation) at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = callerEnv at envEq
    subst env
    have runtimeEq := related.sourceRuntimeEq
    change runtime = sourceRuntime at runtimeEq
    subst runtime
    simp [sourceAfter, executeStep, coreStep, evalLetValue, site.valueEq,
      site.argumentsEvaluated, pushBindFrame]
  · rcases target with ⟨store, control, frames⟩
    have storeEq := related.targetStoreEq
    change store = targetStore at storeEq
    subst store
    have controlEq := related.targetControlEq
    change control = .running targetLocals
      ((targetArguments ++ [.call row.targetFunctionIndex]) ++
        .localSet resultIndex :: targetRest) at controlEq
    subst control
    simpa [targetAfter, List.append_assoc] using
      (argumentsReady.finitePath
        (module := targetModule.wasmModule) (hostEnv := hostEnv)
        (store := targetStore)
        (rest := .call row.targetFunctionIndex ::
          .localSet resultIndex :: targetRest)
        (frames := frames) targetLocals.values)
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      sourceJoinsEq := by simp [sourceAfter]
      sourceFramesEq := by simp [sourceAfter, related.sourceEnvEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      targetFramesEq := by simp [targetAfter]
      continuationAdapted
      callerStateRelated := related.stateRelated
      callerFrameAligned := related.frameAligned
      resultFound
      resultKindAt
      argumentsRelated }

/-- The generated callee locals selected by the direct-call argument relation
have exactly the source symbolic parameter/local shape.  This is the local
frame component needed when the structured machine enters the callee. -/
theorem ConcreteGeneratedInternalDeclaration.entryFrameAligned
    {callerContext calleeContext : Fir.Wasm.Context}
    {callerDecl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceModule : Fir.Wasm.Module}
    {calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    (site : DirectInternalCallSite callerContext callerDecl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    {sourceRuntime : RuntimeState}
    {targetStore : Wasm.Store Host}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    (argumentsRelated :
      ConstructorArgumentsRelated witness site.argumentKinds.toList
        physicalArgs site.semanticArgs.toList) :
    ConcreteLocalFrameAligned calleeFunction sourceRuntime site.calleeEnv
      targetStore (row.targetFunction.toLocals physicalArgs) witness := by
  have classifiedSize := row.parameterKindsSize site.parametersKnown
  have parameterBindings := row.sourceParameterBindings site.parametersKnown
  have sourceParameterSize :
      calleeFunction.params.size = site.parameterKinds.size := by
    have lengths := congrArg List.length parameterBindings
    simp only [Array.length_toList, List.length_map, List.length_zip] at lengths
    rw [← classifiedSize] at lengths
    simpa using lengths
  have parameterRelated :=
    argumentsRelated.ofKindsRefine site.argumentsRefine
  have physicalSize : physicalArgs.length = site.parameterKinds.size := by
    simpa using parameterRelated.physicalLength
  have parameterFrameSize :
      (row.targetFunction.toLocals physicalArgs).params.length =
        calleeFunction.params.size := by
    simpa [Wasm.Function.toLocals] using
      physicalSize.trans sourceParameterSize.symm
  have signature :=
    FirTalos.Correctness.function_preserves_signature row.functionAdapted
  have localFrameSize :
      (row.targetFunction.toLocals physicalArgs).locals.length =
        calleeFunction.locals.size := by
    simp [Wasm.Function.toLocals, signature.2.1]
  exact ⟨parameterFrameSize, localFrameSize⟩

/-- Relation immediately after both machines enter a generated direct callee.
The local code focus is now indexed by the callee's real symbolic and adapted
function rows.  The separate caller fields record exactly the bind/call frame
that must be transported across callee effects before the accepted return
unwinding theorem can be applied. -/
structure ConcreteStructuredDirectCallEntryFocus
    (callerContext calleeContext : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (callerFunction calleeFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  calleeFocus :
    ConcreteStructuredCodeFocus calleeContext sourceModule calleeFunction []
      sourceRuntime site.calleeEnv site.calleeCode targetStore
      (row.targetFunction.toLocals physicalArgs) row.targetFunction.body witness
      source target
  sourceJoinsEq : source.joins = []
  sourceFramesEq :
    source.frames =
      .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames
  targetFramesEq :
    target.frames =
      .call 1 callerRemainder callerLocals
          (.localSet resultIndex :: targetRest) :: targetFrames
  continuationAdapted :
    CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
      continuation
      targetRest
  callerStateRelated :
    StateRelated callerFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  callerFrameAligned :
    ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings callerFunction) decl.fvarId = some resultIndex
  resultKindAt :
    (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
      some site.resultKind
  argumentsRelated :
    ConstructorArgumentsRelated witness site.argumentKinds.toList physicalArgs
      site.semanticArgs.toList

theorem ConcreteStructuredDirectCallEntryFocus.observes
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.calleeFocus.observes

/-- The poised source and target call states enter the same generated callee.
The source dispatcher takes one step; the structured target takes its real
`enterCall` step.  The conclusion contains the callee code focus plus the
exact saved caller frames.  In particular, the target call frame retains the
post-argument operand stack prescribed by Wasm, while its semantic/local
invariant is transported without changing the store or witness. -/
theorem ConcreteStructuredDirectCallReadyFocus.advance_enter
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallReadyFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target) :
    ∃ sourceAfter targetAfter storedCallerLocals,
      storedCallerLocals =
          { callerLocals with
            values := physicalArgs.reverse ++ callerRemainder } ∧
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 1 target
        targetAfter ∧
      ConcreteStructuredDirectCallEntryFocus callerContext calleeContext
        sourceModule callerFunction calleeFunction targetModule site row labels
        sourceRuntime continuation callerJoins sourceFrames targetStore
        storedCallerLocals callerRemainder targetRest targetFrames witness
        physicalArgs resultIndex sourceAfter targetAfter := by
  have argumentCompilation :=
    ConstructorArgsCompiled.ofCompileArgs site.argumentsCompiled
  have argumentSize : site.semanticArgs.size = site.args.size := by
    have semanticLength := related.argumentsRelated.semanticLength
    have compiledLength := argumentCompilation.argumentLength
    simpa using semanticLength.trans compiledLength.symm
  have parameterSize :
      site.semanticArgs.size = site.sourceDeclaration.params.size := by
    by_contra different
    have sizeTest :
        (site.sourceDeclaration.params.size == site.semanticArgs.size) =
          false :=
      beq_eq_false_iff_ne.mpr (Ne.symm different)
    have bound := site.parametersBound
    unfold bindParams at bound
    simp [sizeTest] at bound
  have callArgs :
      site.semanticArgs.extract 0 site.sourceDeclaration.params.size =
        site.semanticArgs := by
    simp [← parameterSize]
  have argumentsNonempty : site.semanticArgs.isEmpty = false := by
    simpa [Array.isEmpty, ← argumentSize, ← parameterSize] using site.nonCached
  have parameterCount :
      physicalArgs.reverse.length = row.targetFunction.numParams :=
    row.targetParameterCount site related.argumentsRelated
  have takeParameters :
      (physicalArgs.reverse ++ callerRemainder).take
          row.targetFunction.numParams =
        physicalArgs.reverse := by
    rw [← parameterCount]
    simp
  have dropParameters :
      (physicalArgs.reverse ++ callerRemainder).drop
          row.targetFunction.numParams =
        callerRemainder := by
    rw [← parameterCount]
    simp
  have calleeLocalsRelated :
      EnvLocalsRelated witness (functionBindings calleeFunction)
        site.calleeEnv (row.targetFunction.toLocals physicalArgs) :=
    row.entryEnvLocalsRelatedOfArguments site related.argumentsRelated
  have calleeStateRelated :
      StateRelated calleeFunction sourceRuntime site.calleeEnv targetStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    ⟨related.callerStateRelated.1,
      related.callerStateRelated.2.1, calleeLocalsRelated⟩
  have calleeFrameAligned :
      ConcreteLocalFrameAligned calleeFunction sourceRuntime site.calleeEnv
        targetStore (row.targetFunction.toLocals physicalArgs) witness :=
    row.entryFrameAligned site related.argumentsRelated
  let storedCallerLocals : Wasm.Locals :=
    { callerLocals with
      values := physicalArgs.reverse ++ callerRemainder }
  have storedCallerStateRelated :
      StateRelated callerFunction sourceRuntime callerEnv targetStore
        storedCallerLocals witness := by
    simpa [storedCallerLocals, StateRelated, EnvLocalsRelated,
      Wasm.Locals.get] using related.callerStateRelated
  have storedCallerFrameAligned :
      ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv
        targetStore storedCallerLocals witness := by
    simpa [storedCallerLocals, ConcreteLocalFrameAligned] using
      related.callerFrameAligned
  let sourceAfter : MachineState :=
    { source with
      control := .code site.calleeCode
      env := site.calleeEnv
      joins := [] }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running (row.targetFunction.toLocals physicalArgs)
        row.targetFunction.body
      frames :=
        .call 1 callerRemainder storedCallerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames }
  refine ⟨sourceAfter, targetAfter, storedCallerLocals, rfl, ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have programEq := related.sourceProgramEq
    change program = callerContext.program at programEq
    subst program
    have controlEq := related.sourceControlEq
    change control = .invokeName site.declaration site.semanticArgs at controlEq
    subst control
    have envEq := related.sourceEnvEq
    change env = callerEnv at envEq
    subst env
    have runtimeEq := related.sourceRuntimeEq
    change runtime = sourceRuntime at runtimeEq
    subst runtime
    have joinsEq := related.sourceJoinsEq
    change joins = callerJoins at joinsEq
    subst joins
    have framesEq := related.sourceFramesEq
    change frames = _ at framesEq
    subst frames
    simp [sourceAfter, executeStep, coreStep, invokeDecl,
      site.declarationFound, argumentsNonempty, parameterSize, callArgs,
      site.parametersBound, site.bodyEq]
  · have entered :
        StructuredWasmStep targetModule.wasmModule hostEnv target
          targetAfter := by
      rcases target with ⟨store, control, frames⟩
      have storeEq := related.targetStoreEq
      change store = targetStore at storeEq
      subst store
      have controlEq := related.targetControlEq
      change control = .running storedCallerLocals
        (.call row.targetFunctionIndex ::
          .localSet resultIndex :: targetRest) at controlEq
      subst control
      have framesEq := related.targetFramesEq
      change frames = targetFrames at framesEq
      subst frames
      simpa only [targetAfter, storedCallerLocals, takeParameters,
        dropParameters, row.singleResult, List.reverse_reverse] using
        (StructuredWasmStep.enterCall
          (module := targetModule.wasmModule) (env := hostEnv)
          (id := row.targetFunctionIndex) (function := row.targetFunction)
          (store := targetStore) (locals := storedCallerLocals)
          (rest := .localSet resultIndex :: targetRest)
          (frames := targetFrames) row.notImport row.targetFunctionFound)
    exact .single entered
  · refine {
      calleeFocus := ?_
      sourceJoinsEq := by simp [sourceAfter]
      sourceFramesEq := by simp [sourceAfter, related.sourceFramesEq]
      targetFramesEq := by simp [targetAfter]
      continuationAdapted := related.continuationAdapted
      callerStateRelated := storedCallerStateRelated
      callerFrameAligned := storedCallerFrameAligned
      resultFound := related.resultFound
      resultKindAt := related.resultKindAt
      argumentsRelated := related.argumentsRelated }
    exact {
      sourceProgramEq := by
        calc
          sourceAfter.program = callerContext.program := by
            simp [sourceAfter, related.sourceProgramEq]
          _ = calleeContext.program := row.contextProgram.symm
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      adapted := by
        rw [row.targetBodyEq]
        exact CodeAdapted.withSuffix row.bodyAdapted
      stateRelated := calleeStateRelated
      frameAligned := calleeFrameAligned }

/-- Entry-to-current runtime transports preserve a suspended caller's local
relation.  The current callee relation supplies the evolved runtime and clear
failure channel; the accumulated witness transport reinterprets every saved
caller local at the current witness without changing either the source
environment or the physical local frame. -/
theorem ReuseCapacityCodeEntryTransports.savedStateRelated
    {entryRuntime currentRuntime : RuntimeState}
    {entryStore currentStore : Wasm.Store Host}
    {entryWitness currentWitness : RefinementWitness}
    {entryFunction currentFunction : Fir.Wasm.Function}
    {entryEnv currentEnv : Env}
    {entryLocals currentLocals : Wasm.Locals}
    (transports :
      ReuseCapacityCodeEntryTransports entryRuntime currentRuntime entryStore
        currentStore entryWitness currentWitness)
    (entryRelated :
      StateRelated entryFunction entryRuntime entryEnv entryStore entryLocals
        entryWitness)
    (currentRelated :
      StateRelated currentFunction currentRuntime currentEnv currentStore
        currentLocals currentWitness) :
    StateRelated entryFunction currentRuntime entryEnv currentStore entryLocals
      currentWitness := by
  exact ⟨currentRelated.1, currentRelated.2.1,
    EnvLocalsRelated.witnessTransport transports.witness entryRelated.2.2⟩

/-- Strengthen a structured direct-call entry with the canonical hereditary
cache frame used by the existing W6 operation-family induction.  The callee
starts with empty reuse facts and reflexive entry transports; its runtime,
budget, handler, cache, ownership, and immutable-table invariants come from
the caller frame through the generated declaration row. -/
theorem
    ConcreteStructuredDirectCallEntryFocus.calleeEntryRelativeCacheFrame
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {entryWitness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {entrySource : MachineState}
    {entryTarget : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels entryRuntime continuation callerJoins sourceFrames
      entryStore callerLocals callerRemainder targetRest targetFrames
      entryWitness physicalArgs resultIndex entrySource entryTarget)
    (callerFrame :
      ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals
        facts remainingBytes entryRuntime callerEnv entryStore callerLocals
        entryWitness) :
    ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals)
      entryRuntime entryStore entryWitness [] remainingBytes entryRuntime
      site.calleeEnv entryStore (row.targetFunction.toLocals physicalArgs)
      entryWitness := by
  exact
    ⟨callerFrame.generatedDirectCalleeEntry site row entry.argumentsRelated,
      ReuseCapacityCodeEntryTransports.refl entryRuntime entryStore
        entryWitness⟩

/-- One saved source bind frame corresponds to the generated call frame whose
caller residual program stores the single returned value and continues with
the adapted source continuation.  Target-only label/loop frames may surround
this constructor later; they are not falsely identified with source frames. -/
structure ConcreteStructuredBindFrameFocus
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (labels : List Lean.FVarId)
    (sourceRuntime : RuntimeState)
    (callerEnv : Env)
    (sourceValue : Value)
    (result : Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (returnedTail : List Wasm.Value)
    (witness : RefinementWitness)
    (kind : AbiKind)
    (physical : Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  sourceProgramEq : source.program = context.program
  sourceControlEq : source.control = .yielded sourceValue
  sourceRuntimeEq : source.runtime = sourceRuntime
  sourceFramesEq :
    source.frames = .bind result continuation callerEnv callerJoins ::
      sourceFrames
  targetStoreEq : target.store = targetStore
  targetControlEq :
    target.control = .returning (physical :: returnedTail)
  targetFramesEq :
    target.frames =
      .call 1 callerRemainder callerLocals
          (.localSet resultIndex :: targetRest) :: targetFrames
  continuationAdapted :
    CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
      targetRest
  stateRelated :
    StateRelated sourceFunction sourceRuntime callerEnv targetStore callerLocals
      witness
  frameAligned :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv targetStore
      callerLocals witness
  resultFound :
    findFVar? (functionBindings sourceFunction) result = some resultIndex
  kindAt :
    (functionBindings sourceFunction)[resultIndex]?.map Prod.snd = some kind
  valueRelated : PhysicalValueRel witness kind physical sourceValue

theorem ConcreteStructuredBindFrameFocus.observes
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  refine ⟨witness, ?_, ?_⟩
  · change target.store.host.runtime.world = source.runtime.world
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.world
  · change ConcreteTraceRel witness target.store.host.runtime.trace
      source.runtime.trace
    rw [related.targetStoreEq, related.sourceRuntimeEq]
    exact related.stateRelated.1.trace

/-- Unified control component of the compiler-derived structured relation.

The constructors collect every focus shape that has already crossed the
instruction-boundary proof: ordinary compiled code and return, pure external
argument/call/bind staging, direct-call argument staging/callee entry/caller
resumption, and saturated-closure staging/callee entry/caller resumption.
Resource/admission evidence remains an orthogonal conjunct of the eventual
ranked relation.  Keeping control unification separate makes missing
intermediate protocols explicit instead of hiding them inside a terminating
path theorem. -/
inductive ConcreteStructuredControlRel :
    MachineState → StructuredWasmState Host → Prop where
  | code
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
          sourceRuntime sourceEnv sourceCode targetStore targetLocals targetCode
          witness source target) :
      ConcreteStructuredControlRel source target
  | yielded
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredYieldFocus context sourceFunction sourceRuntime
          sourceEnv sourceValue targetStore targetLocals witness kind physical
          source target) :
      ConcreteStructuredControlRel source target
  | externalCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredExternalCallReadyFocus program context sourceModule
          sourceFunction targetModule hosts externals site operation
          resolvedResultKind targetImport labels continuation callerJoins
          sourceFrames targetStore callerLocals callerRemainder targetRest
          targetFrames witness physicalArgs callIndex resultIndex source target) :
      ConcreteStructuredControlRel source target
  | externalBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredExternalBindFocus context sourceModule sourceFunction
          labels sourceRuntime callerEnv sourceValue result continuation
          callerJoins sourceFrames targetStore callerLocals callerRemainder
          targetRest targetFrames witness kind physical resultIndex source
          target) :
      ConcreteStructuredControlRel source target
  | directCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredDirectCallReadyFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site row labels
          sourceRuntime continuation callerJoins sourceFrames targetStore
          callerLocals callerRemainder targetRest targetFrames witness
          physicalArgs resultIndex source target) :
      ConcreteStructuredControlRel source target
  | directCallEntry
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredDirectCallEntryFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site row labels
          sourceRuntime continuation callerJoins sourceFrames targetStore
          callerLocals callerRemainder targetRest targetFrames witness
          physicalArgs resultIndex source target) :
      ConcreteStructuredControlRel source target
  | directBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredBindFrameFocus context sourceModule sourceFunction
          labels sourceRuntime callerEnv sourceValue result continuation
          callerJoins sourceFrames targetStore callerLocals callerRemainder
          targetRest targetFrames returnedTail witness kind physical resultIndex
          source target) :
      ConcreteStructuredControlRel source target
  | saturatedCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedCallReadyFocus context sourceModule
          sourceFunction targetModule site labels sourceRuntime continuation
          callerJoins sourceFrames targetStore callerLocals targetValue
          targetRest targetFrames witness resultIndex source target) :
      ConcreteStructuredControlRel source target
  | saturatedCallEntry
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedCallEntryFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site resolution
          row labels sourceRuntime continuation callerJoins sourceFrames
          targetStore callerLocals callerRemainder targetRest targetFrames
          witness physicalArgs resultIndex matcherCount source target) :
      ConcreteStructuredControlRel source target
  | saturatedBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedBindFrameFocus context sourceModule
          sourceFunction labels sourceRuntime callerEnv sourceValue result
          continuation callerJoins sourceFrames targetStore callerLocals
          calleeLocals physicalArgs callerRemainder targetRest targetFrames
          witness kind physical resultIndex matcherCount source target) :
      ConcreteStructuredControlRel source target

/-- Every currently unified compiler-control shape has exact concrete
world/trace agreement.  The eventual `ConcreteRankedTraceSimulation.observes`
field is therefore discharged once, independently of which code, call, or
return constructor is active. -/
theorem ConcreteStructuredControlRel.observes
    {source : MachineState} {target : StructuredWasmState Host}
    (related : ConcreteStructuredControlRel source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  cases related with
  | code focus => exact focus.observes
  | yielded focus => exact focus.observes
  | externalCallReady focus => exact focus.observes
  | externalBind focus => exact focus.observes
  | directCallReady focus => exact focus.observes
  | directCallEntry focus => exact focus.observes
  | directBind focus => exact focus.observes
  | saturatedCallReady focus => exact focus.observes
  | saturatedCallEntry focus => exact focus.calleeFocus.observes
  | saturatedBind focus => exact focus.observes

/-- Recursive correspondence between suspended source bind frames and the
compiler-generated structured target frames that resume them.

The relation is indexed by the *current* runtime, store, and refinement
witness.  Consequently every suspended caller is related to the same current
heap as the running callee, rather than to a frozen entry heap.  This is the
stack component needed by a pointwise simulation: an internal call adds one
constructor, arbitrary nested callee steps transport every constructor, and a
yield consumes only the head constructor.  No callee evaluation or termination
evidence occurs in the relation.

Target-only matcher labels belong to the saturated constructor rather than
being identified with source frames.  The `case` constructor similarly records
the labels retained by a selected constructor-case arm: these frames have no
source counterpart and are removed when that arm returns. -/
inductive ConcreteStructuredFrameRel
    (program : Fir.LeanIR.ImpureProgram)
    (sourceRuntime : RuntimeState)
    (targetStore : Wasm.Store Host)
    (witness : RefinementWitness) :
    Option AbiKind → List Frame → List StructuredWasmFrame → Prop where
  | nil :
      ConcreteStructuredFrameRel program sourceRuntime targetStore witness none
        [] []
  | case
      {expectedResult : Option AbiKind}
      {sourceFrames : List Frame}
      {targetFrames : List StructuredWasmFrame}
      {belowStack : List Wasm.Value}
      {targetRest : Wasm.Program}
      {testCount : Nat}
      (tail :
        ConcreteStructuredFrameRel program sourceRuntime targetStore witness
          expectedResult sourceFrames targetFrames) :
      ConcreteStructuredFrameRel program sourceRuntime targetStore witness
        expectedResult sourceFrames
        (structuredWasmCaseLabels belowStack targetRest testCount ++
          targetFrames)
  | direct
      {context : Fir.Wasm.Context}
      {sourceModule : Fir.Wasm.Module}
      {sourceFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {callerEnv : Env}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {kind : AbiKind}
      {resultIndex : Nat}
      {tailResult : Option AbiKind}
      (programEq : program = context.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
          targetRest)
      (callerStateRelated :
        StateRelated sourceFunction sourceRuntime callerEnv targetStore
          callerLocals witness)
      (callerFrameAligned :
        ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv
          targetStore callerLocals witness)
      (resultFound :
        findFVar? (functionBindings sourceFunction) result = some resultIndex)
      (kindAt :
        (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (tail :
        ConcreteStructuredFrameRel program sourceRuntime targetStore witness
          tailResult sourceFrames targetFrames) :
      ConcreteStructuredFrameRel program sourceRuntime targetStore witness
        (some kind) (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder callerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames)
  | saturated
      {context : Fir.Wasm.Context}
      {sourceModule : Fir.Wasm.Module}
      {sourceFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {callerEnv : Env}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerLocals : Wasm.Locals}
      {physicalArgs callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {kind : AbiKind}
      {resultIndex matcherCount : Nat}
      {tailResult : Option AbiKind}
      (programEq : program = context.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
          targetRest)
      (callerStateRelated :
        StateRelated sourceFunction sourceRuntime callerEnv targetStore
          callerLocals witness)
      (callerFrameAligned :
        ConcreteLocalFrameAligned sourceFunction sourceRuntime callerEnv
          targetStore callerLocals witness)
      (resultFound :
        findFVar? (functionBindings sourceFunction) result = some resultIndex)
      (kindAt :
        (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (tail :
        ConcreteStructuredFrameRel program sourceRuntime targetStore witness
          tailResult sourceFrames targetFrames) :
      ConcreteStructuredFrameRel program sourceRuntime targetStore witness
        (some kind) (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder
            { callerLocals with
              values := physicalArgs.reverse ++ callerRemainder }
            [.localSet resultIndex] ::
          (List.replicate matcherCount (.label 0 callerRemainder []) ++
            .label 0 callerRemainder
                ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
              targetFrames))

/-- Transport every suspended caller across one accumulated runtime segment.
The current running focus supplies the evolved world/heap relation; witness
transport reinterprets saved locals, while frame layout and compiler
adaptation remain static. -/
theorem ConcreteStructuredFrameRel.transport
    {program : Fir.LeanIR.ImpureProgram}
    {entryRuntime currentRuntime : RuntimeState}
    {entryStore currentStore : Wasm.Store Host}
    {entryWitness currentWitness : RefinementWitness}
    {expectedResult : Option AbiKind}
    {sourceFrames : List Frame}
    {targetFrames : List StructuredWasmFrame}
    {currentFunction : Fir.Wasm.Function}
    {currentEnv : Env}
    {currentLocals : Wasm.Locals}
    (related : ConcreteStructuredFrameRel program entryRuntime entryStore
      entryWitness expectedResult sourceFrames targetFrames)
    (transports :
      ReuseCapacityCodeEntryTransports entryRuntime currentRuntime entryStore
        currentStore entryWitness currentWitness)
    (currentRelated :
      StateRelated currentFunction currentRuntime currentEnv currentStore
        currentLocals currentWitness) :
    ConcreteStructuredFrameRel program currentRuntime currentStore
      currentWitness expectedResult sourceFrames targetFrames := by
  induction related with
  | nil => exact .nil
  | case _tail ih => exact .case ih
  | direct programEq continuationAdapted callerStateRelated
      callerFrameAligned resultFound kindAt _tail ih =>
      exact .direct programEq continuationAdapted
        (transports.savedStateRelated callerStateRelated currentRelated)
        (by
          simpa [ConcreteLocalFrameAligned] using callerFrameAligned)
        resultFound kindAt ih
  | saturated programEq continuationAdapted callerStateRelated
      callerFrameAligned resultFound kindAt _tail ih =>
      exact .saturated programEq continuationAdapted
        (transports.savedStateRelated callerStateRelated currentRelated)
        (by
          simpa [ConcreteLocalFrameAligned] using callerFrameAligned)
        resultFound kindAt ih

/-- Stack evidence appropriate to each of the ten local control protocols.

Ordinary code uses the complete recursive frame relation.  A terminal yield
requires the empty/root index.  Staged calls and bind resumptions carry the
caller suffix because their head bind/call protocol is already described by
the local focus.  This separation avoids duplicating or weakening the exact
intermediate-state equalities in those focus relations. -/
inductive ConcreteStructuredControlFrameRel :
    {source : MachineState} → {target : StructuredWasmState Host} →
      ConcreteStructuredControlRel source target → Prop where
  | code
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
          sourceRuntime sourceEnv sourceCode targetStore targetLocals targetCode
          witness source target)
      (frames : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult source.frames target.frames) :
      ConcreteStructuredControlFrameRel (.code related)
  | yielded
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredYieldFocus context sourceFunction sourceRuntime
          sourceEnv sourceValue targetStore targetLocals witness kind physical
          source target)
      (frames : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult source.frames target.frames) :
      ConcreteStructuredControlFrameRel (.yielded related)
  | externalCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredExternalCallReadyFocus program context sourceModule
          sourceFunction targetModule hosts externals site operation
          resolvedResultKind targetImport labels continuation callerJoins
          sourceFrames targetStore callerLocals callerRemainder targetRest
          targetFrames witness physicalArgs callIndex resultIndex source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.externalCallReady related)
  | externalBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredExternalBindFocus context sourceModule sourceFunction
          labels sourceRuntime callerEnv sourceValue result continuation
          callerJoins sourceFrames targetStore callerLocals callerRemainder
          targetRest targetFrames witness kind physical resultIndex source
          target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.externalBind related)
  | directCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredDirectCallReadyFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site row labels
          sourceRuntime continuation callerJoins sourceFrames targetStore
          callerLocals callerRemainder targetRest targetFrames witness
          physicalArgs resultIndex source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.directCallReady related)
  | directCallEntry
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredDirectCallEntryFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site row labels
          sourceRuntime continuation callerJoins sourceFrames targetStore
          callerLocals callerRemainder targetRest targetFrames witness
          physicalArgs resultIndex source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.directCallEntry related)
  | directBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredBindFrameFocus context sourceModule sourceFunction
          labels sourceRuntime callerEnv sourceValue result continuation
          callerJoins sourceFrames targetStore callerLocals callerRemainder
          targetRest targetFrames returnedTail witness kind physical resultIndex
          source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.directBind related)
  | saturatedCallReady
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedCallReadyFocus context sourceModule
          sourceFunction targetModule site labels sourceRuntime continuation
          callerJoins sourceFrames targetStore callerLocals targetValue
          targetRest targetFrames witness resultIndex source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.saturatedCallReady related)
  | saturatedCallEntry
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedCallEntryFocus callerContext calleeContext
          sourceModule callerFunction calleeFunction targetModule site resolution
          row labels sourceRuntime continuation callerJoins sourceFrames
          targetStore callerLocals callerRemainder targetRest targetFrames
          witness physicalArgs resultIndex matcherCount source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.saturatedCallEntry related)
  | saturatedBind
      {source : MachineState} {target : StructuredWasmState Host}
      (related :
        ConcreteStructuredSaturatedBindFrameFocus context sourceModule
          sourceFunction labels sourceRuntime callerEnv sourceValue result
          continuation callerJoins sourceFrames targetStore callerLocals
          calleeLocals physicalArgs callerRemainder targetRest targetFrames
          witness kind physical resultIndex matcherCount source target)
      (tail : ConcreteStructuredFrameRel source.program sourceRuntime
        targetStore witness expectedResult sourceFrames targetFrames) :
      ConcreteStructuredControlFrameRel (.saturatedBind related)

/-- The control-and-stack component of the non-terminating compiler relation.
Resource admission is deliberately a separate conjunct: this component says
only that the current local protocol and every suspended caller came from the
compiler and refine the same current concrete heap. -/
structure ConcreteStructuredStackRel
    (source : MachineState) (target : StructuredWasmState Host) : Prop where
  control : ConcreteStructuredControlRel source target
  frames : ConcreteStructuredControlFrameRel control

theorem ConcreteStructuredStackRel.observes
    {source : MachineState} {target : StructuredWasmState Host}
    (related : ConcreteStructuredStackRel source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.control.observes

/-- Entry-relative resource component of the pointwise relation.

The existential entry anchor is fixed for the currently executing generated
function.  The cache/ownership/fact/budget frame describes the current state,
and `ReuseCapacityCodeEntryTransports` records everything that happened since
that entry.  Entering a nested call stores this predicate for the caller and
starts a fresh reflexive anchor for the callee; returning composes the callee
transports with the stored caller.  No result value, final state, or evaluation
derivation is part of the predicate. -/
def ConcreteStructuredCurrentResource
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (externals : ExternalImpl)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (witness : RefinementWitness) : Prop :=
  ∃ entryRuntime entryStore entryWitness,
    ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness ∧
      ClosureAllocationsAbiAligned context.program witness

/-- Resource scope with its function-entry boundary exposed.

The pointwise simulation keeps this stronger form for the active function.
The entry triple is exactly the runtime, store, and witness at which the call
was entered, so a return can compose the callee transports with the suspended
caller's outer scope. -/
def ConcreteStructuredResourceScope
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (externals : ExternalImpl)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (witness : RefinementWitness) : Prop :=
  ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
      entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
      sourceEnv targetStore targetLocals witness ∧
    ClosureAllocationsAbiAligned context.program witness

/-- Forget the exposed entry boundary after composing it into an enclosing
resource stack. -/
theorem ConcreteStructuredResourceScope.current
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    ConcreteStructuredCurrentResource context sourceModule sourceFunction
      externals facts remainingBytes sourceRuntime sourceEnv targetStore
      targetLocals witness :=
  ⟨entryRuntime, entryStore, entryWitness, scope.1, scope.2⟩

/-- Start a resource scope at the current generated-function entry. -/
theorem ConcreteStructuredResourceScope.root
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (invariant : ConcreteReuseCapacityCacheAbiFrame context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness) :
    ConcreteStructuredResourceScope context sourceModule sourceFunction
      externals sourceRuntime targetStore witness facts remainingBytes
      sourceRuntime sourceEnv targetStore targetLocals witness :=
  ⟨⟨invariant.cacheFrame,
      ReuseCapacityCodeEntryTransports.refl sourceRuntime targetStore witness⟩,
    invariant.closureAbi⟩

theorem ConcreteStructuredResourceScope.stateRelated
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    StateRelated sourceFunction sourceRuntime sourceEnv targetStore targetLocals
      witness :=
  scope.1.1.stateRelated.stateRelated

theorem ConcreteStructuredResourceScope.frameAligned
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    ConcreteLocalFrameAligned sourceFunction sourceRuntime sourceEnv targetStore
      targetLocals witness :=
  scope.1.1.1.1.1.2.2.1

/-- Forget the facts/cache/entry layers while retaining the pure-external
handler and exact address-space budget required by the imported-call rule. -/
theorem ConcreteStructuredResourceScope.budgetedPureExternal
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    ConcreteBudgetedPureExternalFrame sourceFunction externals remainingBytes
      sourceRuntime sourceEnv targetStore targetLocals witness := by
  rcases scope.1.1.1.1 with
    ⟨⟨_capacityRelated, _ordinaryTokens, frameAligned, budget⟩,
      integerImplementation, naturalImplementation, scalarImplementation⟩
  exact ⟨⟨frameAligned, budget⟩, integerImplementation,
    naturalImplementation, scalarImplementation⟩

theorem ConcreteStructuredResourceScope.transports
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    ReuseCapacityCodeEntryTransports entryRuntime sourceRuntime entryStore
      targetStore entryWitness witness :=
  scope.1.2

theorem ConcreteStructuredResourceScope.withValues
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness)
    (values : List Wasm.Value) :
    ConcreteStructuredResourceScope context sourceModule sourceFunction
      externals entryRuntime entryStore entryWitness facts remainingBytes
      sourceRuntime sourceEnv targetStore { targetLocals with values := values }
      witness :=
  ⟨scope.1.withValues values, scope.2⟩

/--
Advance the hereditary resource scope across the imported-call half of a
pure external `let`.

The destination has not been written yet, so the source environment, target
locals, and authoritative reuse facts remain unchanged.  The concrete call
may nevertheless allocate a result and extend the witness; the operation
evidence transports every heap/cache/closure component and consumes exactly
the response-selected budget.
-/
theorem ConcreteStructuredResourceScope.afterExternalCall
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore nextStore : Wasm.Store Host}
    {entryWitness witness nextWitness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes stepCost : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {physicalArgs : List Wasm.Value}
    {sourceValue : Value}
    {physicalResult : Wasm.Value}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness)
    (nextFrame : ConcreteBudgetedPureExternalFrame sourceFunction externals
      (remainingBytes - stepCost) nextRuntime sourceEnv nextStore targetLocals
      nextWitness)
    (finalRelated : StateRelated sourceFunction nextRuntime sourceEnv nextStore
      targetLocals nextWitness)
    (evidence : ConcreteExternalCallEvidence operation resolvedResultKind
      targetStore physicalArgs sourceRuntime nextRuntime sourceValue witness nextStore
      nextWitness physicalResult) :
    ConcreteStructuredResourceScope context sourceModule sourceFunction
      externals entryRuntime entryStore entryWitness facts
      (remainingBytes - stepCost) nextRuntime sourceEnv nextStore targetLocals
      nextWitness := by
  rcases scope with ⟨⟨cacheFrame, entryTransports⟩, closureAbi⟩
  rcases cacheFrame with
    ⟨ownershipFrame, cacheTable, closureTableAgreement⟩
  rcases ownershipFrame with ⟨pureFrame, _descriptorAgreement⟩
  rcases pureFrame with
    ⟨reuseFrame, _integerImplementation, _naturalImplementation,
      _scalarImplementation⟩
  rcases reuseFrame with
    ⟨capacityRelated, ordinaryTokens, _frameAligned, _budget⟩
  rcases nextFrame with
    ⟨⟨nextFrameAligned, nextBudget⟩, nextIntegerImplementation,
      nextNaturalImplementation, nextScalarImplementation⟩
  have nextCapacity :
      ReuseCapacityStateRelated facts sourceFunction nextRuntime sourceEnv
        nextStore targetLocals nextWitness :=
    capacityRelated.transport finalRelated evidence.witnessTransport
      evidence.capacityTransport
  have nextOrdinary :
      ReuseTokenOrdinaryRel facts nextRuntime sourceEnv :=
    ordinaryTokens.transport evidence.ordinaryTransport
  have nextCache :
      LazyCacheGlobalsRel nextWitness sourceModule nextRuntime nextStore :=
    cacheTable.transport evidence.witnessTransport evidence.runtimeGlobals
      evidence.storeGlobals evidence.hostStaticLayout
  have nextClosureTables : ClosureTablesAgree nextStore nextWitness :=
    evidence.closureTables.agree closureTableAgreement
  have nextEntry :
      ReuseCapacityCodeEntryTransports entryRuntime nextRuntime entryStore
        nextStore entryWitness nextWitness :=
    entryTransports.step evidence.witnessTransport
      evidence.closureAllocationsPersistent evidence.capacityTransport
      evidence.ordinaryTransport evidence.externalsPreserved
      evidence.closureTables
  have nextReuseFrame :
      ConcreteReuseCapacityFrame sourceFunction facts
        (remainingBytes - stepCost) nextRuntime sourceEnv nextStore targetLocals
        nextWitness :=
    ⟨nextCapacity, nextOrdinary, nextFrameAligned, nextBudget⟩
  have nextPureFrame :
      ConcreteReuseCapacityPureExternalFrame sourceFunction externals facts
        (remainingBytes - stepCost) nextRuntime sourceEnv nextStore targetLocals
        nextWitness :=
    ⟨nextReuseFrame, nextIntegerImplementation, nextNaturalImplementation,
      nextScalarImplementation⟩
  have nextOwnershipFrame :
      ConcreteReuseCapacityPureExternalOwnershipFrame sourceFunction externals
        facts (remainingBytes - stepCost) nextRuntime sourceEnv nextStore
        targetLocals nextWitness :=
    ⟨nextPureFrame, nextClosureTables.descriptors⟩
  have nextCacheFrame :
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        facts (remainingBytes - stepCost) nextRuntime sourceEnv nextStore
        targetLocals nextWitness :=
    ⟨nextOwnershipFrame, nextCache, nextClosureTables⟩
  exact ⟨⟨nextCacheFrame, nextEntry⟩,
    closureAbi.ofPersistent evidence.closureAllocationsPersistent⟩

/--
Consume the generated destination write after an external call.

This second half changes neither heap nor witness.  It binds the semantic
result, checks the compiler-selected local update, and erases only the stale
reuse fact shadowed by the destination before ordinary code resumes.
-/
theorem ConcreteStructuredResourceScope.afterExternalBind
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals nextLocals : Wasm.Locals}
    {result : Lean.FVarId}
    {resultIndex : Nat}
    {sourceValue : Value}
    {physical : Wasm.Value}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness)
    (finalRelated : StateRelated sourceFunction sourceRuntime
      (bind sourceEnv result sourceValue) targetStore nextLocals witness)
    (finalAligned : ConcreteLocalFrameAligned sourceFunction sourceRuntime
      (bind sourceEnv result sourceValue) targetStore nextLocals witness)
    (resultFound :
      findFVar? (functionBindings sourceFunction) result = some resultIndex)
    (localUpdate : FirTalos.Correctness.LocalUpdate targetLocals nextLocals
      resultIndex physical) :
    ConcreteStructuredResourceScope context sourceModule sourceFunction
      externals entryRuntime entryStore entryWitness
      (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
      (bind sourceEnv result sourceValue) targetStore nextLocals witness := by
  rcases scope with ⟨⟨cacheFrame, entryTransports⟩, closureAbi⟩
  rcases cacheFrame with
    ⟨ownershipFrame, cacheTable, closureTableAgreement⟩
  rcases ownershipFrame with ⟨pureFrame, descriptorAgreement⟩
  rcases pureFrame with
    ⟨reuseFrame, integerImplementation, naturalImplementation,
      scalarImplementation⟩
  rcases reuseFrame with
    ⟨capacityRelated, ordinaryTokens, _frameAligned, budget⟩
  have nextCapacity :
      ReuseCapacityStateRelated (eraseReuseCapacityFact facts result)
        sourceFunction sourceRuntime (bind sourceEnv result sourceValue)
        targetStore nextLocals witness :=
    capacityRelated.eraseResult finalRelated resultFound localUpdate
      (WitnessTransport.refl witness)
      (HeaderCapacityTransport.refl targetStore.host.runtime.heap witness)
  have nextOrdinary :
      ReuseTokenOrdinaryRel (eraseReuseCapacityFact facts result) sourceRuntime
        (bind sourceEnv result sourceValue) :=
    ordinaryTokens.eraseBind (OrdinaryPersistenceTransport.refl sourceRuntime)
  have nextReuseFrame :
      ConcreteReuseCapacityFrame sourceFunction
        (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
        (bind sourceEnv result sourceValue) targetStore nextLocals witness :=
    ⟨nextCapacity, nextOrdinary, finalAligned, budget⟩
  have nextPureFrame :
      ConcreteReuseCapacityPureExternalFrame sourceFunction externals
        (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
        (bind sourceEnv result sourceValue) targetStore nextLocals witness :=
    ⟨nextReuseFrame, integerImplementation, naturalImplementation,
      scalarImplementation⟩
  have nextOwnershipFrame :
      ConcreteReuseCapacityPureExternalOwnershipFrame sourceFunction externals
        (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
        (bind sourceEnv result sourceValue) targetStore nextLocals witness :=
    ⟨nextPureFrame, descriptorAgreement⟩
  have nextCacheFrame :
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
        (bind sourceEnv result sourceValue) targetStore nextLocals witness :=
    ⟨nextOwnershipFrame, cacheTable, closureTableAgreement⟩
  exact ⟨⟨nextCacheFrame, entryTransports⟩, closureAbi⟩

/-- Historical resource scopes for the suspended caller chain.

The three leading indices are the exact entry boundary of the currently
active function.  Each constructor stores its caller scope ending at that
boundary, then recurses from the caller's own entry boundary.  Thus adjacent
scopes share a boundary by construction rather than by a later equality
proof. -/
inductive ConcreteStructuredSuspendedResourceStack
    (externals : ExternalImpl)
    (program : Fir.LeanIR.ImpureProgram) :
    RuntimeState → Wasm.Store Host → RefinementWitness →
      AbiKind → Option AbiKind → List Frame →
        List StructuredWasmFrame → Prop where
  | nil
      {entryRuntime : RuntimeState}
      {entryStore : Wasm.Store Host}
      {entryWitness : RefinementWitness}
      {functionResult : AbiKind} :
      ConcreteStructuredSuspendedResourceStack externals program entryRuntime
        entryStore entryWitness functionResult none [] []
  | case
      {entryRuntime : RuntimeState}
      {entryStore : Wasm.Store Host}
      {entryWitness : RefinementWitness}
      {functionResult : AbiKind}
      {expectedResult : Option AbiKind}
      {sourceFrames : List Frame}
      {targetFrames : List StructuredWasmFrame}
      {belowStack : List Wasm.Value}
      {targetRest : Wasm.Program}
      {testCount : Nat}
      (tail : ConcreteStructuredSuspendedResourceStack externals program
        entryRuntime entryStore entryWitness functionResult expectedResult
        sourceFrames targetFrames) :
      ConcreteStructuredSuspendedResourceStack externals program entryRuntime
        entryStore entryWitness functionResult expectedResult sourceFrames
        (structuredWasmCaseLabels belowStack targetRest testCount ++
          targetFrames)
  | direct
      {activeEntryRuntime callerEntryRuntime : RuntimeState}
      {activeEntryStore callerEntryStore : Wasm.Store Host}
      {activeEntryWitness callerEntryWitness : RefinementWitness}
      {context : Fir.Wasm.Context}
      {sourceModule : Fir.Wasm.Module}
      {sourceFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      {callerEnv : Env}
      {callerLocals : Wasm.Locals}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex : Nat}
      {tailResult : Option AbiKind}
      (callerScope : ConcreteStructuredResourceScope context sourceModule
        sourceFunction externals callerEntryRuntime callerEntryStore
        callerEntryWitness facts remainingBytes activeEntryRuntime callerEnv
        activeEntryStore callerLocals activeEntryWitness)
      (programEq : program = context.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
          targetRest)
      (resultFound :
        findFVar? (functionBindings sourceFunction) result = some resultIndex)
      (kindAt :
        (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (tail : ConcreteStructuredSuspendedResourceStack externals program
        callerEntryRuntime callerEntryStore callerEntryWitness callerResult
        tailResult sourceFrames targetFrames) :
      ConcreteStructuredSuspendedResourceStack externals program
        activeEntryRuntime activeEntryStore activeEntryWitness calleeResult
        (some kind)
        (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder callerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames)
  | saturated
      {activeEntryRuntime callerEntryRuntime : RuntimeState}
      {activeEntryStore callerEntryStore : Wasm.Store Host}
      {activeEntryWitness callerEntryWitness : RefinementWitness}
      {context : Fir.Wasm.Context}
      {sourceModule : Fir.Wasm.Module}
      {sourceFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      {callerEnv : Env}
      {callerLocals : Wasm.Locals}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {physicalArgs callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex matcherCount : Nat}
      {tailResult : Option AbiKind}
      (callerScope : ConcreteStructuredResourceScope context sourceModule
        sourceFunction externals callerEntryRuntime callerEntryStore
        callerEntryWitness facts remainingBytes activeEntryRuntime callerEnv
        activeEntryStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        activeEntryWitness)
      (programEq : program = context.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
          targetRest)
      (resultFound :
        findFVar? (functionBindings sourceFunction) result = some resultIndex)
      (kindAt :
        (functionBindings sourceFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (tail : ConcreteStructuredSuspendedResourceStack externals program
        callerEntryRuntime callerEntryStore callerEntryWitness callerResult
        tailResult sourceFrames targetFrames) :
      ConcreteStructuredSuspendedResourceStack externals program
        activeEntryRuntime activeEntryStore activeEntryWitness calleeResult
        (some kind)
        (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder
            { callerLocals with
              values := physicalArgs.reverse ++ callerRemainder }
            [.localSet resultIndex] ::
          (List.replicate matcherCount (.label 0 callerRemainder []) ++
            .label 0 callerRemainder
                ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
              targetFrames))

/-- The resource component paired with one complete structured frame stack.

`current` evolves within the active function. `suspended` is stable during
those local steps and records the exact scopes and function-result ABI chain
needed to restore callers. Keeping both in the same direct/saturated
constructor prevents protocol and result metadata from diverging. -/
structure ConcreteStructuredResourceStack
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (externals : ExternalImpl)
    (entryRuntime sourceRuntime : RuntimeState)
    (entryStore targetStore : Wasm.Store Host)
    (entryWitness witness : RefinementWitness)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceEnv : Env)
    (targetLocals : Wasm.Locals)
    (functionResult : AbiKind)
    (expectedResult : Option AbiKind)
    (sourceFrames : List Frame)
    (targetFrames : List StructuredWasmFrame) : Prop where
  current : ConcreteStructuredResourceScope context sourceModule
    sourceFunction externals entryRuntime entryStore entryWitness facts
    remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness
  suspended : ConcreteStructuredSuspendedResourceStack externals program
    entryRuntime entryStore entryWitness functionResult expectedResult
    sourceFrames targetFrames

/-- Recover the extensional structural frame relation at the current heap.

The active scope transports each saved caller from the active function's
entry boundary to the current state.  Recursion first reconstructs the older
stack at that boundary, then transports it through the same active scope. -/
theorem ConcreteStructuredSuspendedResourceStack.frameRel
    {externals : ExternalImpl}
    {program : Fir.LeanIR.ImpureProgram}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {expectedResult : Option AbiKind}
    {sourceFrames : List Frame}
    {targetFrames : List StructuredWasmFrame}
    {currentContext : Fir.Wasm.Context}
    {currentModule : Fir.Wasm.Module}
    {currentFunction : Fir.Wasm.Function}
    {currentFacts : ReuseCapacityFacts}
    {currentBytes : Nat}
    {currentEnv : Env}
    {currentLocals : Wasm.Locals}
    (stack : ConcreteStructuredSuspendedResourceStack externals program
      entryRuntime entryStore entryWitness functionResult expectedResult
      sourceFrames targetFrames)
    (currentScope : ConcreteStructuredResourceScope currentContext currentModule
      currentFunction externals entryRuntime entryStore entryWitness
      currentFacts currentBytes sourceRuntime currentEnv targetStore
      currentLocals witness) :
    ConcreteStructuredFrameRel program sourceRuntime targetStore witness
      expectedResult sourceFrames targetFrames := by
  induction stack generalizing currentContext currentModule currentFunction
      currentFacts currentBytes sourceRuntime currentEnv targetStore
      currentLocals witness with
  | nil => exact .nil
  | case _tail ih => exact .case (ih currentScope)
  | direct callerScope programEq continuationAdapted resultFound kindAt
      _calleeCompatible _tail ih =>
      have tailAtEntry := ih callerScope
      have tailAtCurrent :=
        tailAtEntry.transport currentScope.transports currentScope.stateRelated
      exact .direct programEq continuationAdapted
        (currentScope.transports.savedStateRelated callerScope.stateRelated
          currentScope.stateRelated)
        (by
          simpa [ConcreteLocalFrameAligned] using callerScope.frameAligned)
        resultFound kindAt tailAtCurrent
  | saturated callerScope programEq continuationAdapted resultFound kindAt
      _calleeCompatible _tail ih =>
      have tailAtEntry := ih callerScope
      have tailAtCurrent :=
        tailAtEntry.transport currentScope.transports currentScope.stateRelated
      exact .saturated programEq continuationAdapted
        (currentScope.transports.savedStateRelated callerScope.stateRelated
          currentScope.stateRelated)
        (by
          simpa [ConcreteLocalFrameAligned] using callerScope.frameAligned)
        resultFound kindAt tailAtCurrent

theorem ConcreteStructuredResourceStack.frameRel
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {functionResult : AbiKind}
    {expectedResult : Option AbiKind}
    {sourceFrames : List Frame}
    {targetFrames : List StructuredWasmFrame}
    (stack : ConcreteStructuredResourceStack program context sourceModule
      sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
      entryWitness witness facts remainingBytes sourceEnv targetLocals
      functionResult expectedResult sourceFrames targetFrames) :
    ConcreteStructuredFrameRel program sourceRuntime targetStore witness
      expectedResult sourceFrames targetFrames :=
  stack.suspended.frameRel stack.current

theorem ConcreteStructuredResourceStack.root
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {functionResult : AbiKind}
    (scope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals entryRuntime entryStore entryWitness facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness) :
    ConcreteStructuredResourceStack program context sourceModule sourceFunction
      externals entryRuntime sourceRuntime entryStore targetStore entryWitness
      witness facts remainingBytes sourceEnv targetLocals functionResult none
      [] [] :=
  ⟨scope, .nil⟩

/-- Any ordinary cache/ABI frame starts a pointwise resource scope with a
reflexive entry anchor. -/
theorem ConcreteStructuredCurrentResource.root
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (invariant : ConcreteReuseCapacityCacheAbiFrame context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness) :
    ConcreteStructuredCurrentResource context sourceModule sourceFunction
      externals facts remainingBytes sourceRuntime sourceEnv targetStore
      targetLocals witness :=
  ⟨sourceRuntime, targetStore, witness,
    ⟨invariant.cacheFrame,
      ReuseCapacityCodeEntryTransports.refl sourceRuntime targetStore witness⟩,
    invariant.closureAbi⟩

theorem ConcreteStructuredCurrentResource.entryFrame
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (resource : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness) :
    ∃ entryRuntime entryStore entryWitness,
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness := by
  rcases resource with ⟨entryRuntime, entryStore, entryWitness, frame, _abi⟩
  exact ⟨entryRuntime, entryStore, entryWitness, frame⟩

theorem ConcreteStructuredCurrentResource.cacheFrame
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (resource : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness) :
    ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals facts
      remainingBytes sourceRuntime sourceEnv targetStore targetLocals witness := by
  rcases resource.entryFrame with ⟨_entryRuntime, _entryStore, _entryWitness,
    frame⟩
  exact frame.1

theorem ConcreteStructuredCurrentResource.closureAbi
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (resource : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness) :
    ClosureAllocationsAbiAligned context.program witness := by
  rcases resource with ⟨_entryRuntime, _entryStore, _entryWitness, _frame,
    abi⟩
  exact abi

theorem ConcreteStructuredCurrentResource.withValues
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    (resource : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness)
    (values : List Wasm.Value) :
    ConcreteStructuredCurrentResource context sourceModule sourceFunction
      externals facts remainingBytes sourceRuntime sourceEnv targetStore
      { targetLocals with values := values } witness := by
  rcases resource with ⟨entryRuntime, entryStore, entryWitness, frame, abi⟩
  exact ⟨entryRuntime, entryStore, entryWitness, frame.withValues values, abi⟩

/-- Direct generated-call entry pushes one hereditary frame constructor on an
already related caller suffix. -/
theorem ConcreteStructuredDirectCallEntryFocus.frameRel
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {callerFunctionResult : AbiKind}
    {tailResult : Option AbiKind}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames) :
    ConcreteStructuredFrameRel source.program sourceRuntime targetStore witness
      (some site.resultKind) source.frames target.frames := by
  rw [entry.sourceFramesEq, entry.targetFramesEq]
  exact .direct
    (entry.calleeFocus.sourceProgramEq.trans row.contextProgram)
    entry.continuationAdapted entry.callerStateRelated
    entry.callerFrameAligned entry.resultFound entry.resultKindAt tail

/-- Exactly saturated closure entry pushes the call frame and its complete
matcher-label protocol as one hereditary frame constructor. -/
theorem ConcreteStructuredSaturatedCallEntryFocus.frameRel
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {originalRuntime sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite callerContext decl callerEnv}
    {resolution : SaturatedClosureCallResolution callerContext originalRuntime
      site}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    (entry : ConcreteStructuredSaturatedCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site resolution row labels sourceRuntime continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs resultIndex matcherCount source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames) :
    ConcreteStructuredFrameRel source.program sourceRuntime targetStore witness
      (some site.resultKind) source.frames target.frames := by
  rw [entry.sourceFramesEq, entry.targetFramesEq]
  exact .saturated
    (entry.calleeFocus.sourceProgramEq.trans row.contextProgram)
    entry.continuationAdapted entry.callerStateRelated
    entry.callerFrameAligned entry.resultFound entry.resultKindAt tail

/-- Push an exactly saturated closure call, including its matcher-label
protocol, onto the hereditary resource stack. -/
theorem ConcreteStructuredSaturatedCallEntryFocus.pushResourceStack
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {originalRuntime : RuntimeState}
    {site : SaturatedClosureCallSite callerContext decl callerEnv}
    {resolution : SaturatedClosureCallResolution callerContext originalRuntime
      site}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {callerEntryRuntime sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerEntryStore targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {callerEntryWitness witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {callerFunctionResult : AbiKind}
    {tailResult : Option AbiKind}
    (entry : ConcreteStructuredSaturatedCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site resolution row labels sourceRuntime continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness physicalArgs resultIndex matcherCount source target)
    (callerResources : ConcreteStructuredResourceStack callerContext.program
      callerContext sourceModule callerFunction externals callerEntryRuntime
      sourceRuntime callerEntryStore targetStore callerEntryWitness witness facts
      remainingBytes callerEnv callerLocals callerFunctionResult tailResult
      sourceFrames targetFrames)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals sourceRuntime targetStore witness []
      remainingBytes sourceRuntime resolution.calleeEnv targetStore
      (row.targetFunction.toLocals physicalArgs) witness) :
    ConcreteStructuredResourceStack callerContext.program calleeContext
      sourceModule calleeFunction externals sourceRuntime sourceRuntime
      targetStore targetStore witness witness [] remainingBytes
      resolution.calleeEnv (row.targetFunction.toLocals physicalArgs)
      resolution.targetResultKind (some site.resultKind) source.frames
      target.frames := by
  rw [entry.sourceFramesEq, entry.targetFramesEq]
  exact ⟨calleeScope,
    .saturated (callerResources.current.withValues _)
      rfl entry.continuationAdapted entry.resultFound entry.resultKindAt
      resolution.targetResultRefines callerResources.suspended⟩

/-- Classify a related return from the recursive frame stack.

With an empty stack the return is the terminal `yielded` control shape.  A
direct or saturated head is converted to the corresponding bind-resumption
shape using only the head's ABI compatibility.  In particular this theorem
does not need the callee's entry focus or a proof that the callee eventually
returns: it applies at any finite prefix that happens to reach a related
yield. -/
theorem ConcreteStructuredFrameRel.control_of_yield
    {currentContext : Fir.Wasm.Context}
    {currentFunction : Fir.Wasm.Function}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceValue : Value}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness : RefinementWitness}
    {actualKind : AbiKind}
    {physical : Wasm.Value}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {expectedResult : Option AbiKind}
    {sourceFrames : List Frame}
    {targetFrames : List StructuredWasmFrame}
    (yielded : ConcreteStructuredYieldFocus currentContext currentFunction
      sourceRuntime sourceEnv sourceValue targetStore targetLocals witness
      actualKind physical source target)
    (frames : ConcreteStructuredFrameRel source.program sourceRuntime
      targetStore witness expectedResult sourceFrames targetFrames)
    (sourceFramesEq : source.frames = sourceFrames)
    (targetFramesEq : target.frames = targetFrames)
    (resultCompatible :
      match expectedResult with
      | none => True
      | some expected => actualKind.refines expected = true) :
    ConcreteStructuredStackRel source target := by
  cases frames with
  | nil =>
      have rootFrames :
          ConcreteStructuredFrameRel source.program sourceRuntime targetStore
            witness none source.frames target.frames := by
        simpa [sourceFramesEq, targetFramesEq] using
          (ConcreteStructuredFrameRel.nil (program := source.program)
            (sourceRuntime := sourceRuntime) (targetStore := targetStore)
            (witness := witness))
      exact ⟨ConcreteStructuredControlRel.yielded yielded,
        .yielded yielded rootFrames⟩
  | case tail =>
      have caseFrames :
          ConcreteStructuredFrameRel source.program sourceRuntime targetStore
            witness expectedResult source.frames target.frames := by
        simpa [sourceFramesEq, targetFramesEq] using
          (ConcreteStructuredFrameRel.case tail)
      exact ⟨ConcreteStructuredControlRel.yielded yielded,
        .yielded yielded caseFrames⟩
  | @direct context sourceModule sourceFunction labels callerEnv result
      continuation callerJoins sourceFrames callerLocals callerRemainder
      targetRest targetFrames kind resultIndex tailResult programEq
      continuationAdapted callerStateRelated callerFrameAligned resultFound
      kindAt tail =>
      let resumed :
          ConcreteStructuredBindFrameFocus context sourceModule sourceFunction
            labels sourceRuntime callerEnv sourceValue result continuation
            callerJoins sourceFrames targetStore callerLocals callerRemainder
            targetRest targetFrames targetLocals.values witness kind physical
            resultIndex source target := {
        sourceProgramEq := programEq
        sourceControlEq := yielded.sourceControlEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        sourceFramesEq
        targetStoreEq := yielded.targetStoreEq
        targetControlEq := yielded.targetControlEq
        targetFramesEq
        continuationAdapted
        stateRelated := callerStateRelated
        frameAligned := callerFrameAligned
        resultFound
        kindAt
        valueRelated := yielded.valueRelated.ofRefines (by
          exact resultCompatible) }
      exact ⟨ConcreteStructuredControlRel.directBind resumed,
        .directBind resumed tail⟩
  | @saturated context sourceModule sourceFunction labels callerEnv result
      continuation callerJoins sourceFrames callerLocals physicalArgs
      callerRemainder targetRest targetFrames kind resultIndex matcherCount
      tailResult programEq continuationAdapted callerStateRelated
      callerFrameAligned resultFound kindAt tail =>
      let resumed :
          ConcreteStructuredSaturatedBindFrameFocus context sourceModule
            sourceFunction labels sourceRuntime callerEnv sourceValue result
            continuation callerJoins sourceFrames targetStore callerLocals
            targetLocals physicalArgs callerRemainder targetRest targetFrames
            witness kind physical resultIndex matcherCount source target := {
        sourceProgramEq := programEq
        sourceControlEq := yielded.sourceControlEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        sourceFramesEq
        targetStoreEq := yielded.targetStoreEq
        targetControlEq := yielded.targetControlEq
        targetFramesEq
        continuationAdapted
        stateRelated := callerStateRelated
        frameAligned := callerFrameAligned
        resultFound
        kindAt
        valueRelated := yielded.valueRelated.ofRefines (by
          exact resultCompatible) }
      exact ⟨ConcreteStructuredControlRel.saturatedBind resumed,
        .saturatedBind resumed tail⟩

/-- Close the hereditary direct-call scope at a related callee yield.

The callee's current state relation and the accumulated entry transports
reconstruct the saved caller relation at the evolved store and witness.  The
explicit frame equalities state that nested administrative work has unwound
back to this call boundary.  The result is exactly the previously accepted
bind-frame focus, ready for `ConcreteStructuredBindFrameFocus.advance`; no
pre-call store equality or target-execution certificate is assumed. -/
theorem ConcreteStructuredDirectCallEntryFocus.bindFrame_of_yield
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime currentRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryStore currentStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {entryWitness currentWitness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {entrySource : MachineState}
    {entryTarget : StructuredWasmState Host}
    {currentEnv : Env}
    {sourceValue : Value}
    {actualKind : AbiKind}
    {physical : Wasm.Value}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels entryRuntime continuation callerJoins sourceFrames
      entryStore callerLocals callerRemainder targetRest targetFrames
      entryWitness physicalArgs resultIndex entrySource entryTarget)
    (yielded : ConcreteStructuredYieldFocus calleeContext calleeFunction
      currentRuntime currentEnv sourceValue currentStore calleeLocals
      currentWitness actualKind physical source target)
    (transports :
      ReuseCapacityCodeEntryTransports entryRuntime currentRuntime entryStore
        currentStore entryWitness currentWitness)
    (sourceFramesEq :
      source.frames =
        .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames)
    (targetFramesEq :
      target.frames =
        .call 1 callerRemainder callerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames)
    (resultRefines : actualKind.refines site.resultKind = true) :
    ConcreteStructuredBindFrameFocus callerContext sourceModule callerFunction
      labels currentRuntime callerEnv sourceValue decl.fvarId continuation
      callerJoins sourceFrames currentStore callerLocals callerRemainder
      targetRest targetFrames calleeLocals.values currentWitness site.resultKind
      physical resultIndex source target := by
  have callerStateRelated :
      StateRelated callerFunction currentRuntime callerEnv currentStore
        callerLocals currentWitness :=
    transports.savedStateRelated entry.callerStateRelated yielded.stateRelated
  have callerFrameAligned :
      ConcreteLocalFrameAligned callerFunction currentRuntime callerEnv
        currentStore callerLocals currentWitness := by
    simpa [ConcreteLocalFrameAligned] using entry.callerFrameAligned
  exact {
    sourceProgramEq := yielded.sourceProgramEq.trans row.contextProgram
    sourceControlEq := yielded.sourceControlEq
    sourceRuntimeEq := yielded.sourceRuntimeEq
    sourceFramesEq
    targetStoreEq := yielded.targetStoreEq
    targetControlEq := yielded.targetControlEq
    targetFramesEq
    continuationAdapted := entry.continuationAdapted
    stateRelated := callerStateRelated
    frameAligned := callerFrameAligned
    resultFound := entry.resultFound
    kindAt := entry.resultKindAt
    valueRelated := yielded.valueRelated.ofRefines resultRefines }

/-- Entry-relative cache-frame specialization of `bindFrame_of_yield`.

This is the direct composition point with the established hereditary runtime
proof: its final frame already contains the accumulated entry transports, so
clients need not unpack or restate witness/capacity/ownership transport when
closing a structured call. -/
theorem
    ConcreteStructuredDirectCallEntryFocus.bindFrame_of_yield_cacheFrame
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime currentRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryStore currentStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {entryWitness currentWitness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {entrySource : MachineState}
    {entryTarget : StructuredWasmState Host}
    {currentEnv : Env}
    {sourceValue : Value}
    {actualKind : AbiKind}
    {physical : Wasm.Value}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {resultFacts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels entryRuntime continuation callerJoins sourceFrames
      entryStore callerLocals callerRemainder targetRest targetFrames
      entryWitness physicalArgs resultIndex entrySource entryTarget)
    (yielded : ConcreteStructuredYieldFocus calleeContext calleeFunction
      currentRuntime currentEnv sourceValue currentStore calleeLocals
      currentWitness actualKind physical source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals)
        entryRuntime entryStore entryWitness resultFacts remainingBytes
        currentRuntime currentEnv currentStore calleeLocals currentWitness)
    (sourceFramesEq :
      source.frames =
        .bind decl.fvarId continuation callerEnv callerJoins :: sourceFrames)
    (targetFramesEq :
      target.frames =
        .call 1 callerRemainder callerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames)
    (resultRefines : actualKind.refines site.resultKind = true) :
    ConcreteStructuredBindFrameFocus callerContext sourceModule callerFunction
      labels currentRuntime callerEnv sourceValue decl.fvarId continuation
      callerJoins sourceFrames currentStore callerLocals callerRemainder
      targetRest targetFrames calleeLocals.values currentWitness site.resultKind
      physical resultIndex source target :=
  entry.bindFrame_of_yield yielded invariant.2 sourceFramesEq targetFramesEq
    resultRefines

/-- Resuming a related bind/call frame takes one source administrative step
and exactly two structured target steps: unwind the call frame, then store the
returned physical value.  The resulting source code and target residual code
re-enter `ConcreteStructuredCodeFocus` with the semantic result bound in the
caller environment. -/
theorem ConcreteStructuredBindFrameFocus.advance
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target) :
    ∃ sourceAfter targetAfter updated resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      callerLocals.set? resultIndex physical = some updated ∧
      resumedLocals = { updated with values := callerRemainder } ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
          sourceRuntime (bind callerEnv result sourceValue) continuation
          targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
        sourceAfter.joins = callerJoins ∧
        sourceAfter.frames = sourceFrames ∧
        targetAfter.frames = targetFrames := by
  obtain ⟨updated, targetSet, updatedAligned⟩ :=
    related.frameAligned.set?
      (nextRuntime := sourceRuntime)
      (nextEnv := bind callerEnv result sourceValue)
      (nextStore := targetStore)
      (nextWitness := witness)
      related.resultFound
  let resumedLocals : Wasm.Locals :=
    { updated with values := callerRemainder }
  have updatedRelated :
      StateRelated sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    have bound := related.stateRelated.bindPhysical related.resultFound
      related.kindAt related.valueRelated targetSet
    rw [related.stateRelated.clearFailure] at bound
    simpa [resumedLocals, StateRelated, EnvLocalsRelated, Wasm.Locals.get]
      using bound
  have resumedAligned :
      ConcreteLocalFrameAligned sourceFunction sourceRuntime
        (bind callerEnv result sourceValue) targetStore resumedLocals witness := by
    simpa [resumedLocals, ConcreteLocalFrameAligned] using updatedAligned
  let sourceAfter : MachineState :=
    { source with
      control := .code continuation
      env := bind callerEnv result sourceValue
      joins := callerJoins
      frames := sourceFrames }
  let targetAfterCall : StructuredWasmState Host :=
    { target with
      control := .running
        { callerLocals with
          values := physical :: callerRemainder }
        (.localSet resultIndex :: targetRest)
      frames := targetFrames }
  let targetAfter : StructuredWasmState Host :=
    { target with
      control := .running resumedLocals targetRest
      frames := targetFrames }
  refine ⟨sourceAfter, targetAfter, updated, resumedLocals, ?_, ?_, targetSet,
    rfl, ?_, ?_, ?_, ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = .yielded sourceValue at controlEq
    subst control
    have framesEq := related.sourceFramesEq
    change frames = _ at framesEq
    subst frames
    simp [sourceAfter, executeStep, coreStep]
  · have unwindStep :
        StructuredWasmStep module hostEnv target targetAfterCall := by
      rcases target with ⟨store, control, frames⟩
      have storeEq := related.targetStoreEq
      change store = targetStore at storeEq
      subst store
      have controlEq := related.targetControlEq
      change control = .returning (physical :: returnedTail) at controlEq
      subst control
      have framesEq := related.targetFramesEq
      change frames = _ at framesEq
      subst frames
      exact StructuredWasmStep.returnCall
    have stackSet :
        ({ callerLocals with
            values := physical :: callerRemainder }.set? resultIndex physical) =
          some { updated with values := physical :: callerRemainder } :=
      locals_set?_with_values (physical :: callerRemainder) targetSet
    have storeStep :
        StructuredWasmStep module hostEnv targetAfterCall targetAfter := by
      rcases target with ⟨store, control, frames⟩
      have storeEq := related.targetStoreEq
      change store = targetStore at storeEq
      subst store
      apply StructuredWasmStep.atomic (fuel := 1)
      · trivial
      · simp only [Wasm.execOne.eq_def, stackSet]
        rfl
    exact .cons unwindStep (.cons storeStep (.refl targetAfter))
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := by simp [targetAfter, related.targetStoreEq]
      targetControlEq := by simp [targetAfter]
      adapted := related.continuationAdapted
      stateRelated := updatedRelated
      frameAligned := resumedAligned }
  · simp [sourceAfter]
  · simp [sourceAfter]
  · simp [targetAfter]

/-- Re-index a recursively evolved callee frame back to its suspended caller.

The callee's entry-relative transport package carries every representation,
capacity, ownership, handler, cache, and immutable-table fact that may change
inside the call.  The checked caller-local update supplies the only
function-local operation needed at return.  This lemma therefore restores the
complete caller cache frame without packaging the callee as a target
execution certificate. -/
theorem ReuseCapacityEntryRelativeFrame.restoreDirectCaller
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore initial afterCall : Wasm.Store Host}
    {entryWitness initialWitness resultWitness : RefinementWitness}
    {facts calleeResultFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    {sourceEnv calleeEnv : Env}
    {callerLocals calleeLocals resumedLocals : Wasm.Locals}
    {result : Lean.FVarId} {resultIndex : Nat}
    {sourceValue : Value} {physical : Wasm.Value}
    (caller :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals)
        entryRuntime entryStore entryWitness facts callerBytes sourceRuntime
        sourceEnv initial callerLocals initialWitness)
    (callee :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals)
        sourceRuntime initial initialWitness calleeResultFacts resultBytes
        nextRuntime calleeEnv afterCall calleeLocals resultWitness)
    (finalRelated :
      StateRelated callerFunction nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness)
    (finalAligned :
      ConcreteLocalFrameAligned callerFunction nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness)
    (resultFound :
      findFVar? (functionBindings callerFunction) result = some resultIndex)
    (localUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical) :
    ReuseCapacityEntryRelativeFrame
      (ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals)
      entryRuntime entryStore entryWitness (eraseReuseCapacityFact facts result)
      resultBytes nextRuntime (bind sourceEnv result sourceValue) afterCall
      resumedLocals resultWitness := by
  rcases caller.1 with
    ⟨⟨⟨⟨callerRelated, callerOrdinary, _callerAligned, _callerBudget⟩,
      _callerInteger, _callerNatural, _callerScalar⟩, _callerDescriptors⟩,
      _callerCache, _callerClosureTables⟩
  rcases callee.1 with
    ⟨⟨⟨⟨_calleeRelated, _calleeOrdinary, _calleeAligned, resultBudget⟩,
      resultInteger, resultNatural, resultScalar⟩, resultDescriptors⟩,
      resultCache, resultClosureTables⟩
  have resultRelated :
      ReuseCapacityStateRelated (eraseReuseCapacityFact facts result)
        callerFunction nextRuntime (bind sourceEnv result sourceValue)
        afterCall resumedLocals resultWitness :=
    callerRelated.eraseResult finalRelated resultFound localUpdate
      callee.2.witness callee.2.capacity
  have resultOrdinary :
      ReuseTokenOrdinaryRel (eraseReuseCapacityFact facts result) nextRuntime
        (bind sourceEnv result sourceValue) :=
    callerOrdinary.eraseBind callee.2.ordinary
  have resultTransports :
      ReuseCapacityCodeEntryTransports entryRuntime nextRuntime entryStore
        afterCall entryWitness resultWitness :=
    caller.2.step callee.2.witness callee.2.closureAllocationsPersistent
      callee.2.capacity callee.2.ordinary callee.2.externals
      callee.2.toClosureTablesTransport
  have resultBase :
      ConcreteReuseCapacityFrame callerFunction
        (eraseReuseCapacityFact facts result) resultBytes nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness :=
    ⟨resultRelated, resultOrdinary, finalAligned, resultBudget⟩
  have resultPure :
      ConcreteReuseCapacityPureExternalFrame callerFunction externals
        (eraseReuseCapacityFact facts result) resultBytes nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness :=
    ⟨resultBase, resultInteger, resultNatural, resultScalar⟩
  have resultOwnership :
      ConcreteReuseCapacityPureExternalOwnershipFrame callerFunction externals
        (eraseReuseCapacityFact facts result) resultBytes nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness :=
    ⟨resultPure, resultDescriptors⟩
  have resultFrame :
      ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals
        (eraseReuseCapacityFact facts result) resultBytes nextRuntime
        (bind sourceEnv result sourceValue) afterCall resumedLocals
        resultWitness :=
    ⟨resultOwnership, resultCache, resultClosureTables⟩
  exact ⟨resultFrame, resultTransports⟩

/-- Exact-boundary form of caller restoration, used by the recursive resource
stack when popping one call frame. -/
theorem ConcreteStructuredResourceScope.restoreCaller
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {outerRuntime callRuntime currentRuntime : RuntimeState}
    {outerStore callStore currentStore : Wasm.Store Host}
    {outerWitness callWitness currentWitness : RefinementWitness}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    {callerEnv calleeEnv : Env}
    {callerLocals calleeLocals resumedLocals : Wasm.Locals}
    {result : Lean.FVarId}
    {resultIndex : Nat}
    {sourceValue : Value}
    {physical : Wasm.Value}
    (caller : ConcreteStructuredResourceScope callerContext sourceModule
      callerFunction externals outerRuntime outerStore outerWitness facts
      callerBytes callRuntime callerEnv callStore callerLocals callWitness)
    (callee : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes currentRuntime calleeEnv currentStore calleeLocals
      currentWitness)
    (programEq : calleeContext.program = callerContext.program)
    (finalRelated :
      StateRelated callerFunction currentRuntime
        (bind callerEnv result sourceValue) currentStore resumedLocals
        currentWitness)
    (finalAligned :
      ConcreteLocalFrameAligned callerFunction currentRuntime
        (bind callerEnv result sourceValue) currentStore resumedLocals
        currentWitness)
    (resultFound :
      findFVar? (functionBindings callerFunction) result = some resultIndex)
    (localUpdate : FirTalos.Correctness.LocalUpdate callerLocals resumedLocals
      resultIndex physical) :
    ConcreteStructuredResourceScope callerContext sourceModule callerFunction
      externals outerRuntime outerStore outerWitness
      (eraseReuseCapacityFact facts result) resultBytes currentRuntime
      (bind callerEnv result sourceValue) currentStore resumedLocals
      currentWitness := by
  have restored :=
    caller.1.restoreDirectCaller callee.1 finalRelated finalAligned resultFound
      localUpdate
  have callerAbi :
      ClosureAllocationsAbiAligned callerContext.program currentWitness := by
    rw [← programEq]
    exact callee.2
  exact ⟨restored, callerAbi⟩

/-- Compose an exact callee resource scope back into a suspended caller.

This is the resource half of the pointwise call/return simulation rule.  Its
only semantic premises are the related returned value and the checked local
write performed by the return protocol.  In particular, it assumes neither a
terminating evaluation of the callee nor a certificate describing its body. -/
theorem ConcreteStructuredCurrentResource.restoreCaller
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {outerRuntime callRuntime currentRuntime : RuntimeState}
    {outerStore callStore currentStore : Wasm.Store Host}
    {outerWitness callWitness currentWitness : RefinementWitness}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    {callerEnv calleeEnv : Env}
    {callerLocals calleeLocals resumedLocals : Wasm.Locals}
    {result : Lean.FVarId}
    {resultIndex : Nat}
    {sourceValue : Value}
    {physical : Wasm.Value}
    (caller :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule callerFunction externals)
        outerRuntime outerStore outerWitness facts callerBytes callRuntime
        callerEnv callStore callerLocals callWitness)
    (callee : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes currentRuntime calleeEnv currentStore calleeLocals
      currentWitness)
    (programEq : calleeContext.program = callerContext.program)
    (finalRelated :
      StateRelated callerFunction currentRuntime
        (bind callerEnv result sourceValue) currentStore resumedLocals
        currentWitness)
    (finalAligned :
      ConcreteLocalFrameAligned callerFunction currentRuntime
        (bind callerEnv result sourceValue) currentStore resumedLocals
        currentWitness)
    (resultFound :
      findFVar? (functionBindings callerFunction) result = some resultIndex)
    (localUpdate : FirTalos.Correctness.LocalUpdate callerLocals resumedLocals
      resultIndex physical) :
    ConcreteStructuredCurrentResource callerContext sourceModule
      callerFunction externals (eraseReuseCapacityFact facts result)
      resultBytes currentRuntime (bind callerEnv result sourceValue)
      currentStore resumedLocals currentWitness := by
  have restored :=
    caller.restoreDirectCaller callee.1 finalRelated finalAligned resultFound
      localUpdate
  have callerAbi :
      ClosureAllocationsAbiAligned callerContext.program currentWitness := by
    rw [← programEq]
    exact callee.2
  exact ⟨outerRuntime, outerStore, outerWitness, restored, callerAbi⟩

/-- Empty case-selection family for the first recursive structured fragment. -/
def NoStructuredCasesSupported
    (_context : Fir.Wasm.Context) (_runtime : RuntimeState) (_env : Env)
    (_cases : Lean.Compiler.LCNF.Cases .impure)
    (_selected : Lean.Compiler.LCNF.Code .impure) : Prop :=
  False

/-- Source-only recursive admission for direct values and statically named
internal calls.  This is a specialization of the existing hereditary source
relation, not a second evaluator: external, lazy, case, and effect constructors
are disabled until their structured target rules are connected. -/
abbrev ReuseCapacityStructuredDirectCallCodeEvaluates
    (externals : ExternalImpl) :=
  ReuseCapacityDirectHereditaryCodeEvaluates externals
    (fun context => ReuseBudgetedDirectSupported context)
    (fun _context => NoReuseCapacityExternalsSupported)
    (fun _context => NoReuseCapacityLazySupported)
    NoStructuredCasesSupported
    (fun _context => NoEffectsSupported)
    directLetAllocationCost

/-- Source-only recursive admission for the structured fragment proved here.

Lazy initializers are recursive arguments of this relation, just like named
calls.  This is important for compiler correctness: the structured target
proof receives an induction hypothesis for the generated initializer rather
than an opaque runtime certificate.  The cache constructors cover hits and
non-heap misses; a miss recursively evaluates the initializer and then
publishes its result through the concrete host cache and generated Wasm
globals.  Default-only cases are included as compiler-erased control steps,
and arbitrary normalized object-constructor cases execute the
compiler-generated `getTag` chain recursively.  Arbitrary normalized scalar
`UInt8` cases execute the generated direct-comparison chain recursively as
well.  Persistent ownership effects are source-visible recursive steps whose
compiler erasure is matched by a zero-step target path. Successful ordinary
increments execute the exact generated unary-host prefix and recurse through
the updated heap relation. Heap-valued miss publication and the remaining
ownership effects remain separate later widenings. -/
inductive ReuseCapacityStructuredPureExternalLazyCodeEvaluates
    (externals : ExternalImpl) :
    Fir.Wasm.Context → AbiKind → ReuseCapacityFacts → RuntimeState →
      Env → Lean.Compiler.LCNF.Code .impure → ReuseCapacityFacts →
        RuntimeState → Env → Value → Nat → Prop where
  | ret
      {actualResultKind : AbiKind}
      (sourceLookup : lookup sourceEnv result = some sourceValue)
      (resultCompiled :
        Fir.Wasm.getLocal context result =
          .ok (.localGet result, actualResultKind))
      (resultRefines : actualResultKind.refines expectedResult = true) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.return result) facts
        sourceRuntime sourceEnv sourceValue 0
  | letValue
      (supported : ReuseBudgetedDirectSupported context facts decl)
      (sourceStep :
        SourceLetResult context sourceRuntime sourceEnv decl nextRuntime
          sourceValue)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue
        (directLetAllocationCost decl + continuationCost)
  | externalLet
      (supported :
        PureExternalSupported context externals sourceRuntime sourceEnv decl
          continuation nextRuntime sourceValue stepCost)
      (sourceStep :
        SourceExternalLetResult context externals sourceRuntime sourceEnv decl
          continuation nextRuntime sourceValue)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue
        (stepCost + continuationCost)

  | directCallLet
      {calleeFunction : Fir.Wasm.Function}
      (site : DirectInternalCallSite context decl sourceEnv)
      (row :
        LoweredInternalDeclaration context.program context.cachedDeclarations
          site.sourceDeclaration site.calleeCode calleeFunction)
      (callee :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals
          row.context site.calleeResultKind [] sourceRuntime site.calleeEnv
          site.calleeCode calleeResultFacts nextRuntime calleeResultEnv
          sourceValue stepCost)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue
        (stepCost + continuationCost)
  | saturatedClosureCallLet
      (callRuntime : RuntimeState)
      (calleeFunction : Fir.Wasm.Function)
      (calleeResultFacts : ReuseCapacityFacts)
      (calleeResultEnv : Env)
      (site : SaturatedClosureCallSite context decl sourceEnv)
      (resolution :
        SaturatedClosureCallResolution context sourceRuntime site)
      (row :
        LoweredInternalDeclaration context.program context.cachedDeclarations
          resolution.target resolution.calleeCode calleeFunction)
      (sharedCapacity : ∀ parentRuntime,
        setCell sourceRuntime resolution.location
            { resolution.cell with rc := resolution.cell.rc - 1 } =
              .ok parentRuntime →
          ClosureRetainCapacity parentRuntime resolution.captures.toList)
      (application :
        Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
            resolution.location =
          .ok (callRuntime, resolution.function, resolution.arity,
            resolution.captures))
      (callee :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals
          row.context resolution.targetResultKind [] callRuntime
          resolution.calleeEnv resolution.calleeCode calleeResultFacts
          nextRuntime calleeResultEnv sourceValue stepCost)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue
        (stepCost + continuationCost)
  | lazyHit
      (call :
        LazyCacheCallSupported context decl declaration sourceDeclaration
          resultKind)
      (sourceStep :
        SourceLazyLetResult .hit context externals sourceRuntime sourceEnv decl
          continuation nextRuntime sourceValue)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue continuationCost
  | lazyMiss
      {calleeFunction : Fir.Wasm.Function}
      (call :
        LazyCacheInternalMissSupported context decl declaration
          sourceDeclaration resultKind calleeCode)
      (row :
        LoweredInternalDeclaration context.program context.cachedDeclarations
          sourceDeclaration calleeCode calleeFunction)
      (resultClassified :
        Fir.Wasm.abiKind? sourceDeclaration.type = .ok (some resultKind))
      (notObject : resultKind ≠ .object)
      (notTObject : resultKind ≠ .tobject)
      (sourceStep :
        SourceLazyLetResult .miss context externals sourceRuntime sourceEnv decl
          continuation nextRuntime sourceValue)
      (callee :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals
          row.context resultKind [] sourceRuntime [] calleeCode
          calleeResultFacts callRuntime calleeResultEnv sourceValue stepCost)
      (transfer : reuseCapacityLetFacts? facts decl = some nextFacts)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult nextFacts nextRuntime
          (bind sourceEnv decl.fvarId sourceValue) continuation resultFacts
          resultRuntime resultEnv resultValue continuationCost) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.let decl continuation)
        resultFacts resultRuntime resultEnv resultValue
        (stepCost + continuationCost)
  | defaultCase
      (supported :
        DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected)
      (sourceStep :
        SourceCaseResult sourceRuntime sourceEnv cases selected)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts sourceRuntime sourceEnv selected resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.cases cases) resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | singleObjectCase
      (supported :
        SingleObjectConstructorCaseSupported context sourceRuntime sourceEnv
          cases selected)
      (sourceStep :
        SourceCaseResult sourceRuntime sourceEnv cases selected)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts sourceRuntime sourceEnv selected resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.cases cases) resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | twoObjectDefaultCase
      (supported :
        TwoObjectConstructorDefaultCasesSupported context sourceRuntime
          sourceEnv cases selected)
      (sourceStep :
        SourceCaseResult sourceRuntime sourceEnv cases selected)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts sourceRuntime sourceEnv selected resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.cases cases) resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | objectCases
      (supported :
        ObjectConstructorCasesSupported context sourceRuntime sourceEnv cases
          selected)
      (sourceStep :
        SourceCaseResult sourceRuntime sourceEnv cases selected)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts sourceRuntime sourceEnv selected resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.cases cases) resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | scalarUInt8Cases
      (supported :
        ScalarUInt8CasesSupported context sourceRuntime sourceEnv cases
          selected)
      (sourceStep :
        SourceCaseResult sourceRuntime sourceEnv cases selected)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts sourceRuntime sourceEnv selected resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv (.cases cases) resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | persistentOwnershipEffect
      (supported :
        PersistentOwnershipEffectSupported sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | ordinaryIncrementEffect
      (supported :
        OrdinaryIncrementEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | ordinaryDecrementEffect
      (supported :
        OrdinaryDecrementEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | ordinaryDeleteEffect
      (supported :
        OrdinaryDeleteEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | constructorTagEffect
      (supported :
        ConstructorTagEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | objectFieldFVarEffect
      (supported :
        ObjectFieldFVarEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | objectFieldErasedEffect
      (supported :
        ObjectFieldErasedEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | usizeFieldEffect
      (supported :
        USizeFieldEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes
  | scalarFieldEffect
      (supported :
        ScalarFieldEffectSupported context sourceRuntime sourceEnv code
          continuation nextRuntime)
      (sourceStep :
        SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
          continuation)
      (continued :
        ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
          expectedResult facts nextRuntime sourceEnv continuation resultFacts
          resultRuntime resultEnv resultValue requiredBytes) :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes

/-- The structured admission remains an exact finite source execution.  In
particular, the recursive initializer premise of a miss is semantic evidence,
not merely a target-side proof device. -/
theorem ReuseCapacityStructuredPureExternalLazyCodeEvaluates.sourceResult
    {externals : ExternalImpl} {context : Fir.Wasm.Context}
    {expectedResult : AbiKind} {facts resultFacts : ReuseCapacityFacts}
    {sourceRuntime resultRuntime : RuntimeState} {sourceEnv resultEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure} {resultValue : Value}
    {requiredBytes : Nat}
    (evaluation :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv sourceCode resultFacts
        resultRuntime resultEnv resultValue requiredBytes) :
    SourceCodeResult context externals sourceRuntime sourceEnv sourceCode
      resultRuntime resultValue := by
  induction evaluation with
  | ret sourceLookup _resultCompiled _resultRefines =>
      exact (CodeEvaluates.ret sourceLookup).sourceResult externals
  | @letValue context facts decl sourceRuntime sourceEnv nextRuntime
      sourceValue nextFacts expectedResult continuation resultFacts
      resultRuntime resultEnv resultValue continuationCost supported sourceStep
      transfer continued ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          have evaluated :
              evalLetValue
                  (sourceCodeState context sourceRuntime sourceEnv
                    (.let decl continuation)) decl =
                .ok (nextRuntime, .value sourceValue) := by
            unfold SourceLetResult at sourceStep
            cases decl.value <;> exact sourceStep
          unfold sourceCodeState at evaluated
          simp [executeStep, coreStep, sourceCodeState, evaluated])
          (.refl _))
        ih
  | externalLet _ sourceStep _ _ ih =>
      apply SourceCodeResult.ofSteps (prefixCount := 3) ?_ ih
      simpa [SourceExternalLetResult, sourceCodeState] using sourceStep
  | @directCallLet callContext decl sourceEnv sourceRuntime calleeResultFacts
      nextRuntime calleeResultEnv sourceValue stepCost facts nextFacts
      expectedResult continuation resultFacts resultRuntime resultEnv resultValue
      continuationCost calleeFunction site row callee transfer continued
      calleeIH continuedIH =>
      have contexts : DeclarationContextsCoherent callContext row.context :=
        row.contextsCoherent rfl rfl
      obtain ⟨count, steps⟩ :=
        site.sourceCallLetResult contexts calleeIH
      apply SourceCodeResult.ofSteps (prefixCount := count) ?_ continuedIH
      simpa [sourceCodeState] using steps
  | @saturatedClosureCallLet callContext decl sourceEnv sourceRuntime
      nextRuntime sourceValue stepCost facts nextFacts expectedResult
      continuation resultFacts resultRuntime resultEnv resultValue
      continuationCost callRuntime calleeFunction calleeResultFacts
      calleeResultEnv site resolution row sharedCapacity application callee
      transfer continued calleeIH continuedIH =>
      obtain ⟨count, steps⟩ :=
        site.sourceCallLetResult resolution row application calleeIH
      apply SourceCodeResult.ofSteps (prefixCount := count) ?_ continuedIH
      simpa [sourceCodeState] using steps
  | lazyHit _ sourceStep _ _ ih =>
      obtain ⟨count, steps⟩ := sourceStep.execSteps
      apply SourceCodeResult.ofSteps (prefixCount := count) ?_ ih
      simpa [sourceCodeState] using steps
  | lazyMiss _ _ _ _ _ sourceStep _ _ _ _ continuedIH =>
      obtain ⟨count, steps⟩ := sourceStep.execSteps
      apply SourceCodeResult.ofSteps (prefixCount := count) ?_ continuedIH
      simpa [sourceCodeState] using steps
  | defaultCase _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
          simp [executeStep, coreStep, sourceCodeState, found, tagged, chosen])
          (.refl _))
        ih
  | singleObjectCase _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
          simp [executeStep, coreStep, sourceCodeState, found, tagged, chosen])
          (.refl _))
        ih
  | twoObjectDefaultCase _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
          simp [executeStep, coreStep, sourceCodeState, found, tagged, chosen])
          (.refl _))
        ih
  | objectCases _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
          simp [executeStep, coreStep, sourceCodeState, found, tagged, chosen])
          (.refl _))
        ih
  | scalarUInt8Cases _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (by
          rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
          simp [executeStep, coreStep, sourceCodeState, found, tagged, chosen])
          (.refl _))
        ih
  | persistentOwnershipEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | ordinaryIncrementEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | ordinaryDecrementEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | ordinaryDeleteEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | constructorTagEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | objectFieldFVarEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | objectFieldErasedEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | usizeFieldEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih
  | scalarFieldEffect _ sourceStep _ ih =>
      apply SourceCodeResult.ofSteps
        (.step (sourceStep externals) (.refl _)) ih

/-- Recover the ordinary ABI classifier from the direct-call classifier. -/
theorem abiKind?_of_directAbiKind?_eq_some
    {type : Lean.Expr} {kind : AbiKind}
    (classified : Fir.Wasm.directAbiKind? type = some kind) :
    Fir.Wasm.abiKind? type = .ok (some kind) := by
  unfold Fir.Wasm.directAbiKind? at classified
  cases result : Fir.Wasm.abiKind? type with
  | error error => simp [result] at classified
  | ok optional =>
      cases optional with
      | none => simp [result] at classified
      | some actual =>
          simp only [result, Option.some.injEq] at classified
          subst actual
          rfl

/-- ABI representation refinement composes across generated declaration and
caller result rows. -/
theorem AbiKind.refines_trans
    {actual middle expected : AbiKind}
    (left : actual.refines middle = true)
    (right : middle.refines expected = true) :
    actual.refines expected = true := by
  cases actual <;> cases middle <;> cases expected <;>
    simp_all [AbiKind.refines]

/-- Simulation-facing bind-frame rule.  Determinism identifies the successor
constructed by `advance` with the successor supplied by the generic source
transition premise. -/
theorem ConcreteStructuredBindFrameFocus.advance_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter resumedLocals,
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind callerEnv result sourceValue) continuation
        targetStore resumedLocals targetRest witness sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, _updated, resumedLocals, computedStep,
      path, _targetSet, _resumedEq, focus, _sourceJoinsEq, _sourceFramesEq,
      _targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have afterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    injection computedStep
  subst computedAfter
  exact ⟨targetAfter, resumedLocals, path, focus⟩

/-- One successful ordinary reference-count increment advances the source by
one effect step and the structured target by the exact generated unary-host
prefix.  The resulting heap, cache table, immutable closure tables, and all
entry-relative resource transports are reconstructed from the operation
contract; no target execution is admitted as a premise. -/
theorem ConcreteStructuredCodeFocus.advance_ordinaryIncrement
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      OrdinaryIncrementEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            2 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | inc sourceRuntime nextRuntime sourceEnv objectId amount check continuation
      objectKind sourceObject objectCompiled objectRefines objectLookup updated
      fits =>
      obtain ⟨objectIndex, callIndex, targetRest, objectFound, kindAt,
          callFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.inc_eq functionSpec.localsAligned objectCompiled
          related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      obtain ⟨imp, imported, inBounds, contracted, parameterCount,
          resultCount⟩ :=
        functionSpec.incrementCall callFound
      have sourceLookup : lookup sourceEnv objectId = some sourceObject := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value found
          injection objectLookup with valueEq
          subst value
          exact found
        · contradiction
      obtain ⟨physicalObject, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup objectFound kindAt
      have tobjectRelated := physicalRelated.toTObject objectRefines
      cases tobjectRelated with
      | word32 objectRelated =>
          rename_i word
          obtain ⟨heap, operation, runtimeRelated, cursor, capacity⟩ :=
            incrementStep_of_refines_with_capacity related.stateRelated.1
              objectRelated updated fits
          have nextRelated :
              StateRelated sourceFunction nextRuntime sourceEnv
                (replaceHeap targetStore heap) targetLocals witness :=
            ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
              related.stateRelated.2.2⟩
          have effectStep :
              EffectStepSimulates context sourceModule sourceFunction labels
                targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                sourceEnv (.inc objectId amount check false continuation)
                continuation
                ([.localGet objectIndex, .call callIndex] ++ targetCore)
                targetCore targetStore (replaceHeap targetStore heap)
                targetLocals witness witness := by
            apply effectStepSimulates_unaryHost
              (spec := hosts.spec) (step := incrementStep amount check)
            · exact sourceStep
            · exact codeAdapted_inc objectCompiled objectFound callFound
                continuationCoreAdapted
            · exact related.stateRelated
            · exact nextRelated
            · exact targetLookup
            · exact imported
            · exact functionSpec.hostsSatisfy
            · exact inBounds
            · exact contracted
            · exact parameterCount
            · exact resultCount
            · exact operation
          have ordinaryTransport :
              OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
            incValue_ordinaryPersistenceTransport updated
          have sourceGlobals :
              nextRuntime.globals = sourceRuntime.globals := by
            rcases incValue_heapOnly updated with ⟨semanticHeap, runtimeEq⟩
            subst nextRuntime
            rfl
          have nextInvariant :
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv (replaceHeap targetStore heap)
                targetLocals witness :=
            invariant.ofReplaceHeapEffectStep effectStep capacity
              ordinaryTransport sourceGlobals cursor
          let sourceAfter : MachineState := {
            source with control := .code continuation
                        runtime := nextRuntime }
          have sourcePath :
              executeStep externals source = .next sourceAfter := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have programEq := related.sourceProgramEq
            change sourceProgram = context.program at programEq
            subst sourceProgram
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            have envEq := related.sourceEnvEq
            change actualEnv = sourceEnv at envEq
            subst actualEnv
            have runtimeEq := related.sourceRuntimeEq
            change actualRuntime = sourceRuntime at runtimeEq
            subst actualRuntime
            simp [sourceAfter, executeStep, coreStep, objectLookup, updated]
          let targetAfter : StructuredWasmState Host := {
            store := replaceHeap targetStore heap
            control := .running
              { targetLocals with values := targetLocals.values } targetRest
            frames := target.frames }
          have targetPath :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 2
                target targetAfter := by
            rcases target with ⟨actualStore, actualControl, actualFrames⟩
            have storeEq := related.targetStoreEq
            change actualStore = targetStore at storeEq
            subst actualStore
            have controlEq := related.targetControlEq
            change actualControl =
              .running targetLocals
                ([.localGet objectIndex, .call callIndex] ++ targetRest)
              at controlEq
            subst actualControl
            simpa [targetAfter] using
              structuredWasmUnaryHostEffectPrefixFinitePath
                (module := targetModule.wasmModule) (env := hosts.env)
                (spec := hosts.spec) (step := incrementStep amount check)
                (initial := targetStore) (final := replaceHeap targetStore heap)
                (locals := targetLocals) (objectIndex := objectIndex)
                (physicalObject := .i32 (UInt32.ofNat word.value))
                (targetRest := targetRest) (tail := targetLocals.values)
                (frames := actualFrames) targetLookup imported
                functionSpec.hostsSatisfy inBounds contracted parameterCount
                resultCount operation
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation
                (replaceHeap targetStore heap) targetLocals targetRest witness
                sourceAfter targetAfter := {
            sourceProgramEq := by
              simp [sourceAfter, related.sourceProgramEq]
            sourceControlEq := by simp [sourceAfter]
            sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
            sourceRuntimeEq := by simp [sourceAfter]
            targetStoreEq := by simp [targetAfter]
            targetControlEq := by simp [targetAfter]
            adapted := continuationAdapted
            stateRelated := nextRelated
            frameAligned := nextInvariant.1.1.1.1.2.2.1 }
          exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
            targetRest, .single sourcePath, targetPath, nextFocus,
            nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
            by simp [targetAfter]⟩
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- One successful ordinary reference-count decrement advances the source by
one effect step and the structured target by the exact generated unary-host
prefix. Recursive release may update an ownership tree, but the concrete
operation supplies the same capacity and ordinary-persistence transports used
to rebuild the full entry-relative frame. -/
theorem ConcreteStructuredCodeFocus.advance_ordinaryDecrement
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      OrdinaryDecrementEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            2 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | dec sourceRuntime nextRuntime sourceEnv objectId amount check objectFields?
      continuation objectKind sourceObject objectCompiled objectRefines
      objectLookup updated =>
      obtain ⟨objectIndex, callIndex, targetRest, objectFound, kindAt,
          callFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.dec_eq functionSpec.localsAligned objectCompiled
          related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      obtain ⟨imp, imported, inBounds, contracted, parameterCount,
          resultCount⟩ :=
        functionSpec.decrementCall callFound
      have sourceLookup : lookup sourceEnv objectId = some sourceObject := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value found
          injection objectLookup with valueEq
          subst value
          exact found
        · contradiction
      obtain ⟨physicalObject, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup objectFound kindAt
      have tobjectRelated := physicalRelated.toTObject objectRefines
      cases tobjectRelated with
      | word32 objectRelated =>
          rename_i word
          obtain ⟨heap, operation, runtimeRelated, cursor, capacity⟩ :=
            decrementStep_of_refines_with_capacity
              (objectFields? := objectFields?) related.stateRelated.1
              objectRelated invariant.1.1.2 updated
          have nextRelated :
              StateRelated sourceFunction nextRuntime sourceEnv
                (replaceHeap targetStore heap) targetLocals witness :=
            ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
              related.stateRelated.2.2⟩
          have effectStep :
              EffectStepSimulates context sourceModule sourceFunction labels
                targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                sourceEnv
                (.dec objectId amount check false objectFields? continuation)
                continuation
                ([.localGet objectIndex, .call callIndex] ++ targetCore)
                targetCore targetStore (replaceHeap targetStore heap)
                targetLocals witness witness := by
            apply effectStepSimulates_unaryHost
              (spec := hosts.spec)
              (step := decrementStep amount check objectFields?)
            · exact sourceStep
            · exact codeAdapted_dec objectCompiled objectFound callFound
                continuationCoreAdapted
            · exact related.stateRelated
            · exact nextRelated
            · exact targetLookup
            · exact imported
            · exact functionSpec.hostsSatisfy
            · exact inBounds
            · exact contracted
            · exact parameterCount
            · exact resultCount
            · exact operation
          have ordinaryTransport :
              OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
            decValue_ordinaryPersistenceTransport updated
          have sourceGlobals :
              nextRuntime.globals = sourceRuntime.globals :=
            (decValue_runtimeAux updated).globals
          have nextInvariant :
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv (replaceHeap targetStore heap)
                targetLocals witness :=
            invariant.ofReplaceHeapEffectStep effectStep capacity
              ordinaryTransport sourceGlobals cursor
          let sourceAfter : MachineState := {
            source with control := .code continuation
                        runtime := nextRuntime }
          have sourcePath :
              executeStep externals source = .next sourceAfter := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have programEq := related.sourceProgramEq
            change sourceProgram = context.program at programEq
            subst sourceProgram
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            have envEq := related.sourceEnvEq
            change actualEnv = sourceEnv at envEq
            subst actualEnv
            have runtimeEq := related.sourceRuntimeEq
            change actualRuntime = sourceRuntime at runtimeEq
            subst actualRuntime
            simp [sourceAfter, executeStep, coreStep, objectLookup, updated]
          let targetAfter : StructuredWasmState Host := {
            store := replaceHeap targetStore heap
            control := .running
              { targetLocals with values := targetLocals.values } targetRest
            frames := target.frames }
          have targetPath :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 2
                target targetAfter := by
            rcases target with ⟨actualStore, actualControl, actualFrames⟩
            have storeEq := related.targetStoreEq
            change actualStore = targetStore at storeEq
            subst actualStore
            have controlEq := related.targetControlEq
            change actualControl =
              .running targetLocals
                ([.localGet objectIndex, .call callIndex] ++ targetRest)
              at controlEq
            subst actualControl
            simpa [targetAfter] using
              structuredWasmUnaryHostEffectPrefixFinitePath
                (module := targetModule.wasmModule) (env := hosts.env)
                (spec := hosts.spec)
                (step := decrementStep amount check objectFields?)
                (initial := targetStore) (final := replaceHeap targetStore heap)
                (locals := targetLocals) (objectIndex := objectIndex)
                (physicalObject := .i32 (UInt32.ofNat word.value))
                (targetRest := targetRest) (tail := targetLocals.values)
                (frames := actualFrames) targetLookup imported
                functionSpec.hostsSatisfy inBounds contracted parameterCount
                resultCount operation
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation
                (replaceHeap targetStore heap) targetLocals targetRest witness
                sourceAfter targetAfter := {
            sourceProgramEq := by
              simp [sourceAfter, related.sourceProgramEq]
            sourceControlEq := by simp [sourceAfter]
            sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
            sourceRuntimeEq := by simp [sourceAfter]
            targetStoreEq := by simp [targetAfter]
            targetControlEq := by simp [targetAfter]
            adapted := continuationAdapted
            stateRelated := nextRelated
            frameAligned := nextInvariant.1.1.1.1.2.2.1 }
          exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
            targetRest, .single sourcePath, targetPath, nextFocus,
            nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
            by simp [targetAfter]⟩
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- One successful explicit deletion advances the source by one effect step
and the structured target by the exact generated unary-host prefix. Physical
zero is admitted only through the erased-value relation supplied by the shared
delete contract; ordinary object decoding and its mapped-header obligations
remain unchanged. -/
theorem ConcreteStructuredCodeFocus.advance_ordinaryDelete
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      OrdinaryDeleteEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            2 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | del sourceRuntime nextRuntime sourceEnv objectId continuation objectKind
      sourceObject objectCompiled objectLookup updated =>
      obtain ⟨objectIndex, callIndex, targetRest, objectFound, kindAt,
          callFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.del_eq functionSpec.localsAligned objectCompiled
          related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      obtain ⟨imp, imported, inBounds, contracted, parameterCount,
          resultCount⟩ :=
        functionSpec.deleteCall callFound
      have sourceLookup : lookup sourceEnv objectId = some sourceObject := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value found
          injection objectLookup with valueEq
          subst value
          exact found
        · contradiction
      obtain ⟨physicalObject, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup objectFound kindAt
      cases physicalRelated with
      | word32 valueRelated =>
          rename_i word
          obtain ⟨heap, operation, runtimeRelated, capacity, cursor⟩ :=
            deleteStep_of_refines_with_capacity related.stateRelated.1
              valueRelated updated
          have nextRelated :
              StateRelated sourceFunction nextRuntime sourceEnv
                (replaceHeap targetStore heap) targetLocals witness :=
            ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
              related.stateRelated.2.2⟩
          have effectStep :
              EffectStepSimulates context sourceModule sourceFunction labels
                targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                sourceEnv (.del objectId continuation) continuation
                ([.localGet objectIndex, .call callIndex] ++ targetCore)
                targetCore targetStore (replaceHeap targetStore heap)
                targetLocals witness witness := by
            apply effectStepSimulates_unaryHost
              (spec := hosts.spec) (step := deleteStep)
            · exact sourceStep
            · exact codeAdapted_delete objectCompiled objectFound callFound
                continuationCoreAdapted
            · exact related.stateRelated
            · exact nextRelated
            · exact targetLookup
            · exact imported
            · exact functionSpec.hostsSatisfy
            · exact inBounds
            · exact contracted
            · exact parameterCount
            · exact resultCount
            · exact operation
          have ordinaryTransport :
              OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
            deleteValue_ordinaryPersistenceTransport updated
          have sourceGlobals :
              nextRuntime.globals = sourceRuntime.globals :=
            (deleteValue_runtimeAux updated).globals
          have nextInvariant :
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv (replaceHeap targetStore heap)
                targetLocals witness :=
            invariant.ofReplaceHeapEffectStep effectStep capacity
              ordinaryTransport sourceGlobals cursor
          let sourceAfter : MachineState := {
            source with control := .code continuation
                        runtime := nextRuntime }
          have sourcePath :
              executeStep externals source = .next sourceAfter := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have programEq := related.sourceProgramEq
            change sourceProgram = context.program at programEq
            subst sourceProgram
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            have envEq := related.sourceEnvEq
            change actualEnv = sourceEnv at envEq
            subst actualEnv
            have runtimeEq := related.sourceRuntimeEq
            change actualRuntime = sourceRuntime at runtimeEq
            subst actualRuntime
            simp [sourceAfter, executeStep, coreStep, objectLookup, updated]
          let targetAfter : StructuredWasmState Host := {
            store := replaceHeap targetStore heap
            control := .running
              { targetLocals with values := targetLocals.values } targetRest
            frames := target.frames }
          have targetPath :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 2
                target targetAfter := by
            rcases target with ⟨actualStore, actualControl, actualFrames⟩
            have storeEq := related.targetStoreEq
            change actualStore = targetStore at storeEq
            subst actualStore
            have controlEq := related.targetControlEq
            change actualControl =
              .running targetLocals
                ([.localGet objectIndex, .call callIndex] ++ targetRest)
              at controlEq
            subst actualControl
            simpa [targetAfter] using
              structuredWasmUnaryHostEffectPrefixFinitePath
                (module := targetModule.wasmModule) (env := hosts.env)
                (spec := hosts.spec) (step := deleteStep)
                (initial := targetStore) (final := replaceHeap targetStore heap)
                (locals := targetLocals) (objectIndex := objectIndex)
                (physicalObject := .i32 (UInt32.ofNat word.value))
                (targetRest := targetRest) (tail := targetLocals.values)
                (frames := actualFrames) targetLookup imported
                functionSpec.hostsSatisfy inBounds contracted parameterCount
                resultCount operation
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation
                (replaceHeap targetStore heap) targetLocals targetRest witness
                sourceAfter targetAfter := {
            sourceProgramEq := by
              simp [sourceAfter, related.sourceProgramEq]
            sourceControlEq := by simp [sourceAfter]
            sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
            sourceRuntimeEq := by simp [sourceAfter]
            targetStoreEq := by simp [targetAfter]
            targetControlEq := by simp [targetAfter]
            adapted := continuationAdapted
            stateRelated := nextRelated
            frameAligned := nextInvariant.1.1.1.1.2.2.1 }
          exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
            targetRest, .single sourcePath, targetPath, nextFocus,
            nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
            by simp [targetAfter]⟩
      | word64 valueRelated =>
          cases valueRelated <;> simp [deleteValue] at updated
      | float32Bits valueRelated => cases valueRelated
      | float64Bits valueRelated => cases valueRelated

/-- One successful constructor-tag mutation advances the source by one effect
step and the structured target by the exact generated unary-host prefix. The
source admission supplies the live-constructor decoder facts; concrete header
refinement and the common replace-heap transport rebuild the recursive frame. -/
theorem ConcreteStructuredCodeFocus.advance_constructorTag
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      ConstructorTagEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            2 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | setTag sourceRuntime nextRuntime sourceEnv objectId tag continuation
      location cell semantic objectCompiled objectLookup updated found live
      objectEq tagFits =>
      obtain ⟨objectIndex, callIndex, targetRest, objectFound, kindAt,
          callFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.setTag_eq functionSpec.localsAligned objectCompiled
          related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      obtain ⟨imp, imported, inBounds, contracted, parameterCount,
          resultCount⟩ :=
        functionSpec.setTagCall callFound
      have sourceLookup :
          lookup sourceEnv objectId = some (.object (.heap location)) := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value foundLookup
          injection objectLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      obtain ⟨physicalObject, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup objectFound kindAt
      cases physicalRelated with
      | word32 objectRelated =>
          rename_i word
          obtain ⟨heap, operation, runtimeRelated, capacity, cursor⟩ :=
            setTagStep_of_refines_with_capacity related.stateRelated.1
              objectRelated found live objectEq updated tagFits
          have nextRelated :
              StateRelated sourceFunction nextRuntime sourceEnv
                (replaceHeap targetStore heap) targetLocals witness :=
            ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
              related.stateRelated.2.2⟩
          have effectStep :
              EffectStepSimulates context sourceModule sourceFunction labels
                targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                sourceEnv (.setTag objectId tag continuation) continuation
                ([.localGet objectIndex, .call callIndex] ++ targetCore)
                targetCore targetStore (replaceHeap targetStore heap)
                targetLocals witness witness := by
            apply effectStepSimulates_unaryHost
              (spec := hosts.spec) (step := setTagStep tag)
            · exact sourceStep
            · exact codeAdapted_setTag objectCompiled objectFound callFound
                continuationCoreAdapted
            · exact related.stateRelated
            · exact nextRelated
            · exact targetLookup
            · exact imported
            · exact functionSpec.hostsSatisfy
            · exact inBounds
            · exact contracted
            · exact parameterCount
            · exact resultCount
            · exact operation
          have ordinaryTransport :
              OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
            setTag_ordinaryPersistenceTransport updated
          have sourceGlobals :
              nextRuntime.globals = sourceRuntime.globals :=
            (setTag_runtimeAux updated).globals
          have nextInvariant :
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv (replaceHeap targetStore heap)
                targetLocals witness :=
            invariant.ofReplaceHeapEffectStep effectStep capacity
              ordinaryTransport sourceGlobals cursor
          let sourceAfter : MachineState := {
            source with control := .code continuation
                        runtime := nextRuntime }
          have sourcePath :
              executeStep externals source = .next sourceAfter := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have programEq := related.sourceProgramEq
            change sourceProgram = context.program at programEq
            subst sourceProgram
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            have envEq := related.sourceEnvEq
            change actualEnv = sourceEnv at envEq
            subst actualEnv
            have runtimeEq := related.sourceRuntimeEq
            change actualRuntime = sourceRuntime at runtimeEq
            subst actualRuntime
            simp [sourceAfter, executeStep, coreStep, objectLookup, updated]
          let targetAfter : StructuredWasmState Host := {
            store := replaceHeap targetStore heap
            control := .running
              { targetLocals with values := targetLocals.values } targetRest
            frames := target.frames }
          have targetPath :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 2
                target targetAfter := by
            rcases target with ⟨actualStore, actualControl, actualFrames⟩
            have storeEq := related.targetStoreEq
            change actualStore = targetStore at storeEq
            subst actualStore
            have controlEq := related.targetControlEq
            change actualControl =
              .running targetLocals
                ([.localGet objectIndex, .call callIndex] ++ targetRest)
              at controlEq
            subst actualControl
            simpa [targetAfter] using
              structuredWasmUnaryHostEffectPrefixFinitePath
                (module := targetModule.wasmModule) (env := hosts.env)
                (spec := hosts.spec) (step := setTagStep tag)
                (initial := targetStore) (final := replaceHeap targetStore heap)
                (locals := targetLocals) (objectIndex := objectIndex)
                (physicalObject := .i32 (UInt32.ofNat word.value))
                (targetRest := targetRest) (tail := targetLocals.values)
                (frames := actualFrames) targetLookup imported
                functionSpec.hostsSatisfy inBounds contracted parameterCount
                resultCount operation
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation
                (replaceHeap targetStore heap) targetLocals targetRest witness
                sourceAfter targetAfter := {
            sourceProgramEq := by
              simp [sourceAfter, related.sourceProgramEq]
            sourceControlEq := by simp [sourceAfter]
            sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
            sourceRuntimeEq := by simp [sourceAfter]
            targetStoreEq := by simp [targetAfter]
            targetControlEq := by simp [targetAfter]
            adapted := continuationAdapted
            stateRelated := nextRelated
            frameAligned := nextInvariant.1.1.1.1.2.2.1 }
          exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
            targetRest, .single sourcePath, targetPath, nextFocus,
            nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
            by simp [targetAfter]⟩
      | word64 valueRelated => cases valueRelated
      | float32Bits valueRelated => cases valueRelated
      | float64Bits valueRelated => cases valueRelated

/-- One successful FVar object-field mutation advances the source by one
effect step and the structured target by the exact generated binary-host
prefix. Descriptor-slot alignment relates the second local at the compiler's
selected ABI kind; the concrete writer and common replace-heap transport then
restore the recursive entry frame. -/
theorem ConcreteStructuredCodeFocus.advance_objectFieldFVar
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      ObjectFieldFVarEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            3 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | oset sourceRuntime nextRuntime sourceEnv objectId fieldId index continuation
      location cell semantic field fieldKind objectCompiled fieldCompiled
      fieldObjectKind objectLookup fieldLookup updated found live objectEq
      indexValid fieldKindAligned =>
      obtain ⟨objectIndex, fieldIndex, callIndex, targetRest, objectFound,
          objectKindAt, fieldFound, fieldKindAt, callFound,
          continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.objectSetFVar_eq functionSpec.localsAligned objectCompiled
          fieldCompiled related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      have objectSourceLookup :
          lookup sourceEnv objectId = some (.object (.heap location)) := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value foundLookup
          injection objectLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      obtain ⟨physicalObject, targetObjectLookup, physicalObjectRelated⟩ :=
        related.stateRelated.resolve objectSourceLookup objectFound objectKindAt
      cases physicalObjectRelated with
      | word32 objectRelated =>
          rename_i objectWord
          have decoded :
              getConstructor sourceRuntime (.object (.heap location)) =
                .ok (location, cell, semantic) := by
            unfold getConstructor
            simp only [getLiveCell, found, live, if_true, Bind.bind,
              Except.bind]
            rw [objectEq]
            rfl
          have tobjectRelated := objectRelated.object_to_tobject
          obtain ⟨info, fieldKinds, descriptorFound⟩ :=
            ConcreteRuntimeRel.constructorDescriptor_of_getConstructor
              related.stateRelated.1 tobjectRelated decoded
          have fieldDescriptorKindAt :=
            fieldKindAligned tobjectRelated descriptorFound
          obtain ⟨imp, imported, inBounds, contracted, parameterCount,
              resultCount⟩ :=
            functionSpec.objectSetCall callFound
          have fieldArgCompiled :=
            compileArg_fvar_of_getLocal fieldCompiled
          have fieldSourceLookup : lookup sourceEnv fieldId = some field := by
            unfold lookupValue at fieldLookup
            split at fieldLookup
            · rename_i value foundLookup
              injection fieldLookup with valueEq
              subst value
              exact foundLookup
            · contradiction
          obtain ⟨physicalField, targetFieldLookup, physicalFieldRelated⟩ :=
            related.stateRelated.resolve fieldSourceLookup fieldFound fieldKindAt
          cases physicalFieldRelated with
          | word32 fieldRelated =>
              rename_i fieldWord
              obtain ⟨heap, operation, runtimeRelated, capacity, cursor⟩ :=
                objectSetStep_of_refines_with_capacity related.stateRelated.1
                  objectRelated fieldRelated found live objectEq descriptorFound
                  indexValid fieldDescriptorKindAt updated
              have nextRelated :
                  StateRelated sourceFunction nextRuntime sourceEnv
                    (replaceHeap targetStore heap) targetLocals witness :=
                ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
                  related.stateRelated.2.2⟩
              have effectStep :
                  EffectStepSimulates context sourceModule sourceFunction labels
                    targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                    sourceEnv
                    (.oset objectId index (.fvar fieldId) continuation)
                    continuation
                    ([.localGet objectIndex, .localGet fieldIndex,
                        .call callIndex] ++ targetCore)
                    targetCore targetStore (replaceHeap targetStore heap)
                    targetLocals witness witness := by
                apply effectStepSimulates_binaryHost
                  (spec := hosts.spec)
                  (step := objectSetStep index fieldKind)
                · exact sourceStep
                · apply codeAdapted_oset
                    (targetField := [.localGet fieldIndex]) objectCompiled
                    fieldArgCompiled objectFound
                  · apply instructions_localGets (fvarIds := [fieldId])
                      (indices := [fieldIndex])
                    exact .cons
                      (by simpa [functionBindings] using fieldFound) .nil
                  · exact callFound
                  · exact continuationCoreAdapted
                · exact related.stateRelated
                · exact nextRelated
                · exact targetObjectLookup
                · exact targetFieldLookup
                · exact imported
                · exact functionSpec.hostsSatisfy
                · exact inBounds
                · exact contracted
                · exact parameterCount
                · exact resultCount
                · exact operation
              have ordinaryTransport :
                  OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
                modifyConstructor_ordinaryPersistenceTransport updated
              have sourceGlobals :
                  nextRuntime.globals = sourceRuntime.globals :=
                (setObjectField_runtimeAux updated).globals
              have nextInvariant :
                  ReuseCapacityEntryRelativeFrame
                    (ConcreteReuseCapacityCacheFrame sourceModule
                      sourceFunction externals)
                    entryRuntime entryStore entryWitness facts remainingBytes
                    nextRuntime sourceEnv (replaceHeap targetStore heap)
                    targetLocals witness :=
                invariant.ofReplaceHeapEffectStep effectStep capacity
                  ordinaryTransport sourceGlobals cursor
              let sourceAfter : MachineState := {
                source with control := .code continuation
                            runtime := nextRuntime }
              have sourcePath :
                  executeStep externals source = .next sourceAfter := by
                rcases source with
                  ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                    sourceFrames, actualRuntime⟩
                have programEq := related.sourceProgramEq
                change sourceProgram = context.program at programEq
                subst sourceProgram
                have controlEq := related.sourceControlEq
                change sourceControl = _ at controlEq
                subst sourceControl
                have envEq := related.sourceEnvEq
                change actualEnv = sourceEnv at envEq
                subst actualEnv
                have runtimeEq := related.sourceRuntimeEq
                change actualRuntime = sourceRuntime at runtimeEq
                subst actualRuntime
                change evalArg sourceEnv (.fvar fieldId) = .ok field
                  at fieldLookup
                simp [sourceAfter, executeStep, coreStep, objectLookup,
                  fieldLookup, updated]
              let targetAfter : StructuredWasmState Host := {
                store := replaceHeap targetStore heap
                control := .running
                  { targetLocals with values := targetLocals.values } targetRest
                frames := target.frames }
              have targetPath :
                  FinitePath
                    (StructuredWasmStep targetModule.wasmModule hosts.env) 3
                    target targetAfter := by
                rcases target with ⟨actualStore, actualControl, actualFrames⟩
                have storeEq := related.targetStoreEq
                change actualStore = targetStore at storeEq
                subst actualStore
                have controlEq := related.targetControlEq
                change actualControl =
                  .running targetLocals
                    ([.localGet objectIndex, .localGet fieldIndex,
                        .call callIndex] ++ targetRest)
                  at controlEq
                subst actualControl
                simpa [targetAfter] using
                  structuredWasmBinaryHostEffectPrefixFinitePath
                    (module := targetModule.wasmModule) (env := hosts.env)
                    (spec := hosts.spec)
                    (step := objectSetStep index fieldKind)
                    (initial := targetStore)
                    (final := replaceHeap targetStore heap)
                    (locals := targetLocals) (firstIndex := objectIndex)
                    (secondIndex := fieldIndex)
                    (physicalFirst :=
                      .i32 (UInt32.ofNat objectWord.value))
                    (physicalSecond :=
                      .i32 (UInt32.ofNat fieldWord.value))
                    (targetRest := targetRest) (tail := targetLocals.values)
                    (frames := actualFrames) targetObjectLookup
                    targetFieldLookup imported functionSpec.hostsSatisfy
                    inBounds contracted parameterCount resultCount operation
              have nextFocus :
                  ConcreteStructuredCodeFocus context sourceModule
                    sourceFunction labels nextRuntime sourceEnv continuation
                    (replaceHeap targetStore heap) targetLocals targetRest witness
                    sourceAfter targetAfter := {
                sourceProgramEq := by
                  simp [sourceAfter, related.sourceProgramEq]
                sourceControlEq := by simp [sourceAfter]
                sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
                sourceRuntimeEq := by simp [sourceAfter]
                targetStoreEq := by simp [targetAfter]
                targetControlEq := by simp [targetAfter]
                adapted := continuationAdapted
                stateRelated := nextRelated
                frameAligned := nextInvariant.1.1.1.1.2.2.1 }
              exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
                targetRest, .single sourcePath, targetPath, nextFocus,
                nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
                by simp [targetAfter]⟩
          | word64 fieldRelated =>
              cases fieldRelated <;>
                simp [AbiKind.isObjectField] at fieldObjectKind
          | float32Bits fieldRelated => cases fieldRelated
          | float64Bits fieldRelated => cases fieldRelated
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- One successful erased object-field mutation advances the source by one
effect step and the structured target by the exact generated
object-local/zero/imported-call prefix. The canonical zero is justified by the
erased ABI relation and never by ordinary object decoding. -/
theorem ConcreteStructuredCodeFocus.advance_objectFieldErased
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      ObjectFieldErasedEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            3 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | oset sourceRuntime nextRuntime sourceEnv objectId index continuation
      location cell semantic objectCompiled objectLookup updated found live
      objectEq indexValid fieldKindAligned =>
      obtain ⟨objectIndex, callIndex, targetRest, objectFound, objectKindAt,
          callFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.objectSetErased_eq functionSpec.localsAligned
          objectCompiled related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      have objectSourceLookup :
          lookup sourceEnv objectId = some (.object (.heap location)) := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value foundLookup
          injection objectLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      obtain ⟨physicalObject, targetObjectLookup, physicalObjectRelated⟩ :=
        related.stateRelated.resolve objectSourceLookup objectFound objectKindAt
      cases physicalObjectRelated with
      | word32 objectRelated =>
          rename_i objectWord
          have decoded :
              getConstructor sourceRuntime (.object (.heap location)) =
                .ok (location, cell, semantic) := by
            unfold getConstructor
            simp only [getLiveCell, found, live, if_true, Bind.bind,
              Except.bind]
            rw [objectEq]
            rfl
          have tobjectRelated := objectRelated.object_to_tobject
          obtain ⟨info, fieldKinds, descriptorFound⟩ :=
            ConcreteRuntimeRel.constructorDescriptor_of_getConstructor
              related.stateRelated.1 tobjectRelated decoded
          have fieldDescriptorKindAt :=
            fieldKindAligned tobjectRelated descriptorFound
          obtain ⟨imp, imported, inBounds, contracted, parameterCount,
              resultCount⟩ :=
            functionSpec.objectSetCall callFound
          have fieldRelated :
              ValueRel witness .erased (.word32 Word32.zero) .erased :=
            .erased
          obtain ⟨heap, operation, runtimeRelated, capacity, cursor⟩ :=
            objectSetStep_of_refines_with_capacity related.stateRelated.1
              objectRelated fieldRelated found live objectEq descriptorFound
              indexValid fieldDescriptorKindAt updated
          have operationZero :
              objectSetStep index .erased targetStore
                  [.i32 (UInt32.ofNat objectWord.value), .i32 0] =
                .Return [] (replaceHeap targetStore heap) := by
            simpa [Word32.zero] using operation
          have nextRelated :
              StateRelated sourceFunction nextRuntime sourceEnv
                (replaceHeap targetStore heap) targetLocals witness :=
            ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
              related.stateRelated.2.2⟩
          have effectStep :
              EffectStepSimulates context sourceModule sourceFunction labels
                targetModule.wasmModule hosts.env sourceRuntime nextRuntime
                sourceEnv (.oset objectId index .erased continuation)
                continuation
                ([.localGet objectIndex, .const 0, .call callIndex] ++
                  targetCore)
                targetCore targetStore (replaceHeap targetStore heap)
                targetLocals witness witness := by
            apply effectStepSimulates_localI32ConstHost
              (spec := hosts.spec)
              (step := objectSetStep index .erased)
            · exact sourceStep
            · apply codeAdapted_oset
                (arg := .erased) (fieldCode := [.i32Const .erased 0])
                (targetField := [.const 0]) objectCompiled (by rfl)
                objectFound
              · simp [instructions, instruction, pure, Except.pure, Bind.bind,
                  Except.bind]
              · exact callFound
              · exact continuationCoreAdapted
            · exact related.stateRelated
            · exact nextRelated
            · exact targetObjectLookup
            · exact imported
            · exact functionSpec.hostsSatisfy
            · exact inBounds
            · exact contracted
            · exact parameterCount
            · exact resultCount
            · exact operationZero
          have ordinaryTransport :
              OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
            modifyConstructor_ordinaryPersistenceTransport updated
          have sourceGlobals :
              nextRuntime.globals = sourceRuntime.globals :=
            (setObjectField_runtimeAux updated).globals
          have nextInvariant :
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv (replaceHeap targetStore heap)
                targetLocals witness :=
            invariant.ofReplaceHeapEffectStep effectStep capacity
              ordinaryTransport sourceGlobals cursor
          let sourceAfter : MachineState := {
            source with control := .code continuation
                        runtime := nextRuntime }
          have sourcePath :
              executeStep externals source = .next sourceAfter := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have programEq := related.sourceProgramEq
            change sourceProgram = context.program at programEq
            subst sourceProgram
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            have envEq := related.sourceEnvEq
            change actualEnv = sourceEnv at envEq
            subst actualEnv
            have runtimeEq := related.sourceRuntimeEq
            change actualRuntime = sourceRuntime at runtimeEq
            subst actualRuntime
            simp [sourceAfter, executeStep, coreStep, evalArg, objectLookup,
              updated]
          let targetAfter : StructuredWasmState Host := {
            store := replaceHeap targetStore heap
            control := .running
              { targetLocals with values := targetLocals.values } targetRest
            frames := target.frames }
          have targetPath :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 3
                target targetAfter := by
            rcases target with ⟨actualStore, actualControl, actualFrames⟩
            have storeEq := related.targetStoreEq
            change actualStore = targetStore at storeEq
            subst actualStore
            have controlEq := related.targetControlEq
            change actualControl =
              .running targetLocals
                ([.localGet objectIndex, .const 0, .call callIndex] ++
                  targetRest)
              at controlEq
            subst actualControl
            simpa [targetAfter] using
              structuredWasmLocalI32ConstHostEffectPrefixFinitePath
                (module := targetModule.wasmModule) (env := hosts.env)
                (spec := hosts.spec)
                (step := objectSetStep index .erased)
                (initial := targetStore) (final := replaceHeap targetStore heap)
                (locals := targetLocals) (firstIndex := objectIndex)
                (physicalFirst := .i32 (UInt32.ofNat objectWord.value))
                (constant := 0) (targetRest := targetRest)
                (tail := targetLocals.values) (frames := actualFrames)
                targetObjectLookup imported functionSpec.hostsSatisfy inBounds
                contracted parameterCount resultCount operationZero
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation
                (replaceHeap targetStore heap) targetLocals targetRest witness
                sourceAfter targetAfter := {
            sourceProgramEq := by
              simp [sourceAfter, related.sourceProgramEq]
            sourceControlEq := by simp [sourceAfter]
            sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
            sourceRuntimeEq := by simp [sourceAfter]
            targetStoreEq := by simp [targetAfter]
            targetControlEq := by simp [targetAfter]
            adapted := continuationAdapted
            stateRelated := nextRelated
            frameAligned := nextInvariant.1.1.1.1.2.2.1 }
          exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap,
            targetRest, .single sourcePath, targetPath, nextFocus,
            nextInvariant, by simp [sourceAfter], by simp [sourceAfter],
            by simp [targetAfter]⟩
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- One successful `USize` field mutation advances the source by one effect
step and the structured target by the exact generated i32/i64 binary-host
prefix. The concrete checked slot writer preserves the witness, mapped-header
capacity, heap cursor, and all nonheap runtime state needed by recursion. -/
theorem ConcreteStructuredCodeFocus.advance_usizeField
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      USizeFieldEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            3 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | uset sourceRuntime nextRuntime sourceEnv objectId fieldId index continuation
      location cell semantic field objectCompiled fieldCompiled objectLookup
      fieldLookup updated found live objectEq slotStart slotEnd =>
      obtain ⟨objectIndex, fieldIndex, callIndex, targetRest, objectFound,
          objectKindAt, fieldFound, fieldKindAt, callFound,
          continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.usizeSet_eq functionSpec.localsAligned objectCompiled
          fieldCompiled related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      have objectSourceLookup :
          lookup sourceEnv objectId = some (.object (.heap location)) := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value foundLookup
          injection objectLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      have fieldSourceLookup :
          lookup sourceEnv fieldId = some (.usize field) := by
        unfold lookupValue at fieldLookup
        split at fieldLookup
        · rename_i value foundLookup
          injection fieldLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      obtain ⟨physicalObject, targetObjectLookup, physicalObjectRelated⟩ :=
        related.stateRelated.resolve objectSourceLookup objectFound objectKindAt
      obtain ⟨physicalField, targetFieldLookup, physicalFieldRelated⟩ :=
        related.stateRelated.resolve fieldSourceLookup fieldFound fieldKindAt
      cases physicalObjectRelated with
      | word32 objectRelated =>
          rename_i objectWord
          cases physicalFieldRelated with
          | word64 fieldRelated =>
              cases fieldRelated with
              | usize =>
                  obtain ⟨imp, imported, inBounds, contracted, parameterCount,
                      resultCount⟩ :=
                    functionSpec.usizeSetCall callFound
                  obtain ⟨heap, operation, runtimeRelated, capacity, cursor⟩ :=
                    usizeSetStep_of_refines_with_capacity
                      related.stateRelated.1 objectRelated found live objectEq
                      slotStart slotEnd updated
                  have nextRelated :
                      StateRelated sourceFunction nextRuntime sourceEnv
                        (replaceHeap targetStore heap) targetLocals witness :=
                    ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
                      related.stateRelated.2.2⟩
                  have effectStep :
                      EffectStepSimulates context sourceModule sourceFunction labels
                        targetModule.wasmModule hosts.env sourceRuntime
                        nextRuntime sourceEnv
                        (.uset objectId index fieldId continuation) continuation
                        ([.localGet objectIndex, .localGet fieldIndex,
                            .call callIndex] ++ targetCore)
                        targetCore targetStore (replaceHeap targetStore heap)
                        targetLocals witness witness := by
                    apply effectStepSimulates_binaryHost
                      (spec := hosts.spec) (step := usizeSetStep index)
                    · exact sourceStep
                    · exact codeAdapted_uset objectCompiled fieldCompiled
                        objectFound fieldFound callFound continuationCoreAdapted
                    · exact related.stateRelated
                    · exact nextRelated
                    · exact targetObjectLookup
                    · exact targetFieldLookup
                    · exact imported
                    · exact functionSpec.hostsSatisfy
                    · exact inBounds
                    · exact contracted
                    · exact parameterCount
                    · exact resultCount
                    · exact operation
                  have ordinaryTransport :
                      OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
                    setUSizeSlot_ordinaryPersistenceTransport updated
                  have sourceGlobals :
                      nextRuntime.globals = sourceRuntime.globals :=
                    (setUSizeSlot_runtimeAux updated).globals
                  have nextInvariant :
                      ReuseCapacityEntryRelativeFrame
                        (ConcreteReuseCapacityCacheFrame sourceModule
                          sourceFunction externals)
                        entryRuntime entryStore entryWitness facts
                        remainingBytes nextRuntime sourceEnv
                        (replaceHeap targetStore heap) targetLocals witness :=
                    invariant.ofReplaceHeapEffectStep effectStep capacity
                      ordinaryTransport sourceGlobals cursor
                  let sourceAfter : MachineState := {
                    source with control := .code continuation
                                runtime := nextRuntime }
                  have sourcePath :
                      executeStep externals source = .next sourceAfter := by
                    rcases source with
                      ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                        sourceFrames, actualRuntime⟩
                    have programEq := related.sourceProgramEq
                    change sourceProgram = context.program at programEq
                    subst sourceProgram
                    have controlEq := related.sourceControlEq
                    change sourceControl = _ at controlEq
                    subst sourceControl
                    have envEq := related.sourceEnvEq
                    change actualEnv = sourceEnv at envEq
                    subst actualEnv
                    have runtimeEq := related.sourceRuntimeEq
                    change actualRuntime = sourceRuntime at runtimeEq
                    subst actualRuntime
                    simp [sourceAfter, executeStep, coreStep, objectLookup,
                      fieldLookup, updated]
                  let targetAfter : StructuredWasmState Host := {
                    store := replaceHeap targetStore heap
                    control := .running
                      { targetLocals with values := targetLocals.values }
                        targetRest
                    frames := target.frames }
                  have targetPath :
                      FinitePath
                        (StructuredWasmStep targetModule.wasmModule hosts.env) 3
                        target targetAfter := by
                    rcases target with
                      ⟨actualStore, actualControl, actualFrames⟩
                    have storeEq := related.targetStoreEq
                    change actualStore = targetStore at storeEq
                    subst actualStore
                    have controlEq := related.targetControlEq
                    change actualControl =
                      .running targetLocals
                        ([.localGet objectIndex, .localGet fieldIndex,
                            .call callIndex] ++ targetRest)
                      at controlEq
                    subst actualControl
                    simpa [targetAfter] using
                      structuredWasmBinaryHostEffectPrefixFinitePath
                        (module := targetModule.wasmModule) (env := hosts.env)
                        (spec := hosts.spec) (step := usizeSetStep index)
                        (initial := targetStore)
                        (final := replaceHeap targetStore heap)
                        (locals := targetLocals) (firstIndex := objectIndex)
                        (secondIndex := fieldIndex)
                        (physicalFirst :=
                          .i32 (UInt32.ofNat objectWord.value))
                        (physicalSecond := .i64 field)
                        (targetRest := targetRest)
                        (tail := targetLocals.values)
                        (frames := actualFrames) targetObjectLookup
                        targetFieldLookup imported functionSpec.hostsSatisfy
                        inBounds contracted parameterCount resultCount operation
                  have nextFocus :
                      ConcreteStructuredCodeFocus context sourceModule
                        sourceFunction labels nextRuntime sourceEnv continuation
                        (replaceHeap targetStore heap) targetLocals targetRest
                        witness sourceAfter targetAfter := {
                    sourceProgramEq := by
                      simp [sourceAfter, related.sourceProgramEq]
                    sourceControlEq := by simp [sourceAfter]
                    sourceEnvEq := by
                      simp [sourceAfter, related.sourceEnvEq]
                    sourceRuntimeEq := by simp [sourceAfter]
                    targetStoreEq := by simp [targetAfter]
                    targetControlEq := by simp [targetAfter]
                    adapted := continuationAdapted
                    stateRelated := nextRelated
                    frameAligned := nextInvariant.1.1.1.1.2.2.1 }
                  exact ⟨sourceAfter, targetAfter,
                    replaceHeap targetStore heap, targetRest,
                    .single sourcePath, targetPath, nextFocus, nextInvariant,
                    by simp [sourceAfter], by simp [sourceAfter],
                    by simp [targetAfter]⟩
          | word32 fieldRelated => cases fieldRelated
          | float32Bits fieldRelated => cases fieldRelated
          | float64Bits fieldRelated => cases fieldRelated
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- Common structured continuation rule for every supported packed-scalar
writer. Width-specific reasoning supplies only the physical second operand and
the checked concrete operation; the generated binary prefix, source step, and
entry-relative frame reconstruction are shared. -/
private theorem ConcreteStructuredCodeFocus.advance_scalarFieldOperation
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {objectId fieldId : Lean.FVarId} {slotIndex byteOffset : Nat}
    {type : Lean.Expr}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {location : Location} {field : ScalarValue} {fieldKind : AbiKind}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {objectIndex fieldIndex callIndex : Nat}
    {targetRest targetCore : Wasm.Program}
    {objectWord : Word32} {physicalField : Wasm.Value}
    {heap : MemoryState} {imp : Wasm.ImportDecl}
    {source : MachineState} {target : StructuredWasmState Host}
    (objectLookup :
      lookupValue sourceEnv objectId = .ok (.object (.heap location)))
    (fieldLookup : lookupValue sourceEnv fieldId = .ok (.scalar field))
    (updated :
      setScalarField sourceRuntime (.object (.heap location)) slotIndex
          byteOffset (.scalar field) =
        .ok nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv
        (.sset objectId slotIndex byteOffset fieldId type continuation)
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv
        (.sset objectId slotIndex byteOffset fieldId type continuation)
        targetStore targetLocals
        ([.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
          targetRest)
        witness source target)
    (continuationAdapted :
      CodeAdaptedWithSuffix context sourceModule sourceFunction labels continuation
        targetRest)
    (coreAdapted :
      CodeAdapted context sourceModule sourceFunction labels
        (.sset objectId slotIndex byteOffset fieldId type continuation)
        ([.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
          targetCore))
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness)
    (targetObjectLookup :
      targetLocals.get objectIndex =
        some (.i32 (UInt32.ofNat objectWord.value)))
    (targetFieldLookup : targetLocals.get fieldIndex = some physicalField)
    (imported : targetModule.wasmModule.imports[callIndex]? = some imp)
    (inBounds : callIndex < targetModule.wasmModule.imports.length)
    (contracted : hosts.spec.contracts[callIndex]? = some
      (scalarSetContract slotIndex byteOffset fieldKind))
    (parameterCount : imp.params.length = 2)
    (resultCount : imp.results.length = 0)
    (operation :
      scalarSetStep slotIndex byteOffset fieldKind targetStore
          [.i32 (UInt32.ofNat objectWord.value), physicalField] =
        .Return [] (replaceHeap targetStore heap))
    (runtimeRelated :
      ConcreteRuntimeRel (replaceHeap targetStore heap).host.runtime witness
        nextRuntime)
    (capacity :
      MappedHeaderCapacityTransport targetStore.host.runtime.heap heap witness)
    (cursor : heap.heapCursor = targetStore.host.runtime.heap.heapCursor) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            3 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  have nextRelated :
      StateRelated sourceFunction nextRuntime sourceEnv
        (replaceHeap targetStore heap) targetLocals witness :=
    ⟨runtimeRelated, by simp [replaceHeap, clearFailure],
      related.stateRelated.2.2⟩
  have effectStep :
      EffectStepSimulates context sourceModule sourceFunction labels
        targetModule.wasmModule hosts.env sourceRuntime nextRuntime sourceEnv
        (.sset objectId slotIndex byteOffset fieldId type continuation)
        continuation
        ([.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
          targetCore)
        targetCore targetStore (replaceHeap targetStore heap) targetLocals
        witness witness := by
    apply effectStepSimulates_binaryHost
      (spec := hosts.spec)
      (step := scalarSetStep slotIndex byteOffset fieldKind)
    · exact sourceStep
    · exact coreAdapted
    · exact related.stateRelated
    · exact nextRelated
    · exact targetObjectLookup
    · exact targetFieldLookup
    · exact imported
    · exact functionSpec.hostsSatisfy
    · exact inBounds
    · exact contracted
    · exact parameterCount
    · exact resultCount
    · exact operation
  have ordinaryTransport :
      OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
    setScalarField_ordinaryPersistenceTransport updated
  have sourceGlobals : nextRuntime.globals = sourceRuntime.globals :=
    (setScalarField_runtimeAux updated).globals
  have nextInvariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes nextRuntime
        sourceEnv (replaceHeap targetStore heap) targetLocals witness :=
    invariant.ofReplaceHeapEffectStep effectStep capacity ordinaryTransport
      sourceGlobals cursor
  let sourceAfter : MachineState := {
    source with control := .code continuation
                runtime := nextRuntime }
  have sourcePath : executeStep externals source = .next sourceAfter := by
    rcases source with
      ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv, sourceFrames,
        actualRuntime⟩
    have programEq := related.sourceProgramEq
    change sourceProgram = context.program at programEq
    subst sourceProgram
    have controlEq := related.sourceControlEq
    change sourceControl = _ at controlEq
    subst sourceControl
    have envEq := related.sourceEnvEq
    change actualEnv = sourceEnv at envEq
    subst actualEnv
    have runtimeEq := related.sourceRuntimeEq
    change actualRuntime = sourceRuntime at runtimeEq
    subst actualRuntime
    simp [sourceAfter, executeStep, coreStep, objectLookup, fieldLookup,
      updated]
  let targetAfter : StructuredWasmState Host := {
    store := replaceHeap targetStore heap
    control := .running
      { targetLocals with values := targetLocals.values } targetRest
    frames := target.frames }
  have targetPath :
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 3
        target targetAfter := by
    rcases target with ⟨actualStore, actualControl, actualFrames⟩
    have storeEq := related.targetStoreEq
    change actualStore = targetStore at storeEq
    subst actualStore
    have controlEq := related.targetControlEq
    change actualControl =
      .running targetLocals
        ([.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
          targetRest)
      at controlEq
    subst actualControl
    simpa [targetAfter] using
      structuredWasmBinaryHostEffectPrefixFinitePath
        (module := targetModule.wasmModule) (env := hosts.env)
        (spec := hosts.spec)
        (step := scalarSetStep slotIndex byteOffset fieldKind)
        (initial := targetStore) (final := replaceHeap targetStore heap)
        (locals := targetLocals) (firstIndex := objectIndex)
        (secondIndex := fieldIndex)
        (physicalFirst := .i32 (UInt32.ofNat objectWord.value))
        (physicalSecond := physicalField) (targetRest := targetRest)
        (tail := targetLocals.values) (frames := actualFrames)
        targetObjectLookup targetFieldLookup imported functionSpec.hostsSatisfy
        inBounds contracted parameterCount resultCount operation
  have nextFocus :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        nextRuntime sourceEnv continuation (replaceHeap targetStore heap)
        targetLocals targetRest witness sourceAfter targetAfter := {
    sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
    sourceControlEq := by simp [sourceAfter]
    sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
    sourceRuntimeEq := by simp [sourceAfter]
    targetStoreEq := by simp [targetAfter]
    targetControlEq := by simp [targetAfter]
    adapted := continuationAdapted
    stateRelated := nextRelated
    frameAligned := nextInvariant.1.1.1.1.2.2.1 }
  exact ⟨sourceAfter, targetAfter, replaceHeap targetStore heap, targetRest,
    .single sourcePath, targetPath, nextFocus, nextInvariant,
    by simp [sourceAfter], by simp [sourceAfter], by simp [targetAfter]⟩

/-- One successful packed-integer scalar mutation advances the source by one
effect step and the structured target by the exact generated binary-host
prefix. Production inversion and state refinement select the i32 lane for
`UInt8`/`UInt16`/`UInt32` and the i64 lane for `UInt64`; each checked writer
then enters the common same-witness continuation rule. -/
theorem ConcreteStructuredCodeFocus.advance_scalarField
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    {externals : ExternalImpl} {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts} {remainingBytes : Nat}
    {sourceRuntime nextRuntime entryRuntime : RuntimeState}
    {sourceEnv : Env}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (supported :
      ScalarFieldEffectSupported context sourceRuntime sourceEnv code
        continuation nextRuntime)
    (sourceStep :
      SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
        continuation)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts remainingBytes sourceRuntime
        sourceEnv targetStore targetLocals witness) :
    ∃ sourceAfter targetAfter nextStore targetRest,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          1 source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            3 target targetAfter ∧
          ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
              nextRuntime sourceEnv continuation nextStore targetLocals
              targetRest witness sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness facts remainingBytes
              nextRuntime sourceEnv nextStore targetLocals witness ∧
            sourceAfter.joins = source.joins ∧
              sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  cases supported with
  | sset sourceRuntime nextRuntime sourceEnv objectId fieldId slotIndex
      byteOffset type continuation location cell semantic field fieldKind
      objectCompiled fieldCompiled objectLookup fieldLookup updated found live
      objectEq layoutSafe =>
      obtain ⟨objectIndex, fieldIndex, callIndex, targetRest, objectFound,
          objectKindAt, fieldFound, fieldKindAt, callFound,
          continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.scalarSet_eq functionSpec.localsAligned objectCompiled
          fieldCompiled related.adapted
      subst targetCode
      have continuationAdaptedSaved := continuationAdapted
      obtain ⟨targetCore, _targetSuffix, continuationCoreAdapted,
          _targetRestEq⟩ := continuationAdaptedSaved
      have coreAdapted :
          CodeAdapted context sourceModule sourceFunction labels
            (.sset objectId slotIndex byteOffset fieldId type continuation)
            ([.localGet objectIndex, .localGet fieldIndex, .call callIndex] ++
              targetCore) :=
        codeAdapted_sset objectCompiled fieldCompiled objectFound fieldFound
          callFound continuationCoreAdapted
      have objectSourceLookup :
          lookup sourceEnv objectId = some (.object (.heap location)) := by
        unfold lookupValue at objectLookup
        split at objectLookup
        · rename_i value foundLookup
          injection objectLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      have fieldSourceLookup :
          lookup sourceEnv fieldId = some (.scalar field) := by
        unfold lookupValue at fieldLookup
        split at fieldLookup
        · rename_i value foundLookup
          injection fieldLookup with valueEq
          subst value
          exact foundLookup
        · contradiction
      obtain ⟨physicalObject, targetObjectLookup, physicalObjectRelated⟩ :=
        related.stateRelated.resolve objectSourceLookup objectFound objectKindAt
      obtain ⟨physicalField, targetFieldLookup, physicalFieldRelated⟩ :=
        related.stateRelated.resolve fieldSourceLookup fieldFound fieldKindAt
      cases physicalObjectRelated with
      | word32 objectRelated =>
          rename_i objectWord
          have decoded :
              getConstructor sourceRuntime (.object (.heap location)) =
                .ok (location, cell, semantic) := by
            unfold getConstructor
            simp only [getLiveCell, found, live, if_true, Bind.bind,
              Except.bind]
            rw [objectEq]
            rfl
          have tobjectRelated := objectRelated.object_to_tobject
          obtain ⟨info, fieldKinds, descriptorFound⟩ :=
            ConcreteRuntimeRel.constructorDescriptor_of_getConstructor
              related.stateRelated.1 tobjectRelated decoded
          obtain ⟨historySafe, slotIndexEq, fieldFits⟩ :=
            layoutSafe tobjectRelated descriptorFound
          have fieldSupported : PackedIntegerAbiKind fieldKind := by
            cases fieldKind <;>
              simp [PackedIntegerAbiKind] at fieldFits ⊢
          cases fieldKind with
          | uint8 =>
              obtain ⟨imp, imported, inBounds, contracted, parameterCount,
                  resultCount⟩ :=
                functionSpec.scalarSetCall callFound (by trivial)
              cases physicalFieldRelated with
              | word32 fieldRelated =>
                  rename_i fieldWord
                  cases fieldRelated with
                  | uint8 encoded =>
                      obtain ⟨heap, operation, runtimeRelated, capacity,
                          cursor⟩ :=
                        scalarSetStep_uint8_of_refines_with_capacity
                          related.stateRelated.1 objectRelated (.uint8 encoded)
                          found live objectEq descriptorFound
                          (by simpa using historySafe) slotIndexEq
                          (by simpa using fieldFits) updated
                      exact
                        ConcreteStructuredCodeFocus.advance_scalarFieldOperation
                          functionSpec objectLookup fieldLookup updated sourceStep
                          related continuationAdapted coreAdapted invariant
                          targetObjectLookup targetFieldLookup imported inBounds
                          contracted parameterCount resultCount operation
                          runtimeRelated capacity cursor
              | word64 fieldRelated => cases fieldRelated
              | float32Bits fieldRelated => cases fieldRelated
              | float64Bits fieldRelated => cases fieldRelated
          | uint16 =>
              obtain ⟨imp, imported, inBounds, contracted, parameterCount,
                  resultCount⟩ :=
                functionSpec.scalarSetCall callFound (by trivial)
              cases physicalFieldRelated with
              | word32 fieldRelated =>
                  rename_i fieldWord
                  cases fieldRelated with
                  | uint16 encoded =>
                      obtain ⟨heap, operation, runtimeRelated, capacity,
                          cursor⟩ :=
                        scalarSetStep_uint16_of_refines_with_capacity
                          related.stateRelated.1 objectRelated (.uint16 encoded)
                          found live objectEq descriptorFound
                          (by simpa using historySafe) slotIndexEq
                          (by simpa using fieldFits) updated
                      exact
                        ConcreteStructuredCodeFocus.advance_scalarFieldOperation
                          functionSpec objectLookup fieldLookup updated sourceStep
                          related continuationAdapted coreAdapted invariant
                          targetObjectLookup targetFieldLookup imported inBounds
                          contracted parameterCount resultCount operation
                          runtimeRelated capacity cursor
              | word64 fieldRelated => cases fieldRelated
              | float32Bits fieldRelated => cases fieldRelated
              | float64Bits fieldRelated => cases fieldRelated
          | uint32 =>
              obtain ⟨imp, imported, inBounds, contracted, parameterCount,
                  resultCount⟩ :=
                functionSpec.scalarSetCall callFound (by trivial)
              cases physicalFieldRelated with
              | word32 fieldRelated =>
                  rename_i fieldWord
                  cases fieldRelated with
                  | uint32 encoded =>
                      obtain ⟨heap, operation, runtimeRelated, capacity,
                          cursor⟩ :=
                        scalarSetStep_uint32_of_refines_with_capacity
                          related.stateRelated.1 objectRelated (.uint32 encoded)
                          found live objectEq descriptorFound
                          (by simpa using historySafe) slotIndexEq
                          (by simpa using fieldFits) updated
                      exact
                        ConcreteStructuredCodeFocus.advance_scalarFieldOperation
                          functionSpec objectLookup fieldLookup updated sourceStep
                          related continuationAdapted coreAdapted invariant
                          targetObjectLookup targetFieldLookup imported inBounds
                          contracted parameterCount resultCount operation
                          runtimeRelated capacity cursor
              | word64 fieldRelated => cases fieldRelated
              | float32Bits fieldRelated => cases fieldRelated
              | float64Bits fieldRelated => cases fieldRelated
          | uint64 =>
              obtain ⟨imp, imported, inBounds, contracted, parameterCount,
                  resultCount⟩ :=
                functionSpec.scalarSetCall callFound (by trivial)
              cases physicalFieldRelated with
              | word64 fieldRelated =>
                  cases fieldRelated with
                  | uint64 =>
                      obtain ⟨heap, operation, runtimeRelated, capacity,
                          cursor⟩ :=
                        scalarSetStep_uint64_of_refines_with_capacity
                          related.stateRelated.1 objectRelated .uint64 found live
                          objectEq descriptorFound (by simpa using historySafe)
                          slotIndexEq (by simpa using fieldFits) updated
                      exact
                        ConcreteStructuredCodeFocus.advance_scalarFieldOperation
                          functionSpec objectLookup fieldLookup updated sourceStep
                          related continuationAdapted coreAdapted invariant
                          targetObjectLookup targetFieldLookup imported inBounds
                          contracted parameterCount resultCount operation
                          runtimeRelated capacity cursor
              | word32 fieldRelated => cases fieldRelated
              | float32Bits fieldRelated => cases fieldRelated
              | float64Bits fieldRelated => cases fieldRelated
          | object => simp [PackedIntegerAbiKind] at fieldSupported
          | tagged => simp [PackedIntegerAbiKind] at fieldSupported
          | tobject => simp [PackedIntegerAbiKind] at fieldSupported
          | erased => simp [PackedIntegerAbiKind] at fieldSupported
          | reuseToken => simp [PackedIntegerAbiKind] at fieldSupported
          | usize => simp [PackedIntegerAbiKind] at fieldSupported
          | float32 => simp [PackedIntegerAbiKind] at fieldSupported
          | float => simp [PackedIntegerAbiKind] at fieldSupported
      | word64 objectRelated => cases objectRelated
      | float32Bits objectRelated => cases objectRelated
      | float64Bits objectRelated => cases objectRelated

/-- Recursive structured partial correctness for direct values, supported pure
external results, statically named calls, generated lazy caches, erased
default-case wrappers, arbitrary normalized object and scalar `UInt8`
dispatchers, ownership effects through deletion, constructor-tag mutation,
both FVar and erased object-field mutation, `USize` field mutation, and every
supported packed-integer scalar field mutation.

External results traverse the interpreter's exact three-step request protocol
and the compiler-derived imported-call prefix. Named and exactly saturated
closure calls are staged by the production compiler, entered by the structured
machine, discharged recursively in the exact generated declaration row, and
returned through their saved bind/call and matcher-label frames. The explicit
entry ABI premise is static allocation metadata transported from the entry
witness; it is not a target execution certificate. Both machine paths, the
entry-relative concrete resource frame, result ABI refinement, and exact
restoration of the enclosing frame stacks are retained. No target trace,
callee execution package, or translation certificate is a premise. -/
theorem
    ConcreteStructuredCodeFocus.reachesYield_reuseBudgetedDirectPureExternalCallsLazyCacheCases_generated
    {program : Fir.LeanIR.ImpureProgram}
    {rootContext : Fir.Wasm.Context}
    {rootCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {rootFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    {exportName : String}
    (rootSpec :
      ConcreteSupportedExport program rootContext rootCode sourceModule
        rootFunction targetModule hosts exportName)
    (rootGenerated :
      LazyCacheGeneratedEnvironment rootContext sourceModule)
    {context : Fir.Wasm.Context}
    {functionCode code : Lean.Compiler.LCNF.Code .impure}
    {sourceFunction : Fir.Wasm.Function}
    (functionSpec :
      ConcreteSupportedFunction program context functionCode sourceModule
        sourceFunction targetModule hosts)
    (contextCaches :
      context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
    {externals : ExternalImpl}
    {expectedResult : AbiKind}
    {facts resultFacts : ReuseCapacityFacts}
    {sourceRuntime resultRuntime entryRuntime : RuntimeState}
    {sourceEnv resultEnv : Env}
    {resultValue : Value} {requiredBytes slack : Nat}
    {targetStore entryStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {witness entryWitness : RefinementWitness}
    {targetCode : Wasm.Program}
    {source : MachineState} {target : StructuredWasmState Host}
    (evaluation :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv code resultFacts
        resultRuntime resultEnv resultValue requiredBytes)
    (related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction []
        sourceRuntime sourceEnv code targetStore targetLocals targetCode witness
        source target)
    (sourceJoins : source.joins = [])
    (invariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        entryRuntime entryStore entryWitness facts (requiredBytes + slack)
        sourceRuntime sourceEnv targetStore targetLocals witness)
    (entryAbi :
      ClosureAllocationsAbiAligned context.program entryWitness) :
    ∃ sourceAfter targetAfter resultStore resultLocals resultWitness kind
        physical sourceCount targetCount,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          sourceCount source sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            targetCount target targetAfter ∧
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
              resultEnv resultValue resultStore resultLocals resultWitness kind
              physical sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness resultFacts slack
                resultRuntime resultEnv resultStore resultLocals resultWitness ∧
              kind.refines expectedResult = true ∧
                sourceAfter.joins = [] ∧
                sourceAfter.frames = source.frames ∧
                targetAfter.frames = target.frames := by
  induction evaluation generalizing functionCode sourceFunction
      targetStore targetLocals targetCode witness source target entryRuntime
      entryStore entryWitness slack with
  | ret sourceLookup resultCompiled resultRefines =>
      obtain ⟨kind, physical, sourceAfter, targetAfter, targetResultCompiled,
          sourceStep, targetPath, yielded, sourceAfterJoins, sourceFramesEq,
          targetFramesEq⟩ :=
        related.advance_return functionSpec.localsAligned sourceLookup
      rw [resultCompiled] at targetResultCompiled
      have resultPairEq := Except.ok.inj targetResultCompiled
      cases resultPairEq
      exact ⟨sourceAfter, targetAfter, targetStore, targetLocals, witness,
        _, physical, 1, 2, .single sourceStep, targetPath,
        yielded, by simpa using invariant, resultRefines,
        sourceAfterJoins.trans sourceJoins, sourceFramesEq, targetFramesEq⟩
  | @defaultCase sourceRuntime sourceEnv cases selected context expectedResult
      facts resultFacts resultRuntime resultEnv resultValue
      requiredBytes supported sourceStep continued ih =>
      let sourceSelected : MachineState := {
        source with control := .code selected }
      have selectSourceStep :
          executeStep externals source = .next sourceSelected := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.cases cases) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change actualEnv = sourceEnv at envEq
        subst actualEnv
        have runtimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at runtimeEq
        subst actualRuntime
        rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
        simp [sourceSelected, executeStep, coreStep, found, tagged, chosen]
      have selectedFocus :
          ConcreteStructuredCodeFocus context sourceModule sourceFunction []
            sourceRuntime sourceEnv selected targetStore targetLocals targetCode
            witness sourceSelected target := {
        sourceProgramEq := by simp [sourceSelected, related.sourceProgramEq]
        sourceControlEq := by simp [sourceSelected]
        sourceEnvEq := by simp [sourceSelected, related.sourceEnvEq]
        sourceRuntimeEq := by simp [sourceSelected, related.sourceRuntimeEq]
        targetStoreEq := related.targetStoreEq
        targetControlEq := related.targetControlEq
        adapted :=
          CodeAdaptedWithSuffix.defaultOnlyCases_selected supported
            related.adapted
        stateRelated := related.stateRelated
        frameAligned := related.frameAligned }
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetPath, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches selectedFocus
          (by simpa [sourceSelected] using sourceJoins) invariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, targetCount,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          selectSourceStep).trans sourceTail,
        targetPath, yielded, resultInvariant, resultRefines, resultJoins,
        by simpa [sourceSelected] using sourceFramesEq, targetFramesEq⟩
  | @singleObjectCase context sourceRuntime sourceEnv cases selected
      expectedResult facts resultFacts resultRuntime resultEnv resultValue
      requiredBytes supported sourceStep continued ih =>
      rcases supported with
        ⟨info, altsEq, modeEq, expectedTagFits, discrCompiled,
          actualTagFits⟩
      rcases sourceStep with
        ⟨sourceObject, actualTag, lookupFound, tagged, chosen⟩
      have sourceLookup :
          lookup sourceEnv cases.discr = some sourceObject := by
        cases lookupEq : lookup sourceEnv cases.discr with
        | none => simp [lookupValue, lookupEq] at lookupFound
        | some value =>
            have valueEq : value = sourceObject := by
              simpa [lookupValue, lookupEq] using lookupFound
            subst value
            rfl
      have tagEq : actualTag = info.cidx := by
        rw [altsEq] at chosen
        simp [chooseAlt, findCtorAlt, findDefaultAlt] at chosen
        omega
      have actualFits : actualTag < UInt32.size :=
        actualTagFits lookupFound tagged
      have expectedFits : info.cidx < UInt32.size := by
        simpa [Fir.Wasm.constructorTagFitsI32] using expectedTagFits
      obtain ⟨selectedTarget, discrIndex, getTagIndex, targetSuffix,
          selectedAdapted, discrFound, getTagFound, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.singleObjectConstructorCases_eq altsEq modeEq
          expectedTagFits related.adapted
      obtain ⟨alignedIndex, alignedFound, discrKind⟩ :=
        functionSpec.localsAligned discrCompiled
      rw [discrFound] at alignedFound
      have alignedEq : alignedIndex = discrIndex :=
        Option.some.inj alignedFound.symm
      subst alignedIndex
      obtain ⟨discrPhysical, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup discrFound discrKind
      obtain ⟨word, physicalEq, objectRelated⟩ :
          ∃ word : Word32,
            discrPhysical = .i32 (UInt32.ofNat word.value) ∧
              ValueRel witness .tobject (.word32 word) sourceObject := by
        cases physicalRelated with
        | word32 valueRelated => exact ⟨_, rfl, valueRelated⟩
        | word64 valueRelated => cases valueRelated
        | float32Bits valueRelated => cases valueRelated
        | float64Bits valueRelated => cases valueRelated
      subst discrPhysical
      obtain ⟨imp, imported, inBounds, contracted, params, results⟩ :=
        functionSpec.runtimeCallsAligned getTagFound
      have getTagContracted :
          hosts.spec.contracts[getTagIndex]? = some getTagContract := by
        change hosts.spec.contracts[getTagIndex]? =
          some (fun initial args result => result = getTagStep initial args)
        simpa only [resolvedContract?, hostFn?, Option.map_some, getTagFn]
          using contracted
      have parameterCount : imp.params.length = 1 := by
        change imp.params.length = 1 at params
        exact params
      have resultCount : imp.results.length = 1 := by
        change imp.results.length = 1 at results
        exact results
      have tagOperation :
          getTagStep targetStore [.i32 (UInt32.ofNat word.value)] =
            .Return [.i32 (UInt32.ofNat info.cidx)] targetStore := by
        have operation :=
          getTagStep_of_refines related.stateRelated.1 objectRelated tagged
            actualFits
        rw [related.stateRelated.clearFailure, tagEq] at operation
        exact operation
      let sourceSelected : MachineState := {
        source with control := .code selected }
      have selectSourceStep :
          executeStep externals source = .next sourceSelected := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.cases cases) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change actualEnv = sourceEnv at envEq
        subst actualEnv
        have runtimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at runtimeEq
        subst actualRuntime
        simp [sourceSelected, executeStep, coreStep, lookupFound, tagged,
          chosen]
      let targetSelected : StructuredWasmState Host := {
        store := targetStore
        control := .running
          { targetLocals with values := targetLocals.values } selectedTarget
        frames :=
          .label 0 (targetLocals.values.drop 0) targetSuffix :: target.frames }
      have targetPrefix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env) 5 target
            targetSelected := by
        rcases target with ⟨actualStore, actualControl, actualFrames⟩
        have storeEq := related.targetStoreEq
        change actualStore = targetStore at storeEq
        subst actualStore
        have controlEq := related.targetControlEq
        change actualControl = .running targetLocals targetCode at controlEq
        subst actualControl
        subst targetCode
        simpa [targetSelected] using
          structuredWasmObjectCaseHitPrefixFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (spec := hosts.spec) (store := targetStore)
            (locals := targetLocals) (rest := targetSuffix)
            (frames := actualFrames)
            (thenTarget := selectedTarget) (elseTarget := [.unreachable])
            (discrIndex := discrIndex)
            (getTagIndex := getTagIndex) (imp := imp) (word := word)
            (actualTag := info.cidx) (expectedTag := info.cidx) rfl
            targetLookup imported functionSpec.hostsSatisfy
            inBounds getTagContracted parameterCount resultCount tagOperation
      have selectedFocus :
          ConcreteStructuredCodeFocus context sourceModule sourceFunction []
            sourceRuntime sourceEnv selected targetStore
            { targetLocals with values := targetLocals.values } selectedTarget
            witness sourceSelected targetSelected := {
        sourceProgramEq := by simp [sourceSelected, related.sourceProgramEq]
        sourceControlEq := by simp [sourceSelected]
        sourceEnvEq := by simp [sourceSelected, related.sourceEnvEq]
        sourceRuntimeEq := by simp [sourceSelected, related.sourceRuntimeEq]
        targetStoreEq := by simp [targetSelected]
        targetControlEq := by simp [targetSelected]
        adapted := CodeAdapted.withEmptySuffix selectedAdapted
        stateRelated := related.stateRelated.withValues targetLocals.values
        frameAligned := related.frameAligned.withValues targetLocals.values }
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, selectedTargetFramesEq⟩ :=
        ih functionSpec contextCaches selectedFocus
          (by simpa [sourceSelected] using sourceJoins)
          (invariant.withValues targetLocals.values) entryAbi
      let targetFinal : StructuredWasmState Host := {
        store := resultStore
        control := .returning (physical :: resultLocals.values)
        frames := target.frames }
      have unwindTarget :
          StructuredWasmStep targetModule.wasmModule hosts.env targetAfter
            targetFinal := by
        rcases targetAfter with
          ⟨afterStore, afterControl, afterFrames⟩
        have storeEq := yielded.targetStoreEq
        change afterStore = resultStore at storeEq
        subst afterStore
        have controlEq := yielded.targetControlEq
        change afterControl = .returning (physical :: resultLocals.values)
          at controlEq
        subst afterControl
        change afterFrames = _ at selectedTargetFramesEq
        rw [show targetSelected.frames =
          .label 0 (targetLocals.values.drop 0) targetSuffix :: target.frames by
            simp [targetSelected]] at selectedTargetFramesEq
        subst afterFrames
        simpa [targetFinal] using
          (StructuredWasmStep.returnLabel
            (module := targetModule.wasmModule) (env := hosts.env)
            (values := physical :: resultLocals.values) (resultArity := 0)
            (belowStack := targetLocals.values.drop 0) (rest := targetSuffix)
            (store := resultStore) (frames := target.frames))
      have finalYielded :
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
            resultEnv resultValue resultStore resultLocals resultWitness kind
            physical sourceAfter targetFinal := {
        sourceProgramEq := yielded.sourceProgramEq
        sourceControlEq := yielded.sourceControlEq
        sourceEnvEq := yielded.sourceEnvEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        targetStoreEq := by simp [targetFinal]
        targetControlEq := by simp [targetFinal]
        stateRelated := yielded.stateRelated
        frameAligned := yielded.frameAligned
        valueRelated := yielded.valueRelated }
      exact ⟨sourceAfter, targetFinal, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 5 + targetCount + 1,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          selectSourceStep).trans sourceTail,
        (targetPrefix.trans targetTail).trans (.single unwindTarget),
        finalYielded, resultInvariant, resultRefines, resultJoins,
        by simpa [sourceSelected] using sourceFramesEq,
        by simp [targetFinal]⟩
  | @twoObjectDefaultCase context sourceRuntime sourceEnv cases selected
      expectedResult facts resultFacts resultRuntime resultEnv resultValue
      requiredBytes supported sourceStep continued ih =>
      rcases supported with
        ⟨firstInfo, secondInfo, firstBranch, secondBranch, defaultBranch,
          altsEq, modeEq, firstTagFits, secondTagFits, discrCompiled,
          actualTagFits⟩
      rcases sourceStep with
        ⟨sourceObject, actualTag, lookupFound, tagged, chosen⟩
      have sourceLookup :
          lookup sourceEnv cases.discr = some sourceObject := by
        cases lookupEq : lookup sourceEnv cases.discr with
        | none => simp [lookupValue, lookupEq] at lookupFound
        | some value =>
            have valueEq : value = sourceObject := by
              simpa [lookupValue, lookupEq] using lookupFound
            subst value
            rfl
      have actualFits : actualTag < UInt32.size :=
        actualTagFits lookupFound tagged
      have firstExpectedFits : firstInfo.cidx < UInt32.size := by
        simpa [Fir.Wasm.constructorTagFitsI32] using firstTagFits
      have secondExpectedFits : secondInfo.cidx < UInt32.size := by
        simpa [Fir.Wasm.constructorTagFitsI32] using secondTagFits
      obtain ⟨firstTarget, secondTarget, defaultTarget, discrIndex,
          getTagIndex, targetSuffix, firstAdapted, secondAdapted,
          defaultAdapted, discrFound, getTagFound, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.twoObjectConstructorDefaultCases_eq altsEq
          modeEq firstTagFits secondTagFits related.adapted
      obtain ⟨alignedIndex, alignedFound, discrKind⟩ :=
        functionSpec.localsAligned discrCompiled
      rw [discrFound] at alignedFound
      have alignedEq : alignedIndex = discrIndex :=
        Option.some.inj alignedFound.symm
      subst alignedIndex
      obtain ⟨discrPhysical, targetLookup, physicalRelated⟩ :=
        related.stateRelated.resolve sourceLookup discrFound discrKind
      obtain ⟨word, physicalEq, objectRelated⟩ :
          ∃ word : Word32,
            discrPhysical = .i32 (UInt32.ofNat word.value) ∧
              ValueRel witness .tobject (.word32 word) sourceObject := by
        cases physicalRelated with
        | word32 valueRelated => exact ⟨_, rfl, valueRelated⟩
        | word64 valueRelated => cases valueRelated
        | float32Bits valueRelated => cases valueRelated
        | float64Bits valueRelated => cases valueRelated
      subst discrPhysical
      obtain ⟨imp, imported, inBounds, contracted, params, results⟩ :=
        functionSpec.runtimeCallsAligned getTagFound
      have getTagContracted :
          hosts.spec.contracts[getTagIndex]? = some getTagContract := by
        change hosts.spec.contracts[getTagIndex]? =
          some (fun initial args result => result = getTagStep initial args)
        simpa only [resolvedContract?, hostFn?, Option.map_some, getTagFn]
          using contracted
      have parameterCount : imp.params.length = 1 := by
        change imp.params.length = 1 at params
        exact params
      have resultCount : imp.results.length = 1 := by
        change imp.results.length = 1 at results
        exact results
      have tagOperation :
          getTagStep targetStore [.i32 (UInt32.ofNat word.value)] =
            .Return [.i32 (UInt32.ofNat actualTag)] targetStore := by
        have operation :=
          getTagStep_of_refines related.stateRelated.1 objectRelated tagged
            actualFits
        rw [related.stateRelated.clearFailure] at operation
        exact operation
      let sourceSelected : MachineState := {
        source with control := .code selected }
      have selectSourceStep :
          executeStep externals source = .next sourceSelected := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.cases cases) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change actualEnv = sourceEnv at envEq
        subst actualEnv
        have runtimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at runtimeEq
        subst actualRuntime
        simp [sourceSelected, executeStep, coreStep, lookupFound, tagged,
          chosen]
      have targetStateEq :
          target =
            ⟨targetStore, .running targetLocals ([
                .localGet discrIndex, .call getTagIndex,
                .const (UInt32.ofNat firstInfo.cidx), .eq,
                .iff 0 0 firstTarget [
                  .localGet discrIndex, .call getTagIndex,
                  .const (UInt32.ofNat secondInfo.cidx), .eq,
                  .iff 0 0 secondTarget defaultTarget]] ++ targetSuffix),
              target.frames⟩ := by
        rcases target with ⟨actualStore, actualControl, actualFrames⟩
        have storeEq := related.targetStoreEq
        change actualStore = targetStore at storeEq
        subst actualStore
        have controlEq := related.targetControlEq
        change actualControl = .running targetLocals targetCode at controlEq
        subst actualControl
        subst targetCode
        rfl
      have finishCase
          (selectedTarget : Wasm.Program)
          (selectedAdapted :
            CodeAdapted context sourceModule sourceFunction [] selected
              selectedTarget)
          (testCount : Nat)
          (targetPrefix :
            FinitePath
              (StructuredWasmStep targetModule.wasmModule hosts.env)
              (5 * testCount) target
              ⟨targetStore,
                .running
                  { targetLocals with values := targetLocals.values }
                  selectedTarget,
                structuredWasmCaseLabels (targetLocals.values.drop 0)
                    targetSuffix testCount ++
                  target.frames⟩) :
          ∃ sourceAfter targetAfter resultStore resultLocals resultWitness kind
              physical sourceCount targetCount,
            FinitePath
                (fun before after =>
                  executeStep externals before = .next after)
                sourceCount source sourceAfter ∧
              FinitePath
                  (StructuredWasmStep targetModule.wasmModule hosts.env)
                  targetCount target targetAfter ∧
                ConcreteStructuredYieldFocus context sourceFunction
                    resultRuntime resultEnv resultValue resultStore resultLocals
                    resultWitness kind physical sourceAfter targetAfter ∧
                  ReuseCapacityEntryRelativeFrame
                      (ConcreteReuseCapacityCacheFrame sourceModule
                        sourceFunction externals)
                      entryRuntime entryStore entryWitness resultFacts slack
                      resultRuntime resultEnv resultStore resultLocals
                      resultWitness ∧
                    kind.refines expectedResult = true ∧
                      sourceAfter.joins = [] ∧
                        sourceAfter.frames = source.frames ∧
                          targetAfter.frames = target.frames := by
        let targetSelected : StructuredWasmState Host := {
          store := targetStore
          control := .running
            { targetLocals with values := targetLocals.values } selectedTarget
          frames :=
            structuredWasmCaseLabels (targetLocals.values.drop 0)
                targetSuffix testCount ++ target.frames }
        have selectedFocus :
            ConcreteStructuredCodeFocus context sourceModule sourceFunction []
              sourceRuntime sourceEnv selected targetStore
              { targetLocals with values := targetLocals.values }
              selectedTarget witness sourceSelected targetSelected := {
          sourceProgramEq := by simp [sourceSelected, related.sourceProgramEq]
          sourceControlEq := by simp [sourceSelected]
          sourceEnvEq := by simp [sourceSelected, related.sourceEnvEq]
          sourceRuntimeEq := by simp [sourceSelected, related.sourceRuntimeEq]
          targetStoreEq := by simp [targetSelected]
          targetControlEq := by simp [targetSelected]
          adapted := CodeAdapted.withEmptySuffix selectedAdapted
          stateRelated := related.stateRelated.withValues targetLocals.values
          frameAligned := related.frameAligned.withValues targetLocals.values }
        obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
            resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
            targetTail, yielded, resultInvariant, resultRefines, resultJoins,
            sourceFramesEq, selectedTargetFramesEq⟩ :=
          ih functionSpec contextCaches selectedFocus
            (by simpa [sourceSelected] using sourceJoins)
            (invariant.withValues targetLocals.values) entryAbi
        let targetFinal : StructuredWasmState Host := {
          store := resultStore
          control := .returning (physical :: resultLocals.values)
          frames := target.frames }
        have unwindTarget :
            FinitePath
              (StructuredWasmStep targetModule.wasmModule hosts.env)
              testCount targetAfter targetFinal := by
          rcases targetAfter with
            ⟨afterStore, afterControl, afterFrames⟩
          have storeEq := yielded.targetStoreEq
          change afterStore = resultStore at storeEq
          subst afterStore
          have controlEq := yielded.targetControlEq
          change afterControl =
            .returning (physical :: resultLocals.values) at controlEq
          subst afterControl
          change afterFrames = _ at selectedTargetFramesEq
          rw [show targetSelected.frames =
              structuredWasmCaseLabels (targetLocals.values.drop 0)
                  targetSuffix testCount ++
                target.frames by simp [targetSelected]]
            at selectedTargetFramesEq
          subst afterFrames
          simpa [targetFinal] using
            structuredWasmReturnCaseLabelsFinitePath
              (module := targetModule.wasmModule) (hostEnv := hosts.env)
              (store := resultStore)
              (values := physical :: resultLocals.values)
              (belowStack := targetLocals.values.drop 0)
              (rest := targetSuffix) (frames := target.frames) testCount
        have finalYielded :
            ConcreteStructuredYieldFocus context sourceFunction resultRuntime
              resultEnv resultValue resultStore resultLocals resultWitness kind
              physical sourceAfter targetFinal := {
          sourceProgramEq := yielded.sourceProgramEq
          sourceControlEq := yielded.sourceControlEq
          sourceEnvEq := yielded.sourceEnvEq
          sourceRuntimeEq := yielded.sourceRuntimeEq
          targetStoreEq := by simp [targetFinal]
          targetControlEq := by simp [targetFinal]
          stateRelated := yielded.stateRelated
          frameAligned := yielded.frameAligned
          valueRelated := yielded.valueRelated }
        exact ⟨sourceAfter, targetFinal, resultStore, resultLocals,
          resultWitness, kind, physical, 1 + sourceCount,
          5 * testCount + targetCount + testCount,
          (FinitePath.single
            (step := fun before after =>
              executeStep externals before = .next after)
            selectSourceStep).trans sourceTail,
          (targetPrefix.trans targetTail).trans unwindTarget,
          finalYielded, resultInvariant, resultRefines, resultJoins,
          by simpa [sourceSelected] using sourceFramesEq,
          by simp [targetFinal]⟩
      by_cases firstHit : actualTag = firstInfo.cidx
      · have selectedEq : selected = firstBranch := by
          have branchEq : firstBranch = selected := by
            rw [altsEq] at chosen
            simpa [chooseAlt, findCtorAlt, findDefaultAlt, firstHit]
              using chosen
          exact branchEq.symm
        subst selected
        apply finishCase firstTarget firstAdapted 1
        rw [targetStateEq]
        simpa [structuredWasmCaseLabels] using
          structuredWasmObjectCaseHitPrefixFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (spec := hosts.spec) (store := targetStore)
            (locals := targetLocals) (rest := targetSuffix)
            (frames := target.frames)
            (thenTarget := firstTarget)
            (elseTarget := [
              .localGet discrIndex, .call getTagIndex,
              .const (UInt32.ofNat secondInfo.cidx), .eq,
              .iff 0 0 secondTarget defaultTarget])
            (discrIndex := discrIndex) (getTagIndex := getTagIndex)
            (imp := imp) (word := word) (actualTag := actualTag)
            (expectedTag := firstInfo.cidx) firstHit targetLookup imported
            functionSpec.hostsSatisfy inBounds getTagContracted parameterCount
            resultCount tagOperation
      · by_cases secondHit : actualTag = secondInfo.cidx
        · have selectedEq : selected = secondBranch := by
            have firstMiss' : firstInfo.cidx ≠ actualTag :=
              fun equal => firstHit equal.symm
            have different : firstInfo.cidx ≠ secondInfo.cidx :=
              fun equal => firstHit (secondHit.trans equal.symm)
            have branchEq : secondBranch = selected := by
              rw [altsEq] at chosen
              simpa [chooseAlt, findCtorAlt, findDefaultAlt, firstHit,
                firstMiss', secondHit, different] using chosen
            exact branchEq.symm
          subst selected
          let afterFirstMiss : StructuredWasmState Host :=
            ⟨targetStore,
              .running
                { targetLocals with values := targetLocals.values } [
                  .localGet discrIndex, .call getTagIndex,
                  .const (UInt32.ofNat secondInfo.cidx), .eq,
                  .iff 0 0 secondTarget defaultTarget],
              .label 0 (targetLocals.values.drop 0) targetSuffix ::
                target.frames⟩
          let afterSecondHit : StructuredWasmState Host :=
            ⟨targetStore,
              .running
                { targetLocals with values := targetLocals.values }
                secondTarget,
              .label 0 (targetLocals.values.drop 0) [] ::
                .label 0 (targetLocals.values.drop 0) targetSuffix ::
                  target.frames⟩
          have firstPrefix :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 5
                target afterFirstMiss := by
            rw [targetStateEq]
            simpa [afterFirstMiss] using
              structuredWasmObjectCaseMissPrefixFinitePath
                (module := targetModule.wasmModule) (hostEnv := hosts.env)
                (spec := hosts.spec) (store := targetStore)
                (locals := targetLocals) (rest := targetSuffix)
                (frames := target.frames)
                (thenTarget := firstTarget)
                (elseTarget := [
                  .localGet discrIndex, .call getTagIndex,
                  .const (UInt32.ofNat secondInfo.cidx), .eq,
                  .iff 0 0 secondTarget defaultTarget])
                (discrIndex := discrIndex) (getTagIndex := getTagIndex)
                (imp := imp) (word := word) (actualTag := actualTag)
                (expectedTag := firstInfo.cidx) firstHit actualFits
                firstExpectedFits targetLookup imported
                functionSpec.hostsSatisfy inBounds getTagContracted
                parameterCount resultCount tagOperation
          have secondPrefix :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 5
                afterFirstMiss afterSecondHit := by
            simpa [afterFirstMiss, afterSecondHit] using
              structuredWasmObjectCaseHitPrefixFinitePath
                (module := targetModule.wasmModule) (hostEnv := hosts.env)
                (spec := hosts.spec) (store := targetStore)
                (locals := targetLocals)
                (frames :=
                  .label 0 (targetLocals.values.drop 0) targetSuffix ::
                    target.frames)
                (rest := [])
                (thenTarget := secondTarget) (elseTarget := defaultTarget)
                (discrIndex := discrIndex) (getTagIndex := getTagIndex)
                (imp := imp) (word := word) (actualTag := actualTag)
                (expectedTag := secondInfo.cidx) secondHit targetLookup imported
                functionSpec.hostsSatisfy inBounds getTagContracted
                parameterCount resultCount tagOperation
          apply finishCase secondTarget secondAdapted 2
          simpa [afterSecondHit, structuredWasmCaseLabels] using
            firstPrefix.trans secondPrefix
        · have selectedEq : selected = defaultBranch := by
            have firstMiss' : firstInfo.cidx ≠ actualTag :=
              fun equal => firstHit equal.symm
            have secondMiss' : secondInfo.cidx ≠ actualTag :=
              fun equal => secondHit equal.symm
            have branchEq : defaultBranch = selected := by
              rw [altsEq] at chosen
              simpa [chooseAlt, findCtorAlt, findDefaultAlt, firstHit,
                firstMiss', secondHit, secondMiss'] using chosen
            exact branchEq.symm
          subst selected
          let afterFirstMiss : StructuredWasmState Host :=
            ⟨targetStore,
              .running
                { targetLocals with values := targetLocals.values } [
                  .localGet discrIndex, .call getTagIndex,
                  .const (UInt32.ofNat secondInfo.cidx), .eq,
                  .iff 0 0 secondTarget defaultTarget],
              .label 0 (targetLocals.values.drop 0) targetSuffix ::
                target.frames⟩
          let afterSecondMiss : StructuredWasmState Host :=
            ⟨targetStore,
              .running
                { targetLocals with values := targetLocals.values }
                defaultTarget,
              .label 0 (targetLocals.values.drop 0) [] ::
                .label 0 (targetLocals.values.drop 0) targetSuffix ::
                  target.frames⟩
          have firstPrefix :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 5
                target afterFirstMiss := by
            rw [targetStateEq]
            simpa [afterFirstMiss] using
              structuredWasmObjectCaseMissPrefixFinitePath
                (module := targetModule.wasmModule) (hostEnv := hosts.env)
                (spec := hosts.spec) (store := targetStore)
                (locals := targetLocals) (rest := targetSuffix)
                (frames := target.frames)
                (thenTarget := firstTarget)
                (elseTarget := [
                  .localGet discrIndex, .call getTagIndex,
                  .const (UInt32.ofNat secondInfo.cidx), .eq,
                  .iff 0 0 secondTarget defaultTarget])
                (discrIndex := discrIndex) (getTagIndex := getTagIndex)
                (imp := imp) (word := word) (actualTag := actualTag)
                (expectedTag := firstInfo.cidx) firstHit actualFits
                firstExpectedFits targetLookup imported
                functionSpec.hostsSatisfy inBounds getTagContracted
                parameterCount resultCount tagOperation
          have secondPrefix :
              FinitePath
                (StructuredWasmStep targetModule.wasmModule hosts.env) 5
                afterFirstMiss afterSecondMiss := by
            simpa [afterFirstMiss, afterSecondMiss] using
              structuredWasmObjectCaseMissPrefixFinitePath
                (module := targetModule.wasmModule) (hostEnv := hosts.env)
                (spec := hosts.spec) (store := targetStore)
                (locals := targetLocals)
                (frames :=
                  .label 0 (targetLocals.values.drop 0) targetSuffix ::
                    target.frames)
                (rest := [])
                (thenTarget := secondTarget) (elseTarget := defaultTarget)
                (discrIndex := discrIndex) (getTagIndex := getTagIndex)
                (imp := imp) (word := word) (actualTag := actualTag)
                (expectedTag := secondInfo.cidx) secondHit actualFits
                secondExpectedFits targetLookup imported
                functionSpec.hostsSatisfy inBounds getTagContracted
                parameterCount resultCount tagOperation
          apply finishCase defaultTarget defaultAdapted 2
          simpa [afterSecondMiss, structuredWasmCaseLabels] using
            firstPrefix.trans secondPrefix
  | @objectCases context sourceRuntime sourceEnv cases selected expectedResult
      facts resultFacts resultRuntime resultEnv resultValue requiredBytes
      supported sourceStep continued ih =>
      rcases supported with
        ⟨altsSupported, modeEq, discrCompiled, actualTagFits⟩
      rcases sourceStep with
        ⟨sourceObject, actualTag, lookupFound, tagged, chosen⟩
      have sourceLookup :
          lookup sourceEnv cases.discr = some sourceObject := by
        cases lookupEq : lookup sourceEnv cases.discr with
        | none => simp [lookupValue, lookupEq] at lookupFound
        | some value =>
            have valueEq : value = sourceObject := by
              simpa [lookupValue, lookupEq] using lookupFound
            subst value
            rfl
      have actualFits : actualTag < UInt32.size :=
        actualTagFits lookupFound tagged
      rcases CodeAdaptedWithSuffix.cases_eq related.adapted with
        ⟨fallback, targetCore, targetSuffix, fallbackCompiled, chainAdapted,
          targetCodeEq⟩
      obtain ⟨selectedTarget, testCount, selectedAdapted,
          rawTargetPrefix⟩ :=
        functionSpec.objectConstructorCaseChainFinitePath altsSupported
          modeEq discrCompiled chosen sourceLookup tagged actualFits
          related.stateRelated fallbackCompiled chainAdapted
          (targetSuffix := targetSuffix) (frames := target.frames)
      let sourceSelected : MachineState := {
        source with control := .code selected }
      have selectSourceStep :
          executeStep externals source = .next sourceSelected := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.cases cases) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change actualEnv = sourceEnv at envEq
        subst actualEnv
        have runtimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at runtimeEq
        subst actualRuntime
        simp [sourceSelected, executeStep, coreStep, lookupFound, tagged,
          chosen]
      let targetSelected : StructuredWasmState Host := {
        store := targetStore
        control := .running
          { targetLocals with values := targetLocals.values } selectedTarget
        frames :=
          structuredWasmCaseLabels (targetLocals.values.drop 0) targetSuffix
              testCount ++ target.frames }
      have targetPrefix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            (5 * testCount) target targetSelected := by
        rcases target with ⟨actualStore, actualControl, actualFrames⟩
        have storeEq := related.targetStoreEq
        change actualStore = targetStore at storeEq
        subst actualStore
        have controlEq := related.targetControlEq
        change actualControl = .running targetLocals targetCode at controlEq
        subst actualControl
        subst targetCode
        simpa [targetSelected] using rawTargetPrefix
      have selectedFocus :
          ConcreteStructuredCodeFocus context sourceModule sourceFunction []
            sourceRuntime sourceEnv selected targetStore
            { targetLocals with values := targetLocals.values } selectedTarget
            witness sourceSelected targetSelected := {
        sourceProgramEq := by simp [sourceSelected, related.sourceProgramEq]
        sourceControlEq := by simp [sourceSelected]
        sourceEnvEq := by simp [sourceSelected, related.sourceEnvEq]
        sourceRuntimeEq := by simp [sourceSelected, related.sourceRuntimeEq]
        targetStoreEq := by simp [targetSelected]
        targetControlEq := by simp [targetSelected]
        adapted := selectedAdapted
        stateRelated := related.stateRelated.withValues targetLocals.values
        frameAligned := related.frameAligned.withValues targetLocals.values }
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, selectedTargetFramesEq⟩ :=
        ih functionSpec contextCaches selectedFocus
          (by simpa [sourceSelected] using sourceJoins)
          (invariant.withValues targetLocals.values) entryAbi
      let targetFinal : StructuredWasmState Host := {
        store := resultStore
        control := .returning (physical :: resultLocals.values)
        frames := target.frames }
      have unwindTarget :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            testCount targetAfter targetFinal := by
        rcases targetAfter with ⟨afterStore, afterControl, afterFrames⟩
        have storeEq := yielded.targetStoreEq
        change afterStore = resultStore at storeEq
        subst afterStore
        have controlEq := yielded.targetControlEq
        change afterControl =
          .returning (physical :: resultLocals.values) at controlEq
        subst afterControl
        change afterFrames = _ at selectedTargetFramesEq
        rw [show targetSelected.frames =
            structuredWasmCaseLabels (targetLocals.values.drop 0) targetSuffix
                testCount ++ target.frames by
              simp [targetSelected]]
          at selectedTargetFramesEq
        subst afterFrames
        simpa [targetFinal] using
          structuredWasmReturnCaseLabelsFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (store := resultStore)
            (values := physical :: resultLocals.values)
            (belowStack := targetLocals.values.drop 0)
            (rest := targetSuffix) (frames := target.frames) testCount
      have finalYielded :
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
            resultEnv resultValue resultStore resultLocals resultWitness kind
            physical sourceAfter targetFinal := {
        sourceProgramEq := yielded.sourceProgramEq
        sourceControlEq := yielded.sourceControlEq
        sourceEnvEq := yielded.sourceEnvEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        targetStoreEq := by simp [targetFinal]
        targetControlEq := by simp [targetFinal]
        stateRelated := yielded.stateRelated
        frameAligned := yielded.frameAligned
        valueRelated := yielded.valueRelated }
      exact ⟨sourceAfter, targetFinal, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount,
        5 * testCount + targetCount + testCount,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          selectSourceStep).trans sourceTail,
        (targetPrefix.trans targetTail).trans unwindTarget,
        finalYielded, resultInvariant, resultRefines, resultJoins,
        by simpa [sourceSelected] using sourceFramesEq,
        by simp [targetFinal]⟩
  | @scalarUInt8Cases context sourceRuntime sourceEnv cases selected
      expectedResult facts resultFacts resultRuntime resultEnv resultValue
      requiredBytes supported sourceStep continued ih =>
      rcases supported with ⟨altsSupported, modeEq, discrCompiled⟩
      rcases sourceStep with
        ⟨sourceValue, actualTag, lookupFound, tagged, chosen⟩
      have sourceLookup :
          lookup sourceEnv cases.discr = some sourceValue := by
        cases lookupEq : lookup sourceEnv cases.discr with
        | none => simp [lookupValue, lookupEq] at lookupFound
        | some value =>
            have valueEq : value = sourceValue := by
              simpa [lookupValue, lookupEq] using lookupFound
            subst value
            rfl
      rcases CodeAdaptedWithSuffix.cases_eq related.adapted with
        ⟨fallback, targetCore, targetSuffix, fallbackCompiled, chainAdapted,
          targetCodeEq⟩
      obtain ⟨selectedTarget, testCount, selectedAdapted,
          rawTargetPrefix⟩ :=
        functionSpec.scalarUInt8CaseChainFinitePath altsSupported modeEq
          discrCompiled chosen sourceLookup tagged related.stateRelated
          fallbackCompiled chainAdapted (targetSuffix := targetSuffix)
          (frames := target.frames)
      let sourceSelected : MachineState := {
        source with control := .code selected }
      have selectSourceStep :
          executeStep externals source = .next sourceSelected := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.cases cases) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change actualEnv = sourceEnv at envEq
        subst actualEnv
        have runtimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at runtimeEq
        subst actualRuntime
        simp [sourceSelected, executeStep, coreStep, lookupFound, tagged,
          chosen]
      let targetSelected : StructuredWasmState Host := {
        store := targetStore
        control := .running
          { targetLocals with values := targetLocals.values } selectedTarget
        frames :=
          structuredWasmCaseLabels (targetLocals.values.drop 0) targetSuffix
              testCount ++ target.frames }
      have targetPrefix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            (4 * testCount) target targetSelected := by
        rcases target with ⟨actualStore, actualControl, actualFrames⟩
        have storeEq := related.targetStoreEq
        change actualStore = targetStore at storeEq
        subst actualStore
        have controlEq := related.targetControlEq
        change actualControl = .running targetLocals targetCode at controlEq
        subst actualControl
        subst targetCode
        simpa [targetSelected] using rawTargetPrefix
      have selectedFocus :
          ConcreteStructuredCodeFocus context sourceModule sourceFunction []
            sourceRuntime sourceEnv selected targetStore
            { targetLocals with values := targetLocals.values } selectedTarget
            witness sourceSelected targetSelected := {
        sourceProgramEq := by simp [sourceSelected, related.sourceProgramEq]
        sourceControlEq := by simp [sourceSelected]
        sourceEnvEq := by simp [sourceSelected, related.sourceEnvEq]
        sourceRuntimeEq := by simp [sourceSelected, related.sourceRuntimeEq]
        targetStoreEq := by simp [targetSelected]
        targetControlEq := by simp [targetSelected]
        adapted := selectedAdapted
        stateRelated := related.stateRelated.withValues targetLocals.values
        frameAligned := related.frameAligned.withValues targetLocals.values }
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, selectedTargetFramesEq⟩ :=
        ih functionSpec contextCaches selectedFocus
          (by simpa [sourceSelected] using sourceJoins)
          (invariant.withValues targetLocals.values) entryAbi
      let targetFinal : StructuredWasmState Host := {
        store := resultStore
        control := .returning (physical :: resultLocals.values)
        frames := target.frames }
      have unwindTarget :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            testCount targetAfter targetFinal := by
        rcases targetAfter with ⟨afterStore, afterControl, afterFrames⟩
        have storeEq := yielded.targetStoreEq
        change afterStore = resultStore at storeEq
        subst afterStore
        have controlEq := yielded.targetControlEq
        change afterControl =
          .returning (physical :: resultLocals.values) at controlEq
        subst afterControl
        change afterFrames = _ at selectedTargetFramesEq
        rw [show targetSelected.frames =
            structuredWasmCaseLabels (targetLocals.values.drop 0) targetSuffix
                testCount ++ target.frames by
              simp [targetSelected]]
          at selectedTargetFramesEq
        subst afterFrames
        simpa [targetFinal] using
          structuredWasmReturnCaseLabelsFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (store := resultStore)
            (values := physical :: resultLocals.values)
            (belowStack := targetLocals.values.drop 0)
            (rest := targetSuffix) (frames := target.frames) testCount
      have finalYielded :
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
            resultEnv resultValue resultStore resultLocals resultWitness kind
            physical sourceAfter targetFinal := {
        sourceProgramEq := yielded.sourceProgramEq
        sourceControlEq := yielded.sourceControlEq
        sourceEnvEq := yielded.sourceEnvEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        targetStoreEq := by simp [targetFinal]
        targetControlEq := by simp [targetFinal]
        stateRelated := yielded.stateRelated
        frameAligned := yielded.frameAligned
        valueRelated := yielded.valueRelated }
      exact ⟨sourceAfter, targetFinal, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount,
        4 * testCount + targetCount + testCount,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          selectSourceStep).trans sourceTail,
        (targetPrefix.trans targetTail).trans unwindTarget,
        finalYielded, resultInvariant, resultRefines, resultJoins,
        by simpa [sourceSelected] using sourceFramesEq,
        by simp [targetFinal]⟩
  | @persistentOwnershipEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported _sourceStep continued ih =>
      cases supported with
      | inc =>
          let sourceMiddle : MachineState := {
            source with control := .code continuation }
          have firstSourceStep :
              executeStep externals source = .next sourceMiddle := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            rfl
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                [] sourceRuntime sourceEnv continuation targetStore targetLocals
                targetCode witness sourceMiddle target := {
            sourceProgramEq := by
              simp [sourceMiddle, related.sourceProgramEq]
            sourceControlEq := by simp [sourceMiddle]
            sourceEnvEq := by simp [sourceMiddle, related.sourceEnvEq]
            sourceRuntimeEq := by
              simp [sourceMiddle, related.sourceRuntimeEq]
            targetStoreEq := related.targetStoreEq
            targetControlEq := related.targetControlEq
            adapted := CodeAdaptedWithSuffix.incPersistent_eq related.adapted
            stateRelated := related.stateRelated
            frameAligned := related.frameAligned }
          obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
              resultWitness, kind, physical, sourceCount, targetCount,
              sourceTail, targetPath, yielded, resultInvariant,
              resultRefines, resultJoins, sourceFramesEq, targetFramesEq⟩ :=
            ih functionSpec contextCaches nextFocus
              (by simpa [sourceMiddle] using sourceJoins)
              invariant entryAbi
          exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
            resultWitness, kind, physical, 1 + sourceCount, targetCount,
            (FinitePath.single
              (step := fun before after =>
                executeStep externals before = .next after)
              firstSourceStep).trans sourceTail,
            targetPath, yielded, resultInvariant, resultRefines, resultJoins,
            by simpa [sourceMiddle] using sourceFramesEq, targetFramesEq⟩
      | dec =>
          let sourceMiddle : MachineState := {
            source with control := .code continuation }
          have firstSourceStep :
              executeStep externals source = .next sourceMiddle := by
            rcases source with
              ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
                sourceFrames, actualRuntime⟩
            have controlEq := related.sourceControlEq
            change sourceControl = _ at controlEq
            subst sourceControl
            rfl
          have nextFocus :
              ConcreteStructuredCodeFocus context sourceModule sourceFunction
                [] sourceRuntime sourceEnv continuation targetStore targetLocals
                targetCode witness sourceMiddle target := {
            sourceProgramEq := by
              simp [sourceMiddle, related.sourceProgramEq]
            sourceControlEq := by simp [sourceMiddle]
            sourceEnvEq := by simp [sourceMiddle, related.sourceEnvEq]
            sourceRuntimeEq := by
              simp [sourceMiddle, related.sourceRuntimeEq]
            targetStoreEq := related.targetStoreEq
            targetControlEq := related.targetControlEq
            adapted := CodeAdaptedWithSuffix.decPersistent_eq related.adapted
            stateRelated := related.stateRelated
            frameAligned := related.frameAligned }
          obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
              resultWitness, kind, physical, sourceCount, targetCount,
              sourceTail, targetPath, yielded, resultInvariant,
              resultRefines, resultJoins, sourceFramesEq, targetFramesEq⟩ :=
            ih functionSpec contextCaches nextFocus
              (by simpa [sourceMiddle] using sourceJoins)
              invariant entryAbi
          exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
            resultWitness, kind, physical, 1 + sourceCount, targetCount,
            (FinitePath.single
              (step := fun before after =>
                executeStep externals before = .next after)
              firstSourceStep).trans sourceTail,
            targetPath, yielded, resultInvariant, resultRefines, resultJoins,
            by simpa [sourceMiddle] using sourceFramesEq, targetFramesEq⟩
  | @ordinaryIncrementEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_ordinaryIncrement functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 2 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @ordinaryDecrementEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_ordinaryDecrement functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 2 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @ordinaryDeleteEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_ordinaryDelete functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 2 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @constructorTagEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_constructorTag functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 2 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @objectFieldFVarEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_objectFieldFVar functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 3 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @objectFieldErasedEffect sourceRuntime sourceEnv code continuation
      nextRuntime context expectedResult facts resultFacts resultRuntime
      resultEnv resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_objectFieldErased functionSpec supported sourceStep
          invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 3 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @usizeFieldEffect sourceRuntime sourceEnv code continuation nextRuntime
      context expectedResult facts resultFacts resultRuntime resultEnv
      resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_usizeField functionSpec supported sourceStep invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 3 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @scalarFieldEffect sourceRuntime sourceEnv code continuation nextRuntime
      context expectedResult facts resultFacts resultRuntime resultEnv
      resultValue requiredBytes supported sourceStep continued ih =>
      obtain ⟨sourceMiddle, targetMiddle, nextStore, targetRest, sourcePrefix,
          targetPrefix, nextFocus, nextInvariant, sourceMiddleJoins,
          sourceMiddleFrames, targetMiddleFrames⟩ :=
        related.advance_scalarField functionSpec supported sourceStep invariant
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          sourceFramesEq, targetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (sourceMiddleJoins.trans sourceJoins) nextInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount, 3 + targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        sourceFramesEq.trans sourceMiddleFrames,
        targetFramesEq.trans targetMiddleFrames⟩
  | @letValue context facts decl sourceRuntime sourceEnv nextRuntime sourceValue
      nextFacts expectedResult continuation resultFacts resultRuntime resultEnv
      resultValue continuationCost supported sourceStep transfer continued ih =>
      obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
          valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.let_eq related.adapted
      have stepFits :
          directLetAllocationCost decl ≤
            (directLetAllocationCost decl + continuationCost) + slack := by
        omega
      obtain ⟨nextStore, nextLocals, nextWitness, producedFacts, step,
          _externalsPreserved, _hostDescriptorsPreserved,
          _witnessDescriptorsPreserved, _transports, producedTransfer,
          nextInvariant⟩ :=
        (functionSpec.reuseCapacityDirectLetRuntimeRefinesWithCost_reuseBudgetedDirect_pureExternalOwnership_entryRelativeCache
          externals) supported stepFits invariant sourceStep valueCompiled
            valueAdapted resultFound
      rw [transfer] at producedTransfer
      have factsEq := Option.some.inj producedTransfer
      subst producedFacts
      have continuationInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness nextFacts
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            nextWitness := by
        have budgetEq :
            (directLetAllocationCost decl + continuationCost) + slack -
                directLetAllocationCost decl =
              continuationCost + slack := by
          omega
        simpa only [budgetEq] using nextInvariant
      have flat :
          StructuredWasmFlatProgram targetModule.wasmModule
            (targetValue ++ [.localSet resultIndex]) :=
        functionSpec.reuseCapacityDirectTargetFlat_reuseBudgetedDirect
          supported valueCompiled valueAdapted
      obtain ⟨sourceMiddle, targetMiddle, firstSourceStep, targetPrefix,
          nextFocus, firstSourceJoinsEq, firstSourceFramesEq,
          firstTargetFramesEq⟩ :=
        related.advance_flatLet targetCodeEq continuationAdapted flat step
          continuationInvariant.1.1.1.1.2.2.1
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines,
          resultJoins, tailSourceFramesEq, tailTargetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus
          (firstSourceJoinsEq.trans sourceJoins)
          (continuationInvariant.withValues targetLocals.values) entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 1 + sourceCount,
        (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length +
          targetCount,
        (FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          firstSourceStep).trans sourceTail,
        targetPrefix.trans targetTail, yielded, resultInvariant, resultRefines,
        resultJoins, tailSourceFramesEq.trans firstSourceFramesEq,
        tailTargetFramesEq.trans firstTargetFramesEq⟩
  | @directCallLet context decl sourceEnv sourceRuntime calleeResultFacts
      nextRuntime calleeResultEnv sourceValue stepCost facts nextFacts
      expectedResult continuation resultFacts resultRuntime resultEnv resultValue
      continuationCost calleeFunction site loweredRow callee transfer continued
      calleeIH continuedIH =>
      have programEq : context.program = program := functionSpec.contextProgram
      subst program
      have resultClassified :
          Fir.Wasm.abiKind? site.sourceDeclaration.type =
            .ok (some site.calleeResultKind) :=
        abiKind?_of_directAbiKind?_eq_some site.calleeResult
      obtain ⟨generatedRow⟩ :=
        ConcreteGeneratedInternalDeclaration.exists_ofSupportedPipelineAtLowered
          rfl contextCaches rootSpec.programNamesUnique rootSpec.lowered
          rootSpec.adapted site.declarationFound loweredRow resultClassified
      obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest,
          sourceStaged, targetReady, stageSourceStep, targetArgumentsPath,
          ready⟩ :=
        related.advance_directCall_stage site generatedRow
          functionSpec.localsAligned
      obtain ⟨sourceEntry, targetEntry, storedCallerLocals, storedCallerEq,
          enterSourceStep, enterTargetPath, entry⟩ :=
        ready.advance_enter
      have storedCallerFrame :
          ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
            facts ((stepCost + continuationCost) + slack) sourceRuntime
            sourceEnv targetStore storedCallerLocals witness := by
        rw [storedCallerEq]
        exact invariant.1.withValues _
      have calleeEntryInvariant :=
        entry.calleeEntryRelativeCacheFrame storedCallerFrame
      have calleeInvariantWithSlack :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction
              externals)
            sourceRuntime targetStore witness []
            (stepCost + (continuationCost + slack)) sourceRuntime
            site.calleeEnv targetStore
            (generatedRow.targetFunction.toLocals physicalArgs) witness := by
        simpa only [Nat.add_assoc] using calleeEntryInvariant
      have calleeEntryAbi :
          ClosureAllocationsAbiAligned loweredRow.context.program witness := by
        rw [generatedRow.contextProgram]
        exact entryAbi.ofPersistent
          invariant.2.closureAllocationsPersistent
      obtain ⟨sourceYield, targetYield, afterCall, calleeLocals, resultWitness,
          actualKind, physical, calleeSourceCount, calleeTargetCount,
          calleeSourcePath, calleeTargetPath, calleeYielded, calleeInvariant,
          calleeResultRefines, _calleeResultJoins, calleeSourceFramesEq,
          calleeTargetFramesEq⟩ :=
        calleeIH (generatedRow.toSupportedFunction rootSpec)
          generatedRow.contextCaches entry.calleeFocus entry.sourceJoinsEq
          calleeInvariantWithSlack calleeEntryAbi
      have sourceCallFramesEq :
          sourceYield.frames =
            .bind decl.fvarId continuation sourceEnv source.joins ::
              source.frames :=
        calleeSourceFramesEq.trans entry.sourceFramesEq
      have targetCallFramesEq :
          targetYield.frames =
            .call 1 targetLocals.values storedCallerLocals
                (.localSet resultIndex :: targetRest) :: target.frames :=
        calleeTargetFramesEq.trans entry.targetFramesEq
      have bindFocus :=
        entry.bindFrame_of_yield_cacheFrame calleeYielded calleeInvariant
          sourceCallFramesEq targetCallFramesEq
          (AbiKind.refines_trans calleeResultRefines
            site.calleeResultRefines)
      obtain ⟨sourceResumed, targetResumed, updated, resumedLocals,
          bindSourceStep, bindTargetPath, targetSet, resumedEq, resumedFocus,
          resumedSourceJoinsEq, resumedSourceFramesEq,
          resumedTargetFramesEq⟩ :=
        bindFocus.advance
      have storedInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness facts
            ((stepCost + continuationCost) + slack) sourceRuntime sourceEnv
            targetStore storedCallerLocals witness := by
        rw [storedCallerEq]
        exact invariant.withValues _
      have resumedUpdate :
          FirTalos.Correctness.LocalUpdate storedCallerLocals resumedLocals
            resultIndex physical := by
        rw [resumedEq]
        have base := FirTalos.Correctness.localUpdate_of_set? targetSet
        refine ⟨?_, ?_⟩
        · simpa [Wasm.Locals.get] using base.1
        · intro other different
          simpa [Wasm.Locals.get] using base.2 different
      have expectedTransfer :
          reuseCapacityLetFacts? facts decl =
            some (eraseReuseCapacityFact facts decl.fvarId) := by
        simp [reuseCapacityLetFacts?, site.valueEq]
      rw [expectedTransfer] at transfer
      have nextFactsEq : nextFacts = eraseReuseCapacityFact facts decl.fvarId :=
        Option.some.inj transfer.symm
      have callerAfterCall :=
        storedInvariant.restoreDirectCaller calleeInvariant
          resumedFocus.stateRelated resumedFocus.frameAligned entry.resultFound
          resumedUpdate
      have continuationInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness nextFacts
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) afterCall resumedLocals
            resultWitness := by
        simpa only [nextFactsEq] using callerAfterCall
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          finalWitness, kind, resultPhysical, continuationSourceCount,
          continuationTargetCount, continuationSourcePath,
          continuationTargetPath, yielded, resultInvariant, resultRefines,
          resultJoins, continuationSourceFramesEq,
          continuationTargetFramesEq⟩ :=
        continuedIH functionSpec contextCaches resumedFocus
          (resumedSourceJoinsEq.trans sourceJoins)
          continuationInvariant entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals, finalWitness,
        kind, resultPhysical,
        1 + 1 + calleeSourceCount + 1 + continuationSourceCount,
        targetArguments.length + 1 + calleeTargetCount + 2 +
          continuationTargetCount,
        (((FinitePath.single
          (step := fun before after =>
            executeStep externals before = .next after)
          stageSourceStep).trans (.single enterSourceStep)).trans
            calleeSourcePath).trans (.single bindSourceStep) |>.trans
              continuationSourcePath,
        (((targetArgumentsPath.trans enterTargetPath).trans calleeTargetPath).trans
          bindTargetPath).trans continuationTargetPath,
        yielded, resultInvariant, resultRefines, resultJoins,
        continuationSourceFramesEq.trans resumedSourceFramesEq,
        continuationTargetFramesEq.trans resumedTargetFramesEq⟩
  | @saturatedClosureCallLet context decl sourceEnv sourceRuntime nextRuntime
      sourceValue stepCost facts nextFacts expectedResult continuation
      resultFacts resultRuntime resultEnv resultValue continuationCost
      callRuntime calleeFunction calleeResultFacts calleeResultEnv site
      resolution loweredRow sharedCapacity application callee transfer continued
      calleeIH continuedIH =>
      have programEq : context.program = program := functionSpec.contextProgram
      subst program
      obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
          valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.let_eq related.adapted
      have expectedCompiled :
          Fir.Wasm.compileLetValue context decl =
            .ok (compileClosureDispatch context decl.fvarId site.closureId
              site.resultKind site.argumentCode site.argumentKinds) := by
        simp [Fir.Wasm.compileLetValue, Fir.Wasm.letValueKind, site.valueEq,
          site.kindEq, site.closureCompiled, site.argumentsCompiled,
          site.nonempty, Bind.bind, Except.bind, pure, Except.pure]
      have valueCodeEq :
          valueCode =
            compileClosureDispatch context decl.fvarId site.closureId
              site.resultKind site.argumentCode site.argumentKinds := by
        rw [expectedCompiled] at valueCompiled
        exact (Except.ok.inj valueCompiled).symm
      subst valueCode
      obtain ⟨closureIndex, closureFound, closureKindAt⟩ :=
        functionSpec.localsAligned site.closureCompiled
      obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
        functionSpec.localsAligned site.resultCompiled
      rw [resultFound] at alignedResultFound
      injection alignedResultFound with resultIndexEq
      subst alignedResultIndex
      have currentAbi :
          ClosureAllocationsAbiAligned context.program witness :=
        entryAbi.ofPersistent invariant.2.closureAllocationsPersistent
      have abiInvariant :
          ConcreteReuseCapacityCacheAbiFrame context sourceModule
            sourceFunction externals facts
            ((stepCost + continuationCost) + slack) sourceRuntime sourceEnv
            targetStore targetLocals witness :=
        ⟨invariant.1, currentAbi⟩
      obtain ⟨address, closureLocal, mapped⟩ :=
        abiInvariant.cacheFrame.resolveClosureAddress site resolution
          closureFound closureKindAt
      obtain ⟨staticCandidates, staticCandidatesEq⟩ :=
        functionSpec.saturatedClosureCandidateAdapterResolver site
          valueAdapted
      obtain ⟨candidates, executedSources⟩ :=
        ClosureCandidateAdapterCase.executeAll_of_refines staticCandidates
          abiInvariant.cacheFrame.stateRelated.stateRelated.1
          abiInvariant.cacheFrame.2.2.dispatch
          abiInvariant.cacheFrame.2.2.descriptors.symm mapped
          resolution.cellFound resolution.cellLive resolution.cellObjectEq
          sharedCapacity application
      have candidatesEq := staticCandidatesEq.trans executedSources
      obtain ⟨before, selected, suffix, candidatesSplit, _selectedSource,
          beforeNonmatching, selectedMatches, selectedIdentity⟩ :=
        abiInvariant.cacheFrame.closureCandidates_exists_first_match_of_resolution
          site resolution closureLocal mapped candidates candidatesEq
      have selectedMem : selected ∈ candidates := by
        rw [candidatesSplit]
        simp
      obtain ⟨_matcherIdentity, _applicationSnapshot, captureKinds,
          applicationFound, applicationRelated, matcherFrame, matcherCapacity,
          postMatcher⟩ :=
        abiInvariant.cacheFrame.selectedClosureMatcher selected mapped
          resolution.cellFound resolution.cellLive resolution.cellObjectEq
          selectedMatches sharedCapacity application
      obtain ⟨functionIndex, argumentTarget, callFound, argumentAdapted,
          selectedBodyEq⟩ :=
        resolution.candidateBody_of_identity site functionSpec candidatesEq
          selectedMem selectedIdentity resultFound
      obtain ⟨generatedRow⟩ :=
        ConcreteGeneratedInternalDeclaration.exists_ofSupportedPipelineAtLoweredSingleResult
          rfl contextCaches rootSpec.programNamesUnique rootSpec.lowered
          rootSpec.adapted resolution.targetFound loweredRow
          (loweredRow.singleResult_of_effectiveResult resolution.targetResult)
      have exactCallIndex :
          callIndex? sourceModule (.declaration resolution.function) =
            some generatedRow.targetFunctionIndex := by
        simpa [resolution.targetName] using generatedRow.callIndexEq
      rw [exactCallIndex] at callFound
      have functionIndexEq :
          generatedRow.targetFunctionIndex = functionIndex :=
        Option.some.inj callFound
      subst functionIndex
      have dispatchAdapted :
          instructions sourceModule sourceFunction []
              (compileClosureDispatch context decl.fvarId site.closureId
                site.resultKind site.argumentCode site.argumentKinds) =
            .ok
              (resolvedClosureCandidateChain (before ++ selected :: suffix) ++
                [.localGet resultIndex]) := by
        have generatedEq := candidatesEq
        rw [candidatesSplit] at generatedEq
        exact instructions_compileClosureDispatch
          (before ++ selected :: suffix) generatedEq closureFound resultFound
      have targetValueEq :
          targetValue =
            resolvedClosureCandidateChain (before ++ selected :: suffix) ++
              [.localGet resultIndex] := by
        rw [dispatchAdapted] at valueAdapted
        exact (Except.ok.inj valueAdapted).symm
      obtain ⟨physicalArgs, storedCallerLocals, argumentsRelated,
          storedCallerEq, targetCanonicalPath⟩ :=
        resolution.targetDispatchArgumentsAndEnterFinitePath functionSpec
          generatedRow currentAbi closureFound closureLocal argumentAdapted
          applicationFound applicationRelated postMatcher.stateRelated.1
          related.stateRelated.clearFailure
          postMatcher.stateRelated.1.clearFailure beforeNonmatching
          selectedMatches selectedBodyEq
          (tail := targetLocals.values)
          (rest :=
            [.localGet resultIndex, .localSet resultIndex] ++ targetRest)
          (frames := target.frames)
      let sourceEntry : MachineState := {
        program := context.program
        control := .code resolution.calleeCode
        env := resolution.calleeEnv
        joins := []
        frames :=
          .bind decl.fvarId continuation sourceEnv source.joins :: source.frames
        runtime := callRuntime }
      let targetEntry : StructuredWasmState Host := {
        store := selected.nextStore
        control := .running (generatedRow.targetFunction.toLocals physicalArgs)
          generatedRow.targetFunction.body
        frames :=
          .call 1 targetLocals.values storedCallerLocals
              [.localSet resultIndex] ::
            (List.replicate before.length
                (.label 0 targetLocals.values []) ++
              .label 0 targetLocals.values
                  ([.localGet resultIndex, .localSet resultIndex] ++
                    targetRest) ::
                target.frames) }
      have sourcePrefix :
          FinitePath
            (fun before after => executeStep externals before = .next after)
            2 source sourceEntry := by
        rcases source with
          ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv,
            sourceFrames, actualRuntime⟩
        have sourceProgramEq := related.sourceProgramEq
        change sourceProgram = context.program at sourceProgramEq
        subst sourceProgram
        have sourceControlEq := related.sourceControlEq
        change sourceControl = .code (.let decl continuation) at sourceControlEq
        subst sourceControl
        have sourceEnvEq := related.sourceEnvEq
        change actualEnv = sourceEnv at sourceEnvEq
        subst actualEnv
        have sourceRuntimeEq := related.sourceRuntimeEq
        change actualRuntime = sourceRuntime at sourceRuntimeEq
        subst actualRuntime
        simpa [sourceEntry] using
          (resolution.sourceStageAndEnterFinitePath
            (externals := externals) (continuation := continuation)
            (callerJoins := sourceJoinEnv) (sourceFrames := sourceFrames)
            application)
      have targetPrefix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            (3 * (before.length + 1) + argumentTarget.length + 1)
            target targetEntry := by
        rcases target with ⟨actualStore, actualControl, targetFrames⟩
        have targetStoreEq := related.targetStoreEq
        change actualStore = targetStore at targetStoreEq
        subst actualStore
        have targetControlEq := related.targetControlEq
        rw [targetCodeEq, targetValueEq] at targetControlEq
        have targetControlNormalized :
            actualControl = .running targetLocals
              (resolvedClosureCandidateChain
                  (before ++ selected :: suffix) ++
                .localGet resultIndex :: .localSet resultIndex ::
                  targetRest) := by
          simpa only [List.append_assoc, List.singleton_append] using
            targetControlEq
        clear targetControlEq
        subst actualControl
        simpa [targetEntry, List.append_assoc] using targetCanonicalPath
      have calleeFrame :
          ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals
            [] ((stepCost + continuationCost) + slack) callRuntime
            resolution.calleeEnv selected.nextStore
            (generatedRow.targetFunction.toLocals physicalArgs) witness :=
        postMatcher.generatedSaturatedClosureCalleeEntryAtCost site resolution
          generatedRow argumentsRelated
          (Nat.le_refl ((stepCost + continuationCost) + slack))
      have entry :
          ConcreteStructuredSaturatedCallEntryFocus context
            loweredRow.context sourceModule sourceFunction calleeFunction
            targetModule site resolution generatedRow [] callRuntime
            continuation source.joins source.frames selected.nextStore
            targetLocals targetLocals.values targetRest target.frames witness
            physicalArgs resultIndex before.length sourceEntry targetEntry := {
        calleeFocus := {
          sourceProgramEq := by
            exact generatedRow.contextProgram.symm
          sourceControlEq := by simp [sourceEntry]
          sourceEnvEq := by simp [sourceEntry]
          sourceRuntimeEq := by simp [sourceEntry]
          targetStoreEq := by simp [targetEntry]
          targetControlEq := by simp [targetEntry]
          adapted := by
            rw [generatedRow.targetBodyEq]
            exact CodeAdapted.withSuffix generatedRow.bodyAdapted
          stateRelated := calleeFrame.1.1.1.1.1
          frameAligned := calleeFrame.1.1.1.2.2.1 }
        sourceJoinsEq := by simp [sourceEntry]
        sourceFramesEq := by simp [sourceEntry]
        targetFramesEq := by simp [targetEntry, storedCallerEq]
        continuationAdapted
        callerStateRelated := postMatcher.1.1.1.1.1
        callerFrameAligned := postMatcher.1.1.1.2.2.1
        resultFound
        resultKindAt
        argumentsRelated }
      have calleeEntryInvariant :=
        entry.calleeEntryRelativeCacheFrame postMatcher
      have calleeInvariantWithSlack :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction
              externals)
            callRuntime selected.nextStore witness []
            (stepCost + (continuationCost + slack)) callRuntime
            resolution.calleeEnv selected.nextStore
            (generatedRow.targetFunction.toLocals physicalArgs) witness := by
        simpa only [Nat.add_assoc] using calleeEntryInvariant
      have calleeEntryAbi :
          ClosureAllocationsAbiAligned loweredRow.context.program witness := by
        rw [generatedRow.contextProgram]
        exact currentAbi
      obtain ⟨sourceYield, targetYield, afterCall, calleeLocals,
          resultWitness, actualKind, physical, calleeSourceCount,
          calleeTargetCount, calleeSourcePath, calleeTargetPath, calleeYielded,
          calleeInvariant, calleeResultRefines, _calleeResultJoins,
          calleeSourceFramesEq, calleeTargetFramesEq⟩ :=
        calleeIH (generatedRow.toSupportedFunction rootSpec)
          generatedRow.contextCaches entry.calleeFocus entry.sourceJoinsEq
          calleeInvariantWithSlack calleeEntryAbi
      have sourceCallFramesEq :
          sourceYield.frames =
            .bind decl.fvarId continuation sourceEnv source.joins ::
              source.frames :=
        calleeSourceFramesEq.trans entry.sourceFramesEq
      have targetCallFramesEq :
          targetYield.frames =
            .call 1 targetLocals.values
                { targetLocals with
                  values := physicalArgs.reverse ++ targetLocals.values }
                [.localSet resultIndex] ::
              (List.replicate before.length
                    (.label 0 targetLocals.values []) ++
                .label 0 targetLocals.values
                    ([.localGet resultIndex, .localSet resultIndex] ++
                      targetRest) ::
                  target.frames) :=
        calleeTargetFramesEq.trans entry.targetFramesEq
      have bindFocus :=
        entry.bindFrame_of_yield_cacheFrame calleeYielded calleeInvariant
          sourceCallFramesEq targetCallFramesEq
          (AbiKind.refines_trans calleeResultRefines
            resolution.targetResultRefines)
      obtain ⟨sourceResumed, targetResumed, updated, resumedLocals,
          bindSourceStep, bindTargetPath, targetSet, resumedEq, resumedFocus,
          resumedSourceJoinsEq, resumedSourceFramesEq,
          resumedTargetFramesEq⟩ :=
        bindFocus.advance
      have matcherTables :
          ClosureTablesTransport targetStore selected.nextStore witness
            witness := {
        hostDispatchPreserved := matcherFrame.dispatch
        witnessDispatchPreserved := rfl
        hostDescriptorsPreserved := matcherFrame.descriptors
        witnessDescriptorsPreserved := rfl }
      have matcherEntry :
          ReuseCapacityCodeEntryTransports entryRuntime callRuntime entryStore
            selected.nextStore entryWitness witness :=
        invariant.2.step (WitnessTransport.refl witness)
          (ClosureAllocationsPersistent.refl witness) matcherCapacity
          (takeClosureApplication_ordinaryPersistenceTransport application)
          matcherFrame.externals matcherTables
      have storedInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness facts
            ((stepCost + continuationCost) + slack) callRuntime sourceEnv
            selected.nextStore targetLocals witness :=
        ⟨postMatcher, matcherEntry⟩
      have resumedUpdate :
          FirTalos.Correctness.LocalUpdate targetLocals resumedLocals
            resultIndex physical := by
        rw [resumedEq]
        have base := FirTalos.Correctness.localUpdate_of_set? targetSet
        refine ⟨?_, ?_⟩
        · simpa [Wasm.Locals.get] using base.1
        · intro other different
          simpa [Wasm.Locals.get] using base.2 different
      have expectedTransfer :
          reuseCapacityLetFacts? facts decl =
            some (eraseReuseCapacityFact facts decl.fvarId) := by
        simp [reuseCapacityLetFacts?, site.valueEq]
      rw [expectedTransfer] at transfer
      have nextFactsEq : nextFacts = eraseReuseCapacityFact facts decl.fvarId :=
        Option.some.inj transfer.symm
      have callerAfterCall :=
        storedInvariant.restoreDirectCaller calleeInvariant
          resumedFocus.stateRelated resumedFocus.frameAligned entry.resultFound
          resumedUpdate
      have continuationInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness nextFacts
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) afterCall resumedLocals
            resultWitness := by
        simpa only [nextFactsEq] using callerAfterCall
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          finalWitness, kind, resultPhysical, continuationSourceCount,
          continuationTargetCount, continuationSourcePath,
          continuationTargetPath, yielded, resultInvariant, resultRefines,
          resultJoins, continuationSourceFramesEq,
          continuationTargetFramesEq⟩ :=
        continuedIH functionSpec contextCaches resumedFocus
          (resumedSourceJoinsEq.trans sourceJoins) continuationInvariant
          entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals, finalWitness,
        kind, resultPhysical,
        2 + calleeSourceCount + 1 + continuationSourceCount,
        (3 * (before.length + 1) + argumentTarget.length + 1) +
          calleeTargetCount + (before.length + 5) + continuationTargetCount,
        ((sourcePrefix.trans calleeSourcePath).trans
          (.single bindSourceStep)).trans continuationSourcePath,
        ((targetPrefix.trans calleeTargetPath).trans bindTargetPath).trans
          continuationTargetPath,
        yielded, resultInvariant, resultRefines, resultJoins,
        continuationSourceFramesEq.trans resumedSourceFramesEq,
        continuationTargetFramesEq.trans resumedTargetFramesEq⟩
  | @externalLet context sourceRuntime sourceEnv decl continuation nextRuntime
      sourceValue stepCost facts nextFacts expectedResult resultFacts
      resultRuntime resultEnv resultValue continuationCost supported sourceStep
      transfer continued ih =>
      obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
          valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.let_eq related.adapted
      have stepFits :
          stepCost ≤ (stepCost + continuationCost) + slack := by
        omega
      obtain ⟨nextStore, nextLocals, nextWitness, producedFacts, step,
          _externalsPreserved, _hostDescriptorsPreserved,
          _witnessDescriptorsPreserved, producedTransfer, nextInvariant⟩ :=
        (functionSpec.reuseCapacityExternalLetRuntimeRefinesWithCost_pureExternal_entryRelativeCache
          externals) supported stepFits invariant sourceStep valueCompiled
            valueAdapted resultFound
      rw [transfer] at producedTransfer
      have factsEq := Option.some.inj producedTransfer
      subst producedFacts
      have continuationInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness nextFacts
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            nextWitness := by
        have budgetEq :
            (stepCost + continuationCost) + slack - stepCost =
              continuationCost + slack := by
          omega
        simpa only [budgetEq] using nextInvariant
      have flat :
          StructuredWasmFlatProgram targetModule.wasmModule
            (targetValue ++ [.localSet resultIndex]) :=
        supported.structuredFlatProgram functionSpec valueCompiled valueAdapted
      obtain ⟨sourceMiddle, targetMiddle, sourcePrefix, targetPrefix,
          nextFocus, nextSourceJoins, firstSourceFramesEq,
          firstTargetFramesEq⟩ :=
        related.advance_flatExternalLet sourceJoins targetCodeEq
          continuationAdapted flat step
          continuationInvariant.1.1.1.1.2.2.1
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, physical, sourceCount, targetCount, sourceTail,
          targetTail, yielded, resultInvariant, resultRefines, resultJoins,
          tailSourceFramesEq, tailTargetFramesEq⟩ :=
        ih functionSpec contextCaches nextFocus nextSourceJoins
          (continuationInvariant.withValues targetLocals.values) entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, physical, 3 + sourceCount,
        (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length +
          targetCount,
        sourcePrefix.trans sourceTail, targetPrefix.trans targetTail, yielded,
        resultInvariant, resultRefines, resultJoins,
        tailSourceFramesEq.trans firstSourceFramesEq,
        tailTargetFramesEq.trans firstTargetFramesEq⟩
  | @lazyHit context decl declaration sourceDeclaration resultKind
      sourceRuntime sourceEnv continuation nextRuntime sourceValue facts
      nextFacts expectedResult resultFacts resultRuntime resultEnv resultValue
      continuationCost call sourceStep transfer continued ih =>
        obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
            valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
          CodeAdaptedWithSuffix.let_eq related.adapted
        have ordinaryLowering :
            Fir.Wasm.lower program = .ok sourceModule :=
          LazyCacheGeneratedEnvironment.lower_of_lowerSupported
            rootSpec.lowered
        have emittedCacheNames :
            sourceModule.initializers =
              Fir.Wasm.cachedDeclarationNames program :=
          LazyCacheGeneratedEnvironment.initializers_of_lower ordinaryLowering
        have contexts : DeclarationContextsCoherent rootContext context := {
          program :=
            rootSpec.contextProgram.trans functionSpec.contextProgram.symm
          cachedDeclarations :=
            (rootGenerated.cacheNames.trans emittedCacheNames).trans
              contextCaches.symm }
        have generated :
            LazyCacheGeneratedEnvironment context sourceModule :=
          rootGenerated.ofCoherent contexts
        obtain ⟨sourceMiddle, targetMiddle, nextLocals, physical, hit,
            producedTransfer, nextCache, sourcePrefix, targetPrefix, nextFocus,
            nextSourceJoins, firstSourceFramesEq, firstTargetFramesEq⟩ :=
          related.advance_lazyHit_of_compiler functionSpec sourceJoins
            targetCodeEq continuationAdapted call generated sourceStep
            invariant.1 valueCompiled valueAdapted resultFound
        rw [transfer] at producedTransfer
        have nextFactsEq :
            nextFacts = eraseReuseCapacityFact facts decl.fvarId :=
          Option.some.inj producedTransfer
        have stepFits :
            0 ≤ continuationCost + slack := by omega
        obtain ⟨_simulates, _externalsPreserved,
            _hostDescriptorsPreserved, _witnessDescriptorsPreserved,
            _nextTransfer, nextCacheInvariant⟩ :=
          invariant.1.ofLazyCacheResult stepFits hit (fun _ => nextCache)
            resultFound (by
              simpa [nextFactsEq] using transfer)
        have ordinary :
            OrdinaryPersistenceTransport sourceRuntime nextRuntime :=
          SourceLazyLetResult.hit_ordinaryTransport_of_supported call sourceStep
        have nextEntry :
            ReuseCapacityCodeEntryTransports entryRuntime nextRuntime entryStore
              targetStore entryWitness witness :=
          invariant.2.step hit.witnessTransport
            hit.closureAllocationsPersistent hit.capacityTransport ordinary
            hit.externalsPreserved hit.toClosureTablesTransport
        have continuationInvariant :
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness nextFacts
              (continuationCost + slack) nextRuntime
              (bind sourceEnv decl.fvarId sourceValue) targetStore nextLocals
              witness := by
          simpa [nextFactsEq] using
            (show
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness
                (eraseReuseCapacityFact facts decl.fvarId)
                ((continuationCost + slack) - 0) nextRuntime
                (bind sourceEnv decl.fvarId sourceValue) targetStore nextLocals
                witness from ⟨nextCacheInvariant, nextEntry⟩)
        obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
            resultWitness, kind, resultPhysical, sourceCount, targetCount,
            sourceTail, targetTail, yielded, resultInvariant, resultRefines,
            resultJoins, tailSourceFramesEq, tailTargetFramesEq⟩ :=
          ih functionSpec contextCaches nextFocus nextSourceJoins
            (continuationInvariant.withValues targetLocals.values) entryAbi
        exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, resultPhysical, 3 + sourceCount,
          5 + targetCount, sourcePrefix.trans sourceTail,
          targetPrefix.trans targetTail, yielded, resultInvariant,
          resultRefines, resultJoins,
          tailSourceFramesEq.trans firstSourceFramesEq,
          tailTargetFramesEq.trans firstTargetFramesEq⟩
  | @lazyMiss context decl declaration sourceDeclaration resultKind calleeCode
      sourceRuntime sourceEnv continuation nextRuntime sourceValue
      calleeResultFacts callRuntime calleeResultEnv stepCost facts nextFacts
      expectedResult resultFacts resultRuntime resultEnv resultValue
      continuationCost calleeFunction call loweredRow resultClassified
      notObject notTObject sourceStep callee transfer continued calleeIH
      continuedIH =>
      rcases call with
        ⟨⟨valueEq, kindEq, targetEq, targetResultEq, resultRefines, paramsEq,
          resultCompiled⟩, bodyEq⟩
      have programEq : context.program = program := functionSpec.contextProgram
      subst program
      obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
          valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
        CodeAdaptedWithSuffix.let_eq related.adapted
      obtain ⟨targetResultKind, cacheIndex, declarationId, cacheSetId,
          recoveredTargetResultEq, cacheEq, declarationCall, cacheSetCall,
          _valueCodeEq, targetValueEq⟩ :=
        compileCachedLetValue_adapted_inv context sourceModule sourceFunction []
          decl declaration sourceDeclaration _ valueCode targetValue valueEq
          kindEq targetEq paramsEq valueCompiled valueAdapted
      have targetKindEq : targetResultKind = resultKind :=
        Option.some.inj (recoveredTargetResultEq.symm.trans targetResultEq)
      subst targetResultKind
      obtain ⟨generatedRow⟩ :=
        ConcreteGeneratedInternalDeclaration.exists_ofSupportedPipelineAtLowered
          rfl contextCaches rootSpec.programNamesUnique rootSpec.lowered
          rootSpec.adapted targetEq loweredRow resultClassified
      have parametersEmpty : sourceDeclaration.params = #[] :=
        Array.isEmpty_iff.mp paramsEq
      have ordinaryLowering :
          Fir.Wasm.lower context.program = .ok sourceModule :=
        LazyCacheGeneratedEnvironment.lower_of_lowerSupported rootSpec.lowered
      have emittedCacheNames :
          sourceModule.initializers =
            Fir.Wasm.cachedDeclarationNames context.program :=
        LazyCacheGeneratedEnvironment.initializers_of_lower ordinaryLowering
      have contexts : DeclarationContextsCoherent rootContext context := {
        program :=
          rootSpec.contextProgram.trans functionSpec.contextProgram.symm
        cachedDeclarations :=
          (rootGenerated.cacheNames.trans emittedCacheNames).trans
            contextCaches.symm }
      have generated : LazyCacheGeneratedEnvironment context sourceModule :=
        rootGenerated.ofCoherent contexts
      have declarationNameEq : sourceDeclaration.name = declaration :=
        by
          have selected := (Array.find?_eq_some_iff_getElem.mp targetEq).1
          simpa [Fir.LeanIR.Program.findDecl?] using selected
      have exactCallIndex :
          callIndex? sourceModule (.declaration declaration) =
            some generatedRow.targetFunctionIndex := by
        simpa [declarationNameEq] using generatedRow.callIndexEq
      have declarationIdEq :
          declarationId = generatedRow.targetFunctionIndex :=
        Option.some.inj (declarationCall.symm.trans exactCallIndex)
      subst declarationId
      obtain ⟨initializerFound, signature⟩ :=
        generated.select kindEq targetEq targetResultEq paramsEq cacheEq
      obtain ⟨imp, importFound, importInBounds, contractFound,
          parameterCount, resultCount⟩ :=
        functionSpec.cacheSetCall cacheSetCall
      obtain ⟨alignedResultIndex, alignedResultFound, resultKindAt⟩ :=
        functionSpec.localsAligned resultCompiled
      rw [resultFound] at alignedResultFound
      injection alignedResultFound with resultIndexEq
      subst alignedResultIndex
      have cacheFacts :=
        SourceLazyLetResult.miss_cacheFacts_of_callee valueEq targetEq
          parametersEmpty bodyEq sourceStep callee.sourceResult
      have semanticEmpty :
          findGlobal? sourceRuntime.globals declaration = none := cacheFacts.1
      have publicationRuntimeEq :
          nextRuntime = callRuntime.setGlobal declaration sourceValue :=
        cacheFacts.2
      let sourceStaged : MachineState :=
        { program := context.program
          control := .invokeName declaration #[]
          env := sourceEnv
          joins := []
          frames :=
            .bind decl.fvarId continuation sourceEnv [] :: source.frames
          runtime := sourceRuntime }
      let sourceEntry : MachineState :=
        { program := context.program
          control := .code calleeCode
          env := []
          joins := []
          frames := .cache declaration ::
            .bind decl.fvarId continuation sourceEnv [] :: source.frames
          runtime := sourceRuntime }
      let targetEntry : StructuredWasmState Host :=
        { store := targetStore
          control := .running (generatedRow.targetFunction.toLocals [])
            generatedRow.targetFunction.body
          frames :=
            .call 1 targetLocals.values
                { targetLocals with values := targetLocals.values } [
                  .call cacheSetId,
                  .globalSet (2 * cacheIndex + 1),
                  .const 1,
                  .globalSet (2 * cacheIndex)] ::
              .label 0 targetLocals.values
                ([.globalGet (2 * cacheIndex + 1),
                  .localSet resultIndex] ++ targetRest) :: target.frames }
      have stageSourceStep :
          executeStep externals source = .next sourceStaged := by
        rcases source with
          ⟨sourceProgram, sourceControl, sourceStateEnv, sourceStateJoins,
            sourceFrames, sourceStateRuntime⟩
        have programEq := related.sourceProgramEq
        change sourceProgram = context.program at programEq
        subst sourceProgram
        have controlEq := related.sourceControlEq
        change sourceControl = .code (.let decl continuation) at controlEq
        subst sourceControl
        have envEq := related.sourceEnvEq
        change sourceStateEnv = sourceEnv at envEq
        subst sourceStateEnv
        have runtimeEq := related.sourceRuntimeEq
        change sourceStateRuntime = sourceRuntime at runtimeEq
        subst sourceStateRuntime
        change sourceStateJoins = [] at sourceJoins
        subst sourceStateJoins
        simp [sourceStaged, executeStep, coreStep, evalLetValue, valueEq,
          evalArgs, Bind.bind, Except.bind, pure, Except.pure, pushBindFrame]
      have enterSourceStep :
          executeStep externals sourceStaged = .next sourceEntry := by
        have enteredCanonical :
            executeStep externals {
                program := context.program
                control := .invokeName declaration #[]
                env := sourceEnv
                frames := [.bind decl.fvarId continuation sourceEnv []]
                runtime := sourceRuntime } =
              .next {
                program := context.program
                control := .code calleeCode
                env := []
                frames := [
                  .cache declaration,
                  .bind decl.fvarId continuation sourceEnv []]
                runtime := sourceRuntime } := by
          simp [executeStep, coreStep, semanticEmpty, invokeDecl, targetEq,
            parametersEmpty, bodyEq, bindParams]
        have lifted :=
          FirTalos.Correctness.ExecSteps.withFrameSuffix
            (suffix := source.frames)
            (ExecSteps.step enteredCanonical (ExecSteps.refl _))
        cases lifted with
        | step head tail =>
            cases tail
            simpa [sourceStaged, sourceEntry, withFrameSuffix] using head
      have sourcePrefix :
          FinitePath
            (fun before after => executeStep externals before = .next after)
            2 source sourceEntry :=
        .cons stageSourceStep (.cons enterSourceStep (.refl _))
      have flagEmpty :
          targetStore.globals.globals[2 * cacheIndex]? = some (.i32 0) :=
        invariant.1.2.1.emptySlot initializerFound signature semanticEmpty
      have targetParameterCount : generatedRow.targetFunction.numParams = 0 :=
        (generatedRow.targetParameterCount_nullary parametersEmpty).symm
      have targetPrefix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env) 3 target
              targetEntry := by
        rcases target with ⟨targetStateStore, targetControl, targetFrames⟩
        have storeEq := related.targetStoreEq
        change targetStateStore = targetStore at storeEq
        subst targetStateStore
        have controlEq := related.targetControlEq
        rw [targetCodeEq, targetValueEq] at controlEq
        change targetControl = .running targetLocals
          ([.globalGet (2 * cacheIndex),
            .iff 0 0 [] [
              .call generatedRow.targetFunctionIndex,
              .call cacheSetId,
              .globalSet (2 * cacheIndex + 1),
              .const 1,
              .globalSet (2 * cacheIndex)],
            .globalGet (2 * cacheIndex + 1),
            .localSet resultIndex] ++ targetRest) at controlEq
        subst targetControl
        simpa [targetEntry] using
          structuredWasmLazyMissPrefixFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (store := targetStore) (locals := targetLocals)
            (flagIndex := 2 * cacheIndex)
            (valueIndex := 2 * cacheIndex + 1)
            (resultIndex := resultIndex)
            (declarationId := generatedRow.targetFunctionIndex)
            (cacheSetId := cacheSetId)
            (function := generatedRow.targetFunction)
            (rest := targetRest) (frames := targetFrames)
            targetLocals.values flagEmpty generatedRow.notImport
            generatedRow.targetFunctionFound targetParameterCount
            generatedRow.singleResult
      have calleeFrame :
          ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals
            [] (stepCost + (continuationCost + slack)) sourceRuntime []
            targetStore (generatedRow.targetFunction.toLocals []) witness :=
        invariant.1.generatedNullaryCalleeEntryAtCost generatedRow
          parametersEmpty (by omega)
      have calleeFocus :
          ConcreteStructuredCodeFocus loweredRow.context sourceModule
            calleeFunction [] sourceRuntime [] calleeCode targetStore
            (generatedRow.targetFunction.toLocals [])
            generatedRow.targetFunction.body witness sourceEntry targetEntry := {
        sourceProgramEq := by
          calc
            sourceEntry.program = context.program := by
              simp [sourceEntry]
            _ = loweredRow.context.program := generatedRow.contextProgram.symm
        sourceControlEq := by simp [sourceEntry]
        sourceEnvEq := by simp [sourceEntry]
        sourceRuntimeEq := by simp [sourceEntry]
        targetStoreEq := by simp [targetEntry]
        targetControlEq := by simp [targetEntry]
        adapted := by
          rw [generatedRow.targetBodyEq]
          exact CodeAdapted.withSuffix generatedRow.bodyAdapted
        stateRelated := calleeFrame.1.1.1.1.1
        frameAligned := calleeFrame.1.1.1.2.2.1 }
      have calleeEntryInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule calleeFunction
              externals)
            sourceRuntime targetStore witness []
            (stepCost + (continuationCost + slack)) sourceRuntime []
            targetStore (generatedRow.targetFunction.toLocals []) witness :=
        ⟨calleeFrame,
          ReuseCapacityCodeEntryTransports.refl sourceRuntime targetStore
            witness⟩
      have calleeEntryAbi :
          ClosureAllocationsAbiAligned loweredRow.context.program witness := by
        rw [generatedRow.contextProgram]
        exact entryAbi.ofPersistent
          invariant.2.closureAllocationsPersistent
      obtain ⟨sourceYield, targetYield, afterCall, calleeLocals, callWitness,
          actualKind, physical, calleeSourceCount, calleeTargetCount,
          calleeSourcePath, calleeTargetPath, calleeYielded, calleeInvariant,
          calleeResultRefines, _calleeResultJoins, calleeSourceFramesEq,
          calleeTargetFramesEq⟩ :=
        calleeIH (generatedRow.toSupportedFunction rootSpec)
          generatedRow.contextCaches calleeFocus rfl calleeEntryInvariant
          calleeEntryAbi
      have sourceCallFramesEq :
          sourceYield.frames =
            .cache declaration ::
              .bind decl.fvarId continuation sourceEnv [] :: source.frames := by
        rw [calleeSourceFramesEq]
      have targetCallFramesEq :
          targetYield.frames =
            .call 1 targetLocals.values targetLocals [
                .call cacheSetId,
                .globalSet (2 * cacheIndex + 1),
                .const 1,
                .globalSet (2 * cacheIndex)] ::
              .label 0 targetLocals.values
                ([.globalGet (2 * cacheIndex + 1),
                  .localSet resultIndex] ++ targetRest) :: target.frames := by
        rw [calleeTargetFramesEq]
      obtain ⟨cacheSlot, cacheFound, cacheKindEq⟩ :=
        calleeInvariant.1.2.1.hostSlot initializerFound signature
      have valueRelated :
          PhysicalValueRel callWitness resultKind physical sourceValue :=
        calleeYielded.valueRelated.ofRefines calleeResultRefines
      have cacheDescriptorsEq :
          afterCall.host.closureDescriptors = callWitness.closureDescriptors :=
        calleeInvariant.1.1.2
      obtain ⟨runtimeAfter, operation, _runtimeAfterRelated,
          _valueStillRelated, _mappedCapacity⟩ :=
        cacheSetStep_of_refines calleeYielded.stateRelated.1 valueRelated
          cacheFound cacheKindEq cacheDescriptorsEq
      let afterCache := replaceRuntime afterCall runtimeAfter
      have operationEq :
          cacheSetStep declaration resultKind afterCall [physical] =
            .Return [physical] afterCache := by
        simpa [afterCache] using operation
      obtain ⟨oldFlag, oldValue, flagAfterCall, valueAfterCall⟩ :=
        calleeInvariant.1.2.1.slotLanesPresent initializerFound signature
      have valueAfterCache :
          afterCache.globals.globals[2 * cacheIndex + 1]? = some oldValue := by
        rw [cacheSetStep_preserves_wasmGlobals operationEq]
        exact valueAfterCall
      have flagAfterCache :
          afterCache.globals.globals[2 * cacheIndex]? = some oldFlag := by
        rw [cacheSetStep_preserves_wasmGlobals operationEq]
        exact flagAfterCall
      let valueStore :=
        writeWasmGlobal afterCache (2 * cacheIndex + 1) physical
      have valueStoreEq :
          valueStore =
            writeWasmGlobal afterCache (2 * cacheIndex + 1) physical := rfl
      have flagAfterValue :
          valueStore.globals.globals[2 * cacheIndex]? = some oldFlag := by
        rw [valueStoreEq, writeWasmGlobal_get_ne (by omega)]
        exact flagAfterCache
      let nextStore :=
        writeWasmGlobal valueStore (2 * cacheIndex) (.i32 1)
      obtain ⟨nextLocals, targetSet, nextAligned⟩ :=
        invariant.1.1.1.1.2.2.1.set?
          (nextRuntime := nextRuntime)
          (nextEnv := bind sourceEnv decl.fvarId sourceValue)
          (nextStore := nextStore) (nextWitness := callWitness)
          (physical := physical) resultFound
      let sourcePublished : MachineState := {
        program := context.program
        control := .yielded sourceValue
        env := calleeResultEnv
        joins := []
        frames := .bind decl.fvarId continuation sourceEnv [] :: source.frames
        runtime := nextRuntime }
      let sourceResumed : MachineState := {
        program := context.program
        control := .code continuation
        env := bind sourceEnv decl.fvarId sourceValue
        joins := []
        frames := source.frames
        runtime := nextRuntime }
      let targetResumed : StructuredWasmState Host := {
        store := nextStore
        control := .running
          { nextLocals with values := targetLocals.values } targetRest
        frames := target.frames }
      have publishSourceStep :
          executeStep externals sourceYield = .next sourcePublished := by
        rcases sourceYield with
          ⟨yieldProgram, yieldControl, yieldEnv, yieldJoins, yieldFrames,
            yieldRuntime⟩
        have programEq := calleeYielded.sourceProgramEq
        change yieldProgram = loweredRow.context.program at programEq
        rw [generatedRow.contextProgram] at programEq
        subst yieldProgram
        have controlEq := calleeYielded.sourceControlEq
        change yieldControl = .yielded sourceValue at controlEq
        subst yieldControl
        have envEq := calleeYielded.sourceEnvEq
        change yieldEnv = calleeResultEnv at envEq
        subst yieldEnv
        have runtimeEq := calleeYielded.sourceRuntimeEq
        change yieldRuntime = callRuntime at runtimeEq
        subst yieldRuntime
        change yieldJoins = [] at _calleeResultJoins
        subst yieldJoins
        change yieldFrames = _ at sourceCallFramesEq
        subst yieldFrames
        simp [sourcePublished, executeStep, coreStep, publicationRuntimeEq]
      have bindSourceStep :
          executeStep externals sourcePublished = .next sourceResumed := by
        simp [sourcePublished, sourceResumed, executeStep, coreStep]
      have sourceSuffix :
          FinitePath
            (fun before after => executeStep externals before = .next after)
            2 sourceYield sourceResumed :=
        .cons publishSourceStep (.cons bindSourceStep (.refl _))
      have targetSuffix :
          FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env) 8
            targetYield targetResumed := by
        rcases targetYield with
          ⟨yieldStore, yieldControl, yieldFrames⟩
        have storeEq := calleeYielded.targetStoreEq
        change yieldStore = afterCall at storeEq
        subst yieldStore
        have controlEq := calleeYielded.targetControlEq
        change yieldControl = .returning (physical :: calleeLocals.values)
          at controlEq
        subst yieldControl
        change yieldFrames = _ at targetCallFramesEq
        subst yieldFrames
        simpa [targetResumed, nextStore] using
          structuredWasmLazyMissSuffixFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (spec := hosts.spec) (cacheSetId := cacheSetId) (imp := imp)
            (declaration := declaration) (kind := resultKind)
            (afterCall := afterCall) (afterCache := afterCache)
            (valueStore := valueStore) (callerLocals := targetLocals)
            (calleeLocals := calleeLocals) (nextLocals := nextLocals)
            (physical := physical) (oldValue := oldValue)
            (oldFlag := oldFlag) (flagIndex := 2 * cacheIndex)
            (valueIndex := 2 * cacheIndex + 1)
            (resultIndex := resultIndex) (rest := targetRest)
            (frames := target.frames) targetLocals.values importFound
            functionSpec.hostsSatisfy importInBounds contractFound
            parameterCount resultCount operationEq valueAfterCache
            valueStoreEq flagAfterValue (by omega) targetSet
      have nextRelated :
          StateRelated sourceFunction nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness := by
        rw [publicationRuntimeEq]
        exact invariant.1.1.1.1.1.1.bindAfterCacheSet
          calleeYielded.stateRelated.1 calleeInvariant.2.witness valueRelated
          cacheFound cacheKindEq cacheDescriptorsEq operationEq valueStoreEq
          resultFound resultKindAt targetSet
      have nonHeap : IsNonHeapReference sourceValue :=
        valueRelated.isNonHeapReference_of_kind notObject notTObject
      have publicationOrdinary :
          ReuseTokenOrdinaryBindTransport facts decl.fvarId callRuntime
            nextRuntime sourceEnv sourceValue := by
        rw [publicationRuntimeEq]
        exact ReuseTokenOrdinaryBindTransport.ofPublicationDisjoint declaration
          (ReuseTokenPublicationDisjoint.of_nonHeapReference nonHeap)
      have nextOrdinary :
          ReuseTokenOrdinaryRel (eraseReuseCapacityFact facts decl.fvarId)
            nextRuntime (bind sourceEnv decl.fvarId sourceValue) :=
        publicationOrdinary
          (invariant.1.1.1.1.2.1.transport calleeInvariant.2.ordinary)
      have publicationCapacity :
          HeaderCapacityTransport afterCall.host.runtime.heap
            nextStore.host.runtime.heap callWitness := by
        simpa [nextStore, writeWasmGlobal, valueStoreEq] using
          cacheSetStep_preserves_mappedHeaderCapacity_of_related
            calleeYielded.stateRelated.1 valueRelated cacheFound cacheKindEq
            cacheDescriptorsEq operationEq
      have currentCapacity :
          HeaderCapacityTransport targetStore.host.runtime.heap
            nextStore.host.runtime.heap witness :=
        calleeInvariant.2.capacity.transAcross calleeInvariant.2.witness
          publicationCapacity
      have nextReuseRelated :
          ReuseCapacityStateRelated
            (eraseReuseCapacityFact facts decl.fvarId) sourceFunction
            nextRuntime (bind sourceEnv decl.fvarId sourceValue) nextStore
            nextLocals callWitness :=
        invariant.1.1.1.1.1.eraseResult nextRelated resultFound
          (localUpdate_of_set? targetSet) calleeInvariant.2.witness
          currentCapacity
      have nextBudget :
          nextStore.host.runtime.heap.AddressSpaceBudget
            (continuationCost + slack) := by
        simpa [nextStore] using
          cachePublication_preserves_addressSpaceBudget operationEq valueStoreEq
            calleeInvariant.1.1.1.1.2.2.2
      have publicationExternals :
          nextStore.host.externals = afterCall.host.externals := by
        simp [nextStore, valueStore, afterCache, writeWasmGlobal,
          replaceRuntime, clearFailure]
      have publicationDescriptors :
          nextStore.host.closureDescriptors =
            afterCall.host.closureDescriptors := by
        simp [nextStore, valueStore, afterCache, writeWasmGlobal,
          replaceRuntime, clearFailure]
      have publicationDispatch :
          nextStore.host.closureDispatch = afterCall.host.closureDispatch := by
        simp [nextStore, valueStore, afterCache, writeWasmGlobal,
          replaceRuntime, clearFailure]
      have nextInteger :
          nextStore.host.externals.IntegerResultRefines externals := by
        rw [publicationExternals, calleeInvariant.2.externals]
        exact invariant.1.1.1.2.1
      have nextNatural :
          FirTalos.Concrete.ConcreteExternalImpl.NaturalResultRefines
            nextStore.host.externals externals := by
        rw [publicationExternals, calleeInvariant.2.externals]
        exact invariant.1.1.1.2.2.1
      have nextScalar :
          FirTalos.Concrete.ConcreteExternalImpl.ScalarResultRefines
            nextStore.host.externals externals := by
        rw [publicationExternals, calleeInvariant.2.externals]
        exact invariant.1.1.1.2.2.2
      have nextDescriptors :
          nextStore.host.closureDescriptors = callWitness.closureDescriptors :=
        publicationDescriptors.trans cacheDescriptorsEq
      have nextCache :
          LazyCacheGlobalsRel callWitness sourceModule nextRuntime nextStore := by
        have afterHost :=
          calleeInvariant.1.2.1.afterCacheSet operationEq
        simpa [nextStore] using
          afterHost.publish initializerFound signature publicationRuntimeEq
            valueRelated valueStoreEq
      have nextClosureTables : ClosureTablesAgree nextStore callWitness := by
        exact {
          dispatch :=
            calleeInvariant.1.2.2.dispatch.trans publicationDispatch.symm
          descriptors :=
            publicationDescriptors.trans
              calleeInvariant.1.2.2.descriptors }
      have nextBase :
          ConcreteReuseCapacityFrame sourceFunction
            (eraseReuseCapacityFact facts decl.fvarId)
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness :=
        ⟨nextReuseRelated, nextOrdinary, nextAligned, nextBudget⟩
      have nextPure :
          ConcreteReuseCapacityPureExternalFrame sourceFunction externals
            (eraseReuseCapacityFact facts decl.fvarId)
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness :=
        ⟨nextBase, nextInteger, nextNatural, nextScalar⟩
      have nextOwnership :
          ConcreteReuseCapacityPureExternalOwnershipFrame sourceFunction
            externals (eraseReuseCapacityFact facts decl.fvarId)
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness :=
        ⟨nextPure, nextDescriptors⟩
      have nextFrame :
          ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
            (eraseReuseCapacityFact facts decl.fvarId)
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness :=
        ⟨nextOwnership, nextCache, nextClosureTables⟩
      have publicationOrdinaryPersistence :
          OrdinaryPersistenceTransport callRuntime nextRuntime := by
        apply (OrdinaryPersistenceTransport.refl callRuntime).congrAfter
        rw [publicationRuntimeEq]
        exact
          (RuntimeState.setGlobal_heap_eq_of_nonHeapReference callRuntime
            declaration sourceValue nonHeap).symm
      have publicationClosureTables :
          ClosureTablesTransport afterCall nextStore callWitness
            callWitness := {
        hostDispatchPreserved := publicationDispatch
        witnessDispatchPreserved := rfl
        hostDescriptorsPreserved := publicationDescriptors
        witnessDescriptorsPreserved := rfl }
      have currentToNext :
          ReuseCapacityCodeEntryTransports sourceRuntime nextRuntime
            targetStore nextStore witness callWitness :=
        calleeInvariant.2.step (WitnessTransport.refl callWitness)
          (ClosureAllocationsPersistent.refl callWitness)
          publicationCapacity publicationOrdinaryPersistence
          publicationExternals publicationClosureTables
      have nextEntry :
          ReuseCapacityCodeEntryTransports entryRuntime nextRuntime entryStore
            nextStore entryWitness callWitness :=
        invariant.2.step currentToNext.witness
          currentToNext.closureAllocationsPersistent currentToNext.capacity
          currentToNext.ordinary currentToNext.externals
          currentToNext.toClosureTablesTransport
      have expectedTransfer :
          reuseCapacityLetFacts? facts decl =
            some (eraseReuseCapacityFact facts decl.fvarId) := by
        simp [reuseCapacityLetFacts?, valueEq]
      have nextFactsEq :
          nextFacts = eraseReuseCapacityFact facts decl.fvarId :=
        Option.some.inj (transfer.symm.trans expectedTransfer)
      have continuationInvariant :
          ReuseCapacityEntryRelativeFrame
            (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
              externals)
            entryRuntime entryStore entryWitness nextFacts
            (continuationCost + slack) nextRuntime
            (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
            callWitness := by
        simpa [nextFactsEq] using
          (show
            ReuseCapacityEntryRelativeFrame
              (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                externals)
              entryRuntime entryStore entryWitness
              (eraseReuseCapacityFact facts decl.fvarId)
              (continuationCost + slack) nextRuntime
              (bind sourceEnv decl.fvarId sourceValue) nextStore nextLocals
              callWitness from ⟨nextFrame, nextEntry⟩)
      have nextFocus :
          ConcreteStructuredCodeFocus context sourceModule sourceFunction []
            nextRuntime (bind sourceEnv decl.fvarId sourceValue) continuation
            nextStore { nextLocals with values := targetLocals.values }
            targetRest callWitness sourceResumed targetResumed := {
        sourceProgramEq := by simp [sourceResumed]
        sourceControlEq := by simp [sourceResumed]
        sourceEnvEq := by simp [sourceResumed]
        sourceRuntimeEq := by simp [sourceResumed]
        targetStoreEq := by simp [targetResumed]
        targetControlEq := by simp [targetResumed]
        adapted := continuationAdapted
        stateRelated := by simpa using nextRelated.withValues targetLocals.values
        frameAligned := by simpa using nextAligned.withValues targetLocals.values }
      obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals,
          resultWitness, kind, resultPhysical, continuationSourceCount,
          continuationTargetCount, continuationSourcePath,
          continuationTargetPath, yielded, resultInvariant, resultRefines,
          resultJoins, continuationSourceFramesEq,
          continuationTargetFramesEq⟩ :=
        continuedIH functionSpec contextCaches nextFocus rfl
          (continuationInvariant.withValues targetLocals.values) entryAbi
      exact ⟨sourceAfter, targetAfter, resultStore, resultLocals,
        resultWitness, kind, resultPhysical,
        2 + calleeSourceCount + 2 + continuationSourceCount,
        3 + calleeTargetCount + 8 + continuationTargetCount,
        ((sourcePrefix.trans calleeSourcePath).trans sourceSuffix).trans
          continuationSourcePath,
        ((targetPrefix.trans calleeTargetPath).trans targetSuffix).trans
          continuationTargetPath,
        yielded, resultInvariant, resultRefines, resultJoins,
        continuationSourceFramesEq, continuationTargetFramesEq⟩

/-- Canonical structured-machine entry for one already-selected generated
function body.  The exported-function lookup and Talos invocation boundary
select this function; the compiler proof starts from its initialized locals,
body, and empty structured frame stack. -/
def concreteStructuredFunctionEntry
    (function : Wasm.Function) (store : Wasm.Store Host)
    (parameters : List Wasm.Value) : StructuredWasmState Host :=
  { store
    control := .running (function.toLocals parameters.reverse) function.body
    frames := [] }

/-- Compiler-produced exact execution for the current recursive structured
fragment, specialized to the canonical source and generated-function entries.

This theorem removes the caller-supplied `ConcreteStructuredCodeFocus`: the
real lowering/adaptation proof and the ordinary initial cache/ownership frame
construct it.  The recursive source derivation then produces both finite
machine paths, a related returned value, the evolved entry-relative resource
frame, and exact endpoint observations.  It is deliberately a terminating
fragment theorem; the later ranked relation removes `evaluation` and gives
every finite source prefix independently of termination. -/
theorem
    ConcreteSupportedExport.reachesYield_reuseBudgetedStructured_generated
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule} {hosts : ResolvedHosts}
    {exportName : String}
    (spec :
      ConcreteSupportedExport program context sourceCode sourceModule
        sourceFunction targetModule hosts exportName)
    (contextCaches :
      context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
    (generated : LazyCacheGeneratedEnvironment context sourceModule)
    {externals : ExternalImpl}
    {expectedResult : AbiKind}
    {facts resultFacts : ReuseCapacityFacts}
    {sourceRuntime resultRuntime : RuntimeState}
    {sourceEnv resultEnv : Env}
    {resultValue : Value} {requiredBytes : Nat}
    {initial : Wasm.Store Host}
    {initialWitness : RefinementWitness}
    {parameters : List Wasm.Value}
    (evaluation :
      ReuseCapacityStructuredPureExternalLazyCodeEvaluates externals context
        expectedResult facts sourceRuntime sourceEnv sourceCode resultFacts
        resultRuntime resultEnv resultValue requiredBytes)
    (invariant :
      ConcreteReuseCapacityCacheAbiFrame context sourceModule sourceFunction
        externals facts requiredBytes sourceRuntime sourceEnv initial
        (spec.targetFunction.toLocals parameters.reverse) initialWitness) :
    ∃ sourceAfter targetAfter resultStore resultLocals resultWitness kind
        physical sourceCount targetCount,
      FinitePath
          (fun before after => executeStep externals before = .next after)
          sourceCount
          (sourceCodeState context sourceRuntime sourceEnv sourceCode)
          sourceAfter ∧
        FinitePath
            (StructuredWasmStep targetModule.wasmModule hosts.env)
            targetCount
            (concreteStructuredFunctionEntry spec.targetFunction initial
              parameters)
            targetAfter ∧
          ConcreteStructuredYieldFocus context sourceFunction resultRuntime
              resultEnv resultValue resultStore resultLocals resultWitness kind
              physical sourceAfter targetAfter ∧
            ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                sourceRuntime initial initialWitness resultFacts 0 resultRuntime
                resultEnv resultStore resultLocals resultWitness ∧
              kind.refines expectedResult = true ∧
                ConcretePrefixObservationRel
                  (sourcePrefixObservation sourceAfter)
                  (concretePrefixObservation targetAfter.store) ∧
                  sourceAfter.joins = [] ∧
                    sourceAfter.frames = [] ∧ targetAfter.frames = [] := by
  let sourceInitial :=
    sourceCodeState context sourceRuntime sourceEnv sourceCode
  let targetLocals := spec.targetFunction.toLocals parameters.reverse
  let targetInitial :=
    concreteStructuredFunctionEntry spec.targetFunction initial parameters
  have related :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction []
        sourceRuntime sourceEnv sourceCode initial targetLocals
        spec.targetFunction.body initialWitness sourceInitial targetInitial := {
    sourceProgramEq := by simp [sourceInitial, sourceCodeState]
    sourceControlEq := by simp [sourceInitial, sourceCodeState]
    sourceEnvEq := by simp [sourceInitial, sourceCodeState]
    sourceRuntimeEq := by simp [sourceInitial, sourceCodeState]
    targetStoreEq := by simp [targetInitial, concreteStructuredFunctionEntry]
    targetControlEq := by
      simp [targetInitial, targetLocals, concreteStructuredFunctionEntry]
    adapted := by
      rw [spec.targetBodyEq]
      exact CodeAdapted.withSuffix spec.bodyAdapted
    stateRelated := invariant.cacheFrame.stateRelated.stateRelated
    frameAligned := invariant.cacheFrame.1.1.1.2.2.1 }
  have entryInvariant :
      ReuseCapacityEntryRelativeFrame
        (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals)
        sourceRuntime initial initialWitness facts (requiredBytes + 0)
        sourceRuntime sourceEnv initial targetLocals initialWitness :=
    ⟨by simpa [targetLocals] using invariant.cacheFrame,
      ReuseCapacityCodeEntryTransports.refl sourceRuntime initial
        initialWitness⟩
  obtain ⟨sourceAfter, targetAfter, resultStore, resultLocals, resultWitness,
      kind, physical, sourceCount, targetCount, sourcePath, targetPath, yielded,
      resultInvariant, resultRefines, resultJoins, sourceFrames, targetFrames⟩ :=
    related.reachesYield_reuseBudgetedDirectPureExternalCallsLazyCacheCases_generated
      spec generated spec.toConcreteSupportedFunction contextCaches evaluation
      (by simp [sourceInitial, sourceCodeState]) entryInvariant
      invariant.closureAbi
  exact ⟨sourceAfter, targetAfter, resultStore, resultLocals, resultWitness,
    kind, physical, sourceCount, targetCount,
    by simpa [sourceInitial] using sourcePath,
    by simpa [targetInitial] using targetPath, yielded,
    by simpa using resultInvariant, resultRefines, yielded.observes, resultJoins,
    by simpa [sourceInitial, sourceCodeState] using sourceFrames,
    by simpa [targetInitial, concreteStructuredFunctionEntry] using
      targetFrames⟩

/-- Structural rank used when lowering erases a source control step.  It
counts the currently admitted chains of persistent ownership instructions and
default-only case wrappers.  Later frame slices add their own continuation
component; the local erased-step laws need only this strictly decreasing
code-control component. -/
def compilerCodeSilenceDepth : Lean.Compiler.LCNF.Code .impure → Nat
  | .inc _ _ _ true continuation =>
      compilerCodeSilenceDepth continuation + 1
  | .dec _ _ _ true _ continuation =>
      compilerCodeSilenceDepth continuation + 1
  | .cases ⟨_, _, _, ⟨[.default selected]⟩⟩ =>
      compilerCodeSilenceDepth selected + 1
  | _ => 0

def compilerCodeSilenceRank (state : MachineState) : Nat :=
  match state.control with
  | .code code => compilerCodeSilenceDepth code
  | _ => 0

theorem compilerCodeSilenceDepth_defaultOnly_lt
    {sourceRuntime : RuntimeState} {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    (supported :
      DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected) :
    compilerCodeSilenceDepth selected <
      compilerCodeSilenceDepth (.cases cases) := by
  rcases cases with ⟨name, type, discr, alts⟩
  rcases alts with ⟨alts⟩
  change alts = [.default selected] at supported
  subst alts
  simp [compilerCodeSilenceDepth]

/-- Rank for the currently exposed structured-control protocols.  The low bit
distinguishes a compiler code node from its staged invocation state; the high
component counts chains of compiler-erased ownership and default-only case
operations.
Consequently both an empty generated argument prefix and a genuinely erased
instruction still make strict source-side progress.  Later erased join slices
extend the high component without changing the protocol phase convention. -/
def compilerStructuredControlRank (state : MachineState) : Nat :=
  2 * compilerCodeSilenceRank state +
    match state.control with
    | .code _ => 1
    | _ => 0

theorem compilerStructuredControlRank_invokeName_lt_code
    {before after : MachineState}
    {code : Lean.Compiler.LCNF.Code .impure}
    {name : Lean.Name} {args : Array Value}
    (beforeControl : before.control = .code code)
    (afterControl : after.control = .invokeName name args) :
    compilerStructuredControlRank after <
      compilerStructuredControlRank before := by
  simp [compilerStructuredControlRank, compilerCodeSilenceRank,
    beforeControl, afterControl]

theorem compilerStructuredControlRank_invokeValue_lt_code
    {before after : MachineState}
    {code : Lean.Compiler.LCNF.Code .impure}
    {value : Value} {args : Array Value}
    (beforeControl : before.control = .code code)
    (afterControl : after.control = .invokeValue value args) :
    compilerStructuredControlRank after <
      compilerStructuredControlRank before := by
  simp [compilerStructuredControlRank, compilerCodeSilenceRank,
    beforeControl, afterControl]

theorem compilerStructuredControlRank_lt_of_codeSilenceRank_lt
    {before after : MachineState}
    {beforeCode afterCode : Lean.Compiler.LCNF.Code .impure}
    (beforeControl : before.control = .code beforeCode)
    (afterControl : after.control = .code afterCode)
    (decreases :
      compilerCodeSilenceRank after < compilerCodeSilenceRank before) :
    compilerStructuredControlRank after <
      compilerStructuredControlRank before := by
  simp [compilerStructuredControlRank, compilerCodeSilenceRank,
    beforeControl, afterControl] at decreases ⊢
  omega

/-- A default-only source case is erased by production compilation.  This
local rule exposes the exact one-source/zero-target transition and the strict
syntactic rank decrease needed by the unified simulation. -/
theorem ConcreteStructuredCodeFocus.advance_defaultOnlyCase_ranked
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (supported :
      DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected)
    (sourceStep : SourceCaseResult sourceRuntime sourceEnv cases selected)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.cases cases) targetStore targetLocals
      targetCode witness source target) :
    ∃ sourceAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 0 target target ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv selected targetStore targetLocals targetCode
        witness sourceAfter target ∧
      sourceAfter.frames = source.frames ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  let sourceAfter : MachineState := { source with control := .code selected }
  have selectSourceStep :
      executeStep externals source = .next sourceAfter := by
    rcases source with
      ⟨sourceProgram, sourceControl, actualEnv, sourceJoinEnv, sourceFrames,
        actualRuntime⟩
    have programEq := related.sourceProgramEq
    change sourceProgram = context.program at programEq
    subst sourceProgram
    have controlEq := related.sourceControlEq
    change sourceControl = .code (.cases cases) at controlEq
    subst sourceControl
    have envEq := related.sourceEnvEq
    change actualEnv = sourceEnv at envEq
    subst actualEnv
    have runtimeEq := related.sourceRuntimeEq
    change actualRuntime = sourceRuntime at runtimeEq
    subst actualRuntime
    rcases sourceStep with ⟨discrValue, tag, found, tagged, chosen⟩
    simp [sourceAfter, executeStep, coreStep, found, tagged, chosen]
  have selectedFocus :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv selected targetStore targetLocals targetCode
        witness sourceAfter target := {
    sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
    sourceControlEq := by simp [sourceAfter]
    sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
    sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
    targetStoreEq := related.targetStoreEq
    targetControlEq := related.targetControlEq
    adapted :=
      CodeAdaptedWithSuffix.defaultOnlyCases_selected supported related.adapted
    stateRelated := related.stateRelated
    frameAligned := related.frameAligned }
  have silenceDecreases :
      compilerCodeSilenceRank sourceAfter <
        compilerCodeSilenceRank source := by
    simp only [compilerCodeSilenceRank, selectedFocus.sourceControlEq,
      related.sourceControlEq]
    exact compilerCodeSilenceDepth_defaultOnly_lt supported
  exact ⟨sourceAfter, selectSourceStep, .refl target, selectedFocus,
    by simp [sourceAfter],
    compilerStructuredControlRank_lt_of_codeSilenceRank_lt
      related.sourceControlEq selectedFocus.sourceControlEq silenceDecreases⟩

/-- Ranked form of saturated-call staging.  Its target path is reflexive by
construction, and the code-to-invocation phase change supplies the strict
rank decrease required by `ConcreteRankedTraceSimulation.advance`. -/
theorem ConcreteStructuredCodeFocus.advance_saturatedCall_stage_ranked
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime callerEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target) :
    ∃ targetValue targetRest resultIndex sourceAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 0 target
        target ∧
      ConcreteStructuredSaturatedCallReadyFocus context sourceModule
        sourceFunction targetModule site labels sourceRuntime continuation
        source.joins source.frames targetStore targetLocals targetValue
        targetRest target.frames witness resultIndex sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨targetValue, targetRest, resultIndex, sourceAfter, sourceStep,
      targetPath, ready⟩ :=
    related.advance_saturatedCall_stage (targetModule := targetModule)
      (hostEnv := hostEnv) site localsAligned
  exact ⟨targetValue, targetRest, resultIndex, sourceAfter, sourceStep,
    targetPath, ready,
    compilerStructuredControlRank_invokeValue_lt_code
      related.sourceControlEq ready.sourceControlEq⟩

/-- A persistent increment is one source step, no target steps, and a strict
drop in the local silence rank. -/
theorem ConcreteStructuredCodeFocus.advance_incPersistent
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {objectId : Lean.FVarId}
    {amount : Nat}
    {check : Bool}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv
      (.inc objectId amount check true continuation) targetStore targetLocals
      targetCode witness source target) :
    ∃ sourceAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 0 target target ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv continuation targetStore targetLocals targetCode
        witness sourceAfter target ∧
      sourceAfter.frames = source.frames ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  let sourceAfter : MachineState :=
    { source with control := .code continuation }
  refine ⟨sourceAfter, ?_, .refl target, ?_, by simp [sourceAfter], ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = _ at controlEq
    subst control
    rfl
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := related.targetStoreEq
      targetControlEq := related.targetControlEq
      adapted := CodeAdaptedWithSuffix.incPersistent_eq related.adapted
      stateRelated := related.stateRelated
      frameAligned := related.frameAligned }
  · apply compilerStructuredControlRank_lt_of_codeSilenceRank_lt
      (beforeCode := .inc objectId amount check true continuation)
      (afterCode := continuation) related.sourceControlEq
      (by simp [sourceAfter])
    simp [compilerCodeSilenceRank, compilerCodeSilenceDepth, sourceAfter,
      related.sourceControlEq]

/-- A persistent decrement has the same ranked zero-step target match as a
persistent increment. -/
theorem ConcreteStructuredCodeFocus.advance_decPersistent
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {objectId : Lean.FVarId}
    {amount : Nat}
    {check : Bool}
    {objectFields? : Option Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv
      (.dec objectId amount check true objectFields? continuation) targetStore
      targetLocals targetCode witness source target) :
    ∃ sourceAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 0 target target ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv continuation targetStore targetLocals targetCode
        witness sourceAfter target ∧
      sourceAfter.frames = source.frames ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  let sourceAfter : MachineState :=
    { source with control := .code continuation }
  refine ⟨sourceAfter, ?_, .refl target, ?_, by simp [sourceAfter], ?_⟩
  · rcases source with
      ⟨program, control, env, joins, frames, runtime⟩
    have controlEq := related.sourceControlEq
    change control = _ at controlEq
    subst control
    rfl
  · exact {
      sourceProgramEq := by simp [sourceAfter, related.sourceProgramEq]
      sourceControlEq := by simp [sourceAfter]
      sourceEnvEq := by simp [sourceAfter, related.sourceEnvEq]
      sourceRuntimeEq := by simp [sourceAfter, related.sourceRuntimeEq]
      targetStoreEq := related.targetStoreEq
      targetControlEq := related.targetControlEq
      adapted := CodeAdaptedWithSuffix.decPersistent_eq related.adapted
      stateRelated := related.stateRelated
      frameAligned := related.frameAligned }
  · apply compilerStructuredControlRank_lt_of_codeSilenceRank_lt
      (beforeCode := .dec objectId amount check true objectFields? continuation)
      (afterCode := continuation) related.sourceControlEq
      (by simp [sourceAfter])
    simp [compilerCodeSilenceRank, compilerCodeSilenceDepth, sourceAfter,
      related.sourceControlEq]

/-- Lift direct-call entry through the recursive caller stack.  This is the
first genuinely interprocedural `ConcreteStructuredStackRel` transition: the
local theorem supplies the source/target steps and callee focus, while the
frame theorem preserves every older suspended caller. -/
theorem ConcreteStructuredDirectCallEntryFocus.calleeResource
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target)
    (callerResource : ConcreteStructuredCurrentResource callerContext
      sourceModule callerFunction externals facts remainingBytes sourceRuntime
      callerEnv targetStore callerLocals witness) :
    ConcreteStructuredCurrentResource calleeContext sourceModule calleeFunction
      externals [] remainingBytes sourceRuntime site.calleeEnv targetStore
      (row.targetFunction.toLocals physicalArgs) witness := by
  have calleeFrame :=
    entry.calleeEntryRelativeCacheFrame callerResource.cacheFrame
  have calleeAbi :
      ClosureAllocationsAbiAligned calleeContext.program witness := by
    rw [row.contextProgram]
    exact callerResource.closureAbi
  exact ⟨sourceRuntime, targetStore, witness, calleeFrame, calleeAbi⟩

/-- Push a generated direct call onto the hereditary resource stack. -/
theorem ConcreteStructuredDirectCallEntryFocus.pushResourceStack
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {callerEntryRuntime sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerEntryStore targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {callerEntryWitness witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {callerFunctionResult : AbiKind}
    {tailResult : Option AbiKind}
    (entry : ConcreteStructuredDirectCallEntryFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target)
    (callerResources : ConcreteStructuredResourceStack callerContext.program
      callerContext sourceModule callerFunction externals callerEntryRuntime
      sourceRuntime callerEntryStore targetStore callerEntryWitness witness facts
      remainingBytes callerEnv callerLocals callerFunctionResult tailResult
      sourceFrames targetFrames)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals sourceRuntime targetStore witness []
      remainingBytes sourceRuntime site.calleeEnv targetStore
      (row.targetFunction.toLocals physicalArgs) witness) :
    ConcreteStructuredResourceStack callerContext.program calleeContext
      sourceModule calleeFunction externals sourceRuntime sourceRuntime
      targetStore targetStore witness witness [] remainingBytes site.calleeEnv
      (row.targetFunction.toLocals physicalArgs) site.calleeResultKind
      (some site.resultKind) source.frames target.frames := by
  rw [entry.sourceFramesEq, entry.targetFramesEq]
  exact ⟨calleeScope,
    .direct callerResources.current rfl entry.continuationAdapted
      entry.resultFound entry.resultKindAt site.calleeResultRefines
      callerResources.suspended⟩

theorem ConcreteStructuredDirectCallReadyFocus.advance_enter_stack
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    (related : ConcreteStructuredDirectCallReadyFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames) :
    ∃ (sourceAfter : MachineState) (targetAfter : StructuredWasmState Host),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 1 target
        targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter := by
  obtain ⟨sourceAfter, targetAfter, storedCallerLocals, _storedEq,
      sourceStep, targetPath, entry⟩ :=
    related.advance_enter (hostEnv := hostEnv) (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program := by
    calc
      sourceAfter.program = calleeContext.program :=
        entry.calleeFocus.sourceProgramEq
      _ = callerContext.program := row.contextProgram
      _ = source.program := related.sourceProgramEq.symm
  have tailAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceFrames targetFrames := by
    rw [sourceProgramEq]
    exact tail
  exact ⟨sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.directCallEntry entry,
      .directCallEntry entry tailAfter⟩⟩

/-- Resource-complete direct-call push.  The caller resource is stored
unchanged because argument staging and `enterCall` do not change the heap; the
callee receives empty local reuse facts and a fresh reflexive entry anchor. -/
theorem ConcreteStructuredDirectCallReadyFocus.advance_enter_stack_resource
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    (related : ConcreteStructuredDirectCallReadyFocus callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row labels sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals callerRemainder targetRest targetFrames witness
      physicalArgs resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames)
    (callerResource : ConcreteStructuredCurrentResource callerContext
      sourceModule callerFunction externals facts remainingBytes sourceRuntime
      callerEnv targetStore callerLocals witness) :
    ∃ (sourceAfter : MachineState) (targetAfter : StructuredWasmState Host),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 1 target
        targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter ∧
      ConcreteStructuredCurrentResource callerContext sourceModule
        callerFunction externals facts remainingBytes sourceRuntime callerEnv
        targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        witness ∧
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals sourceRuntime targetStore witness []
        remainingBytes sourceRuntime site.calleeEnv targetStore
        (row.targetFunction.toLocals physicalArgs) witness := by
  obtain ⟨sourceAfter, targetAfter, storedCallerLocals, storedEq,
      sourceStep, targetPath, entry⟩ :=
    related.advance_enter (hostEnv := hostEnv) (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program := by
    calc
      sourceAfter.program = calleeContext.program :=
        entry.calleeFocus.sourceProgramEq
      _ = callerContext.program := row.contextProgram
      _ = source.program := related.sourceProgramEq.symm
  have tailAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceFrames targetFrames := by
    rw [sourceProgramEq]
    exact tail
  subst storedCallerLocals
  have storedResource :
      ConcreteStructuredCurrentResource callerContext sourceModule
        callerFunction externals facts remainingBytes sourceRuntime callerEnv
        targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        witness :=
    callerResource.withValues _
  have calleeAbi :
      ClosureAllocationsAbiAligned calleeContext.program witness := by
    rw [row.contextProgram]
    exact storedResource.closureAbi
  have calleeScope :
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals sourceRuntime targetStore witness []
        remainingBytes sourceRuntime site.calleeEnv targetStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    ⟨entry.calleeEntryRelativeCacheFrame storedResource.cacheFrame,
      calleeAbi⟩
  exact ⟨sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.directCallEntry entry,
      .directCallEntry entry tailAfter⟩,
    storedResource, calleeScope⟩

/-- Lift the compiler-derived named-call staging prefix through an arbitrary
recursive caller stack. -/
theorem ConcreteStructuredCodeFocus.advance_directCall_stage_stack
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {expectedResult : Option AbiKind}
    (localsAligned : LocalLayoutAligned callerContext callerFunction)
    (related : ConcreteStructuredCodeFocus callerContext sourceModule
      callerFunction labels sourceRuntime callerEnv (.let decl continuation)
      targetStore targetLocals targetCode witness source target)
    (frames : ConcreteStructuredFrameRel source.program sourceRuntime
      targetStore witness expectedResult source.frames target.frames) :
    ∃ (targetArguments : Wasm.Program) (sourceAfter : MachineState)
        (targetAfter : StructuredWasmState Host),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv)
        targetArguments.length target targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter := by
  obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest,
      sourceAfter, targetAfter, sourceStep, targetPath, ready⟩ :=
    related.advance_directCall_stage (targetModule := targetModule)
      (hostEnv := hostEnv) (externals := externals) site row localsAligned
  have sourceProgramEq : sourceAfter.program = source.program :=
    ready.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness expectedResult source.frames target.frames := by
    rw [sourceProgramEq]
    exact frames
  exact ⟨targetArguments, sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.directCallReady ready,
      .directCallReady ready framesAfter⟩⟩

/-- Lift the silent source staging step of an exactly saturated closure call
through an arbitrary recursive caller stack.  The strict rank fact is kept
for the eventual relation-wide anti-stuttering proof. -/
theorem ConcreteStructuredCodeFocus.advance_saturatedCall_stage_stack
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    {labels : List Lean.FVarId}
    {sourceRuntime : RuntimeState}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {expectedResult : Option AbiKind}
    (localsAligned : LocalLayoutAligned context sourceFunction)
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime callerEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (frames : ConcreteStructuredFrameRel source.program sourceRuntime
      targetStore witness expectedResult source.frames target.frames) :
    ∃ sourceAfter : MachineState,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 0 target
        target ∧
      ConcreteStructuredStackRel sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨targetValue, targetRest, resultIndex, sourceAfter, sourceStep,
      targetPath, ready, rank⟩ :=
    related.advance_saturatedCall_stage_ranked (targetModule := targetModule)
      (hostEnv := hostEnv) site localsAligned
  have sourceProgramEq : sourceAfter.program = source.program :=
    ready.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness expectedResult source.frames target.frames := by
    rw [sourceProgramEq]
    exact frames
  exact ⟨sourceAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.saturatedCallReady ready,
      .saturatedCallReady ready framesAfter⟩, rank⟩

/-- Consume a staged saturated closure call while transporting the complete
older caller stack across the matcher/closure-consumption heap update.  The
resource premises are the existing source/runtime admission and cache/ABI
invariant; the theorem still derives the target matcher, arguments, selected
callee, path, and pushed frame itself. -/
theorem ConcreteStructuredSaturatedCallReadyFocus.advance_enter_stack
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {tailResult : Option AbiKind}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
      sourceFunction targetModule site labels sourceRuntime continuation
      callerJoins sourceFrames targetStore callerLocals targetValue targetRest
      targetFrames witness resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames)
    (invariant : ConcreteReuseCapacityCacheAbiFrame context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime callerEnv
      targetStore callerLocals witness)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    ∃ (sourceEntry : MachineState) (targetEntry : StructuredWasmState Host)
        (nextStore : Wasm.Store Host) (physicalArgs : List Wasm.Value)
        (matcherCount argumentCount : Nat),
      executeStep externals source = .next sourceEntry ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (matcherCount + 1) + argumentCount + 1) target targetEntry ∧
      ConcreteStructuredStackRel sourceEntry targetEntry ∧
      ClosureMatchStoreFrame targetStore nextStore ∧
      HeaderCapacityTransport targetStore.host.runtime.heap
        nextStore.host.runtime.heap witness ∧
      ConcreteReuseCapacityCacheFrame sourceModule sourceFunction externals
        facts remainingBytes callRuntime callerEnv nextStore callerLocals
        witness ∧
      ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals
        [] remainingBytes callRuntime resolution.calleeEnv nextStore
        (row.targetFunction.toLocals physicalArgs) witness := by
  obtain ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
      argumentCount, sourceStep, targetPath, entry, matcherFrame,
      matcherCapacity, calleeFrame⟩ :=
    related.advance_enter resolution row spec invariant sharedCapacity
      application
  have matcherTables :
      ClosureTablesTransport targetStore nextStore witness witness := {
    hostDispatchPreserved := matcherFrame.dispatch
    witnessDispatchPreserved := rfl
    hostDescriptorsPreserved := matcherFrame.descriptors
    witnessDescriptorsPreserved := rfl }
  have matcherTransports :
      ReuseCapacityCodeEntryTransports sourceRuntime callRuntime targetStore
        nextStore witness witness :=
    (ReuseCapacityCodeEntryTransports.refl sourceRuntime targetStore
      witness).step (WitnessTransport.refl witness)
        (ClosureAllocationsPersistent.refl witness) matcherCapacity
        (takeClosureApplication_ordinaryPersistenceTransport application)
        matcherFrame.externals matcherTables
  have transportedTail :
      ConcreteStructuredFrameRel source.program callRuntime nextStore witness
        tailResult sourceFrames targetFrames :=
    tail.transport matcherTransports entry.calleeFocus.stateRelated
  have sourceProgramEq : sourceEntry.program = source.program := by
    calc
      sourceEntry.program = calleeContext.program :=
        entry.calleeFocus.sourceProgramEq
      _ = context.program := row.contextProgram
      _ = source.program := related.sourceProgramEq.symm
  have tailAfter :
      ConcreteStructuredFrameRel sourceEntry.program callRuntime nextStore
        witness tailResult sourceFrames targetFrames := by
    rw [sourceProgramEq]
    exact transportedTail
  have calleeEntryFrame :
      ConcreteReuseCapacityCacheFrame sourceModule calleeFunction externals []
        remainingBytes callRuntime resolution.calleeEnv nextStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    (entry.calleeEntryRelativeCacheFrame calleeFrame).1
  exact ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
    argumentCount, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.saturatedCallEntry entry,
      .saturatedCallEntry entry tailAfter⟩,
    matcherFrame, matcherCapacity, calleeFrame, calleeEntryFrame⟩

/-- Resource-complete saturated-call push.

The suspended caller keeps its original outer entry anchor while its current
cache frame and transports advance through closure consumption.  The generated
callee starts a fresh reflexive anchor with empty local reuse facts and the
same remaining allocation budget.  This is the pointwise replacement for the
terminating hereditary induction's recursive-call bookkeeping. -/
theorem ConcreteStructuredSaturatedCallReadyFocus.advance_enter_stack_resource
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {tailResult : Option AbiKind}
    (spec : ConcreteSupportedFunction program context sourceCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
      sourceFunction targetModule site labels sourceRuntime continuation
      callerJoins sourceFrames targetStore callerLocals targetValue targetRest
      targetFrames witness resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames)
    (callerResource : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime callerEnv
      targetStore callerLocals witness)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    ∃ (sourceEntry : MachineState) (targetEntry : StructuredWasmState Host)
        (nextStore : Wasm.Store Host) (physicalArgs : List Wasm.Value)
        (matcherCount argumentCount : Nat),
      executeStep externals source = .next sourceEntry ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (matcherCount + 1) + argumentCount + 1) target targetEntry ∧
      ConcreteStructuredStackRel sourceEntry targetEntry ∧
      ConcreteStructuredCurrentResource context sourceModule sourceFunction
        externals facts remainingBytes callRuntime callerEnv nextStore
        callerLocals witness ∧
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals callRuntime nextStore witness []
        remainingBytes callRuntime resolution.calleeEnv nextStore
        (row.targetFunction.toLocals physicalArgs) witness := by
  rcases callerResource with
    ⟨entryRuntime, entryStore, entryWitness, callerEntry, callerAbi⟩
  have invariant :
      ConcreteReuseCapacityCacheAbiFrame context sourceModule sourceFunction
        externals facts remainingBytes sourceRuntime callerEnv targetStore
        callerLocals witness :=
    ⟨callerEntry.1, callerAbi⟩
  obtain ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
      argumentCount, sourceStep, targetPath, stack, matcherFrame,
      matcherCapacity, callerAfterFrame, calleeFrame⟩ :=
    related.advance_enter_stack resolution row spec tail invariant
      sharedCapacity application
  have matcherTables :
      ClosureTablesTransport targetStore nextStore witness witness := {
    hostDispatchPreserved := matcherFrame.dispatch
    witnessDispatchPreserved := rfl
    hostDescriptorsPreserved := matcherFrame.descriptors
    witnessDescriptorsPreserved := rfl }
  have storedCaller :
      ConcreteStructuredCurrentResource context sourceModule sourceFunction
        externals facts remainingBytes callRuntime callerEnv nextStore
        callerLocals witness := by
    exact ⟨entryRuntime, entryStore, entryWitness,
      ⟨callerAfterFrame, callerEntry.2.step
        (WitnessTransport.refl witness)
        (ClosureAllocationsPersistent.refl witness) matcherCapacity
        (takeClosureApplication_ordinaryPersistenceTransport application)
        matcherFrame.externals matcherTables⟩,
      callerAbi⟩
  have calleeAbi :
      ClosureAllocationsAbiAligned calleeContext.program witness := by
    rw [row.contextProgram]
    exact callerAbi
  have calleeScope :
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals callRuntime nextStore witness []
        remainingBytes callRuntime resolution.calleeEnv nextStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    ConcreteStructuredResourceScope.root ⟨calleeFrame, calleeAbi⟩
  exact ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
    argumentCount, sourceStep, targetPath, stack, storedCaller,
    calleeScope⟩

/-- Lift ordinary call-frame resumption back to compiled caller code while
discarding exactly one recursive frame constructor. -/
theorem ConcreteStructuredBindFrameFocus.advance_stack
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter := by
  obtain ⟨sourceAfter, targetAfter, _updated, _resumedLocals, sourceStep,
      targetPath, _targetSet, _resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program :=
    focus.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact tail
  exact ⟨sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.code focus, .code focus framesAfter⟩⟩

/-- Resource-complete direct return/pop.

The exposed callee entry boundary must be the state stored by the suspended
caller.  The return protocol then composes all callee transports into the
caller's outer entry scope, erases exactly the result's stale reuse fact, and
removes one structural call frame. -/
theorem ConcreteStructuredBindFrameFocus.advance_stack_resource
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {callRuntime sourceRuntime : RuntimeState}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callStore targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {callWitness witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames)
    (storedCaller : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts callerBytes callRuntime callerEnv callStore
      callerLocals callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore calleeLocals witness)
    (programEq : calleeContext.program = context.program) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter ∧
      ConcreteStructuredCurrentResource context sourceModule sourceFunction
        externals (eraseReuseCapacityFact facts result) resultBytes
        sourceRuntime (bind callerEnv result sourceValue) targetStore
        resumedLocals witness := by
  obtain ⟨sourceAfter, targetAfter, _updated, resumedLocals, sourceStep,
      targetPath, targetSet, resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program :=
    focus.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact tail
  have resumedUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical := by
    rw [resumedEq]
    have base := FirTalos.Correctness.localUpdate_of_set? targetSet
    refine ⟨?_, ?_⟩
    · simpa [Wasm.Locals.get] using base.1
    · intro other different
      simpa [Wasm.Locals.get] using base.2 different
  rcases storedCaller with
    ⟨outerRuntime, outerStore, outerWitness, callerFrame, _callerAbi⟩
  have resumedResource :=
    ConcreteStructuredCurrentResource.restoreCaller callerFrame calleeScope
      programEq focus.stateRelated focus.frameAligned related.resultFound
      resumedUpdate
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.code focus, .code focus framesAfter⟩,
    resumedResource⟩

/-- Pop a direct call from the unified hereditary resource stack. -/
theorem ConcreteStructuredBindFrameFocus.advance_popResourceStack
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {outerRuntime callRuntime sourceRuntime : RuntimeState}
    {outerStore callStore targetStore : Wasm.Store Host}
    {outerWitness callWitness witness : RefinementWitness}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target)
    (callerScope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals outerRuntime outerStore outerWitness facts
      callerBytes callRuntime callerEnv callStore callerLocals callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore calleeLocals witness)
    (programEq : calleeContext.program = context.program)
    (tail : ConcreteStructuredSuspendedResourceStack externals context.program
      outerRuntime outerStore outerWitness callerFunctionResult tailResult
      sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind callerEnv result sourceValue) continuation
        targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceAfter.frames targetAfter.frames ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨sourceAfter, targetAfter, _updated, resumedLocals, sourceStep,
      targetPath, targetSet, resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have resumedUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical := by
    rw [resumedEq]
    have base := FirTalos.Correctness.localUpdate_of_set? targetSet
    refine ⟨?_, ?_⟩
    · simpa [Wasm.Locals.get] using base.1
    · intro other different
      simpa [Wasm.Locals.get] using base.2 different
  have restoredScope :=
    callerScope.restoreCaller calleeScope programEq focus.stateRelated
      focus.frameAligned related.resultFound resumedUpdate
  have resources :
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceFrames targetFrames :=
    ⟨restoredScope, tail⟩
  have sourceProgramEq : sourceAfter.program = context.program :=
    focus.sourceProgramEq
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact resources.frameRel
  have stack : ConcreteStructuredStackRel sourceAfter targetAfter := by
    exact ⟨ConcreteStructuredControlRel.code focus,
      .code focus framesAfter⟩
  have resourcesAfter :
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact resources
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    stack, focus, resourcesAfter, sourceFramesEq, targetFramesEq⟩

/-- Saturated closure resumption removes the call frame and every generated
matcher label as one source-level frame transition, exposing the same older
recursive suffix. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance_stack
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {callerFunctionResult : AbiKind}
    {tailResult : Option AbiKind}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5) target
        targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter := by
  obtain ⟨sourceAfter, targetAfter, _updated, _resumedLocals, sourceStep,
      targetPath, _targetSet, _resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program :=
    focus.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact tail
  exact ⟨sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.code focus, .code focus framesAfter⟩⟩

/-- Resource-complete saturated-closure return/pop.  Matcher labels affect the
target path length but not the resource composition rule: the exact callee
entry scope is folded into the suspended caller in the same way as a direct
call. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance_stack_resource
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {callRuntime sourceRuntime : RuntimeState}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callStore targetStore : Wasm.Store Host}
    {callerLocals calleeLocals : Wasm.Locals}
    {scopeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {callWitness witness : RefinementWitness}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target)
    (tail : ConcreteStructuredFrameRel source.program sourceRuntime targetStore
      witness tailResult sourceFrames targetFrames)
    (storedCaller : ConcreteStructuredCurrentResource context sourceModule
      sourceFunction externals facts callerBytes callRuntime callerEnv callStore
      callerLocals callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore scopeLocals witness)
    (programEq : calleeContext.program = context.program) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5) target
        targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter ∧
      ConcreteStructuredCurrentResource context sourceModule sourceFunction
        externals (eraseReuseCapacityFact facts result) resultBytes
        sourceRuntime (bind callerEnv result sourceValue) targetStore
        resumedLocals witness := by
  obtain ⟨sourceAfter, targetAfter, _updated, resumedLocals, sourceStep,
      targetPath, targetSet, resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have sourceProgramEq : sourceAfter.program = source.program :=
    focus.sourceProgramEq.trans related.sourceProgramEq.symm
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact tail
  have resumedUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical := by
    rw [resumedEq]
    have base := FirTalos.Correctness.localUpdate_of_set? targetSet
    refine ⟨?_, ?_⟩
    · simpa [Wasm.Locals.get] using base.1
    · intro other different
      simpa [Wasm.Locals.get] using base.2 different
  rcases storedCaller with
    ⟨outerRuntime, outerStore, outerWitness, callerFrame, _callerAbi⟩
  have resumedResource :=
    ConcreteStructuredCurrentResource.restoreCaller callerFrame calleeScope
      programEq focus.stateRelated focus.frameAligned related.resultFound
      resumedUpdate
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    ⟨ConcreteStructuredControlRel.code focus, .code focus framesAfter⟩,
    resumedResource⟩

/-- Pop a saturated closure call and all matcher labels from the unified
hereditary resource stack. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance_popResourceStack
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {outerRuntime callRuntime sourceRuntime : RuntimeState}
    {outerStore callStore targetStore : Wasm.Store Host}
    {outerWitness callWitness witness : RefinementWitness}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals calleeLocals scopeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target)
    (callerScope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals outerRuntime outerStore outerWitness facts
      callerBytes callRuntime callerEnv callStore
      { callerLocals with
        values := physicalArgs.reverse ++ callerRemainder }
      callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore scopeLocals witness)
    (programEq : calleeContext.program = context.program)
    (tail : ConcreteStructuredSuspendedResourceStack externals context.program
      outerRuntime outerStore outerWitness callerFunctionResult tailResult
      sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5) target
        targetAfter ∧
      ConcreteStructuredStackRel sourceAfter targetAfter ∧
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime (bind callerEnv result sourceValue) continuation
        targetStore resumedLocals targetRest witness sourceAfter targetAfter ∧
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceAfter.frames targetAfter.frames ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨sourceAfter, targetAfter, _updated, resumedLocals, sourceStep,
      targetPath, targetSet, resumedEq, focus, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.advance (module := module) (hostEnv := hostEnv)
      (externals := externals)
  have resumedUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical := by
    rw [resumedEq]
    have base := FirTalos.Correctness.localUpdate_of_set? targetSet
    refine ⟨?_, ?_⟩
    · simpa [Wasm.Locals.get] using base.1
    · intro other different
      simpa [Wasm.Locals.get] using base.2 different
  have callerScopeBase :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals outerRuntime outerStore outerWitness facts callerBytes
        callRuntime callerEnv callStore callerLocals callWitness := by
    simpa using callerScope.withValues callerLocals.values
  have restoredScope :=
    callerScopeBase.restoreCaller calleeScope programEq focus.stateRelated
      focus.frameAligned related.resultFound resumedUpdate
  have resources :
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceFrames targetFrames :=
    ⟨restoredScope, tail⟩
  have sourceProgramEq : sourceAfter.program = context.program :=
    focus.sourceProgramEq
  have framesAfter :
      ConcreteStructuredFrameRel sourceAfter.program sourceRuntime targetStore
        witness tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceProgramEq, sourceFramesEq, targetFramesEq]
    exact resources.frameRel
  have stack : ConcreteStructuredStackRel sourceAfter targetAfter :=
    ⟨ConcreteStructuredControlRel.code focus, .code focus framesAfter⟩
  have resourcesAfter :
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals outerRuntime sourceRuntime outerStore
        targetStore outerWitness witness (eraseReuseCapacityFact facts result)
        resultBytes (bind callerEnv result sourceValue) resumedLocals
        callerFunctionResult tailResult sourceAfter.frames targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact resources
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    stack, focus, resourcesAfter, sourceFramesEq, targetFramesEq⟩

/-! ## Source-only pointwise admission

The terminating hereditary evaluator above remains useful for finite endpoint
theorems, but it is intentionally not part of the compiler simulation
relation. The following judgment classifies only the current source node and
its exact one-step allocation requirement. It contains no source step, future
fact transfer, continuation admission, final value, final state, target path,
or recursive evaluation derivation.
-/

/-- The admitted direct-value family contains no function application.  This
small static inversion is what lets an ordinary successful interpreter step
recover a `SourceLetResult`, while named and closure calls remain in their
separate pointwise branches. -/
theorem ReuseBudgetedDirectSupported.value_ne_fap
    {context : Fir.Wasm.Context} {facts : ReuseCapacityFacts}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    (supported : ReuseBudgetedDirectSupported context facts decl)
    (name : Lean.Name) (args : Array (Lean.Compiler.LCNF.Arg .impure)) :
    decl.value ≠ .fap name args := by
  intro valueEq
  rcases supported with constructorBox | naturalOrString
  · rcases constructorBox with readOnlyConstructor | box
    · rcases readOnlyConstructor with readOnly | constructor
      · rcases readOnly with reuseReaders | unboxShared
        · rcases reuseReaders with reuseAlias | readers
          · rcases reuseAlias with reuse | localAlias
            · cases reuse
              simp_all
            · cases localAlias
              simp_all
          · rcases readers with immediate | usizeOrObjectOrScalar
            · cases immediate
              simp_all
            · rcases usizeOrObjectOrScalar with usize | objectOrScalar
              · cases usize
                simp_all
              · rcases objectOrScalar with object | scalar
                · cases object
                  simp_all
                · cases scalar
                  simp_all
        · rcases unboxShared with unbox | shared
          · cases unbox
            simp_all
          · cases shared
            simp_all
      · cases constructor
        simp_all
    · cases box
      simp_all
  · rcases naturalOrString with natural | string
    · cases natural
      simp_all
    · cases string
      simp_all

/-- A direct-value local reference is an alias, never an application: its
argument array is empty. -/
theorem ReuseBudgetedDirectSupported.fvar_args_eq_empty
    {context : Fir.Wasm.Context} {facts : ReuseCapacityFacts}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    (supported : ReuseBudgetedDirectSupported context facts decl)
    (id : Lean.FVarId) (args : Array (Lean.Compiler.LCNF.Arg .impure))
    (valueEq : decl.value = .fvar id args) :
    args = #[] := by
  rcases supported with constructorBox | naturalOrString
  · rcases constructorBox with readOnlyConstructor | box
    · rcases readOnlyConstructor with readOnly | constructor
      · rcases readOnly with reuseReaders | unboxShared
        · rcases reuseReaders with reuseAlias | readers
          · rcases reuseAlias with reuse | localAlias
            · cases reuse
              simp_all
            · cases localAlias
              simp_all
          · rcases readers with immediate | usizeOrObjectOrScalar
            · cases immediate
              simp_all
            · rcases usizeOrObjectOrScalar with usize | objectOrScalar
              · cases usize
                simp_all
              · rcases objectOrScalar with object | scalar
                · cases object
                  simp_all
                · cases scalar
                  simp_all
        · rcases unboxShared with unbox | shared
          · cases unbox
            simp_all
          · cases shared
            simp_all
      · cases constructor
        simp_all
    · cases box
      simp_all
  · rcases naturalOrString with natural | string
    · cases natural
      simp_all
    · cases string
      simp_all

/-- Structural admission for the first pointwise code fragment.

Admission classifies only the current code node and its exact one-step
allocation requirement. It deliberately does not store continuation
admission: future state-dependent call/case/effect classification is attached
only after the corresponding source step has occurred. Dynamic source results
are therefore never stored in this judgment. -/
inductive ConcreteStructuredCodeStepAdmission
    (context : Fir.Wasm.Context) (externals : ExternalImpl)
    (expectedResult : AbiKind) :
    ReuseCapacityFacts → RuntimeState → Env → Nat →
      Lean.Compiler.LCNF.Code .impure → Prop where
  | ret
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {result : Lean.FVarId} {actualResult : AbiKind}
      (resultCompiled :
        Fir.Wasm.getLocal context result =
          .ok (.localGet result, actualResult))
      (resultRefines : actualResult.refines expectedResult = true) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 (.return result)
  | directLet
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : ReuseBudgetedDirectSupported context facts decl) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv (directLetAllocationCost decl)
        (.let decl continuation)
  | pureExternal
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {sourceValue : Value}
      {stepCost : Nat}
      (supported : PureExternalSupported context externals sourceRuntime
        sourceEnv decl continuation nextRuntime sourceValue stepCost) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv stepCost (.let decl continuation)
  | directCall
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      (site : DirectInternalCallSite context decl sourceEnv) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 (.let decl continuation)
  | saturatedCall
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      (site : SaturatedClosureCallSite context decl sourceEnv)
      (resolution : SaturatedClosureCallResolution context sourceRuntime site)
      (sharedCapacity : ∀ parentRuntime,
        setCell sourceRuntime resolution.location
            { resolution.cell with rc := resolution.cell.rc - 1 } =
              .ok parentRuntime →
          ClosureRetainCapacity parentRuntime resolution.captures.toList) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 (.let decl continuation)
  | defaultOnlyCase
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {cases : Lean.Compiler.LCNF.Cases .impure}
      {selected : Lean.Compiler.LCNF.Code .impure}
      (supported :
        DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 (.cases cases)
  | singleObjectCase
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {cases : Lean.Compiler.LCNF.Cases .impure}
      {selected : Lean.Compiler.LCNF.Code .impure}
      (supported : SingleObjectConstructorCaseSupported context sourceRuntime
        sourceEnv cases selected) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 (.cases cases)
  | incPersistent
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {objectId : Lean.FVarId}
      {amount : Nat}
      {check : Bool}
      {continuation : Lean.Compiler.LCNF.Code .impure} :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0
          (.inc objectId amount check true continuation)
  | decPersistent
      {facts : ReuseCapacityFacts}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {objectId : Lean.FVarId}
      {amount : Nat}
      {check : Bool}
      {objectFields? : Option Nat}
      {continuation : Lean.Compiler.LCNF.Code .impure} :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0
          (.dec objectId amount check true objectFields? continuation)
  | ordinaryIncrement
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : OrdinaryIncrementEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | ordinaryDecrement
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : OrdinaryDecrementEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | ordinaryDelete
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : OrdinaryDeleteEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | constructorTag
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : ConstructorTagEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | objectFieldFVar
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : ObjectFieldFVarEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | objectFieldErased
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : ObjectFieldErasedEffectSupported context sourceRuntime
        sourceEnv code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | usizeField
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : USizeFieldEffectSupported context sourceRuntime sourceEnv
        code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code
  | scalarField
      {facts : ReuseCapacityFacts}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {code continuation : Lean.Compiler.LCNF.Code .impure}
      (supported : ScalarFieldEffectSupported context sourceRuntime sourceEnv
        code continuation nextRuntime) :
      ConcreteStructuredCodeStepAdmission context externals expectedResult facts
        sourceRuntime sourceEnv 0 code

/-- Ordinary increment admission already contains the successful semantic
lookup and update for the current state.  Those facts construct the canonical
one-step source law; no execution derivation needs to be retained in the
pointwise relation. -/
theorem OrdinaryIncrementEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : OrdinaryIncrementEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | inc sourceRuntime nextRuntime sourceEnv objectId amount check continuation
      objectKind sourceObject objectCompiled objectRefines objectLookup updated
      fits =>
      intro externals
      simp [executeStep, coreStep, objectLookup, updated]

/-- Ordinary decrement admission likewise determines its canonical successful
source effect step. Recursive release remains an implementation fact derived
from the concrete runtime relation, not evidence stored in admission. -/
theorem OrdinaryDecrementEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : OrdinaryDecrementEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | dec sourceRuntime nextRuntime sourceEnv objectId amount check objectFields?
      continuation objectKind sourceObject objectCompiled objectRefines
      objectLookup updated =>
      intro externals
      simp [executeStep, coreStep, objectLookup, updated]

/-- Explicit-delete admission determines the canonical successful source
effect step, including the erased reset token. The target zero no-op and
ordinary heap update remain consequences of concrete refinement. -/
theorem OrdinaryDeleteEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : OrdinaryDeleteEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | del sourceRuntime nextRuntime sourceEnv objectId continuation objectKind
      sourceObject objectCompiled objectLookup updated =>
      intro externals
      simp [executeStep, coreStep, objectLookup, updated]

/-- Constructor-tag admission determines the canonical successful source
effect step without retaining an execution certificate. -/
theorem ConstructorTagEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : ConstructorTagEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | setTag sourceRuntime nextRuntime sourceEnv objectId tag continuation
      location cell semantic objectCompiled objectLookup updated found live
      objectEq tagFits =>
      intro externals
      simp [executeStep, coreStep, objectLookup, updated]

/-- FVar object-field admission determines the canonical successful source
effect step; the physical operand remains a refinement consequence. -/
theorem ObjectFieldFVarEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : ObjectFieldFVarEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | oset sourceRuntime nextRuntime sourceEnv objectId fieldId index continuation
      location cell semantic field fieldKind objectCompiled fieldCompiled
      fieldObjectKind objectLookup fieldLookup updated found live objectEq
      indexValid fieldKindAligned =>
      intro externals
      change evalArg sourceEnv (.fvar fieldId) = .ok field at fieldLookup
      simp [executeStep, coreStep, objectLookup, fieldLookup, updated]

/-- Erased object-field admission determines the canonical successful source
effect step, including evaluation of the erased argument. -/
theorem ObjectFieldErasedEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : ObjectFieldErasedEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | oset sourceRuntime nextRuntime sourceEnv objectId index continuation
      location cell semantic objectCompiled objectLookup updated found live
      objectEq indexValid fieldKindAligned =>
      intro externals
      simp [executeStep, coreStep, evalArg, objectLookup, updated]

/-- `USize` field admission determines the canonical successful source effect
step; slot bounds are consumed only by the concrete refinement theorem. -/
theorem USizeFieldEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : USizeFieldEffectSupported context sourceRuntime sourceEnv code
      continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | uset sourceRuntime nextRuntime sourceEnv objectId fieldId index continuation
      location cell semantic field objectCompiled fieldCompiled objectLookup
      fieldLookup updated found live objectEq slotStart slotEnd =>
      intro externals
      simp [executeStep, coreStep, objectLookup, fieldLookup, updated]

/-- Packed-scalar field admission determines the canonical successful source
effect step; descriptor/layout safety is used only to justify target memory. -/
theorem ScalarFieldEffectSupported.sourceEffectResult
    {context : Fir.Wasm.Context}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {code continuation : Lean.Compiler.LCNF.Code .impure}
    (supported : ScalarFieldEffectSupported context sourceRuntime sourceEnv code
      continuation nextRuntime) :
    SourceEffectResult context sourceRuntime nextRuntime sourceEnv code
      continuation := by
  cases supported with
  | sset sourceRuntime nextRuntime sourceEnv objectId fieldId slotIndex
      byteOffset type continuation location cell semantic field fieldKind
      objectCompiled fieldCompiled objectLookup fieldLookup updated found live
      objectEq layoutSafe =>
      intro externals
      simp [executeStep, coreStep, objectLookup, fieldLookup, updated]

theorem ConcreteStructuredCodeStepAdmission.return_cases
    {context : Fir.Wasm.Context} {externals : ExternalImpl}
    {expectedResult : AbiKind}
    {facts : ReuseCapacityFacts} {sourceRuntime : RuntimeState}
    {sourceEnv : Env} {requiredBytes : Nat}
    {result : Lean.FVarId}
    (admitted : ConcreteStructuredCodeStepAdmission context externals
      expectedResult facts
      sourceRuntime sourceEnv requiredBytes (.return result)) :
    ∃ actualResult,
      Fir.Wasm.getLocal context result =
          .ok (.localGet result, actualResult) ∧
      actualResult.refines expectedResult = true ∧
      requiredBytes = 0 := by
  cases admitted with
  | ret resultCompiled resultRefines =>
      exact ⟨_, resultCompiled, resultRefines, rfl⟩
  | ordinaryIncrement supported => cases supported
  | ordinaryDecrement supported => cases supported
  | ordinaryDelete supported => cases supported
  | constructorTag supported => cases supported
  | objectFieldFVar supported => cases supported
  | objectFieldErased supported => cases supported
  | usizeField supported => cases supported
  | scalarField supported => cases supported

/-- Exhaustive inversion of an admitted `let` node.

The closure-call branch exposes the heap-dependent resolution and ownership
capacity that prove exact saturation.  Consequently a merely nonempty closure
application cannot be classified as a saturated call. -/
theorem ConcreteStructuredCodeStepAdmission.let_cases
    {context : Fir.Wasm.Context} {externals : ExternalImpl}
    {expectedResult : AbiKind}
    {facts : ReuseCapacityFacts} {sourceRuntime : RuntimeState}
    {sourceEnv : Env} {requiredBytes : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    (admitted : ConcreteStructuredCodeStepAdmission context externals
      expectedResult facts
      sourceRuntime sourceEnv requiredBytes (.let decl continuation)) :
    (requiredBytes = directLetAllocationCost decl ∧
        ReuseBudgetedDirectSupported context facts decl) ∨
      (∃ nextRuntime sourceValue stepCost,
        requiredBytes = stepCost ∧
          PureExternalSupported context externals sourceRuntime sourceEnv decl
            continuation nextRuntime sourceValue stepCost) ∨
      (∃ site : DirectInternalCallSite context decl sourceEnv,
        requiredBytes = 0) ∨
      ∃ (site : SaturatedClosureCallSite context decl sourceEnv)
          (resolution :
            SaturatedClosureCallResolution context sourceRuntime site),
        requiredBytes = 0 ∧
          ∀ parentRuntime,
            setCell sourceRuntime resolution.location
                { resolution.cell with rc := resolution.cell.rc - 1 } =
                  .ok parentRuntime →
              ClosureRetainCapacity parentRuntime
                resolution.captures.toList := by
  cases admitted with
  | directLet supported =>
      exact .inl ⟨rfl, supported⟩
  | pureExternal supported =>
      exact .inr (.inl ⟨_, _, _, rfl, supported⟩)
  | directCall site =>
      exact .inr (.inr (.inl ⟨site, rfl⟩))
  | saturatedCall site resolution sharedCapacity =>
      exact .inr (.inr (.inr ⟨site, resolution, rfl, sharedCapacity⟩))
  | ordinaryIncrement supported => cases supported
  | ordinaryDecrement supported => cases supported
  | ordinaryDelete supported => cases supported
  | constructorTag supported => cases supported
  | objectFieldFVar supported => cases supported
  | objectFieldErased supported => cases supported
  | usizeField supported => cases supported
  | scalarField supported => cases supported

/-- Compatibility between an active generated function's result ABI and the
optional ABI expected by its suspended caller. -/
def ConcreteStructuredResultCompatible
    (functionResult : AbiKind) : Option AbiKind → Prop
  | none => True
  | some expected => functionResult.refines expected = true

/-- The resource stack also owns hereditary static result-ABI metadata. Its
call constructors retain the outer function result kind, and this projection
recovers compatibility for the active function without a parallel stack that
could disagree about which call protocol was pushed. -/
theorem ConcreteStructuredSuspendedResourceStack.resultCompatible
    {externals : ExternalImpl}
    {program : Fir.LeanIR.ImpureProgram}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {sourceFrames : List Frame}
    {targetFrames : List StructuredWasmFrame}
    (stack : ConcreteStructuredSuspendedResourceStack externals program
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      sourceFrames targetFrames) :
    ConcreteStructuredResultCompatible functionResult callerExpectedResult := by
  induction stack with
  | nil => trivial
  | case _tail ih => exact ih
  | direct _ _ _ _ _ compatible _tail _ih => exact compatible
  | saturated _ _ _ _ _ compatible _tail _ih => exact compatible

/-- Compiler focus plus hereditary resources, independent of which source
operation family is currently admitted.  This is the invariant preserved by
local operation theorems before the relation-wide source-step classifier
attaches admission for the successor state. -/
structure ConcreteStructuredCodeCoreRel
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (sourceCode : Lean.Compiler.LCNF.Code .impure)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (targetCode : Wasm.Program)
    (witness : RefinementWitness)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  focus : ConcreteStructuredCodeFocus context sourceModule sourceFunction
    labels sourceRuntime sourceEnv sourceCode targetStore targetLocals
    targetCode witness source target
  resources : ConcreteStructuredResourceStack program context sourceModule
    sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
    entryWitness witness facts remainingBytes sourceEnv targetLocals
    functionResult callerExpectedResult source.frames target.frames

/-- Core relation while a compiler-generated named call has evaluated its
argument prefix and both machines are poised to enter the callee. The active
caller resources and hereditary result metadata remain indexed by the frames
that preceded this call; the ready focus records the newly staged bind/call
protocol around them. -/
structure ConcreteStructuredDirectCallReadyCoreRel
    (program : Fir.LeanIR.ImpureProgram)
    (callerContext calleeContext : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (callerFunction calleeFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  ready : ConcreteStructuredDirectCallReadyFocus callerContext calleeContext
    sourceModule callerFunction calleeFunction targetModule site row labels
    sourceRuntime continuation callerJoins sourceFrames targetStore callerLocals
    callerRemainder targetRest targetFrames witness physicalArgs resultIndex
    source target
  resources : ConcreteStructuredResourceStack program callerContext
    sourceModule callerFunction externals entryRuntime sourceRuntime entryStore
    targetStore entryWitness witness facts remainingBytes callerEnv callerLocals
    functionResult callerExpectedResult sourceFrames targetFrames

theorem ConcreteStructuredDirectCallReadyCoreRel.observes
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallReadyCoreRel program callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs resultIndex
      source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.ready.observes

/-- Core relation after the source has staged an exactly saturated closure
call and before the closure/matcher transition. The target is unchanged, so
the caller's complete hereditary resources remain available verbatim. -/
structure ConcreteStructuredSaturatedCallReadyCoreRel
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (targetValue targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  ready : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
    sourceFunction targetModule site labels sourceRuntime continuation
    callerJoins sourceFrames targetStore callerLocals targetValue targetRest
    targetFrames witness resultIndex source target
  resources : ConcreteStructuredResourceStack program context sourceModule
    sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
    entryWitness witness facts remainingBytes callerEnv callerLocals
    functionResult callerExpectedResult sourceFrames targetFrames

theorem ConcreteStructuredSaturatedCallReadyCoreRel.observes
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : SaturatedClosureCallSite context decl callerEnv}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
      sourceModule sourceFunction targetModule site externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals targetValue targetRest targetFrames witness
      resultIndex source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.ready.observes

/--
Hereditary resource relation at the staged pure-external call boundary.

The saved source bind frame is protocol state, not a suspended generated
function call.  Consequently the resource and supported stacks remain
indexed by the caller frames beneath it while the focus records the exact
poised import and compiler-selected destination.
-/
structure ConcreteStructuredExternalCallReadyCoreRel
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (externals : ExternalImpl)
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceValue : Value}
    {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    (site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost)
    (operation : ExternalOperation)
    (resolvedResultKind : AbiKind)
    (targetImport : Wasm.ImportDecl)
    (labels : List Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (physicalArgs : List Wasm.Value)
    (callIndex resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  ready : ConcreteStructuredExternalCallReadyFocus program context sourceModule
    sourceFunction targetModule hosts externals site operation
    resolvedResultKind targetImport labels continuation callerJoins sourceFrames
    targetStore callerLocals callerRemainder targetRest targetFrames witness
    physicalArgs callIndex resultIndex source target
  resources : ConcreteStructuredResourceStack program context sourceModule
    sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
    entryWitness witness facts remainingBytes sourceEnv callerLocals
    functionResult callerExpectedResult sourceFrames targetFrames
  stepFits : stepCost ≤ remainingBytes

theorem ConcreteStructuredExternalCallReadyCoreRel.observes
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceValue : Value}
    {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyCoreRel program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames entryRuntime entryStore entryWitness functionResult
      callerExpectedResult facts remainingBytes targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs callIndex
      resultIndex source target) :
    ConcretePrefixObservationRel (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.ready.observes

/-- Hereditary resource relation between the imported call and its generated
destination write.  Allocation and all semantic transports have happened,
but the destination fact is deliberately not erased until `local.set` binds
the result on both sides. -/
structure ConcreteStructuredExternalBindCoreRel
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (callerEnv : Env)
    (sourceValue : Value)
    (result : Lean.FVarId)
    (continuation : Lean.Compiler.LCNF.Code .impure)
    (callerJoins : JoinEnv)
    (sourceFrames : List Frame)
    (targetStore : Wasm.Store Host)
    (callerLocals : Wasm.Locals)
    (callerRemainder : List Wasm.Value)
    (targetRest : Wasm.Program)
    (targetFrames : List StructuredWasmFrame)
    (witness : RefinementWitness)
    (kind : AbiKind)
    (physical : Wasm.Value)
    (resultIndex : Nat)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  bindFocus : ConcreteStructuredExternalBindFocus context sourceModule
    sourceFunction labels sourceRuntime callerEnv sourceValue result
    continuation callerJoins sourceFrames targetStore callerLocals
    callerRemainder targetRest targetFrames witness kind physical resultIndex
    source target
  resources : ConcreteStructuredResourceStack program context sourceModule
    sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
    entryWitness witness facts remainingBytes callerEnv callerLocals
    functionResult callerExpectedResult sourceFrames targetFrames

theorem ConcreteStructuredExternalBindCoreRel.observes
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalBindCoreRel program context
      sourceModule sourceFunction externals labels entryRuntime entryStore
      entryWitness functionResult callerExpectedResult facts remainingBytes
      sourceRuntime callerEnv sourceValue result continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness kind physical resultIndex source target) :
    ConcretePrefixObservationRel (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.bindFocus.observes

/-- One source external-call step matches the one resolved target import and
preserves the complete resource stack at the pre-bind boundary. -/
theorem ConcreteStructuredExternalCallReadyCoreRel.advance_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceValue : Value}
    {stepCost : Nat}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : PureExternalCallShape context externals sourceRuntime sourceEnv decl
      nextRuntime sourceValue stepCost}
    {operation : ExternalOperation}
    {resolvedResultKind : AbiKind}
    {targetImport : Wasm.ImportDecl}
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {physicalArgs : List Wasm.Value}
    {callIndex resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalCallReadyCoreRel program context
      sourceModule sourceFunction targetModule hosts externals site operation
      resolvedResultKind targetImport labels continuation callerJoins
      sourceFrames entryRuntime entryStore entryWitness functionResult
      callerExpectedResult facts remainingBytes targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs callIndex
      resultIndex source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ nextStore nextWitness physicalResult targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1
          target targetAfter ∧
        ConcreteStructuredExternalBindCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts
          (remainingBytes - stepCost) nextRuntime sourceEnv sourceValue
          decl.fvarId continuation callerJoins sourceFrames nextStore
          callerLocals callerRemainder targetRest targetFrames nextWitness
          site.resultKind physicalResult resultIndex sourceAfter targetAfter := by
  obtain ⟨nextStore, nextWitness, physicalResult, evidence, nextFrame⟩ :=
    related.ready.callEvidence_of_budget
      related.resources.current.budgetedPureExternal related.stepFits
  obtain ⟨computedSource, targetAfter, computedStep, targetPath, bindFocus⟩ :=
    related.ready.advance_call evidence
  have sourceEq : sourceAfter = computedSource := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedSource
  have nextScope :=
    related.resources.current.afterExternalCall nextFrame
      bindFocus.stateRelated evidence
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness nextWitness facts (remainingBytes - stepCost) sourceEnv
        callerLocals functionResult callerExpectedResult sourceFrames
        targetFrames :=
    ⟨nextScope, related.resources.suspended⟩
  exact ⟨nextStore, nextWitness, physicalResult, targetAfter, targetPath,
    ⟨bindFocus, nextResources⟩⟩

/-- The generated destination write closes the external protocol, erases the
shadowed destination fact, and returns to ordinary compiler code with the
same hereditary caller stack. -/
theorem ConcreteStructuredExternalBindCoreRel.advance_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {callerEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredExternalBindCoreRel program context
      sourceModule sourceFunction externals labels entryRuntime entryStore
      entryWitness functionResult callerExpectedResult facts remainingBytes
      sourceRuntime callerEnv sourceValue result continuation callerJoins
      sourceFrames targetStore callerLocals callerRemainder targetRest
      targetFrames witness kind physical resultIndex source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter resumedLocals,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1
          target targetAfter ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult
          (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
          (bind callerEnv result sourceValue) continuation targetStore
          resumedLocals targetRest witness sourceAfter targetAfter ∧
        sourceAfter.frames = sourceFrames ∧
        targetAfter.frames = targetFrames := by
  obtain ⟨computedSource, targetAfter, _updated, resumedLocals, computedStep,
      targetPath, targetSet, resumedEq, nextFocus, sourceJoinsEq,
      sourceFramesEq, targetFramesEq⟩ :=
    related.bindFocus.advance (module := targetModule.wasmModule)
      (hostEnv := hosts.env) (externals := externals)
  have sourceEq : sourceAfter = computedSource := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedSource
  have resumedUpdate :
      FirTalos.Correctness.LocalUpdate callerLocals resumedLocals resultIndex
        physical := by
    rw [resumedEq]
    have base := FirTalos.Correctness.localUpdate_of_set? targetSet
    refine ⟨?_, ?_⟩
    · simpa [Wasm.Locals.get] using base.1
    · intro other different
      simpa [Wasm.Locals.get] using base.2 different
  have nextScope :=
    related.resources.current.afterExternalBind nextFocus.stateRelated
      nextFocus.frameAligned related.bindFocus.resultFound resumedUpdate
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness (eraseReuseCapacityFact facts result)
        remainingBytes (bind callerEnv result sourceValue) resumedLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact ⟨nextScope, related.resources.suspended⟩
  exact ⟨targetAfter, resumedLocals, targetPath,
    ⟨nextFocus, nextResources⟩, sourceFramesEq, targetFramesEq⟩

/-- The ordinary-code component of the compiler-produced pointwise relation.

`focus` is the actual lowering/adapter relation, `resources` is the hereditary
entry-relative heap invariant, and `admitted` classifies only the current
source node. The active function result kind is kept separately from the
result expected by a suspended caller, while the hereditary resource stack
stores their compatibility atomically with each call protocol. No continuation
certificate, terminating evaluation, or target execution witness occurs in
the relation. -/
structure ConcreteStructuredCodePointwiseRel
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (functionCode : Lean.Compiler.LCNF.Code .impure)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (requiredBytes : Nat)
    (remainingBytes : Nat)
    (sourceRuntime : RuntimeState)
    (sourceEnv : Env)
    (sourceCode : Lean.Compiler.LCNF.Code .impure)
    (targetStore : Wasm.Store Host)
    (targetLocals : Wasm.Locals)
    (targetCode : Wasm.Program)
    (witness : RefinementWitness)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop where
  contextCaches :
    context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program
  focus : ConcreteStructuredCodeFocus context sourceModule sourceFunction
    labels sourceRuntime sourceEnv sourceCode targetStore targetLocals
    targetCode witness source target
  resources : ConcreteStructuredResourceStack program context sourceModule
    sourceFunction externals entryRuntime sourceRuntime entryStore targetStore
    entryWitness witness facts remainingBytes sourceEnv targetLocals
    functionResult callerExpectedResult source.frames target.frames
  admitted : ConcreteStructuredCodeStepAdmission context externals
    functionResult facts
    sourceRuntime sourceEnv requiredBytes sourceCode
  budget : requiredBytes ≤ remainingBytes

/-- The four honest successor shapes of one admitted ordinary-code step.

The continued-code branch deliberately carries only the admission-free core
plus the static cache-table equality: admission for that dynamically reached
node is attached separately.  Call branches retain the exact generated row
needed by their next entry step.  This is a control-state sum, not a finite
execution or termination certificate. -/
inductive ConcreteStructuredCodeStepOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (functionCode : Lean.Compiler.LCNF.Code .impure)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat) :
    MachineState → StructuredWasmState Host → Prop where
  | code
      {nextFacts : ReuseCapacityFacts}
      {nextRemainingBytes : Nat}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceCode : Lean.Compiler.LCNF.Code .impure}
      {targetStore : Wasm.Store Host}
      {targetLocals : Wasm.Locals}
      {targetCode : Wasm.Program}
      {witness : RefinementWitness}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (related : ConcreteStructuredCodeCoreRel program context sourceModule
        sourceFunction externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult nextFacts nextRemainingBytes
        sourceRuntime sourceEnv sourceCode targetStore targetLocals targetCode
        witness source target) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | directReady
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      (site : DirectInternalCallSite context decl callerEnv)
      (row : ConcreteGeneratedInternalDeclaration context.program
        site.sourceDeclaration calleeContext site.calleeCode sourceModule
        calleeFunction targetModule)
      {sourceRuntime : RuntimeState}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {resultIndex : Nat}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (related : ConcreteStructuredDirectCallReadyCoreRel program context
        calleeContext sourceModule sourceFunction calleeFunction targetModule
        site row externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        continuation callerJoins sourceFrames targetStore callerLocals
        callerRemainder targetRest targetFrames witness physicalArgs resultIndex
        source target) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | saturatedReady
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {sourceRuntime : RuntimeState}
      (site : SaturatedClosureCallSite context decl callerEnv)
      (resolution : SaturatedClosureCallResolution context sourceRuntime site)
      (sharedCapacity : ∀ parentRuntime,
        setCell sourceRuntime resolution.location
            { resolution.cell with rc := resolution.cell.rc - 1 } =
              .ok parentRuntime →
          ClosureRetainCapacity parentRuntime resolution.captures.toList)
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      (row : ConcreteGeneratedInternalDeclaration context.program
        resolution.target calleeContext resolution.calleeCode sourceModule
        calleeFunction targetModule)
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {targetValue targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {resultIndex : Nat}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
        sourceModule sourceFunction targetModule site externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
        targetStore callerLocals targetValue targetRest targetFrames witness
        resultIndex source target) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | externalReady
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceValue : Value}
      {stepCost : Nat}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {site : PureExternalCallShape context externals sourceRuntime sourceEnv
        decl nextRuntime sourceValue stepCost}
      {operation : ExternalOperation}
      {resolvedResultKind : AbiKind}
      {targetImport : Wasm.ImportDecl}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {readyFacts : ReuseCapacityFacts}
      {readyRemainingBytes : Nat}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {callIndex resultIndex : Nat}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (related : ConcreteStructuredExternalCallReadyCoreRel program context
        sourceModule sourceFunction targetModule hosts externals site operation
        resolvedResultKind targetImport labels continuation callerJoins
        sourceFrames entryRuntime entryStore entryWitness functionResult
        callerExpectedResult readyFacts readyRemainingBytes targetStore
        callerLocals callerRemainder targetRest targetFrames witness physicalArgs
        callIndex resultIndex source target) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | externalBind
      {bindFacts : ReuseCapacityFacts}
      {bindRemainingBytes : Nat}
      {sourceRuntime : RuntimeState}
      {callerEnv : Env}
      {sourceValue : Value}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {kind : AbiKind}
      {physical : Wasm.Value}
      {resultIndex : Nat}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (related : ConcreteStructuredExternalBindCoreRel program context
        sourceModule sourceFunction externals labels entryRuntime entryStore
        entryWitness functionResult callerExpectedResult bindFacts
        bindRemainingBytes sourceRuntime callerEnv sourceValue result
        continuation callerJoins sourceFrames targetStore callerLocals
        callerRemainder targetRest targetFrames witness kind physical
        resultIndex source target) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | returned
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceValue : Value}
      {targetStore : Wasm.Store Host}
      {targetLocals : Wasm.Locals}
      {witness : RefinementWitness}
      {kind : AbiKind}
      {physical : Wasm.Value}
      {source : MachineState}
      {target : StructuredWasmState Host}
      (yielded : ConcreteStructuredYieldFocus context sourceFunction
        sourceRuntime sourceEnv sourceValue targetStore targetLocals witness
        kind physical source target)
      (compatible :
        ConcreteStructuredResultCompatible kind callerExpectedResult)
      (resources : ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult source.frames
        target.frames) :
      ConcreteStructuredCodeStepOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target

theorem ConcreteStructuredCodePointwiseRel.core
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target) :
    ConcreteStructuredCodeCoreRel program context sourceModule sourceFunction
      externals labels entryRuntime entryStore entryWitness functionResult
      callerExpectedResult facts remainingBytes sourceRuntime sourceEnv
      sourceCode targetStore targetLocals targetCode witness source target :=
  ⟨related.focus, related.resources⟩

/-- Attach the classifier for the newly reached source node to an
admission-free compiler/resource core.  Keeping this operation explicit is
what prevents the simulation relation from carrying a recursive certificate:
the admission premise may be established only after the dynamic successor is
known. -/
theorem ConcreteStructuredCodeCoreRel.withAdmission
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeCoreRel program context sourceModule
      sourceFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      sourceEnv sourceCode targetStore targetLocals targetCode witness source
      target)
    (contextCaches :
      context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
    (admitted : ConcreteStructuredCodeStepAdmission context externals
      functionResult facts
      sourceRuntime sourceEnv requiredBytes sourceCode)
    (budget : requiredBytes ≤ remainingBytes) :
    ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target :=
  ⟨contextCaches, related.focus, related.resources, admitted, budget⟩

/-- Re-anchor the module-wide supported-function facts at a selected internal
declaration.  A caller function already carries every global lower/adapt/host
contract; the generated row contributes only the callee-specific indices,
layout, body, and result facts. -/
def ConcreteGeneratedInternalDeclaration.toSupportedFunctionOfFunction
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {callerCode calleeCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {declaration : Lean.Compiler.LCNF.Decl .impure}
    (spec : ConcreteSupportedFunction program callerContext callerCode
      sourceModule callerFunction targetModule hosts)
    (row : ConcreteGeneratedInternalDeclaration program declaration
      calleeContext calleeCode sourceModule calleeFunction targetModule) :
    ConcreteSupportedFunction program calleeContext calleeCode sourceModule
      calleeFunction targetModule hosts := {
  programSupported := spec.programSupported
  programNamesUnique := spec.programNamesUnique
  contextProgram := row.contextProgram
  lowered := spec.lowered
  sourceFunctionIndex := row.sourceFunctionIndex
  sourceFunctionFound := row.sourceFunctionFound
  localsAligned := row.localsAligned
  adapted := spec.adapted
  hostsResolved := spec.hostsResolved
  hostsAligned := spec.hostsAligned
  runtimeCallsAligned := spec.runtimeCallsAligned
  externalCallsAligned := spec.externalCallsAligned
  targetFunctionIndex := row.targetFunctionIndex
  targetFunction := row.targetFunction
  notImport := row.notImport
  targetFunctionFound := row.targetFunctionFound
  targetBody := row.targetBody
  targetBodyEq := row.targetBodyEq
  bodyAdapted := row.bodyAdapted
  singleResult := row.singleResult }

/-- Select the exact production-generated callee row for a named call from
the static data already carried by the pointwise relation. -/
theorem ConcreteStructuredCodePointwiseRel.directCallRow
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    (site : DirectInternalCallSite context decl sourceEnv) :
    ∃ calleeContext calleeFunction,
      DeclarationContextsCoherent context calleeContext ∧
        Nonempty (ConcreteGeneratedInternalDeclaration program
          site.sourceDeclaration calleeContext site.calleeCode sourceModule
          calleeFunction targetModule) := by
  have resultClassified :
      Fir.Wasm.abiKind? site.sourceDeclaration.type =
        .ok (some site.calleeResultKind) :=
    abiKind?_of_directAbiKind?_eq_some site.calleeResult
  have declarationFound :
      program.findDecl? site.declaration = some site.sourceDeclaration := by
    rw [← spec.contextProgram]
    exact site.declarationFound
  exact
    ConcreteGeneratedInternalDeclaration.exists_ofSupportedPipeline
      spec.contextProgram related.contextCaches spec.programNamesUnique
      spec.lowered spec.adapted declarationFound site.bodyEq
      resultClassified

/-- Select the exact production-generated callee row for an exactly saturated
closure call.  Unlike the direct-call selector, this uses the effective result
lane because closure dispatch may refine a publicly tagged result. -/
theorem ConcreteStructuredCodePointwiseRel.saturatedCallRow
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target)
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {site : SaturatedClosureCallSite context decl sourceEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site) :
    ∃ calleeContext calleeFunction,
      DeclarationContextsCoherent context calleeContext ∧
        Nonempty (ConcreteGeneratedInternalDeclaration program
          resolution.target calleeContext resolution.calleeCode sourceModule
          calleeFunction targetModule) := by
  have declarationFound :
      program.findDecl? resolution.function = some resolution.target := by
    rw [← spec.contextProgram]
    exact resolution.targetFound
  have ordinaryLowering : Fir.Wasm.lower program = .ok sourceModule :=
    LazyCacheGeneratedEnvironment.lower_of_lowerSupported spec.lowered
  obtain ⟨_sourceFunctionIndex, calleeFunction, _sourceFunctionFound,
      ⟨loweredRow⟩⟩ :=
    LoweredInternalDeclaration.exists_of_lower ordinaryLowering
      declarationFound resolution.bodyEq
  have callerRow :
      LoweredInternalDeclaration context.program context.cachedDeclarations
        resolution.target resolution.calleeCode calleeFunction := by
    simpa only [spec.contextProgram, related.contextCaches] using loweredRow
  have generated :=
    ConcreteGeneratedInternalDeclaration.exists_ofSupportedPipelineAtLoweredSingleResult
      spec.contextProgram related.contextCaches spec.programNamesUnique
      spec.lowered spec.adapted declarationFound callerRow
      (callerRow.singleResult_of_effectiveResult resolution.targetResult)
  exact ⟨callerRow.context, calleeFunction,
    callerRow.contextsCoherent rfl rfl, generated⟩

/-- Stage one compiler-generated named call while preserving the complete
caller core. Dynamic argument values and the exact target prefix are derived
from the source call site and the production compiler/adaptor equations. -/
theorem ConcreteStructuredCodeCoreRel.advance_directCall_stage
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program callerContext functionCode
      sourceModule callerFunction targetModule hosts)
    (related : ConcreteStructuredCodeCoreRel program callerContext sourceModule
      callerFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      callerEnv (.let decl continuation) targetStore targetLocals targetCode
      witness source target) :
    ∃ (physicalArgs : List Wasm.Value) (resultIndex : Nat)
        (targetArguments targetRest : Wasm.Program)
        (sourceAfter : MachineState) (targetAfter : StructuredWasmState Host),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        targetArguments.length target targetAfter ∧
      ConcreteStructuredDirectCallReadyCoreRel program callerContext
        calleeContext sourceModule callerFunction calleeFunction targetModule
        site row externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        continuation source.joins source.frames targetStore targetLocals
        targetLocals.values targetRest target.frames witness physicalArgs
        resultIndex sourceAfter targetAfter := by
  obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest, sourceAfter,
      targetAfter, sourceStep, targetPath, ready⟩ :=
    related.focus.advance_directCall_stage
      (targetModule := targetModule) (hostEnv := hosts.env)
      (externals := externals) site row spec.localsAligned
  exact ⟨physicalArgs, resultIndex, targetArguments, targetRest, sourceAfter,
    targetAfter, sourceStep, targetPath,
    ⟨ready, related.resources⟩⟩

/-- Ranked named-call staging.  This strengthens the exact staging theorem for
the weak-simulation interface: even when the compiled argument prefix is
empty, the source code-to-invocation phase change strictly decreases the
compiler control rank. -/
theorem ConcreteStructuredCodeCoreRel.advance_directCall_stage_ranked
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : DirectInternalCallSite callerContext decl callerEnv)
    (row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program callerContext functionCode
      sourceModule callerFunction targetModule hosts)
    (related : ConcreteStructuredCodeCoreRel program callerContext sourceModule
      callerFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      callerEnv (.let decl continuation) targetStore targetLocals targetCode
      witness source target) :
    ∃ (physicalArgs : List Wasm.Value) (resultIndex : Nat)
        (targetArguments targetRest : Wasm.Program)
        (sourceAfter : MachineState) (targetAfter : StructuredWasmState Host),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        targetArguments.length target targetAfter ∧
      ConcreteStructuredDirectCallReadyCoreRel program callerContext
        calleeContext sourceModule callerFunction calleeFunction targetModule
        site row externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        continuation source.joins source.frames targetStore targetLocals
        targetLocals.values targetRest target.frames witness physicalArgs
        resultIndex sourceAfter targetAfter ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest, sourceAfter,
      targetAfter, sourceStep, targetPath, ready⟩ :=
    related.advance_directCall_stage site row spec
  have rank :
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source :=
    compilerStructuredControlRank_invokeName_lt_code
      related.focus.sourceControlEq ready.ready.sourceControlEq
  exact ⟨physicalArgs, resultIndex, targetArguments, targetRest, sourceAfter,
    targetAfter, sourceStep, targetPath, ready, rank⟩

/-- Stage one exactly saturated closure call. The source takes its invocation
step, the target remains at the generated dispatcher, and the compiler rank
strictly decreases for the zero-target-step match. -/
theorem ConcreteStructuredCodeCoreRel.advance_saturatedCall_stage
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    (site : SaturatedClosureCallSite context decl callerEnv)
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredCodeCoreRel program context sourceModule
      sourceFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      callerEnv (.let decl continuation) targetStore targetLocals targetCode
      witness source target) :
    ∃ (targetValue targetRest : Wasm.Program) (resultIndex : Nat)
        (sourceAfter : MachineState),
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 0 target
        target ∧
      ConcreteStructuredSaturatedCallReadyCoreRel program context sourceModule
        sourceFunction targetModule site externals labels entryRuntime
        entryStore entryWitness functionResult callerExpectedResult facts
        remainingBytes sourceRuntime continuation source.joins source.frames
        targetStore targetLocals targetValue targetRest target.frames witness
        resultIndex sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨targetValue, targetRest, resultIndex, sourceAfter, sourceStep,
      targetPath, ready, rank⟩ :=
    related.focus.advance_saturatedCall_stage_ranked
      (targetModule := targetModule) (hostEnv := hosts.env) site
      spec.localsAligned
  exact ⟨targetValue, targetRest, resultIndex, sourceAfter, sourceStep,
    targetPath, ⟨ready, related.resources⟩, rank⟩

/-- A successful ordinary source step from the saturated-ready control state
determines the exact closure-consumption result.  The global simulation never
needs a second dynamic application premise in addition to its source step. -/
theorem ConcreteStructuredSaturatedCallReadyFocus.application_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    {labels : List Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSaturatedCallReadyFocus context sourceModule
      sourceFunction targetModule site labels sourceRuntime continuation
      callerJoins sourceFrames targetStore callerLocals targetValue targetRest
      targetFrames witness resultIndex source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ callRuntime,
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures) := by
  rcases source with
    ⟨sourceProgram, sourceControl, sourceEnv, sourceJoins, sourceFrames,
      runtime⟩
  have sourceProgramEq := related.sourceProgramEq
  change sourceProgram = context.program at sourceProgramEq
  subst sourceProgram
  have sourceControlEq := related.sourceControlEq
  change sourceControl =
    .invokeValue site.sourceClosure site.semanticArgs at sourceControlEq
  subst sourceControl
  have sourceEnvEq := related.sourceEnvEq
  change sourceEnv = callerEnv at sourceEnvEq
  subst sourceEnv
  have sourceRuntimeEq := related.sourceRuntimeEq
  change runtime = sourceRuntime at sourceRuntimeEq
  subst runtime
  have sourceJoinsEq := related.sourceJoinsEq
  change sourceJoins = callerJoins at sourceJoinsEq
  subst sourceJoins
  have sourceFramesEq := related.sourceFramesEq
  change sourceFrames =
    .bind decl.fvarId continuation callerEnv callerJoins ::
      _ at sourceFramesEq
  subst sourceFrames
  cases application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
        resolution.location with
  | error fault =>
      simp [executeStep, coreStep, invokeClosure, resolution.sourceClosureEq,
        application, fail] at sourceStep
  | ok result =>
      rcases result with ⟨callRuntime, function, arity, captures⟩
      have exactMetadata :
          function = resolution.function ∧ arity = resolution.arity ∧
            captures = resolution.captures := by
        unfold Fir.LeanIR.Impure.takeClosureApplication at application
        simp only [getLiveCell, resolution.cellFound, resolution.cellLive,
          if_true, Bind.bind, Except.bind] at application
        rw [resolution.cellObjectEq] at application
        aesop (add simp [pure, Except.pure])
      rcases exactMetadata with ⟨rfl, rfl, rfl⟩
      exact ⟨callRuntime, by simpa using application⟩

/-- Enter a staged named call and reconstruct the callee's complete code core.
The outer result ABI is retained by the hereditary result stack, while the
callee receives the declared ABI that refines the caller's result slot. -/
theorem ConcreteStructuredDirectCallReadyCoreRel.advance_enter
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallReadyCoreRel program callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs resultIndex
      source target)
    (contextProgram : callerContext.program = program) :
    ∃ sourceAfter targetAfter,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 1 target
        targetAfter ∧
      ConcreteStructuredCodeCoreRel program calleeContext sourceModule
        calleeFunction externals [] sourceRuntime targetStore witness
        site.calleeResultKind (some site.resultKind) [] remainingBytes
        sourceRuntime site.calleeEnv site.calleeCode targetStore
        (row.targetFunction.toLocals physicalArgs) row.targetFunction.body
        witness sourceAfter targetAfter ∧
      ConcreteStructuredResourceScope callerContext sourceModule
        callerFunction externals entryRuntime entryStore entryWitness facts
        remainingBytes sourceRuntime callerEnv targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        witness ∧
      ConcreteStructuredDirectCallEntryFocus callerContext calleeContext
        sourceModule callerFunction calleeFunction targetModule site row labels
        sourceRuntime continuation callerJoins sourceFrames targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        callerRemainder targetRest targetFrames witness physicalArgs resultIndex
        sourceAfter targetAfter := by
  obtain ⟨sourceAfter, targetAfter, storedCallerLocals, storedEq, sourceStep,
      targetPath, entry⟩ :=
    related.ready.advance_enter (hostEnv := hostEnv) (externals := externals)
  subst storedCallerLocals
  have callerResources :
      ConcreteStructuredResourceStack callerContext.program callerContext
        sourceModule callerFunction externals entryRuntime sourceRuntime
        entryStore targetStore entryWitness witness facts remainingBytes
        callerEnv
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        functionResult callerExpectedResult sourceFrames targetFrames := by
    rw [contextProgram]
    exact ⟨related.resources.current.withValues _,
      related.resources.suspended⟩
  have calleeAbi :
      ClosureAllocationsAbiAligned calleeContext.program witness := by
    rw [row.contextProgram]
    exact callerResources.current.2
  have calleeScope :
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals sourceRuntime targetStore witness []
        remainingBytes sourceRuntime site.calleeEnv targetStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    ⟨entry.calleeEntryRelativeCacheFrame callerResources.current.1.1,
      calleeAbi⟩
  have pushedResources := entry.pushResourceStack callerResources calleeScope
  have nextResources :
      ConcreteStructuredResourceStack program calleeContext sourceModule
        calleeFunction externals sourceRuntime sourceRuntime targetStore
        targetStore witness witness [] remainingBytes site.calleeEnv
        (row.targetFunction.toLocals physicalArgs) site.calleeResultKind
        (some site.resultKind) sourceAfter.frames targetAfter.frames := by
    simpa [contextProgram] using pushedResources
  exact ⟨sourceAfter, targetAfter, sourceStep, targetPath,
    ⟨entry.calleeFocus, nextResources⟩, callerResources.current, entry⟩

/-- Relation-facing named-call entry rule.  The supplied ordinary source step
is identified with the compiler-derived entry step by determinism; callers do
not provide a second source transition witness. -/
theorem ConcreteStructuredDirectCallReadyCoreRel.advance_enter_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredDirectCallReadyCoreRel program callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs resultIndex
      source target)
    (contextProgram : callerContext.program = program)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hostEnv) 1 target
        targetAfter ∧
      ConcreteStructuredCodeCoreRel program calleeContext sourceModule
        calleeFunction externals [] sourceRuntime targetStore witness
        site.calleeResultKind (some site.resultKind) [] remainingBytes
        sourceRuntime site.calleeEnv site.calleeCode targetStore
        (row.targetFunction.toLocals physicalArgs) row.targetFunction.body
        witness sourceAfter targetAfter ∧
      ConcreteStructuredResourceScope callerContext sourceModule
        callerFunction externals entryRuntime entryStore entryWitness facts
        remainingBytes sourceRuntime callerEnv targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        witness ∧
      ConcreteStructuredDirectCallEntryFocus callerContext calleeContext
        sourceModule callerFunction calleeFunction targetModule site row labels
        sourceRuntime continuation callerJoins sourceFrames targetStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        callerRemainder targetRest targetFrames witness physicalArgs resultIndex
        sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, computedStep, targetPath, nextCore,
      callerScope, entry⟩ :=
    related.advance_enter contextProgram
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  exact ⟨targetAfter, targetPath, nextCore, callerScope, entry⟩

/-- Consume a staged saturated closure call and reconstruct the selected
callee core. Matcher selection and closure ownership update the saved caller
scope before it is pushed; the generated callee starts at the resulting store
with empty local reuse facts and its declared result ABI. -/
theorem ConcreteStructuredSaturatedCallReadyCoreRel.advance_enter
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime callRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
      sourceModule sourceFunction targetModule site externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals targetValue targetRest targetFrames witness
      resultIndex source target)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (application :
      Fir.LeanIR.Impure.takeClosureApplication sourceRuntime
          resolution.location =
        .ok (callRuntime, resolution.function, resolution.arity,
          resolution.captures)) :
    ∃ (sourceEntry : MachineState) (targetEntry : StructuredWasmState Host)
        (nextStore : Wasm.Store Host) (physicalArgs : List Wasm.Value)
        (matcherCount argumentCount : Nat),
      executeStep externals source = .next sourceEntry ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (matcherCount + 1) + argumentCount + 1) target targetEntry ∧
      ConcreteStructuredCodeCoreRel program calleeContext sourceModule
        calleeFunction externals [] callRuntime nextStore witness
        resolution.targetResultKind (some site.resultKind) [] remainingBytes
        callRuntime resolution.calleeEnv resolution.calleeCode nextStore
        (row.targetFunction.toLocals physicalArgs) row.targetFunction.body
        witness sourceEntry targetEntry ∧
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        callRuntime callerEnv nextStore callerLocals witness ∧
      ConcreteStructuredSaturatedCallEntryFocus context calleeContext
        sourceModule sourceFunction calleeFunction targetModule site resolution
        row labels callRuntime continuation callerJoins sourceFrames nextStore
        callerLocals callerLocals.values targetRest targetFrames witness
        physicalArgs resultIndex matcherCount sourceEntry targetEntry := by
  have invariant :
      ConcreteReuseCapacityCacheAbiFrame context sourceModule sourceFunction
        externals facts remainingBytes sourceRuntime callerEnv targetStore
        callerLocals witness :=
    ⟨related.resources.current.1.1, related.resources.current.2⟩
  obtain ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
      argumentCount, sourceStep, targetPath, entry, matcherFrame,
      matcherCapacity, callerAfterFrame⟩ :=
    related.ready.advance_enter resolution row spec invariant sharedCapacity
      application
  have matcherTables :
      ClosureTablesTransport targetStore nextStore witness witness := {
    hostDispatchPreserved := matcherFrame.dispatch
    witnessDispatchPreserved := rfl
    hostDescriptorsPreserved := matcherFrame.descriptors
    witnessDescriptorsPreserved := rfl }
  have callerAfterScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        callRuntime callerEnv nextStore callerLocals witness :=
    ⟨⟨callerAfterFrame, related.resources.current.1.2.step
        (WitnessTransport.refl witness)
        (ClosureAllocationsPersistent.refl witness) matcherCapacity
        (takeClosureApplication_ordinaryPersistenceTransport application)
        matcherFrame.externals matcherTables⟩,
      related.resources.current.2⟩
  have callerResourcesAfter :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime callRuntime entryStore nextStore
        entryWitness witness facts remainingBytes callerEnv callerLocals
        functionResult callerExpectedResult sourceFrames targetFrames :=
    ⟨callerAfterScope, related.resources.suspended⟩
  have callerResourcesAtContext :
      ConcreteStructuredResourceStack context.program context sourceModule
        sourceFunction externals entryRuntime callRuntime entryStore nextStore
        entryWitness witness facts remainingBytes callerEnv callerLocals
        functionResult callerExpectedResult sourceFrames targetFrames := by
    rw [spec.contextProgram]
    exact callerResourcesAfter
  have calleeAbi :
      ClosureAllocationsAbiAligned calleeContext.program witness := by
    rw [row.contextProgram]
    exact callerAfterScope.2
  have calleeScope :
      ConcreteStructuredResourceScope calleeContext sourceModule
        calleeFunction externals callRuntime nextStore witness [] remainingBytes
        callRuntime resolution.calleeEnv nextStore
        (row.targetFunction.toLocals physicalArgs) witness :=
    ⟨entry.calleeEntryRelativeCacheFrame callerAfterFrame, calleeAbi⟩
  have pushedResources :=
    entry.pushResourceStack callerResourcesAtContext calleeScope
  have nextResources :
      ConcreteStructuredResourceStack program calleeContext sourceModule
        calleeFunction externals callRuntime callRuntime nextStore nextStore
        witness witness [] remainingBytes resolution.calleeEnv
        (row.targetFunction.toLocals physicalArgs) resolution.targetResultKind
        (some site.resultKind) sourceEntry.frames targetEntry.frames := by
    simpa [spec.contextProgram] using pushedResources
  exact ⟨sourceEntry, targetEntry, nextStore, physicalArgs, matcherCount,
    argumentCount, sourceStep, targetPath,
    ⟨entry.calleeFocus, nextResources⟩, callerAfterScope, entry⟩

/-- Relation-facing saturated-call entry rule.  Exact closure consumption is
recovered from the ordinary successful source step, then determinism identifies
the source successor produced by the concrete entry theorem. -/
theorem ConcreteStructuredSaturatedCallReadyCoreRel.advance_enter_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    (resolution : SaturatedClosureCallResolution context sourceRuntime site)
    (row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule)
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
      sourceModule sourceFunction targetModule site externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals targetValue targetRest targetFrames witness
      resultIndex source target)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetEntry nextStore physicalArgs matcherCount argumentCount callRuntime,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        (3 * (matcherCount + 1) + argumentCount + 1) target targetEntry ∧
      ConcreteStructuredCodeCoreRel program calleeContext sourceModule
        calleeFunction externals [] callRuntime nextStore witness
        resolution.targetResultKind (some site.resultKind) [] remainingBytes
        callRuntime resolution.calleeEnv resolution.calleeCode nextStore
        (row.targetFunction.toLocals physicalArgs) row.targetFunction.body
        witness sourceAfter targetEntry ∧
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        callRuntime callerEnv nextStore callerLocals witness ∧
      ConcreteStructuredSaturatedCallEntryFocus context calleeContext
        sourceModule sourceFunction calleeFunction targetModule site resolution
        row labels callRuntime continuation callerJoins sourceFrames nextStore
        callerLocals callerLocals.values targetRest targetFrames witness
        physicalArgs resultIndex matcherCount sourceAfter targetEntry := by
  obtain ⟨callRuntime, application⟩ :=
    related.ready.application_of_step resolution sourceStep
  obtain ⟨computedAfter, targetEntry, nextStore, physicalArgs, matcherCount,
      argumentCount, computedStep, targetPath, nextCore, callerScope, entry⟩ :=
    related.advance_enter resolution row spec sharedCapacity application
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  exact ⟨targetEntry, nextStore, physicalArgs, matcherCount, argumentCount,
    callRuntime, targetPath, nextCore, callerScope, entry⟩

/-- Direct bind resumption reconstructs the caller's complete code core. The
outer function result ABI is recovered from the same hereditary resource
constructor that restores its heap/cache scope, so protocol and ABI metadata
cannot diverge. -/
theorem ConcreteStructuredBindFrameFocus.advance_popCore
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {outerRuntime callRuntime sourceRuntime : RuntimeState}
    {outerStore callStore targetStore : Wasm.Store Host}
    {outerWitness callWitness witness : RefinementWitness}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals calleeLocals : Wasm.Locals}
    {callerRemainder returnedTail : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind callerFunctionResult : AbiKind}
    {physical : Wasm.Value}
    {resultIndex : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames returnedTail witness kind physical
      resultIndex source target)
    (callerScope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals outerRuntime outerStore outerWitness facts
      callerBytes callRuntime callerEnv callStore callerLocals callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore calleeLocals witness)
    (programEq : calleeContext.program = context.program)
    (tail : ConcreteStructuredSuspendedResourceStack externals context.program
      outerRuntime outerStore outerWitness callerFunctionResult tailResult
      sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) 2 target targetAfter ∧
      ConcreteStructuredCodeCoreRel context.program context sourceModule
        sourceFunction externals labels outerRuntime outerStore outerWitness
        callerFunctionResult tailResult (eraseReuseCapacityFact facts result)
        resultBytes sourceRuntime (bind callerEnv result sourceValue)
        continuation targetStore resumedLocals targetRest witness sourceAfter
        targetAfter ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
      _stack, focus, resources, sourceFramesEq, targetFramesEq⟩ :=
    related.advance_popResourceStack callerScope calleeScope programEq tail
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    ⟨focus, resources⟩, sourceFramesEq, targetFramesEq⟩

/-- Saturated-closure bind resumption reconstructs the caller's complete code
core after consuming the call frame and every generated matcher label. The
same hereditary constructor restores both the caller resource scope and its
function-result ABI. -/
theorem ConcreteStructuredSaturatedBindFrameFocus.advance_popCore
    {context calleeContext : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {module : Wasm.Module}
    {hostEnv : Wasm.HostEnv Host}
    {externals : ExternalImpl}
    {outerRuntime callRuntime sourceRuntime : RuntimeState}
    {outerStore callStore targetStore : Wasm.Store Host}
    {outerWitness callWitness witness : RefinementWitness}
    {callerEnv calleeEnv : Env}
    {sourceValue : Value}
    {result : Lean.FVarId}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals calleeLocals scopeLocals : Wasm.Locals}
    {physicalArgs callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {kind callerFunctionResult : AbiKind}
    {physical : Wasm.Value}
    {resultIndex matcherCount : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    {tailResult : Option AbiKind}
    {facts calleeFacts : ReuseCapacityFacts}
    {callerBytes resultBytes : Nat}
    (related : ConcreteStructuredSaturatedBindFrameFocus context sourceModule
      sourceFunction labels sourceRuntime callerEnv sourceValue result
      continuation callerJoins sourceFrames targetStore callerLocals
      calleeLocals physicalArgs callerRemainder targetRest targetFrames witness
      kind physical resultIndex matcherCount source target)
    (callerScope : ConcreteStructuredResourceScope context sourceModule
      sourceFunction externals outerRuntime outerStore outerWitness facts
      callerBytes callRuntime callerEnv callStore
      { callerLocals with
        values := physicalArgs.reverse ++ callerRemainder }
      callWitness)
    (calleeScope : ConcreteStructuredResourceScope calleeContext sourceModule
      calleeFunction externals callRuntime callStore callWitness calleeFacts
      resultBytes sourceRuntime calleeEnv targetStore scopeLocals witness)
    (programEq : calleeContext.program = context.program)
    (tail : ConcreteStructuredSuspendedResourceStack externals context.program
      outerRuntime outerStore outerWitness callerFunctionResult tailResult
      sourceFrames targetFrames) :
    ∃ sourceAfter targetAfter resumedLocals,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep module hostEnv) (matcherCount + 5) target
        targetAfter ∧
      ConcreteStructuredCodeCoreRel context.program context sourceModule
        sourceFunction externals labels outerRuntime outerStore outerWitness
        callerFunctionResult tailResult (eraseReuseCapacityFact facts result)
        resultBytes sourceRuntime (bind callerEnv result sourceValue)
        continuation targetStore resumedLocals targetRest witness sourceAfter
        targetAfter ∧
      sourceAfter.frames = sourceFrames ∧
      targetAfter.frames = targetFrames := by
  obtain ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
      _stack, focus, resources, sourceFramesEq, targetFramesEq⟩ :=
    related.advance_popResourceStack callerScope calleeScope programEq tail
  exact ⟨sourceAfter, targetAfter, resumedLocals, sourceStep, targetPath,
    ⟨focus, resources⟩, sourceFramesEq, targetFramesEq⟩

theorem ConcreteStructuredCodeCoreRel.observes
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeCoreRel program context sourceModule
      sourceFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      sourceEnv sourceCode targetStore targetLocals targetCode witness source
      target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) :=
  related.focus.observes

/-- The core already supplies the compiler-derived control-and-stack relation;
the supported-function program equation only reconciles its two program
indices. -/
theorem ConcreteStructuredCodeCoreRel.stackRel
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeCoreRel program context sourceModule
      sourceFunction externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      sourceEnv sourceCode targetStore targetLocals targetCode witness source
      target)
    (contextProgram : context.program = program) :
    ConcreteStructuredStackRel source target := by
  have frames :
      ConcreteStructuredFrameRel source.program sourceRuntime targetStore
        witness callerExpectedResult source.frames target.frames := by
    rw [related.focus.sourceProgramEq, contextProgram]
    exact related.resources.frameRel
  exact ⟨ConcreteStructuredControlRel.code related.focus,
    .code related.focus frames⟩

/-- Static compiler identity for every suspended generated caller.

The existing resource stack owns all dynamic heap/cache/budget transports.
This companion contains only the supported caller function, its canonical
cache-table equation, and the already generated bind/call-frame layout.  Its
indices are the same source and target frame lists, so it cannot describe a
different call history.  In particular it contains no future admission,
source evaluation, target path, or termination evidence. -/
inductive ConcreteStructuredSupportedFrameStack
    (program : Fir.LeanIR.ImpureProgram)
    (sourceModule : Fir.Wasm.Module)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts) :
    AbiKind → Option AbiKind → List Frame →
      List StructuredWasmFrame → Prop where
  | nil
      {functionResult : AbiKind} :
      ConcreteStructuredSupportedFrameStack program sourceModule targetModule
        hosts functionResult none [] []
  | case
      {functionResult : AbiKind}
      {expectedResult : Option AbiKind}
      {sourceFrames : List Frame}
      {targetFrames : List StructuredWasmFrame}
      {belowStack : List Wasm.Value}
      {targetRest : Wasm.Program}
      {testCount : Nat}
      (tail : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult expectedResult sourceFrames
        targetFrames) :
      ConcreteStructuredSupportedFrameStack program sourceModule targetModule
        hosts functionResult expectedResult sourceFrames
        (structuredWasmCaseLabels belowStack targetRest testCount ++
          targetFrames)
  | direct
      {callerContext : Fir.Wasm.Context}
      {callerCode : Lean.Compiler.LCNF.Code .impure}
      {callerFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {callerEnv : Env}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex : Nat}
      {tailResult : Option AbiKind}
      (spec : ConcreteSupportedFunction program callerContext callerCode
        sourceModule callerFunction targetModule hosts)
      (contextCaches :
        callerContext.cachedDeclarations =
          Fir.Wasm.cachedDeclarationNames program)
      (continuationAdapted :
        CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
          continuation targetRest)
      (resultFound :
        findFVar? (functionBindings callerFunction) result = some resultIndex)
      (kindAt :
        (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (tail : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts callerResult tailResult sourceFrames targetFrames) :
      ConcreteStructuredSupportedFrameStack program sourceModule targetModule
        hosts calleeResult (some kind)
        (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder callerLocals
            (.localSet resultIndex :: targetRest) :: targetFrames)
  | saturated
      {callerContext : Fir.Wasm.Context}
      {callerCode : Lean.Compiler.LCNF.Code .impure}
      {callerFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {callerEnv : Env}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerLocals : Wasm.Locals}
      {physicalArgs callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex matcherCount : Nat}
      {tailResult : Option AbiKind}
      (spec : ConcreteSupportedFunction program callerContext callerCode
        sourceModule callerFunction targetModule hosts)
      (contextCaches :
        callerContext.cachedDeclarations =
          Fir.Wasm.cachedDeclarationNames program)
      (continuationAdapted :
        CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
          continuation targetRest)
      (resultFound :
        findFVar? (functionBindings callerFunction) result = some resultIndex)
      (kindAt :
        (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (tail : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts callerResult tailResult sourceFrames targetFrames) :
      ConcreteStructuredSupportedFrameStack program sourceModule targetModule
        hosts calleeResult (some kind)
        (.bind result continuation callerEnv callerJoins :: sourceFrames)
        (.call 1 callerRemainder
            { callerLocals with
              values := physicalArgs.reverse ++ callerRemainder }
            [.localSet resultIndex] ::
          (List.replicate matcherCount (.label 0 callerRemainder []) ++
            .label 0 callerRemainder
                ([.localGet resultIndex, .localSet resultIndex] ++ targetRest) ::
              targetFrames))

/-- The static supported-function stack and the dynamic resource stack describe
the same call history constructor by constructor.

Sharing only their source/target frame-list indices is not enough: a direct
frame and a degenerate saturated frame can have extensionally equal lists.
This relation rules out that ambiguity.  It is the proof-facing join between
compiler identity and the already established dynamic heap/resource stack;
neither side carries an execution certificate. -/
inductive ConcreteStructuredSupportedFrameStack.Agrees
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl} :
    {entryRuntime : RuntimeState} →
    {entryStore : Wasm.Store Host} →
    {entryWitness : RefinementWitness} →
    {functionResult : AbiKind} →
    {expectedResult : Option AbiKind} →
    {sourceFrames : List Frame} →
    {targetFrames : List StructuredWasmFrame} →
    ConcreteStructuredSupportedFrameStack program sourceModule targetModule
      hosts functionResult expectedResult sourceFrames targetFrames →
    ConcreteStructuredSuspendedResourceStack externals program entryRuntime
      entryStore entryWitness functionResult expectedResult sourceFrames
      targetFrames → Prop where
  | nil
      {entryRuntime : RuntimeState}
      {entryStore : Wasm.Store Host}
      {entryWitness : RefinementWitness}
      {functionResult : AbiKind} :
      ConcreteStructuredSupportedFrameStack.Agrees
        (ConcreteStructuredSupportedFrameStack.nil
          (program := program) (sourceModule := sourceModule)
          (targetModule := targetModule) (hosts := hosts)
          (functionResult := functionResult))
        (ConcreteStructuredSuspendedResourceStack.nil
          (externals := externals) (program := program)
          (entryRuntime := entryRuntime) (entryStore := entryStore)
          (entryWitness := entryWitness) (functionResult := functionResult))
  | case
      {entryRuntime : RuntimeState}
      {entryStore : Wasm.Store Host}
      {entryWitness : RefinementWitness}
      {functionResult : AbiKind}
      {expectedResult : Option AbiKind}
      {sourceFrames : List Frame}
      {targetFrames : List StructuredWasmFrame}
      {belowStack : List Wasm.Value}
      {targetRest : Wasm.Program}
      {testCount : Nat}
      (supportedTail : ConcreteStructuredSupportedFrameStack program
        sourceModule targetModule hosts functionResult expectedResult
        sourceFrames targetFrames)
      (resourceTail : ConcreteStructuredSuspendedResourceStack externals program
        entryRuntime entryStore entryWitness functionResult expectedResult
        sourceFrames targetFrames)
      (tailAgrees : supportedTail.Agrees resourceTail) :
      ConcreteStructuredSupportedFrameStack.Agrees
        (.case (belowStack := belowStack) (targetRest := targetRest)
          (testCount := testCount) supportedTail)
        (.case (belowStack := belowStack) (targetRest := targetRest)
          (testCount := testCount) resourceTail)
  | direct
      {activeEntryRuntime callerEntryRuntime : RuntimeState}
      {activeEntryStore callerEntryStore : Wasm.Store Host}
      {activeEntryWitness callerEntryWitness : RefinementWitness}
      {callerContext : Fir.Wasm.Context}
      {callerCode : Lean.Compiler.LCNF.Code .impure}
      {callerFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      {callerEnv : Env}
      {callerLocals : Wasm.Locals}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex : Nat}
      {tailResult : Option AbiKind}
      (spec : ConcreteSupportedFunction program callerContext callerCode
        sourceModule callerFunction targetModule hosts)
      (contextCaches :
        callerContext.cachedDeclarations =
          Fir.Wasm.cachedDeclarationNames program)
      (callerScope : ConcreteStructuredResourceScope callerContext sourceModule
        callerFunction externals callerEntryRuntime callerEntryStore
        callerEntryWitness facts remainingBytes activeEntryRuntime callerEnv
        activeEntryStore callerLocals activeEntryWitness)
      (programEq : program = callerContext.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
          continuation targetRest)
      (resultFound :
        findFVar? (functionBindings callerFunction) result = some resultIndex)
      (kindAt :
        (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (supportedTail : ConcreteStructuredSupportedFrameStack program
        sourceModule targetModule hosts callerResult tailResult sourceFrames
        targetFrames)
      (resourceTail : ConcreteStructuredSuspendedResourceStack externals program
        callerEntryRuntime callerEntryStore callerEntryWitness callerResult
        tailResult sourceFrames targetFrames)
      (tailAgrees : supportedTail.Agrees resourceTail) :
      ConcreteStructuredSupportedFrameStack.Agrees
        (.direct (callerJoins := callerJoins)
          (callerRemainder := callerRemainder) spec contextCaches
          continuationAdapted resultFound kindAt calleeCompatible supportedTail)
        (.direct (callerJoins := callerJoins)
          (callerRemainder := callerRemainder) callerScope programEq
          continuationAdapted resultFound kindAt calleeCompatible resourceTail)
  | saturated
      {activeEntryRuntime callerEntryRuntime : RuntimeState}
      {activeEntryStore callerEntryStore : Wasm.Store Host}
      {activeEntryWitness callerEntryWitness : RefinementWitness}
      {callerContext : Fir.Wasm.Context}
      {callerCode : Lean.Compiler.LCNF.Code .impure}
      {callerFunction : Fir.Wasm.Function}
      {labels : List Lean.FVarId}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      {callerEnv : Env}
      {callerLocals : Wasm.Locals}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {physicalArgs callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {calleeResult callerResult kind : AbiKind}
      {resultIndex matcherCount : Nat}
      {tailResult : Option AbiKind}
      (spec : ConcreteSupportedFunction program callerContext callerCode
        sourceModule callerFunction targetModule hosts)
      (contextCaches :
        callerContext.cachedDeclarations =
          Fir.Wasm.cachedDeclarationNames program)
      (callerScope : ConcreteStructuredResourceScope callerContext sourceModule
        callerFunction externals callerEntryRuntime callerEntryStore
        callerEntryWitness facts remainingBytes activeEntryRuntime callerEnv
        activeEntryStore
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
        activeEntryWitness)
      (programEq : program = callerContext.program)
      (continuationAdapted :
        CodeAdaptedWithSuffix callerContext sourceModule callerFunction labels
          continuation targetRest)
      (resultFound :
        findFVar? (functionBindings callerFunction) result = some resultIndex)
      (kindAt :
        (functionBindings callerFunction)[resultIndex]?.map Prod.snd =
          some kind)
      (calleeCompatible : calleeResult.refines kind = true)
      (supportedTail : ConcreteStructuredSupportedFrameStack program
        sourceModule targetModule hosts callerResult tailResult sourceFrames
        targetFrames)
      (resourceTail : ConcreteStructuredSuspendedResourceStack externals program
        callerEntryRuntime callerEntryStore callerEntryWitness callerResult
        tailResult sourceFrames targetFrames)
      (tailAgrees : supportedTail.Agrees resourceTail) :
      ConcreteStructuredSupportedFrameStack.Agrees
        (.saturated (callerJoins := callerJoins)
          (matcherCount := matcherCount) spec contextCaches
          continuationAdapted resultFound kindAt calleeCompatible supportedTail)
        (.saturated (callerJoins := callerJoins)
          (matcherCount := matcherCount) callerScope programEq
          continuationAdapted resultFound kindAt calleeCompatible resourceTail)

/-- Reindex an aligned static/resource tail across the frame equalities exposed
by a call-pop theorem.  The replacement resource proof is propositionally
irrelevant once the indices agree. -/
theorem ConcreteStructuredSupportedFrameStack.Agrees.reindex
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {expectedResult : Option AbiKind}
    {sourceFrames targetSourceFrames : List Frame}
    {targetFrames targetTargetFrames : List StructuredWasmFrame}
    {supported : ConcreteStructuredSupportedFrameStack program sourceModule
      targetModule hosts functionResult expectedResult sourceFrames
      targetFrames}
    {resources : ConcreteStructuredSuspendedResourceStack externals program
      entryRuntime entryStore entryWitness functionResult expectedResult
      sourceFrames targetFrames}
    (agrees : supported.Agrees resources)
    (sourceFramesEq : targetSourceFrames = sourceFrames)
    (targetFramesEq : targetTargetFrames = targetFrames)
    (targetResources : ConcreteStructuredSuspendedResourceStack externals
      program entryRuntime entryStore entryWitness functionResult
      expectedResult targetSourceFrames targetTargetFrames) :
    ∃ targetSupported : ConcreteStructuredSupportedFrameStack program
        sourceModule targetModule hosts functionResult expectedResult
        targetSourceFrames targetTargetFrames,
      targetSupported.Agrees targetResources := by
  subst targetSourceFrames
  subst targetTargetFrames
  exact ⟨supported, agrees⟩

/-- Branch-exact admission-free control for the strong simulation relation.

This is a direct inductive sum rather than a graph indexed by a proof-valued
`ConcreteStructuredCodeStepOutcome`.  Lean proof irrelevance makes an outcome
proof unsuitable as a branch discriminator.  Each constructor therefore owns
its compiler/resource core and the supported stack that agrees with that same
core.  The code constructor deliberately omits current-step admission. -/
inductive ConcreteStructuredSupportedOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (functionCode : Lean.Compiler.LCNF.Code .impure)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind) :
    MachineState → StructuredWasmState Host → Prop where
  | code
      {source target facts remainingBytes sourceRuntime sourceEnv sourceCode
        targetStore targetLocals targetCode witness}
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (related : ConcreteStructuredCodeCoreRel program context sourceModule
        sourceFunction externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        sourceEnv sourceCode targetStore targetLocals targetCode witness source
        target)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult source.frames
        target.frames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target
  | directReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      {site : DirectInternalCallSite context decl callerEnv}
      {row : ConcreteGeneratedInternalDeclaration context.program
        site.sourceDeclaration calleeContext site.calleeCode sourceModule
        calleeFunction targetModule}
      {sourceRuntime : RuntimeState}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {resultIndex : Nat}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      (related : ConcreteStructuredDirectCallReadyCoreRel program context
        calleeContext sourceModule sourceFunction calleeFunction targetModule
        site row externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        continuation callerJoins sourceFrames targetStore callerLocals
        callerRemainder targetRest targetFrames witness physicalArgs resultIndex
        source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target
  | saturatedReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {sourceRuntime : RuntimeState}
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      {sourceFrames : List Frame}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {targetValue targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {resultIndex : Nat}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      {site : SaturatedClosureCallSite context decl callerEnv}
      {resolution : SaturatedClosureCallResolution context sourceRuntime site}
      (row : ConcreteGeneratedInternalDeclaration context.program
        resolution.target calleeContext resolution.calleeCode sourceModule
        calleeFunction targetModule)
      (sharedCapacity : ∀ parentRuntime,
        setCell sourceRuntime resolution.location
            { resolution.cell with rc := resolution.cell.rc - 1 } =
              .ok parentRuntime →
          ClosureRetainCapacity parentRuntime resolution.captures.toList)
      (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
        sourceModule sourceFunction targetModule site externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
        targetStore callerLocals targetValue targetRest targetFrames witness
        resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target
  | externalReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceValue : Value}
      {stepCost : Nat}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {site : PureExternalCallShape context externals sourceRuntime sourceEnv
        decl nextRuntime sourceValue stepCost}
      {operation : ExternalOperation}
      {resolvedResultKind : AbiKind}
      {targetImport : Wasm.ImportDecl}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {callIndex resultIndex : Nat}
      {facts : ReuseCapacityFacts}
      {remainingBytes : Nat}
      (related : ConcreteStructuredExternalCallReadyCoreRel program context
        sourceModule sourceFunction targetModule hosts externals site operation
        resolvedResultKind targetImport labels continuation callerJoins
        sourceFrames entryRuntime entryStore entryWitness functionResult
        callerExpectedResult facts remainingBytes targetStore callerLocals
        callerRemainder targetRest targetFrames witness physicalArgs callIndex
        resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target
  | externalBind
      {source target facts remainingBytes sourceRuntime callerEnv sourceValue
        result continuation callerJoins sourceFrames targetStore callerLocals
        callerRemainder targetRest targetFrames witness kind physical
        resultIndex}
      (related : ConcreteStructuredExternalBindCoreRel program context
        sourceModule sourceFunction externals labels entryRuntime entryStore
        entryWitness functionResult callerExpectedResult facts remainingBytes
        sourceRuntime callerEnv sourceValue result continuation callerJoins
        sourceFrames targetStore callerLocals callerRemainder targetRest
        targetFrames witness kind physical resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target
  | returned
      {source target facts remainingBytes sourceRuntime sourceEnv sourceValue
        targetStore targetLocals witness kind physical}
      (yielded : ConcreteStructuredYieldFocus context sourceFunction
        sourceRuntime sourceEnv sourceValue targetStore targetLocals witness kind
        physical source target)
      (compatible : ConcreteStructuredResultCompatible kind callerExpectedResult)
      (resources : ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult source.frames
        target.frames)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult source.frames
        target.frames)
      (agrees : supported.Agrees resources.suspended) :
      ConcreteStructuredSupportedOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        source target

/-- Module-wide open successor relation for the pointwise simulation.

The existential package hides the currently active generated function,
entry anchor, resource budget, and caller-result ABI.  Its inner control sum
still distinguishes ordinary code, poised calls, and returned control.  This
is the first relation whose type remains unchanged when an internal call moves
from a caller context/function to its generated callee. -/
def ConcreteStructuredGlobalOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (sourceModule : Fir.Wasm.Module)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (externals : ExternalImpl)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop :=
  ∃ (context : Fir.Wasm.Context)
      (functionCode : Lean.Compiler.LCNF.Code .impure)
      (sourceFunction : Fir.Wasm.Function)
      (spec : ConcreteSupportedFunction program context functionCode
        sourceModule sourceFunction targetModule hosts)
      (labels : List Lean.FVarId)
      (entryRuntime : RuntimeState)
      (entryStore : Wasm.Store Host)
      (entryWitness : RefinementWitness)
      (functionResult : AbiKind)
      (callerExpectedResult : Option AbiKind)
      (facts : ReuseCapacityFacts)
      (remainingBytes : Nat),
    ConcreteStructuredCodeStepOutcome program context functionCode sourceModule
      sourceFunction targetModule hosts spec externals labels entryRuntime
      entryStore entryWitness functionResult callerExpectedResult facts
      remainingBytes source target

/-- The recursively stable module-wide compiler relation.

`ConcreteStructuredGlobalOutcome` is intentionally the lower open control
sum.  This stronger relation additionally retains the compiler identity of
every suspended caller and proves that the static and dynamic stacks agree
constructor by constructor.  It still contains no future source admission,
source evaluation, target execution path, or termination evidence. -/
def ConcreteStructuredSupportedGlobalOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (sourceModule : Fir.Wasm.Module)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (externals : ExternalImpl)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop :=
  ∃ (context : Fir.Wasm.Context)
      (functionCode : Lean.Compiler.LCNF.Code .impure)
      (sourceFunction : Fir.Wasm.Function)
      (spec : ConcreteSupportedFunction program context functionCode
        sourceModule sourceFunction targetModule hosts)
      (labels : List Lean.FVarId)
      (entryRuntime : RuntimeState)
      (entryStore : Wasm.Store Host)
      (entryWitness : RefinementWitness)
      (functionResult : AbiKind)
      (callerExpectedResult : Option AbiKind),
    ConcreteStructuredSupportedOutcome program context functionCode sourceModule
      sourceFunction targetModule hosts spec externals labels entryRuntime
      entryStore entryWitness functionResult callerExpectedResult source target

/-- Hide the active generated-function indices of a branch-exact supported
control state. -/
theorem ConcreteStructuredSupportedOutcome.toGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSupportedOutcome program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      source target) :
    ConcreteStructuredSupportedGlobalOutcome program sourceModule targetModule
      hosts externals source target :=
  ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
    entryStore, entryWitness, functionResult, callerExpectedResult, related⟩

/-- Lift one function-local pointwise successor into the module-wide relation.
No proof data are changed; only the active-function indices become hidden. -/
theorem ConcreteStructuredCodeStepOutcome.toGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeStepOutcome program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes source target) :
    ConcreteStructuredGlobalOutcome program sourceModule targetModule hosts
      externals source target :=
  ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
    entryStore, entryWitness, functionResult, callerExpectedResult, facts,
    remainingBytes, related⟩

/-- Every open module-wide successor preserves the exact world/trace prefix.
Observation is independent of whether control is ordinary code, a poised
generated call, or a yielded/returned stack protocol. -/
theorem ConcreteStructuredGlobalOutcome.observes
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredGlobalOutcome program sourceModule
      targetModule hosts externals source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  rcases related with
    ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
      entryStore, entryWitness, functionResult, callerExpectedResult, facts,
      remainingBytes, outcome⟩
  cases outcome with
  | code _ core => exact core.observes
  | directReady _site _row ready => exact ready.observes
  | saturatedReady _site _resolution _sharedCapacity _row ready =>
      exact ready.observes
  | externalReady ready => exact ready.observes
  | externalBind ready => exact ready.observes
  | returned yielded _compatible _resources => exact yielded.observes

/-- The strong module-wide relation inherits the same exact world/trace
observation law as its lower open outcome. -/
theorem ConcreteStructuredSupportedGlobalOutcome.observes
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredSupportedGlobalOutcome program sourceModule
      targetModule hosts externals source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  rcases related with
    ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
      entryStore, entryWitness, functionResult, callerExpectedResult, active⟩
  cases active with
  | code _contextCaches core _supported _agrees => exact core.observes
  | directReady ready _contextCaches _supported _agrees =>
      exact ready.observes
  | saturatedReady _row _sharedCapacity ready _contextCaches _supported
      _agrees => exact ready.observes
  | externalReady ready _contextCaches _supported _agrees =>
      exact ready.observes
  | externalBind ready _contextCaches _supported _agrees =>
      exact ready.observes
  | returned yielded _compatible _resources _contextCaches _supported
      _agrees => exact yielded.observes

/-- A poised generated named call takes one source step and one target call
step into the same module-wide relation, now existentially anchored at the
callee function. -/
theorem ConcreteStructuredDirectCallReadyCoreRel.advance_global_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {callerCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program callerContext callerCode
      sourceModule callerFunction targetModule hosts)
    (related : ConcreteStructuredDirectCallReadyCoreRel program callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs resultIndex
      source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1 target
          targetAfter ∧
        ConcreteStructuredGlobalOutcome program sourceModule targetModule hosts
          externals sourceAfter targetAfter := by
  obtain ⟨targetAfter, targetPath, nextCore, _callerScope, _entry⟩ :=
    related.advance_enter_of_step spec.contextProgram sourceStep
  have rowAtProgram :
      ConcreteGeneratedInternalDeclaration program site.sourceDeclaration
        calleeContext site.calleeCode sourceModule calleeFunction
        targetModule := by
    simpa only [spec.contextProgram] using row
  let calleeSpec :
      ConcreteSupportedFunction program calleeContext site.calleeCode
        sourceModule calleeFunction targetModule hosts :=
    rowAtProgram.toSupportedFunctionOfFunction spec
  have nextOutcome :
      ConcreteStructuredCodeStepOutcome program calleeContext site.calleeCode
        sourceModule calleeFunction targetModule hosts calleeSpec externals []
        sourceRuntime targetStore witness site.calleeResultKind
        (some site.resultKind) [] remainingBytes sourceAfter targetAfter :=
    .code rowAtProgram.contextCaches nextCore
  exact ⟨targetAfter, targetPath, nextOutcome.toGlobal⟩

/-- A staged named call also preserves the recursively stable relation when
the caller's supported-function identity is aligned with its resource tail.
The entry theorem supplies the exact saved caller scope and bind/call layout;
this theorem pushes their static and dynamic descriptions together before
reindexing them to the callee states. -/
theorem ConcreteStructuredDirectCallReadyCoreRel.advance_supportedGlobal_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {callerContext calleeContext : Fir.Wasm.Context}
    {callerCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {callerFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {site : DirectInternalCallSite callerContext decl callerEnv}
    {row : ConcreteGeneratedInternalDeclaration callerContext.program
      site.sourceDeclaration calleeContext site.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {callerLocals : Wasm.Locals}
    {callerRemainder : List Wasm.Value}
    {targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {physicalArgs : List Wasm.Value}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program callerContext callerCode
      sourceModule callerFunction targetModule hosts)
    (related : ConcreteStructuredDirectCallReadyCoreRel program callerContext
      calleeContext sourceModule callerFunction calleeFunction targetModule
      site row externals labels entryRuntime entryStore entryWitness
      functionResult callerExpectedResult facts remainingBytes sourceRuntime
      continuation callerJoins sourceFrames targetStore callerLocals
      callerRemainder targetRest targetFrames witness physicalArgs resultIndex
      source target)
    (contextCaches :
      callerContext.cachedDeclarations =
        Fir.Wasm.cachedDeclarationNames program)
    (supported : ConcreteStructuredSupportedFrameStack program sourceModule
      targetModule hosts functionResult callerExpectedResult sourceFrames
      targetFrames)
    (agrees : supported.Agrees related.resources.suspended)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 1 target
          targetAfter ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter := by
  obtain ⟨targetAfter, targetPath, nextCore, callerScope, entry⟩ :=
    related.advance_enter_of_step (hostEnv := hosts.env) spec.contextProgram
      sourceStep
  have rowAtProgram :
      ConcreteGeneratedInternalDeclaration program site.sourceDeclaration
        calleeContext site.calleeCode sourceModule calleeFunction
        targetModule := by
    simpa only [spec.contextProgram] using row
  let calleeSpec :
      ConcreteSupportedFunction program calleeContext site.calleeCode
        sourceModule calleeFunction targetModule hosts :=
    rowAtProgram.toSupportedFunctionOfFunction spec
  let storedCallerLocals : Wasm.Locals :=
    { callerLocals with
      values := physicalArgs.reverse ++ callerRemainder }
  let pushedSupported :=
    ConcreteStructuredSupportedFrameStack.direct
      (callerEnv := callerEnv) (callerJoins := callerJoins)
      (callerLocals := storedCallerLocals)
      (callerRemainder := callerRemainder) spec contextCaches
      entry.continuationAdapted entry.resultFound entry.resultKindAt
      site.calleeResultRefines supported
  let pushedResources :=
    ConcreteStructuredSuspendedResourceStack.direct
      (callerEnv := callerEnv) (callerJoins := callerJoins)
      (callerLocals := storedCallerLocals)
      (callerRemainder := callerRemainder) callerScope spec.contextProgram.symm
      entry.continuationAdapted entry.resultFound entry.resultKindAt
      site.calleeResultRefines related.resources.suspended
  have pushedAgrees : pushedSupported.Agrees pushedResources := by
    exact ConcreteStructuredSupportedFrameStack.Agrees.direct
      spec contextCaches callerScope spec.contextProgram.symm
      entry.continuationAdapted entry.resultFound entry.resultKindAt
      site.calleeResultRefines supported related.resources.suspended agrees
  obtain ⟨supportedAfter, agreesAfter⟩ :=
    pushedAgrees.reindex entry.sourceFramesEq entry.targetFramesEq
      nextCore.resources.suspended
  let nextActive :
      ConcreteStructuredSupportedOutcome program calleeContext site.calleeCode
        sourceModule calleeFunction targetModule hosts calleeSpec externals []
        sourceRuntime targetStore witness site.calleeResultKind
        (some site.resultKind) sourceAfter targetAfter :=
    .code rowAtProgram.contextCaches nextCore supportedAfter agreesAfter
  exact ⟨targetAfter, targetPath, calleeContext, site.calleeCode,
    calleeFunction, calleeSpec, [], sourceRuntime, targetStore, witness,
    site.calleeResultKind, some site.resultKind, nextActive⟩

/-- A poised exactly saturated closure call likewise enters its selected
generated callee inside the unchanged module-wide relation.  The only dynamic
premise is the ordinary source step; closure consumption is reconstructed
internally. -/
theorem ConcreteStructuredSaturatedCallReadyCoreRel.advance_global_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    {resolution : SaturatedClosureCallResolution context sourceRuntime site}
    {row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
      sourceModule sourceFunction targetModule site externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals targetValue targetRest targetFrames witness
      resultIndex source target)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        0 < targetCount ∧
        ConcreteStructuredGlobalOutcome program sourceModule targetModule hosts
          externals sourceAfter targetAfter := by
  obtain ⟨targetAfter, nextStore, physicalArgs, matcherCount, argumentCount,
      callRuntime, targetPath, nextCore, _callerScope, _entry⟩ :=
    related.advance_enter_of_step resolution row spec sharedCapacity sourceStep
  have rowAtProgram :
      ConcreteGeneratedInternalDeclaration program resolution.target
        calleeContext resolution.calleeCode sourceModule calleeFunction
        targetModule := by
    simpa only [spec.contextProgram] using row
  let calleeSpec :
      ConcreteSupportedFunction program calleeContext resolution.calleeCode
        sourceModule calleeFunction targetModule hosts :=
    rowAtProgram.toSupportedFunctionOfFunction spec
  have nextOutcome :
      ConcreteStructuredCodeStepOutcome program calleeContext
        resolution.calleeCode sourceModule calleeFunction targetModule hosts
        calleeSpec externals [] callRuntime nextStore witness
        resolution.targetResultKind (some site.resultKind) [] remainingBytes
        sourceAfter targetAfter :=
    .code rowAtProgram.contextCaches nextCore
  refine ⟨3 * (matcherCount + 1) + argumentCount + 1, targetAfter,
    targetPath, ?_, nextOutcome.toGlobal⟩
  omega

/-- Exactly saturated closure entry preserves the recursively stable relation.
The saved caller scope is first updated to the consumed call store and then to
the physical-argument prefix recorded by the generated call frame.  Static
support and dynamic resources are pushed with the same matcher protocol. -/
theorem ConcreteStructuredSaturatedCallReadyCoreRel.advance_supportedGlobal_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context calleeContext : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction calleeFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {callerEnv : Env}
    {sourceRuntime : RuntimeState}
    {site : SaturatedClosureCallSite context decl callerEnv}
    {resolution : SaturatedClosureCallResolution context sourceRuntime site}
    {row : ConcreteGeneratedInternalDeclaration context.program
      resolution.target calleeContext resolution.calleeCode sourceModule
      calleeFunction targetModule}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {callerJoins : JoinEnv}
    {sourceFrames : List Frame}
    {targetStore : Wasm.Store Host}
    {callerLocals : Wasm.Locals}
    {targetValue targetRest : Wasm.Program}
    {targetFrames : List StructuredWasmFrame}
    {witness : RefinementWitness}
    {resultIndex : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
      sourceModule sourceFunction targetModule site externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
      targetStore callerLocals targetValue targetRest targetFrames witness
      resultIndex source target)
    (contextCaches :
      context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
    (supported : ConcreteStructuredSupportedFrameStack program sourceModule
      targetModule hosts functionResult callerExpectedResult sourceFrames
      targetFrames)
    (agrees : supported.Agrees related.resources.suspended)
    (sharedCapacity : ∀ parentRuntime,
      setCell sourceRuntime resolution.location
          { resolution.cell with rc := resolution.cell.rc - 1 } =
            .ok parentRuntime →
        ClosureRetainCapacity parentRuntime resolution.captures.toList)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        0 < targetCount ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter := by
  obtain ⟨targetAfter, nextStore, physicalArgs, matcherCount, argumentCount,
      callRuntime, targetPath, nextCore, callerScope, entry⟩ :=
    related.advance_enter_of_step resolution row spec sharedCapacity sourceStep
  have rowAtProgram :
      ConcreteGeneratedInternalDeclaration program resolution.target
        calleeContext resolution.calleeCode sourceModule calleeFunction
        targetModule := by
    simpa only [spec.contextProgram] using row
  let calleeSpec :
      ConcreteSupportedFunction program calleeContext resolution.calleeCode
        sourceModule calleeFunction targetModule hosts :=
    rowAtProgram.toSupportedFunctionOfFunction spec
  let savedCallerScope :=
    callerScope.withValues (physicalArgs.reverse ++ callerLocals.values)
  let pushedSupported :=
    ConcreteStructuredSupportedFrameStack.saturated
      (callerEnv := callerEnv) (callerJoins := callerJoins)
      (callerLocals := callerLocals) (physicalArgs := physicalArgs)
      (callerRemainder := callerLocals.values) (matcherCount := matcherCount)
      spec contextCaches entry.continuationAdapted entry.resultFound
      entry.resultKindAt resolution.targetResultRefines supported
  let pushedResources :=
    ConcreteStructuredSuspendedResourceStack.saturated
      (callerEnv := callerEnv) (callerJoins := callerJoins)
      (callerLocals := callerLocals) (physicalArgs := physicalArgs)
      (callerRemainder := callerLocals.values) (matcherCount := matcherCount)
      savedCallerScope spec.contextProgram.symm entry.continuationAdapted
      entry.resultFound entry.resultKindAt resolution.targetResultRefines
      related.resources.suspended
  have pushedAgrees : pushedSupported.Agrees pushedResources := by
    exact ConcreteStructuredSupportedFrameStack.Agrees.saturated
      spec contextCaches savedCallerScope spec.contextProgram.symm
      entry.continuationAdapted entry.resultFound entry.resultKindAt
      resolution.targetResultRefines supported related.resources.suspended
      agrees
  obtain ⟨supportedAfter, agreesAfter⟩ :=
    pushedAgrees.reindex entry.sourceFramesEq entry.targetFramesEq
      nextCore.resources.suspended
  let nextActive :
      ConcreteStructuredSupportedOutcome program calleeContext
        resolution.calleeCode sourceModule calleeFunction targetModule hosts
        calleeSpec externals [] callRuntime nextStore witness
        resolution.targetResultKind (some site.resultKind) sourceAfter
        targetAfter :=
    .code rowAtProgram.contextCaches nextCore supportedAfter agreesAfter
  refine ⟨3 * (matcherCount + 1) + argumentCount + 1, targetAfter,
    targetPath, ?_, calleeContext, resolution.calleeCode, calleeFunction,
    calleeSpec, [], callRuntime, nextStore, witness,
    resolution.targetResultKind, some site.resultKind, nextActive⟩
  omega

/-- Close one yielded generated callee against its exact supported/resource
caller head.

The source supplies only its ordinary successful step.  `Agrees` determines
whether the saved protocol is direct or saturated, recovers the corresponding
supported caller function, and rules out the empty/root stack (which is
terminal).  The established pop theorem then restores caller code and the
tail agreement relation. -/
theorem ConcreteStructuredYieldFocus.advance_pop_supportedGlobal_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {currentContext : Fir.Wasm.Context}
    {currentCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {currentFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {currentFacts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {currentFunctionResult actualKind : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {sourceValue : Value}
    {physical : Wasm.Value}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (currentSpec : ConcreteSupportedFunction program currentContext currentCode
      sourceModule currentFunction targetModule hosts)
    (yielded : ConcreteStructuredYieldFocus currentContext currentFunction
      sourceRuntime sourceEnv sourceValue targetStore targetLocals witness
      actualKind physical source target)
    (compatible :
      ConcreteStructuredResultCompatible actualKind callerExpectedResult)
    (resources : ConcreteStructuredResourceStack program currentContext
      sourceModule currentFunction externals entryRuntime sourceRuntime
      entryStore targetStore entryWitness witness currentFacts remainingBytes
      sourceEnv targetLocals currentFunctionResult callerExpectedResult
      source.frames target.frames)
    (supported : ConcreteStructuredSupportedFrameStack program sourceModule
      targetModule hosts currentFunctionResult callerExpectedResult
      source.frames target.frames)
    (agrees : supported.Agrees resources.suspended)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        0 < targetCount ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter := by
  generalize sourceFramesEq : source.frames = sourceFrames at resources supported agrees
  generalize targetFramesEq : target.frames = targetFrames at resources supported agrees
  rcases resources with ⟨currentScope, resourceStack⟩
  change supported.Agrees resourceStack at agrees
  induction agrees generalizing target with
  | @case _ _ _ _ _ sourceFrames targetFrames belowStack targetRest testCount
      supportedTail resourceTail tailAgrees ih =>
      subst sourceFrames
      let targetPopped : StructuredWasmState Host := {
        store := targetStore
        control := .returning (physical :: targetLocals.values)
        frames := targetFrames }
      have unwindTarget :
          FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
            testCount target targetPopped := by
        rcases target with ⟨actualStore, actualControl, actualFrames⟩
        have storeEq := yielded.targetStoreEq
        change actualStore = targetStore at storeEq
        subst actualStore
        have controlEq := yielded.targetControlEq
        change actualControl = .returning (physical :: targetLocals.values)
          at controlEq
        subst actualControl
        change actualFrames = _ at targetFramesEq
        subst actualFrames
        simpa [targetPopped] using
          (structuredWasmReturnCaseLabelsFinitePath
            (module := targetModule.wasmModule) (hostEnv := hosts.env)
            (store := targetStore)
            (values := physical :: targetLocals.values)
            (belowStack := belowStack) (rest := targetRest)
            (frames := targetFrames) testCount)
      have yieldedPopped :
          ConcreteStructuredYieldFocus currentContext currentFunction
            sourceRuntime sourceEnv sourceValue targetStore targetLocals witness
            actualKind physical source targetPopped := {
        sourceProgramEq := yielded.sourceProgramEq
        sourceControlEq := yielded.sourceControlEq
        sourceEnvEq := yielded.sourceEnvEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        targetStoreEq := by simp [targetPopped]
        targetControlEq := by simp [targetPopped]
        stateRelated := yielded.stateRelated
        frameAligned := yielded.frameAligned
        valueRelated := yielded.valueRelated }
      obtain ⟨tailCount, targetAfter, tailPath, tailPositive, nextGlobal⟩ :=
        ih yieldedPopped compatible (by rfl) (by simp [targetPopped])
          currentScope
      exact ⟨testCount + tailCount, targetAfter,
        unwindTarget.trans tailPath, by omega, nextGlobal⟩
  | nil =>
      rcases source with
        ⟨sourceProgram, sourceControl, stateEnv, stateJoins, sourceFrames,
          runtime⟩
      have controlEq := yielded.sourceControlEq
      change sourceControl = .yielded sourceValue at controlEq
      subst sourceControl
      change sourceFrames = [] at sourceFramesEq
      subst sourceFrames
      simp [executeStep, coreStep] at sourceStep
  | @direct activeEntryRuntime callerEntryRuntime activeEntryStore
      callerEntryStore activeEntryWitness callerEntryWitness callerContext
      callerCode callerFunction labels facts callerBytes callerEnv callerLocals
      result continuation callerJoins sourceFrames callerRemainder targetRest
      targetFrames calleeResult callerResult kind resultIndex tailResult
      callerSpec contextCaches callerScope programEq continuationAdapted
      resultFound kindAt calleeCompatible supportedTail resourceTail
      tailAgrees =>
      have callerStateRelated :
          StateRelated callerFunction sourceRuntime callerEnv targetStore
            callerLocals witness :=
        currentScope.transports.savedStateRelated callerScope.stateRelated
          currentScope.stateRelated
      have callerFrameAligned :
          ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv
            targetStore callerLocals witness := by
        simpa [ConcreteLocalFrameAligned] using callerScope.frameAligned
      let resumed : ConcreteStructuredBindFrameFocus callerContext sourceModule
          callerFunction labels sourceRuntime callerEnv sourceValue result
          continuation callerJoins sourceFrames targetStore callerLocals
          callerRemainder targetRest targetFrames targetLocals.values witness
          kind physical resultIndex source target := {
        sourceProgramEq := yielded.sourceProgramEq.trans
          (currentSpec.contextProgram.trans programEq)
        sourceControlEq := yielded.sourceControlEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        sourceFramesEq
        targetStoreEq := yielded.targetStoreEq
        targetControlEq := yielded.targetControlEq
        targetFramesEq
        continuationAdapted
        stateRelated := callerStateRelated
        frameAligned := callerFrameAligned
        resultFound
        kindAt
        valueRelated := yielded.valueRelated.ofRefines compatible }
      have resourceTailAtCaller :
          ConcreteStructuredSuspendedResourceStack externals
            callerContext.program callerEntryRuntime callerEntryStore
            callerEntryWitness callerResult tailResult sourceFrames
            targetFrames := by
        exact programEq ▸ resourceTail
      obtain ⟨computedAfter, targetAfter, resumedLocals, computedStep,
          targetPath, nextCore, sourceFramesAfter, targetFramesAfter⟩ :=
        resumed.advance_popCore (module := targetModule.wasmModule)
          (hostEnv := hosts.env) callerScope currentScope
          (currentSpec.contextProgram.trans programEq) resourceTailAtCaller
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      have nextCoreAtProgram :
          ConcreteStructuredCodeCoreRel program callerContext sourceModule
            callerFunction externals labels callerEntryRuntime callerEntryStore
            callerEntryWitness callerResult tailResult
            (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
            (bind callerEnv result sourceValue) continuation targetStore
            resumedLocals targetRest witness sourceAfter targetAfter := by
        simpa only [programEq] using nextCore
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        tailAgrees.reindex sourceFramesAfter targetFramesAfter
          nextCoreAtProgram.resources.suspended
      let nextActive :
          ConcreteStructuredSupportedOutcome program callerContext callerCode
            sourceModule callerFunction targetModule hosts callerSpec externals
            labels callerEntryRuntime callerEntryStore callerEntryWitness
            callerResult tailResult sourceAfter targetAfter :=
        .code contextCaches nextCoreAtProgram supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath, by omega,
        ⟨callerContext, callerCode, callerFunction, callerSpec, labels,
          callerEntryRuntime, callerEntryStore, callerEntryWitness,
          callerResult, tailResult, nextActive⟩⟩
  | @saturated activeEntryRuntime callerEntryRuntime activeEntryStore
      callerEntryStore activeEntryWitness callerEntryWitness callerContext
      callerCode callerFunction labels facts callerBytes callerEnv callerLocals
      result continuation callerJoins sourceFrames physicalArgs callerRemainder
      targetRest targetFrames calleeResult callerResult kind resultIndex
      matcherCount tailResult callerSpec contextCaches callerScope programEq
      continuationAdapted resultFound kindAt calleeCompatible supportedTail
      resourceTail tailAgrees =>
      let savedCallerLocals : Wasm.Locals :=
        { callerLocals with
          values := physicalArgs.reverse ++ callerRemainder }
      have callerStateRelated :
          StateRelated callerFunction sourceRuntime callerEnv targetStore
            savedCallerLocals witness := by
        simpa [savedCallerLocals] using
          currentScope.transports.savedStateRelated callerScope.stateRelated
            currentScope.stateRelated
      have callerFrameAligned :
          ConcreteLocalFrameAligned callerFunction sourceRuntime callerEnv
            targetStore savedCallerLocals witness := by
        simpa [savedCallerLocals, ConcreteLocalFrameAligned] using
          callerScope.frameAligned
      let resumed : ConcreteStructuredSaturatedBindFrameFocus callerContext
          sourceModule callerFunction labels sourceRuntime callerEnv sourceValue
          result continuation callerJoins sourceFrames targetStore
          savedCallerLocals targetLocals physicalArgs callerRemainder targetRest
          targetFrames witness kind physical resultIndex matcherCount source
          target := {
        sourceProgramEq := yielded.sourceProgramEq.trans
          (currentSpec.contextProgram.trans programEq)
        sourceControlEq := yielded.sourceControlEq
        sourceRuntimeEq := yielded.sourceRuntimeEq
        sourceFramesEq
        targetStoreEq := yielded.targetStoreEq
        targetControlEq := yielded.targetControlEq
        targetFramesEq := by
          simpa [savedCallerLocals] using targetFramesEq
        continuationAdapted
        stateRelated := callerStateRelated
        frameAligned := callerFrameAligned
        resultFound
        kindAt
        valueRelated := yielded.valueRelated.ofRefines compatible }
      have callerScopeAtSaved :
          ConcreteStructuredResourceScope callerContext sourceModule
            callerFunction externals callerEntryRuntime callerEntryStore
            callerEntryWitness facts callerBytes activeEntryRuntime callerEnv
            activeEntryStore
            { savedCallerLocals with
              values := physicalArgs.reverse ++ callerRemainder }
            activeEntryWitness := by
        simpa [savedCallerLocals] using callerScope
      have resourceTailAtCaller :
          ConcreteStructuredSuspendedResourceStack externals
            callerContext.program callerEntryRuntime callerEntryStore
            callerEntryWitness callerResult tailResult sourceFrames
            targetFrames := by
        exact programEq ▸ resourceTail
      obtain ⟨computedAfter, targetAfter, resumedLocals, computedStep,
          targetPath, nextCore, sourceFramesAfter, targetFramesAfter⟩ :=
        resumed.advance_popCore (module := targetModule.wasmModule)
          (hostEnv := hosts.env) callerScopeAtSaved currentScope
          (currentSpec.contextProgram.trans programEq) resourceTailAtCaller
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      have nextCoreAtProgram :
          ConcreteStructuredCodeCoreRel program callerContext sourceModule
            callerFunction externals labels callerEntryRuntime callerEntryStore
            callerEntryWitness callerResult tailResult
            (eraseReuseCapacityFact facts result) remainingBytes sourceRuntime
            (bind callerEnv result sourceValue) continuation targetStore
            resumedLocals targetRest witness sourceAfter targetAfter := by
        simpa only [programEq] using nextCore
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        tailAgrees.reindex sourceFramesAfter targetFramesAfter
          nextCoreAtProgram.resources.suspended
      let nextActive :
          ConcreteStructuredSupportedOutcome program callerContext callerCode
            sourceModule callerFunction targetModule hosts callerSpec externals
            labels callerEntryRuntime callerEntryStore callerEntryWitness
            callerResult tailResult sourceAfter targetAfter :=
        .code contextCaches nextCoreAtProgram supportedAfter agreesAfter
      exact ⟨matcherCount + 5, targetAfter, targetPath, by omega,
        ⟨callerContext, callerCode, callerFunction, callerSpec, labels,
          callerEntryRuntime, callerEntryStore, callerEntryWitness,
          callerResult, tailResult, nextActive⟩⟩

/-- A compiler-produced root focus and its ordinary resource invariant start
the pointwise relation.  The empty source/target stacks are proved at the
canonical entry rather than assumed by later transition rules. -/
theorem ConcreteStructuredCodePointwiseRel.root
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {facts : ReuseCapacityFacts}
    {requiredBytes : Nat}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {sourceCode : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {functionResult : AbiKind}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (focus : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv sourceCode targetStore targetLocals
      targetCode witness source target)
    (invariant : ConcreteReuseCapacityCacheAbiFrame context sourceModule
      sourceFunction externals facts remainingBytes sourceRuntime sourceEnv
      targetStore targetLocals witness)
    (contextCaches :
      context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
    (admitted : ConcreteStructuredCodeStepAdmission context externals
      functionResult facts
      sourceRuntime sourceEnv requiredBytes sourceCode)
    (budget : requiredBytes ≤ remainingBytes)
    (sourceFrames : source.frames = [])
    (targetFrames : target.frames = []) :
    ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      sourceRuntime targetStore witness functionResult none facts requiredBytes
      remainingBytes sourceRuntime sourceEnv sourceCode targetStore targetLocals
      targetCode witness source target := by
  have scope := ConcreteStructuredResourceScope.root invariant
  have resourcesAtRoot :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals sourceRuntime sourceRuntime targetStore
        targetStore witness witness facts remainingBytes sourceEnv targetLocals
        functionResult none [] [] :=
    ConcreteStructuredResourceStack.root scope
  have resources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals sourceRuntime sourceRuntime targetStore
        targetStore witness witness facts remainingBytes sourceEnv targetLocals
        functionResult none source.frames target.frames := by
    rw [sourceFrames, targetFrames]
    exact resourcesAtRoot
  exact ⟨contextCaches, focus, resources, admitted, budget⟩

/-- A successful interpreter step at a default-only case recovers the source
branch-selection boundary.  The admission fixes the unique selected branch;
the ordinary step supplies only the successful discriminator lookup and tag. -/
theorem ConcreteStructuredCodeFocus.defaultOnlyCaseResult_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.cases cases) targetStore targetLocals
      targetCode witness source target)
    (supported :
      DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    SourceCaseResult sourceRuntime sourceEnv cases selected := by
  rcases source with
    ⟨sourceProgram, sourceControl, actualEnv, sourceJoins, sourceFrames,
      actualRuntime⟩
  have programEq := related.sourceProgramEq
  change sourceProgram = context.program at programEq
  subst sourceProgram
  have controlEq := related.sourceControlEq
  change sourceControl = .code (.cases cases) at controlEq
  subst sourceControl
  have envEq := related.sourceEnvEq
  change actualEnv = sourceEnv at envEq
  subst actualEnv
  have runtimeEq := related.sourceRuntimeEq
  change actualRuntime = sourceRuntime at runtimeEq
  subst actualRuntime
  cases found : lookupValue sourceEnv cases.discr with
  | error fault =>
      simp [executeStep, coreStep, found, fail] at sourceStep
  | ok discrValue =>
      cases tagged : getTag sourceRuntime discrValue with
      | error fault =>
          simp [executeStep, coreStep, found, tagged, fail] at sourceStep
      | ok tag =>
          have chosen :
              chooseAlt tag cases.alts.toList = some selected := by
            unfold DefaultOnlyCaseSupported at supported
            rw [supported]
            simp [chooseAlt, findCtorAlt, findDefaultAlt]
          exact ⟨discrValue, tag, found, tagged, chosen⟩

/-- A successful interpreter step at a singleton object-constructor case
recovers both its source selection fact and the exact selected-arm machine
state.  The compiler admission fixes the only possible arm; no execution
certificate is retained after this current step. -/
theorem ConcreteStructuredCodeFocus.singleObjectCaseResult_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {externals : ExternalImpl}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.cases cases) targetStore targetLocals
      targetCode witness source target)
    (supported : SingleObjectConstructorCaseSupported context sourceRuntime
      sourceEnv cases selected)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    SourceCaseResult sourceRuntime sourceEnv cases selected ∧
      sourceAfter = { source with control := .code selected } := by
  rcases supported with
    ⟨info, altsEq, _modeEq, _expectedTagFits, _discrCompiled,
      _actualTagFits⟩
  rcases source with
    ⟨sourceProgram, sourceControl, actualEnv, sourceJoins, sourceFrames,
      actualRuntime⟩
  have programEq := related.sourceProgramEq
  change sourceProgram = context.program at programEq
  subst sourceProgram
  have controlEq := related.sourceControlEq
  change sourceControl = .code (.cases cases) at controlEq
  subst sourceControl
  have envEq := related.sourceEnvEq
  change actualEnv = sourceEnv at envEq
  subst actualEnv
  have runtimeEq := related.sourceRuntimeEq
  change actualRuntime = sourceRuntime at runtimeEq
  subst actualRuntime
  cases found : lookupValue sourceEnv cases.discr with
  | error fault =>
      simp [executeStep, coreStep, found, fail] at sourceStep
  | ok discrValue =>
      cases tagged : getTag sourceRuntime discrValue with
      | error fault =>
          simp [executeStep, coreStep, found, tagged, fail] at sourceStep
      | ok tag =>
          cases chosen : chooseAlt tag cases.alts.toList with
          | none =>
              simp [executeStep, coreStep, found, tagged, chosen, fail] at sourceStep
          | some selectedArm =>
              have selectedEq : selectedArm = selected := by
                rw [altsEq] at chosen
                simp [chooseAlt, findCtorAlt, findDefaultAlt] at chosen
                simp_all
              subst selectedArm
              have afterEq :
                  ({ program := context.program
                     control := .code selected
                     env := sourceEnv
                     joins := sourceJoins
                     frames := sourceFrames
                     runtime := sourceRuntime } : MachineState) = sourceAfter := by
                simpa [executeStep, coreStep, found, tagged, chosen] using
                  sourceStep
              exact ⟨⟨discrValue, tag, found, tagged, chosen⟩, afterEq.symm⟩

/-- A successful interpreter step at an admitted direct-value node exposes the
existing source semantic boundary.  The focus identifies the actual machine
with the compiled code state; static direct-value admission rules out both
named and closure invocation results. -/
theorem ConcreteStructuredCodeFocus.directLetResult_of_step
    {context : Fir.Wasm.Context}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {labels : List Lean.FVarId}
    {externals : ExternalImpl}
    {facts : ReuseCapacityFacts}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodeFocus context sourceModule sourceFunction
      labels sourceRuntime sourceEnv (.let decl continuation) targetStore
      targetLocals targetCode witness source target)
    (supported : ReuseBudgetedDirectSupported context facts decl)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ nextRuntime sourceValue,
      SourceLetResult context sourceRuntime sourceEnv decl nextRuntime
        sourceValue := by
  rcases source with
    ⟨sourceProgram, sourceControl, actualEnv, sourceJoins, sourceFrames,
      actualRuntime⟩
  have programEq := related.sourceProgramEq
  change sourceProgram = context.program at programEq
  subst sourceProgram
  have controlEq := related.sourceControlEq
  change sourceControl = .code (.let decl continuation) at controlEq
  subst sourceControl
  have envEq := related.sourceEnvEq
  change actualEnv = sourceEnv at envEq
  subst actualEnv
  have runtimeEq := related.sourceRuntimeEq
  change actualRuntime = sourceRuntime at runtimeEq
  subst actualRuntime
  cases evaluated :
      evalLetValue {
        program := context.program
        control := .code (.let decl continuation)
        env := sourceEnv
        joins := sourceJoins
        frames := sourceFrames
        runtime := sourceRuntime } decl with
  | error fault =>
      simp [executeStep, coreStep, evaluated, fail] at sourceStep
  | ok result =>
      rcases result with ⟨nextRuntime, outcome⟩
      cases outcome with
      | value sourceValue =>
          refine ⟨nextRuntime, sourceValue, ?_⟩
          unfold SourceLetResult
          cases decl.value <;> exact evaluated
      | invokeName name args =>
          exfalso
          have notFap : ∀ name args, decl.value ≠ .fap name args :=
            supported.value_ne_fap
          cases valueEq : decl.value
          all_goals
            simp_all [evalLetValue, Bind.bind, Except.bind, pure, Except.pure]
            repeat' first | split at evaluated | contradiction
          all_goals
            have actionEq := congrArg Prod.snd (Except.ok.inj evaluated)
            cases actionEq
          all_goals simp_all [evalArgs, pure, Except.pure]
      | invokeValue function args =>
          exfalso
          have notFap : ∀ name args, decl.value ≠ .fap name args :=
            supported.value_ne_fap
          have aliases : ∀ id args, decl.value = .fvar id args → args = #[] :=
            supported.fvar_args_eq_empty
          cases valueEq : decl.value
          all_goals
            simp_all [evalLetValue, Bind.bind, Except.bind, pure, Except.pure]
            repeat' first | split at evaluated | contradiction
          all_goals
            have actionEq := congrArg Prod.snd (Except.ok.inj evaluated)
            cases actionEq
          all_goals simp_all [evalArgs, pure, Except.pure]

/-- One admitted direct-value binding preserves the complete compiler/resource
core.

The dynamic premise is only the current interpreter operation result.  The
production runtime theorem constructs the target execution, next concrete
store/witness, fact transfer, and resource transports. Successor admission is
intentionally left to the relation-wide classifier after the dynamic source
result is known. -/
theorem ConcreteStructuredCodePointwiseRel.advance_directLet
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes : Nat}
    {remainingBytes : Nat}
    {sourceRuntime nextRuntime : RuntimeState}
    {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv
      (.let decl continuation) targetStore targetLocals targetCode witness source
      target)
    (supported : ReuseBudgetedDirectSupported context facts decl)
    (requiredEq : requiredBytes = directLetAllocationCost decl)
    (sourceResult : SourceLetResult context sourceRuntime sourceEnv decl
      nextRuntime sourceValue) :
    ∃ sourceAfter targetAfter nextStore resumedLocals nextWitness nextFacts
        targetRest targetCount,
      executeStep externals source = .next sourceAfter ∧
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        targetCount target targetAfter ∧
      0 < targetCount ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames ∧
      ConcreteStructuredCodeCoreRel program context sourceModule sourceFunction
        externals labels entryRuntime entryStore entryWitness functionResult
        callerExpectedResult nextFacts
        (remainingBytes - directLetAllocationCost decl) nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) continuation nextStore
        resumedLocals targetRest nextWitness sourceAfter targetAfter := by
  have fits : directLetAllocationCost decl ≤ remainingBytes := by
    rw [← requiredEq]
    exact related.budget
  obtain ⟨valueCode, targetValue, targetRest, resultIndex, valueCompiled,
      valueAdapted, resultFound, continuationAdapted, targetCodeEq⟩ :=
    CodeAdaptedWithSuffix.let_eq related.focus.adapted
  obtain ⟨nextStore, nextLocals, nextWitness, nextFacts, step,
      _externalsPreserved, _hostDescriptorsPreserved,
      _witnessDescriptorsPreserved, transports, _producedTransfer,
      nextInvariant⟩ :=
    (spec.reuseCapacityDirectLetRuntimeRefinesWithCost_reuseBudgetedDirect_pureExternalOwnership_entryRelativeCache
      externals) supported fits related.resources.current.1 sourceResult
        valueCompiled valueAdapted resultFound
  have flat : StructuredWasmFlatProgram targetModule.wasmModule
      (targetValue ++ [.localSet resultIndex]) :=
    spec.reuseCapacityDirectTargetFlat_reuseBudgetedDirect
      supported valueCompiled valueAdapted
  have nextAbi : ClosureAllocationsAbiAligned context.program nextWitness :=
    ClosureAllocationsAbiAligned.ofPersistent related.resources.current.2
      transports.closureAllocationsPersistent
  let resumedLocals : Wasm.Locals :=
    { nextLocals with values := targetLocals.values }
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness nextFacts
        (remainingBytes - directLetAllocationCost decl) nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) nextStore resumedLocals
        nextWitness := by
    exact ⟨by
      simpa [resumedLocals] using
        nextInvariant.withValues targetLocals.values,
      nextAbi⟩
  obtain ⟨sourceAfter, targetAfter, sourceStep, targetPath, nextFocus,
      _sourceJoinsEq, sourceFramesEq, targetFramesEq⟩ :=
    related.focus.advance_flatLet targetCodeEq continuationAdapted flat
      step nextScope.frameAligned
  have nextResourcesBefore :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore
        nextStore entryWitness nextWitness nextFacts
        (remainingBytes - directLetAllocationCost decl)
        (bind sourceEnv decl.fvarId sourceValue) resumedLocals
        functionResult callerExpectedResult source.frames target.frames :=
    ⟨nextScope, related.resources.suspended⟩
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore
        nextStore entryWitness nextWitness nextFacts
        (remainingBytes - directLetAllocationCost decl)
        (bind sourceEnv decl.fvarId sourceValue) resumedLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact nextResourcesBefore
  let targetCount :=
    (targetValue ++ [Wasm.Instruction.localSet resultIndex]).length
  refine ⟨sourceAfter, targetAfter, nextStore, resumedLocals, nextWitness,
    nextFacts, targetRest, targetCount, sourceStep, ?_, ?_, sourceFramesEq,
    targetFramesEq, ?_⟩
  · simpa [targetCount] using targetPath
  · simp [targetCount]
  · exact ⟨nextFocus, nextResources⟩

/-- Relation-facing direct-value rule.  Its only dynamic premise is the
ordinary interpreter step used by `RankedObservedWeakSimulation.advance`;
the exact semantic value result and the generated successor are recovered
internally and identified by determinism. -/
theorem ConcreteStructuredCodePointwiseRel.advance_directLet_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv
      (.let decl continuation) targetStore targetLocals targetCode witness source
      target)
    (supported : ReuseBudgetedDirectSupported context facts decl)
    (requiredEq : requiredBytes = directLetAllocationCost decl)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextRuntime sourceValue nextStore resumedLocals nextWitness
        nextFacts targetRest targetCount,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
        targetCount target targetAfter ∧
      0 < targetCount ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames ∧
      ConcreteStructuredCodeCoreRel program context sourceModule sourceFunction
        externals labels entryRuntime entryStore entryWitness functionResult
        callerExpectedResult nextFacts
        (remainingBytes - directLetAllocationCost decl) nextRuntime
        (bind sourceEnv decl.fvarId sourceValue) continuation nextStore
        resumedLocals targetRest nextWitness sourceAfter targetAfter := by
  obtain ⟨nextRuntime, sourceValue, sourceResult⟩ :=
    related.focus.directLetResult_of_step supported sourceStep
  obtain ⟨computedAfter, targetAfter, nextStore, resumedLocals, nextWitness,
      nextFacts, targetRest, targetCount, computedStep, targetPath,
      targetPositive, sourceFramesEq, targetFramesEq, nextCore⟩ :=
    related.advance_directLet spec supported requiredEq sourceResult
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  exact ⟨targetAfter, nextRuntime, sourceValue, nextStore, resumedLocals,
    nextWitness, nextFacts, targetRest, targetCount, targetPath,
    targetPositive, by simpa using sourceFramesEq, targetFramesEq, nextCore⟩

/-- An admitted return is classified pointwise as either a terminal result or
the appropriate direct/saturated bind protocol from the hereditary frame
stack.  The successful source step supplies the lookup; admission supplies
only the static result ABI refinement. -/
theorem ConcreteStructuredCodePointwiseRel.advance_return
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes : Nat}
    {remainingBytes : Nat}
    {sourceRuntime : RuntimeState}
    {sourceEnv : Env}
    {result : Lean.FVarId}
    {targetStore : Wasm.Store Host}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {witness : RefinementWitness}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv
      (.return result) targetStore targetLocals targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter sourceValue actualKind physical,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2 target
        targetAfter ∧
      ConcreteStructuredYieldFocus context sourceFunction sourceRuntime
        sourceEnv sourceValue targetStore targetLocals witness actualKind
        physical sourceAfter targetAfter ∧
      ConcreteStructuredResultCompatible actualKind callerExpectedResult ∧
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames ∧
      sourceAfter.frames = source.frames ∧
      targetAfter.frames = target.frames := by
  obtain ⟨admittedResult, resultCompiled, resultRefines, _requiredEq⟩ :=
    related.admitted.return_cases
  cases sourceLookup : lookup sourceEnv result with
  | none =>
      rcases source with
        ⟨sourceProgram, sourceControl, sourceStateEnv, sourceJoins,
          sourceFrames, runtime⟩
      have controlEq := related.focus.sourceControlEq
      change sourceControl = .code (.return result) at controlEq
      subst sourceControl
      have envEq := related.focus.sourceEnvEq
      change sourceStateEnv = sourceEnv at envEq
      subst sourceStateEnv
      simp [executeStep, coreStep, lookupValue, sourceLookup, fail] at sourceStep
  | some sourceValue =>
      obtain ⟨actualKind, physical, computedAfter, targetAfter,
          localCompiled, computedStep, targetPath, yielded,
          _sourceJoinsEq, sourceFramesEq, targetFramesEq⟩ :=
        related.focus.advance_return spec.localsAligned sourceLookup
      have afterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        injection computedStep
      subst computedAfter
      have kindEq : actualKind = admittedResult := by
        rw [resultCompiled] at localCompiled
        have pairEq := Except.ok.inj localCompiled
        exact (congrArg Prod.snd pairEq).symm
      subst actualKind
      have actualCompatible :
          ConcreteStructuredResultCompatible admittedResult
            callerExpectedResult := by
        cases callerExpectedResult with
        | none => trivial
        | some expected =>
            exact AbiKind.refines_trans resultRefines
              related.resources.suspended.resultCompatible
      have resourcesAfter :
          ConcreteStructuredResourceStack program context sourceModule
            sourceFunction externals entryRuntime sourceRuntime entryStore
            targetStore entryWitness witness facts remainingBytes sourceEnv
            targetLocals functionResult callerExpectedResult sourceAfter.frames
            targetAfter.frames := by
        rw [sourceFramesEq, targetFramesEq]
        exact related.resources
      exact ⟨targetAfter, sourceValue, admittedResult, physical, targetPath,
        yielded, actualCompatible, resourcesAfter, sourceFramesEq,
        targetFramesEq⟩

/-- A default-only case preserves the complete resource core across its
compiler-erased source step. -/
theorem ConcreteStructuredCodePointwiseRel.advance_defaultOnlyCase_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv (.cases cases) targetStore
      targetLocals targetCode witness source target)
    (supported :
      DefaultOnlyCaseSupported sourceRuntime sourceEnv cases selected)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 0 target
        target ∧
      sourceAfter.frames = source.frames ∧
      ConcreteStructuredCodeCoreRel program context sourceModule
        sourceFunction externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        sourceEnv selected targetStore targetLocals targetCode witness
        sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  have sourceResult :=
    related.focus.defaultOnlyCaseResult_of_step supported sourceStep
  obtain ⟨computedAfter, computedStep, targetPath, nextFocus, framesEq, rank⟩ :=
    related.focus.advance_defaultOnlyCase_ranked supported sourceResult
  have afterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult sourceAfter.frames
        target.frames := by
    rw [framesEq]
    exact related.resources
  exact ⟨targetPath, framesEq, ⟨nextFocus, nextResources⟩, rank⟩

/-- A singleton object-constructor case executes its generated five-step tag
test and enters the selected arm under one target-only case label.  The active
resource scope is unchanged (modulo the compiler's stack-value reset), while
the suspended stack records exactly the label that must be unwound when the
arm returns. -/
theorem ConcreteStructuredCodePointwiseRel.advance_singleObjectCase_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {cases : Lean.Compiler.LCNF.Cases .impure}
    {selected : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv (.cases cases) targetStore
      targetLocals targetCode witness source target)
    (supported : SingleObjectConstructorCaseSupported context sourceRuntime
      sourceEnv cases selected)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter selectedTarget, ∃ targetSuffix : Wasm.Program,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 5 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames =
          structuredWasmCaseLabels (targetLocals.values.drop 0) targetSuffix 1 ++
            target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes sourceRuntime
          sourceEnv selected targetStore
          { targetLocals with values := targetLocals.values } selectedTarget
          witness sourceAfter targetAfter := by
  obtain ⟨sourceResult, sourceAfterEq⟩ :=
    related.focus.singleObjectCaseResult_of_step supported sourceStep
  obtain ⟨selectedTarget, targetSuffix, selectedAdapted, rawTargetPrefix⟩ :=
    spec.singleObjectConstructorCaseFinitePath supported sourceResult
      related.focus.stateRelated related.focus.adapted
      (frames := target.frames)
  let targetAfter : StructuredWasmState Host := {
    store := targetStore
    control := .running
      { targetLocals with values := targetLocals.values } selectedTarget
    frames := structuredWasmCaseLabels (targetLocals.values.drop 0)
      targetSuffix 1 ++ target.frames }
  have targetPrefix :
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 5 target
        targetAfter := by
    rcases target with ⟨actualStore, actualControl, actualFrames⟩
    have storeEq := related.focus.targetStoreEq
    change actualStore = targetStore at storeEq
    subst actualStore
    have controlEq := related.focus.targetControlEq
    change actualControl = .running targetLocals targetCode at controlEq
    subst actualControl
    simpa [targetAfter] using rawTargetPrefix
  have nextFocus :
      ConcreteStructuredCodeFocus context sourceModule sourceFunction labels
        sourceRuntime sourceEnv selected targetStore
        { targetLocals with values := targetLocals.values } selectedTarget
        witness sourceAfter targetAfter := {
    sourceProgramEq := by
      rw [sourceAfterEq]
      simp [related.focus.sourceProgramEq]
    sourceControlEq := by simp [sourceAfterEq]
    sourceEnvEq := by
      rw [sourceAfterEq]
      simpa using related.focus.sourceEnvEq
    sourceRuntimeEq := by
      rw [sourceAfterEq]
      simpa using related.focus.sourceRuntimeEq
    targetStoreEq := by simp [targetAfter]
    targetControlEq := by simp [targetAfter]
    adapted := CodeAdapted.withEmptySuffix selectedAdapted
    stateRelated := related.focus.stateRelated.withValues targetLocals.values
    frameAligned := related.focus.frameAligned.withValues targetLocals.values }
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        sourceRuntime sourceEnv targetStore
        { targetLocals with values := targetLocals.values } witness :=
    related.resources.current.withValues targetLocals.values
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        { targetLocals with values := targetLocals.values } functionResult
        callerExpectedResult sourceAfter.frames targetAfter.frames := by
    refine ⟨nextScope, ?_⟩
    rw [sourceAfterEq]
    simpa [targetAfter] using
      (ConcreteStructuredSuspendedResourceStack.case
        (belowStack := targetLocals.values.drop 0)
        (targetRest := targetSuffix) (testCount := 1)
        related.resources.suspended)
  exact ⟨targetAfter, selectedTarget, targetSuffix, targetPrefix,
    by simp [sourceAfterEq], by simp [targetAfter],
    ⟨nextFocus, nextResources⟩⟩

/-- Persistent increments are erased by lowering and preserve the complete
resource core while consuming one ranked source step. -/
theorem ConcreteStructuredCodePointwiseRel.advance_incPersistent_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {objectId : Lean.FVarId}
    {amount : Nat}
    {check : Bool}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv
      (.inc objectId amount check true continuation) targetStore targetLocals
      targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 0 target
        target ∧
      sourceAfter.frames = source.frames ∧
      ConcreteStructuredCodeCoreRel program context sourceModule
        sourceFunction externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        sourceEnv continuation targetStore targetLocals targetCode witness
        sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨computedAfter, computedStep, targetPath, nextFocus, framesEq, rank⟩ :=
    related.focus.advance_incPersistent
      (module := targetModule.wasmModule) (hostEnv := hosts.env)
  have afterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult sourceAfter.frames
        target.frames := by
    rw [framesEq]
    exact related.resources
  exact ⟨targetPath, framesEq, ⟨nextFocus, nextResources⟩, rank⟩

/-- Persistent decrements have the same resource-preserving erased-step law. -/
theorem ConcreteStructuredCodePointwiseRel.advance_decPersistent_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {objectId : Lean.FVarId}
    {amount : Nat}
    {check : Bool}
    {objectFields? : Option Nat}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv
      (.dec objectId amount check true objectFields? continuation) targetStore
      targetLocals targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 0 target
        target ∧
      sourceAfter.frames = source.frames ∧
      ConcreteStructuredCodeCoreRel program context sourceModule
        sourceFunction externals labels entryRuntime entryStore entryWitness
        functionResult callerExpectedResult facts remainingBytes sourceRuntime
        sourceEnv continuation targetStore targetLocals targetCode witness
        sourceAfter target ∧
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source := by
  obtain ⟨computedAfter, computedStep, targetPath, nextFocus, framesEq, rank⟩ :=
    related.focus.advance_decPersistent
      (module := targetModule.wasmModule) (hostEnv := hosts.env)
  have afterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult sourceAfter.frames
        target.frames := by
    rw [framesEq]
    exact related.resources
  exact ⟨targetPath, framesEq, ⟨nextFocus, nextResources⟩, rank⟩

/-- A successful ordinary increment preserves the full admission-free
compiler relation across its exact two-step generated host prefix.

The current-node admission supplies only source/compiler facts.  The concrete
heap transition, target execution, entry-relative resource transport, and
successor core are reconstructed here, after the actual source step is known.
The successor carries no admission for its continuation. -/
theorem ConcreteStructuredCodePointwiseRel.advance_ordinaryIncrement_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : OrdinaryIncrementEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, nextStore, targetRest, sourcePath,
      targetPath, nextFocus, nextInvariant, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.focus.advance_ordinaryIncrement spec supported
      supported.sourceEffectResult related.resources.current.1
  have computedStep :
      executeStep externals source = .next computedAfter := by
    cases sourcePath with
    | cons head tail =>
        cases tail
        exact head
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        nextRuntime sourceEnv nextStore targetLocals witness :=
    ⟨nextInvariant, related.resources.current.2⟩
  have nextResourcesBefore :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult source.frames target.frames :=
    ⟨nextScope, related.resources.suspended⟩
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact nextResourcesBefore
  exact ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
    targetFramesEq, ⟨nextFocus, nextResources⟩⟩

/-- A successful ordinary decrement preserves the full admission-free
compiler relation across its exact two-step generated host prefix.

This is the recursive-release counterpart of ordinary increment. The current
admission supplies only the successful source lookup/update; concrete heap
release, capacity transport, target execution, and continuation resources are
derived from the existing decrement refinement theorem. -/
theorem ConcreteStructuredCodePointwiseRel.advance_ordinaryDecrement_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : OrdinaryDecrementEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, nextStore, targetRest, sourcePath,
      targetPath, nextFocus, nextInvariant, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.focus.advance_ordinaryDecrement spec supported
      supported.sourceEffectResult related.resources.current.1
  have computedStep :
      executeStep externals source = .next computedAfter := by
    cases sourcePath with
    | cons head tail =>
        cases tail
        exact head
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        nextRuntime sourceEnv nextStore targetLocals witness :=
    ⟨nextInvariant, related.resources.current.2⟩
  have nextResourcesBefore :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult source.frames target.frames :=
    ⟨nextScope, related.resources.suspended⟩
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact nextResourcesBefore
  exact ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
    targetFramesEq, ⟨nextFocus, nextResources⟩⟩

/-- A successful explicit deletion preserves the full admission-free compiler
relation across its exact two-step generated host prefix.

The same theorem covers an ordinary heap deletion and deletion of the erased
reset token. Admission records only the successful source lookup/update;
physical zero-as-no-op, concrete heap transport, and target execution are
derived from the shared delete refinement. -/
theorem ConcreteStructuredCodePointwiseRel.advance_ordinaryDelete_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : OrdinaryDeleteEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, nextStore, targetRest, sourcePath,
      targetPath, nextFocus, nextInvariant, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ :=
    related.focus.advance_ordinaryDelete spec supported
      supported.sourceEffectResult related.resources.current.1
  have computedStep :
      executeStep externals source = .next computedAfter := by
    cases sourcePath with
    | cons head tail =>
        cases tail
        exact head
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        nextRuntime sourceEnv nextStore targetLocals witness :=
    ⟨nextInvariant, related.resources.current.2⟩
  have nextResourcesBefore :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult source.frames target.frames :=
    ⟨nextScope, related.resources.suspended⟩
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact nextResourcesBefore
  exact ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
    targetFramesEq, ⟨nextFocus, nextResources⟩⟩

/-- Shared relation-level transport for a successful concrete mutation.

The operation-specific theorem still derives both finite paths from source
admission, generated code, and concrete refinement. This lemma only performs
the common replacement of the computed source successor and reconstructs the
resource-indexed continuation relation; it adds no certificate to admission. -/
private theorem ConcreteStructuredCodePointwiseRel.advance_mutation_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes targetCount : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter)
    (advanced :
      ∃ computedAfter targetAfter nextStore targetRest,
        FinitePath
            (fun before after => executeStep externals before = .next after)
            1 source computedAfter ∧
          FinitePath
              (StructuredWasmStep targetModule.wasmModule hosts.env)
              targetCount target targetAfter ∧
            ConcreteStructuredCodeFocus context sourceModule sourceFunction
                labels nextRuntime sourceEnv continuation nextStore
                targetLocals targetRest witness computedAfter targetAfter ∧
              ReuseCapacityEntryRelativeFrame
                (ConcreteReuseCapacityCacheFrame sourceModule sourceFunction
                  externals)
                entryRuntime entryStore entryWitness facts remainingBytes
                nextRuntime sourceEnv nextStore targetLocals witness ∧
              computedAfter.joins = source.joins ∧
                computedAfter.frames = source.frames ∧
                  targetAfter.frames = target.frames) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  obtain ⟨computedAfter, targetAfter, nextStore, targetRest, sourcePath,
      targetPath, nextFocus, nextInvariant, _joinsEq, sourceFramesEq,
      targetFramesEq⟩ := advanced
  have computedStep :
      executeStep externals source = .next computedAfter := by
    cases sourcePath with
    | cons head tail =>
        cases tail
        exact head
  have sourceAfterEq : sourceAfter = computedAfter := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedAfter
  have nextScope :
      ConcreteStructuredResourceScope context sourceModule sourceFunction
        externals entryRuntime entryStore entryWitness facts remainingBytes
        nextRuntime sourceEnv nextStore targetLocals witness :=
    ⟨nextInvariant, related.resources.current.2⟩
  have nextResourcesBefore :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult source.frames target.frames :=
    ⟨nextScope, related.resources.suspended⟩
  have nextResources :
      ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime nextRuntime entryStore nextStore
        entryWitness witness facts remainingBytes sourceEnv targetLocals
        functionResult callerExpectedResult sourceAfter.frames
        targetAfter.frames := by
    rw [sourceFramesEq, targetFramesEq]
    exact nextResourcesBefore
  exact ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
    targetFramesEq, ⟨nextFocus, nextResources⟩⟩

section MutationPointwise

variable
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode code continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}

/-- Constructor-tag mutation preserves the pointwise compiler relation across
the exact generated local-get/import-call prefix. -/
theorem ConcreteStructuredCodePointwiseRel.advance_constructorTag_of_step
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : ConstructorTagEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 2 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  apply related.advance_mutation_of_step sourceStep
  exact related.focus.advance_constructorTag spec supported
    supported.sourceEffectResult related.resources.current.1

/-- FVar object-field mutation preserves the pointwise compiler relation
across the exact generated binary-host prefix. -/
theorem ConcreteStructuredCodePointwiseRel.advance_objectFieldFVar_of_step
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : ObjectFieldFVarEffectSupported context sourceRuntime sourceEnv
      code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 3 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  apply related.advance_mutation_of_step sourceStep
  exact related.focus.advance_objectFieldFVar spec supported
    supported.sourceEffectResult related.resources.current.1

/-- Erased object-field mutation preserves the pointwise compiler relation
across the exact generated object-local/zero/import-call prefix. -/
theorem ConcreteStructuredCodePointwiseRel.advance_objectFieldErased_of_step
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : ObjectFieldErasedEffectSupported context sourceRuntime
      sourceEnv code continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 3 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  apply related.advance_mutation_of_step sourceStep
  exact related.focus.advance_objectFieldErased spec supported
    supported.sourceEffectResult related.resources.current.1

/-- `USize` field mutation preserves the pointwise compiler relation across
the exact generated binary-host prefix. -/
theorem ConcreteStructuredCodePointwiseRel.advance_usizeField_of_step
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : USizeFieldEffectSupported context sourceRuntime sourceEnv code
      continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 3 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  apply related.advance_mutation_of_step sourceStep
  exact related.focus.advance_usizeField spec supported
    supported.sourceEffectResult related.resources.current.1

/-- Packed-scalar field mutation preserves the pointwise compiler relation
across the exact generated binary-host prefix. -/
theorem ConcreteStructuredCodePointwiseRel.advance_scalarField_of_step
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts 0 remainingBytes sourceRuntime sourceEnv code targetStore
      targetLocals targetCode witness source target)
    (supported : ScalarFieldEffectSupported context sourceRuntime sourceEnv code
      continuation nextRuntime)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetAfter nextStore targetRest,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env) 3 target
          targetAfter ∧
        sourceAfter.frames = source.frames ∧
        targetAfter.frames = target.frames ∧
        ConcreteStructuredCodeCoreRel program context sourceModule
          sourceFunction externals labels entryRuntime entryStore entryWitness
          functionResult callerExpectedResult facts remainingBytes nextRuntime
          sourceEnv continuation nextStore targetLocals targetRest witness
          sourceAfter targetAfter := by
  apply related.advance_mutation_of_step sourceStep
  exact related.focus.advance_scalarField spec supported
    supported.sourceEffectResult related.resources.current.1

end MutationPointwise

/-- Stage one admitted pure external into the resource-indexed call-ready
relation.  The source result and allocation cost come from current-node
admission, while the production compiler and adapter determine the exact
argument prefix, import, and destination local. -/
theorem ConcreteStructuredCodePointwiseRel.advance_pureExternal_stage_of_step
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime nextRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes stepCost : Nat}
    {sourceEnv : Env}
    {decl : Lean.Compiler.LCNF.LetDecl .impure}
    {continuation : Lean.Compiler.LCNF.Code .impure}
    {sourceValue : Value}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv
      (.let decl continuation) targetStore targetLocals targetCode witness source
      target)
    (supported : PureExternalSupported context externals sourceRuntime sourceEnv
      decl continuation nextRuntime sourceValue stepCost)
    (requiredEq : requiredBytes = stepCost)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ (site : PureExternalCallShape context externals sourceRuntime sourceEnv
        decl nextRuntime sourceValue stepCost)
      (physicalArgs : List Wasm.Value) (operation : ExternalOperation)
      (resolvedResultKind : AbiKind) (targetImport : Wasm.ImportDecl)
      (callIndex resultIndex : Nat) (targetArguments targetRest : Wasm.Program)
      (targetAfter : StructuredWasmState Host),
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetArguments.length target targetAfter ∧
        ConcreteStructuredExternalCallReadyCoreRel program context sourceModule
          sourceFunction targetModule hosts externals site operation
          resolvedResultKind targetImport labels continuation source.joins
          source.frames entryRuntime entryStore entryWitness functionResult
          callerExpectedResult facts remainingBytes targetStore targetLocals
          targetLocals.values targetRest target.frames witness physicalArgs
          callIndex resultIndex sourceAfter targetAfter ∧
        compilerStructuredControlRank sourceAfter <
          compilerStructuredControlRank source := by
  obtain ⟨site, physicalArgs, operation, resolvedResultKind, targetImport,
      callIndex, resultIndex, targetArguments, targetRest, computedSource,
      targetAfter, computedStep, targetPath, ready⟩ :=
    related.focus.advance_external_stage spec supported spec.localsAligned
  have sourceEq : sourceAfter = computedSource := by
    rw [sourceStep] at computedStep
    exact ExecResult.next.inj computedStep
  subst computedSource
  have fits : stepCost ≤ remainingBytes := by
    rw [← requiredEq]
    exact related.budget
  have rank :
      compilerStructuredControlRank sourceAfter <
        compilerStructuredControlRank source :=
    compilerStructuredControlRank_invokeName_lt_code
      related.focus.sourceControlEq ready.sourceControlEq
  exact ⟨site, physicalArgs, operation, resolvedResultKind, targetImport,
    callIndex, resultIndex, targetArguments, targetRest, targetAfter,
    targetPath, ⟨ready, related.resources, fits⟩, rank⟩

/-- The first relation-wide one-source-step law for compiler-produced code.

It dispatches solely from admission of the current node, derives every target
path and generated callee row from the production pipeline, and returns the
small successor control sum above.  If the matching target path is empty, the
compiler-derived source rank strictly decreases.  Continued code contains no
future admission; a client attaches that fact only after inspecting the
dynamically reached source state. -/
theorem ConcreteStructuredCodePointwiseRel.advance
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        ConcreteStructuredCodeStepOutcome program context functionCode
          sourceModule sourceFunction targetModule hosts spec externals labels
          entryRuntime entryStore entryWitness functionResult
          callerExpectedResult facts remainingBytes sourceAfter targetAfter ∧
        (targetCount = 0 →
          compilerStructuredControlRank sourceAfter <
            compilerStructuredControlRank source) := by
  cases admission : related.admitted with
  | ret resultCompiled resultRefines =>
      obtain ⟨targetAfter, sourceValue, actualKind, physical, targetPath,
          yielded, compatible, resources, _sourceFramesEq,
          _targetFramesEq⟩ :=
        related.advance_return spec sourceStep
      refine ⟨2, targetAfter, targetPath,
        .returned yielded compatible resources, ?_⟩
      omega
  | directLet supported =>
      obtain ⟨targetAfter, nextRuntime, sourceValue, nextStore, resumedLocals,
          nextWitness, nextFacts, targetRest, targetCount, targetPath,
          targetPositive, _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_directLet_of_step spec supported rfl sourceStep
      refine ⟨targetCount, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | pureExternal supported =>
      obtain ⟨site, physicalArgs, operation, resolvedResultKind,
          targetImport, callIndex, resultIndex, targetArguments, targetRest,
          targetAfter, targetPath, ready, rank⟩ :=
        related.advance_pureExternal_stage_of_step supported rfl sourceStep
      exact ⟨targetArguments.length, targetAfter, targetPath,
        .externalReady ready, fun _ => rank⟩
  | directCall site =>
      obtain ⟨calleeContext, calleeFunction, _contexts,
          ⟨generatedRow⟩⟩ := related.directCallRow site
      have row :
          ConcreteGeneratedInternalDeclaration context.program
            site.sourceDeclaration calleeContext site.calleeCode sourceModule
            calleeFunction targetModule := by
        rw [spec.contextProgram]
        exact generatedRow
      obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest,
          computedAfter, targetAfter, computedStep, targetPath, ready, rank⟩ :=
        related.core.advance_directCall_stage_ranked site row spec
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      exact ⟨targetArguments.length, targetAfter, targetPath,
        .directReady site row ready, fun _ => rank⟩
  | saturatedCall site resolution sharedCapacity =>
      obtain ⟨calleeContext, calleeFunction, _contexts,
          ⟨generatedRow⟩⟩ := related.saturatedCallRow resolution
      have row :
          ConcreteGeneratedInternalDeclaration context.program
            resolution.target calleeContext resolution.calleeCode sourceModule
            calleeFunction targetModule := by
        rw [spec.contextProgram]
        exact generatedRow
      obtain ⟨targetValue, targetRest, resultIndex, computedAfter,
          computedStep, targetPath, ready, rank⟩ :=
        related.core.advance_saturatedCall_stage site spec
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      exact ⟨0, target, targetPath,
        .saturatedReady site resolution sharedCapacity row ready, fun _ => rank⟩
  | defaultOnlyCase supported =>
      obtain ⟨targetPath, _sourceFramesEq, nextCore, rank⟩ :=
        related.advance_defaultOnlyCase_of_step supported sourceStep
      exact ⟨0, target, targetPath,
        .code related.contextCaches nextCore, fun _ => rank⟩
  | singleObjectCase caseSupported =>
      obtain ⟨targetAfter, selectedTarget, targetSuffix, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_singleObjectCase_of_step caseSupported sourceStep
      exact ⟨5, targetAfter, targetPath,
        .code related.contextCaches nextCore, by omega⟩
  | incPersistent =>
      obtain ⟨targetPath, _sourceFramesEq, nextCore, rank⟩ :=
        related.advance_incPersistent_of_step sourceStep
      exact ⟨0, target, targetPath,
        .code related.contextCaches nextCore, fun _ => rank⟩
  | decPersistent =>
      obtain ⟨targetPath, _sourceFramesEq, nextCore, rank⟩ :=
        related.advance_decPersistent_of_step sourceStep
      exact ⟨0, target, targetPath,
        .code related.contextCaches nextCore, fun _ => rank⟩
  | ordinaryIncrement supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryIncrement_of_step supported sourceStep
      refine ⟨2, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | ordinaryDecrement supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryDecrement_of_step supported sourceStep
      refine ⟨2, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | ordinaryDelete supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryDelete_of_step supported sourceStep
      refine ⟨2, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | constructorTag supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_constructorTag_of_step supported sourceStep
      refine ⟨2, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | objectFieldFVar supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_objectFieldFVar_of_step supported sourceStep
      refine ⟨3, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | objectFieldErased supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_objectFieldErased_of_step supported sourceStep
      refine ⟨3, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | usizeField supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_usizeField_of_step supported sourceStep
      refine ⟨3, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega
  | scalarField supported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath,
          _sourceFramesEq, _targetFramesEq, nextCore⟩ :=
        related.advance_scalarField_of_step supported sourceStep
      refine ⟨3, targetAfter, targetPath,
        .code related.contextCaches nextCore, ?_⟩
      omega

/-- Strong form of the pointwise ordinary-code step law.

Besides classifying the generated successor, this theorem transports the
static supported-call stack across the exact frame equalities established by
direct values, returns, default-only cases, and persistent ownership erasure.
Call staging leaves that stack unchanged; the subsequent entry laws push the
caller.  Consequently every successor already belongs to the recursively
stable module-wide relation.  Every empty target path carries strict
compiler-control-rank descent. -/
theorem ConcreteStructuredCodePointwiseRel.advance_supportedGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target)
    (supported : ConcreteStructuredSupportedFrameStack program sourceModule
      targetModule hosts functionResult callerExpectedResult source.frames
      target.frames)
    (agrees : supported.Agrees related.resources.suspended)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter ∧
        (targetCount = 0 →
          compilerStructuredControlRank sourceAfter <
            compilerStructuredControlRank source) := by
  cases admission : related.admitted with
  | ret resultCompiled resultRefines =>
      obtain ⟨targetAfter, sourceValue, actualKind, physical, targetPath,
          yielded, compatible, resources, sourceFramesEq, targetFramesEq⟩ :=
        related.advance_return spec sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .returned yielded compatible resources related.contextCaches
          supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | directLet directSupported =>
      obtain ⟨targetAfter, nextRuntime, sourceValue, nextStore, resumedLocals,
          nextWitness, nextFacts, targetRest, targetCount, targetPath,
          targetPositive, sourceFramesEq, targetFramesEq, nextCore⟩ :=
        related.advance_directLet_of_step spec directSupported rfl sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨targetCount, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | pureExternal externalSupported =>
      obtain ⟨site, physicalArgs, operation, resolvedResultKind,
          targetImport, callIndex, resultIndex, targetArguments, targetRest,
          targetAfter, targetPath, ready, rank⟩ :=
        related.advance_pureExternal_stage_of_step externalSupported rfl
          sourceStep
      have nextAgrees : supported.Agrees ready.resources.suspended := by
        simpa using agrees
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .externalReady ready related.contextCaches supported nextAgrees
      exact ⟨targetArguments.length, targetAfter, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | directCall site =>
      obtain ⟨calleeContext, calleeFunction, _contexts,
          ⟨generatedRow⟩⟩ := related.directCallRow site
      have row :
          ConcreteGeneratedInternalDeclaration context.program
            site.sourceDeclaration calleeContext site.calleeCode sourceModule
            calleeFunction targetModule := by
        rw [spec.contextProgram]
        exact generatedRow
      obtain ⟨physicalArgs, resultIndex, targetArguments, targetRest,
          computedAfter, targetAfter, computedStep, targetPath, ready, rank⟩ :=
        related.core.advance_directCall_stage_ranked site row spec
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      have nextAgrees : supported.Agrees ready.resources.suspended := by
        simpa using agrees
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .directReady ready related.contextCaches supported nextAgrees
      exact ⟨targetArguments.length, targetAfter, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | saturatedCall site resolution sharedCapacity =>
      obtain ⟨calleeContext, calleeFunction, _contexts,
          ⟨generatedRow⟩⟩ := related.saturatedCallRow resolution
      have row :
          ConcreteGeneratedInternalDeclaration context.program
            resolution.target calleeContext resolution.calleeCode sourceModule
            calleeFunction targetModule := by
        rw [spec.contextProgram]
        exact generatedRow
      obtain ⟨targetValue, targetRest, resultIndex, computedAfter,
          computedStep, targetPath, ready, rank⟩ :=
        related.core.advance_saturatedCall_stage site spec
      have sourceAfterEq : sourceAfter = computedAfter := by
        rw [sourceStep] at computedStep
        exact ExecResult.next.inj computedStep
      subst computedAfter
      have nextAgrees : supported.Agrees ready.resources.suspended := by
        simpa using agrees
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter target :=
        .saturatedReady row sharedCapacity ready related.contextCaches supported
          nextAgrees
      exact ⟨0, target, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | defaultOnlyCase caseSupported =>
      obtain ⟨targetPath, sourceFramesEq, nextCore, rank⟩ :=
        related.advance_defaultOnlyCase_of_step caseSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq (by rfl) nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter target :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨0, target, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | singleObjectCase caseSupported =>
      obtain ⟨targetAfter, selectedTarget, targetSuffix, targetPath,
          sourceFramesEq, targetFramesEq, nextCore⟩ :=
        related.advance_singleObjectCase_of_step caseSupported sourceStep
      let stackedSupported :=
        ConcreteStructuredSupportedFrameStack.case
          (belowStack := targetLocals.values.drop 0)
          (targetRest := targetSuffix) (testCount := 1) supported
      let stackedResources :=
        ConcreteStructuredSuspendedResourceStack.case
          (belowStack := targetLocals.values.drop 0)
          (targetRest := targetSuffix) (testCount := 1)
          related.resources.suspended
      have stackedAgrees : stackedSupported.Agrees stackedResources := by
        exact ConcreteStructuredSupportedFrameStack.Agrees.case supported
          related.resources.suspended agrees
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        stackedAgrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨5, targetAfter, targetPath, nextActive.toGlobal, by omega⟩
  | incPersistent =>
      obtain ⟨targetPath, sourceFramesEq, nextCore, rank⟩ :=
        related.advance_incPersistent_of_step sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq (by rfl) nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter target :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨0, target, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | decPersistent =>
      obtain ⟨targetPath, sourceFramesEq, nextCore, rank⟩ :=
        related.advance_decPersistent_of_step sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq (by rfl) nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter target :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨0, target, targetPath,
        nextActive.toGlobal, fun _ => rank⟩
  | ordinaryIncrement incrementSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryIncrement_of_step incrementSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | ordinaryDecrement decrementSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryDecrement_of_step decrementSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | ordinaryDelete deleteSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_ordinaryDelete_of_step deleteSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | constructorTag tagSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_constructorTag_of_step tagSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨2, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | objectFieldFVar fieldSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_objectFieldFVar_of_step fieldSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨3, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | objectFieldErased fieldSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_objectFieldErased_of_step fieldSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨3, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | usizeField fieldSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_usizeField_of_step fieldSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨3, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | scalarField fieldSupported =>
      obtain ⟨targetAfter, nextStore, targetRest, targetPath, sourceFramesEq,
          targetFramesEq, nextCore⟩ :=
        related.advance_scalarField_of_step fieldSupported sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code related.contextCaches nextCore supportedAfter agreesAfter
      exact ⟨3, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩

/-- Strong, locally runnable control states for the compiler simulation.

The constructors combine the open successor shape, its aligned supported
caller stack, and exactly the premise needed for the current source step.
Ordinary code therefore carries one pointwise classifier and budget; staged
calls and yielded control carry no future-code evidence.  This direct control
sum avoids indexing extra evidence by proof-valued outcomes, which would lose
branch identity through proof irrelevance. -/
inductive ConcreteStructuredRunnableOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (context : Fir.Wasm.Context)
    (functionCode : Lean.Compiler.LCNF.Code .impure)
    (sourceModule : Fir.Wasm.Module)
    (sourceFunction : Fir.Wasm.Function)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts)
    (externals : ExternalImpl)
    (labels : List Lean.FVarId)
    (entryRuntime : RuntimeState)
    (entryStore : Wasm.Store Host)
    (entryWitness : RefinementWitness)
    (functionResult : AbiKind)
    (callerExpectedResult : Option AbiKind)
    (facts : ReuseCapacityFacts)
    (remainingBytes : Nat) :
    MachineState → StructuredWasmState Host → Prop where
  | code
      {source : MachineState}
      {target : StructuredWasmState Host}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceCode : Lean.Compiler.LCNF.Code .impure}
      {targetStore : Wasm.Store Host}
      {targetLocals : Wasm.Locals}
      {targetCode : Wasm.Program}
      {witness : RefinementWitness}
      {requiredBytes : Nat}
      (related : ConcreteStructuredCodePointwiseRel program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
        targetStore targetLocals targetCode witness source target)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult source.frames
        target.frames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | directReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      {site : DirectInternalCallSite context decl callerEnv}
      {row : ConcreteGeneratedInternalDeclaration context.program
        site.sourceDeclaration calleeContext site.calleeCode sourceModule
        calleeFunction targetModule}
      {sourceRuntime : RuntimeState}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {resultIndex : Nat}
      (related : ConcreteStructuredDirectCallReadyCoreRel program context
        calleeContext sourceModule sourceFunction calleeFunction targetModule
        site row externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
        targetStore callerLocals callerRemainder targetRest targetFrames witness
        physicalArgs resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | saturatedReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {callerEnv : Env}
      {sourceRuntime : RuntimeState}
      {site : SaturatedClosureCallSite context decl callerEnv}
      {resolution : SaturatedClosureCallResolution context sourceRuntime site}
      {calleeContext : Fir.Wasm.Context}
      {calleeFunction : Fir.Wasm.Function}
      (row : ConcreteGeneratedInternalDeclaration context.program
        resolution.target calleeContext resolution.calleeCode sourceModule
        calleeFunction targetModule)
      {sourceFrames : List Frame}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {targetValue targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {resultIndex : Nat}
      (sharedCapacity : ∀ parentRuntime,
        setCell sourceRuntime resolution.location
            { resolution.cell with rc := resolution.cell.rc - 1 } =
              .ok parentRuntime →
          ClosureRetainCapacity parentRuntime resolution.captures.toList)
      (related : ConcreteStructuredSaturatedCallReadyCoreRel program context
        sourceModule sourceFunction targetModule site externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes sourceRuntime continuation callerJoins sourceFrames
        targetStore callerLocals targetValue targetRest targetFrames witness
        resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | externalReady
      {source : MachineState}
      {target : StructuredWasmState Host}
      {sourceRuntime nextRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceValue : Value}
      {stepCost : Nat}
      {decl : Lean.Compiler.LCNF.LetDecl .impure}
      {site : PureExternalCallShape context externals sourceRuntime sourceEnv
        decl nextRuntime sourceValue stepCost}
      {operation : ExternalOperation}
      {resolvedResultKind : AbiKind}
      {targetImport : Wasm.ImportDecl}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {physicalArgs : List Wasm.Value}
      {callIndex resultIndex : Nat}
      (related : ConcreteStructuredExternalCallReadyCoreRel program context
        sourceModule sourceFunction targetModule hosts externals site operation
        resolvedResultKind targetImport labels continuation callerJoins
        sourceFrames entryRuntime entryStore entryWitness functionResult
        callerExpectedResult facts remainingBytes targetStore callerLocals
        callerRemainder targetRest targetFrames witness physicalArgs callIndex
        resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | externalBind
      {source : MachineState}
      {target : StructuredWasmState Host}
      {sourceRuntime : RuntimeState}
      {callerEnv : Env}
      {sourceValue : Value}
      {result : Lean.FVarId}
      {continuation : Lean.Compiler.LCNF.Code .impure}
      {callerJoins : JoinEnv}
      {sourceFrames : List Frame}
      {targetStore : Wasm.Store Host}
      {callerLocals : Wasm.Locals}
      {callerRemainder : List Wasm.Value}
      {targetRest : Wasm.Program}
      {targetFrames : List StructuredWasmFrame}
      {witness : RefinementWitness}
      {kind : AbiKind}
      {physical : Wasm.Value}
      {resultIndex : Nat}
      (related : ConcreteStructuredExternalBindCoreRel program context
        sourceModule sourceFunction externals labels entryRuntime entryStore
        entryWitness functionResult callerExpectedResult facts remainingBytes
        sourceRuntime callerEnv sourceValue result continuation callerJoins
        sourceFrames targetStore callerLocals callerRemainder targetRest
        targetFrames witness kind physical resultIndex source target)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult sourceFrames
        targetFrames)
      (agrees : supported.Agrees related.resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target
  | returned
      {source : MachineState}
      {target : StructuredWasmState Host}
      {sourceRuntime : RuntimeState}
      {sourceEnv : Env}
      {sourceValue : Value}
      {targetStore : Wasm.Store Host}
      {targetLocals : Wasm.Locals}
      {witness : RefinementWitness}
      {kind : AbiKind}
      {physical : Wasm.Value}
      (yielded : ConcreteStructuredYieldFocus context sourceFunction
        sourceRuntime sourceEnv sourceValue targetStore targetLocals witness
        kind physical source target)
      (compatible : ConcreteStructuredResultCompatible kind callerExpectedResult)
      (resources : ConcreteStructuredResourceStack program context sourceModule
        sourceFunction externals entryRuntime sourceRuntime entryStore
        targetStore entryWitness witness facts remainingBytes sourceEnv
        targetLocals functionResult callerExpectedResult source.frames
        target.frames)
      (contextCaches :
        context.cachedDeclarations = Fir.Wasm.cachedDeclarationNames program)
      (supported : ConcreteStructuredSupportedFrameStack program sourceModule
        targetModule hosts functionResult callerExpectedResult source.frames
        target.frames)
      (agrees : supported.Agrees resources.suspended) :
      ConcreteStructuredRunnableOutcome program context functionCode
        sourceModule sourceFunction targetModule hosts spec externals labels
        entryRuntime entryStore entryWitness functionResult callerExpectedResult
        facts remainingBytes source target

/-- One ordinary source step from any runnable open outcome preserves the
strong module-wide relation.

This is the constructor-complete dispatcher: ordinary code uses the
pointwise theorem above, staged named and saturated calls enter their
generated callees, and yielded control pops its supported caller.  It is a
ranked finite-prefix statement and therefore makes no termination claim. -/
theorem ConcreteStructuredRunnableOutcome.advance_supportedGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableOutcome program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter ∧
        (targetCount = 0 →
          compilerStructuredControlRank sourceAfter <
            compilerStructuredControlRank source) := by
  cases related with
  | code pointwise supported agrees =>
      exact pointwise.advance_supportedGlobal supported agrees sourceStep
  | directReady ready contextCaches supported agrees =>
      obtain ⟨targetAfter, targetPath, nextGlobal⟩ :=
        ready.advance_supportedGlobal_of_step spec contextCaches supported
          agrees sourceStep
      exact ⟨1, targetAfter, targetPath, nextGlobal, by omega⟩
  | saturatedReady row sharedCapacity ready contextCaches supported agrees =>
      obtain ⟨targetCount, targetAfter, targetPath, targetPositive,
          nextGlobal⟩ :=
        ready.advance_supportedGlobal_of_step (row := row) spec contextCaches
          supported agrees sharedCapacity sourceStep
      exact ⟨targetCount, targetAfter, targetPath, nextGlobal, by omega⟩
  | externalReady ready contextCaches supported agrees =>
      obtain ⟨nextStore, nextWitness, physicalResult, targetAfter,
          targetPath, bindCore⟩ :=
        ready.advance_of_step sourceStep
      have nextAgrees : supported.Agrees bindCore.resources.suspended := by
        simpa using agrees
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .externalBind bindCore contextCaches supported nextAgrees
      exact ⟨1, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | externalBind bindCore contextCaches supported agrees =>
      obtain ⟨targetAfter, resumedLocals, targetPath, nextCore,
          sourceFramesEq, targetFramesEq⟩ :=
        bindCore.advance_of_step (targetModule := targetModule)
          (hosts := hosts) sourceStep
      obtain ⟨supportedAfter, agreesAfter⟩ :=
        agrees.reindex sourceFramesEq targetFramesEq
          nextCore.resources.suspended
      let nextActive : ConcreteStructuredSupportedOutcome program context
          functionCode sourceModule sourceFunction targetModule hosts spec
          externals labels entryRuntime entryStore entryWitness functionResult
          callerExpectedResult sourceAfter targetAfter :=
        .code contextCaches nextCore supportedAfter agreesAfter
      exact ⟨1, targetAfter, targetPath,
        nextActive.toGlobal, by omega⟩
  | returned yielded compatible resources contextCaches supported agrees =>
      obtain ⟨targetCount, targetAfter, targetPath, targetPositive,
          nextGlobal⟩ :=
        yielded.advance_pop_supportedGlobal_of_step spec compatible resources
          supported agrees sourceStep
      exact ⟨targetCount, targetAfter, targetPath, nextGlobal, by omega⟩

/-- Forget the current-node classifier while retaining the strong global
compiler/resource relation. -/
theorem ConcreteStructuredRunnableOutcome.toSupportedGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableOutcome program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes source target) :
    ConcreteStructuredSupportedGlobalOutcome program sourceModule targetModule
      hosts externals source target := by
  apply ConcreteStructuredSupportedOutcome.toGlobal
    (functionCode := functionCode) (spec := spec)
  cases related with
  | code pointwise supported agrees =>
      exact .code pointwise.contextCaches pointwise.core supported agrees
  | directReady ready contextCaches supported agrees =>
      exact .directReady ready contextCaches supported agrees
  | saturatedReady row sharedCapacity ready contextCaches supported agrees =>
      exact .saturatedReady row sharedCapacity ready contextCaches supported
        agrees
  | externalReady ready contextCaches supported agrees =>
      exact .externalReady ready contextCaches supported agrees
  | externalBind bindCore contextCaches supported agrees =>
      exact .externalBind bindCore contextCaches supported agrees
  | returned yielded compatible resources contextCaches supported agrees =>
      exact .returned yielded compatible resources contextCaches supported agrees

/-- Module-wide runnable relation for the current source node.

All active-function, entry-anchor, budget, and caller-result indices are
hidden, while `ConcreteStructuredRunnableOutcome` retains the branch-exact
supported stack and only the current node's admission.  A successor is first
returned in `ConcreteStructuredSupportedGlobalOutcome`; its own local
classifier can then be attached after that dynamic state is known. -/
def ConcreteStructuredRunnableGlobalOutcome
    (program : Fir.LeanIR.ImpureProgram)
    (sourceModule : Fir.Wasm.Module)
    (targetModule : AdaptedModule)
    (hosts : ResolvedHosts)
    (externals : ExternalImpl)
    (source : MachineState)
    (target : StructuredWasmState Host) : Prop :=
  ∃ (context : Fir.Wasm.Context)
      (functionCode : Lean.Compiler.LCNF.Code .impure)
      (sourceFunction : Fir.Wasm.Function)
      (spec : ConcreteSupportedFunction program context functionCode
        sourceModule sourceFunction targetModule hosts)
      (labels : List Lean.FVarId)
      (entryRuntime : RuntimeState)
      (entryStore : Wasm.Store Host)
      (entryWitness : RefinementWitness)
      (functionResult : AbiKind)
      (callerExpectedResult : Option AbiKind)
      (facts : ReuseCapacityFacts)
      (remainingBytes : Nat),
    ConcreteStructuredRunnableOutcome program context functionCode sourceModule
      sourceFunction targetModule hosts spec externals labels entryRuntime
      entryStore entryWitness functionResult callerExpectedResult facts
      remainingBytes source target

/-- Hide the active generated-function and resource indices of one runnable
control state. -/
theorem ConcreteStructuredRunnableOutcome.toRunnableGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime : RuntimeState}
    {entryStore : Wasm.Store Host}
    {entryWitness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {remainingBytes : Nat}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableOutcome program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts remainingBytes source target) :
    ConcreteStructuredRunnableGlobalOutcome program sourceModule targetModule
      hosts externals source target :=
  ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
    entryStore, entryWitness, functionResult, callerExpectedResult, facts,
    remainingBytes, related⟩

/-- The runnable relation is a current-node strengthening of the recursively
stable supported relation. -/
theorem ConcreteStructuredRunnableGlobalOutcome.toSupportedGlobal
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableGlobalOutcome program sourceModule
      targetModule hosts externals source target) :
    ConcreteStructuredSupportedGlobalOutcome program sourceModule targetModule
      hosts externals source target := by
  rcases related with
    ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
      entryStore, entryWitness, functionResult, callerExpectedResult, facts,
      remainingBytes, active⟩
  exact active.toSupportedGlobal

/-- Runnable states preserve the exact world/trace prefix before taking their
next step. -/
theorem ConcreteStructuredRunnableGlobalOutcome.observes
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {source : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableGlobalOutcome program sourceModule
      targetModule hosts externals source target) :
    ConcretePrefixObservationRel
      (sourcePrefixObservation source)
      (concretePrefixObservation target.store) := by
  rcases related with
    ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
      entryStore, entryWitness, functionResult, callerExpectedResult, facts,
      remainingBytes, active⟩
  cases active with
  | code related _supported _agrees => exact related.core.observes
  | directReady related _contextCaches _supported _agrees =>
      exact related.observes
  | saturatedReady _row _sharedCapacity related _contextCaches _supported
      _agrees => exact related.observes
  | externalReady related _contextCaches _supported _agrees =>
      exact related.observes
  | externalBind related _contextCaches _supported _agrees =>
      exact related.observes
  | returned yielded _compatible _resources _contextCaches _supported
      _agrees => exact yielded.observes

/-- One-source-step ranked finite-prefix closure for the complete strong
compiler control relation.

The result is again the same module-wide supported relation after any local
value, call staging/entry, or return/pop transition.  Zero target steps are
permitted only with strict descent of `compilerStructuredControlRank`; the
theorem is independent of termination and of future execution certificates. -/
theorem ConcreteStructuredRunnableGlobalOutcome.advance
    {program : Fir.LeanIR.ImpureProgram}
    {sourceModule : Fir.Wasm.Module}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {externals : ExternalImpl}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredRunnableGlobalOutcome program sourceModule
      targetModule hosts externals source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        ConcreteStructuredSupportedGlobalOutcome program sourceModule
          targetModule hosts externals sourceAfter targetAfter ∧
        (targetCount = 0 →
          compilerStructuredControlRank sourceAfter <
            compilerStructuredControlRank source) := by
  rcases related with
    ⟨context, functionCode, sourceFunction, spec, labels, entryRuntime,
      entryStore, entryWitness, functionResult, callerExpectedResult, facts,
      remainingBytes, active⟩
  exact active.advance_supportedGlobal sourceStep

/-- Module-stable form of the pointwise code law.  All active-function,
entry-anchor, and budget indices are hidden in `ConcreteStructuredGlobalOutcome`,
so the conclusion has the same type before and after an internal call changes
the current generated function. -/
theorem ConcreteStructuredCodePointwiseRel.advance_global
    {program : Fir.LeanIR.ImpureProgram}
    {context : Fir.Wasm.Context}
    {functionCode sourceCode : Lean.Compiler.LCNF.Code .impure}
    {sourceModule : Fir.Wasm.Module}
    {sourceFunction : Fir.Wasm.Function}
    {targetModule : AdaptedModule}
    {hosts : ResolvedHosts}
    {spec : ConcreteSupportedFunction program context functionCode sourceModule
      sourceFunction targetModule hosts}
    {externals : ExternalImpl}
    {labels : List Lean.FVarId}
    {entryRuntime sourceRuntime : RuntimeState}
    {entryStore targetStore : Wasm.Store Host}
    {entryWitness witness : RefinementWitness}
    {functionResult : AbiKind}
    {callerExpectedResult : Option AbiKind}
    {facts : ReuseCapacityFacts}
    {requiredBytes remainingBytes : Nat}
    {sourceEnv : Env}
    {targetLocals : Wasm.Locals}
    {targetCode : Wasm.Program}
    {source sourceAfter : MachineState}
    {target : StructuredWasmState Host}
    (related : ConcreteStructuredCodePointwiseRel program context functionCode
      sourceModule sourceFunction targetModule hosts spec externals labels
      entryRuntime entryStore entryWitness functionResult callerExpectedResult
      facts requiredBytes remainingBytes sourceRuntime sourceEnv sourceCode
      targetStore targetLocals targetCode witness source target)
    (sourceStep : executeStep externals source = .next sourceAfter) :
    ∃ targetCount targetAfter,
      FinitePath (StructuredWasmStep targetModule.wasmModule hosts.env)
          targetCount target targetAfter ∧
        ConcreteStructuredGlobalOutcome program sourceModule targetModule hosts
          externals sourceAfter targetAfter ∧
        (targetCount = 0 →
          compilerStructuredControlRank sourceAfter <
            compilerStructuredControlRank source) := by
  obtain ⟨targetCount, targetAfter, targetPath, outcome, rank⟩ :=
    related.advance sourceStep
  exact ⟨targetCount, targetAfter, targetPath, outcome.toGlobal, rank⟩

end FirTalos.Concrete
